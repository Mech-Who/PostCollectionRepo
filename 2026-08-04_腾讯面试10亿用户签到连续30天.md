---
title: 腾讯面试：10亿用户签到记一年，怎么算连续30天？
date: 2026-08-04
category: AI技术
depth: 深度
layer: layer2
tags: [腾讯, 面试, 签到系统, Bitmap, 位图, 连续签到, 算法, 大数据, 面试方法论]
summary: 经典海量数据面试题：10 亿用户签到记录存储一年，如何判断"连续签到 30 天"。核心方案：Bitmap 位图——每个用户 365 个 bit 记录一年签到（约 46 字节/人/年，10 亿用户约 43GB，相比存储完整日期字符串省 99%+）；按用户维度建 key（user_id → 位图）；判断连续 30 天用"五次移位"技巧：x &= x>>1 后连续 k 天可被检测（x>>1/2/4/8/14 依次移位并相与，最后看是否非零）。面试答题五步模板：澄清需求→数据规模估算→方案设计→复杂度分析→扩展性讨论。底层洞察：用比特位承载布尔信息，用位运算做聚合统计——大数据面试题的本质是"信息压缩 + 计算下推"。
source_url: https://mp.weixin.qq.com/s/b-2JdTnJIWcTNYfaUe980w
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** "10 亿用户签到记一年，怎么算连续 30 天？"——腾讯高频面试题，考的不是算法模板而是工程思维。方案：Bitmap 位图，一个用户一年 365 个 bit（约 46 字节），10 亿用户约 43GB，相比存日期字符串（每用户至少几 KB）压缩百倍以上；按用户维度设计 key（user_id → Bitmap），支持位运算快速聚合。判断"连续 30 天"的关键技巧：**五次移位**——把位图 x 依次执行 `x &= x >> 1`（表示连续 2 天）、`x >> 2`（连续 4 天）、`x >> 4`（连续 8 天）、`x >> 8`（连续 16 天）、`x >> 14`（连续 30 天），每轮相与后若仍有非零位，说明存在对应长度的连续签到段。面试应答五步模板：① 澄清需求（连续是指自然日还是活跃日？跨月怎么办？）→ ② 数据量估算 → ③ 方案设计 → ④ 复杂度分析（空间 O(N×365bit)、判断 O(1)）→ ⑤ 扩展性（跨年、多平台、只算工作日）。底层洞察：用比特承载布尔信息、用移位做聚合统计——面试题本质是"信息压缩 + 计算下推"。

## 我的理解

> 由小林生成，供小涵审阅修改

1. **这道题的考点不是 Bitmap 本身，而是"面试官在考察什么"**：10 亿用户 × 365 天 = 3650 亿个数据点，任何"按条存储"的方案都会爆炸。面试官想看到的是——候选人能否先估算数据量、识别信息冗余（签到只有 0/1 两个状态）、然后想到用"位"来承载。这比背算法模板重要得多。
2. **五次移位的本质是"倍增思想"**：`x &= x>>1` 把"连续 2 天"检测完，`x>>2` 是在上一轮结果上再倍增到 4 天、8 天、16 天、30 天。5 次操作就能覆盖到 30，是因为 2+4+8+16=30（用 1,2,4,8,14 覆盖所有需求长度）。这是二分/倍增在统计场景的变体，和 RMQ、倍增 LCA 是同一家族。
3. **"按用户维度 key"的设计决策**：签到查询天然是"某个用户"的连续性查询（运营关心的是 DAU 连续签到，不是跨用户的位运算），所以 Bitmap 必须按 user_id 分片，而不是全局一张大位图。**数据结构的组织方式要服从查询模式**——这是面试里最容易丢分的一层。
4. **可迁移到游戏/产品场景**：连续登录奖励（七日签到）、活跃度统计、留存分析都是同一模型。Unity 客户端本地也可以用小 Bitmap 记录玩家离线签到日，用同样的移位技巧判断"连续 N 天"——代码量极小。

## 📌 关键要点

1. **存储方案**：Bitmap 位图，1 bit = 1 天签到状态。每用户每年 365 bit ≈ 46 字节（含 bit 对齐），10 亿用户 ≈ 43GB。对比：存日期字符串每用户每年几 KB，位图压缩 99%+。
2. **Key 设计**：按用户维度，`user_id → Bitmap(365bit)`。查询"某用户是否连续 N 天"直接对该用户位图做位运算，天然支持 Redis BITFIELD / Redis bit 操作。
3. **连续 30 天检测（五次移位）**：
   ```python
   x = bitmap  # 当前用户的年度签到位图（bit 位=1 表示当天签到）
   x &= x >> 1   # 连续 2 天
   x &= x >> 2   # 连续 4 天
   x &= x >> 4   # 连续 8 天
   x &= x >> 8   # 连续 16 天
   x &= x >> 14  # 连续 30 天（16+14）
   # 若 x 非零，说明存在连续 30 天签到
   ```
   原理：每次 `x &= x>>k` 后，某位为 1 当且仅当"从该位起连续 2k 天都签到"，倍增覆盖到 30。
4. **面试五步模板**：① 澄清需求（连续=自然日？跨月/跨年？只算工作日？）→ ② 数据量估算（10 亿×365，逐条存不可行）→ ③ 方案设计（Bitmap + 用户分片）→ ④ 复杂度分析（空间 O(N×365bit)，检测 O(1) 位运算）→ ⑤ 扩展性（跨年用时间戳分段、多平台合并、冷数据压缩）。
5. **底层洞察**：签到本质是布尔事件流，用比特承载、用移位聚合——"信息压缩 + 计算下推"是海量数据面试题的通用解题思路。

## 原文

（微信公众号「Fox爱分享」2026-08-04 原创文章，内容为整理版）

# 腾讯面试：10亿用户签到记一年，怎么算连续30天？

面试官："我们有 10 亿用户，每天可以签到。签到记录要保存一年。现在运营想知道：有多少用户连续签到了 30 天？你怎么设计？"

## 需求澄清

先别急着写代码——10 亿 × 365 = 3650 亿条记录，任何"一行一条"的方案都不可行。签到信息本质只有两个状态：签了 / 没签。这是典型的**布尔事件流**，应该想到用位图。

## 方案：Bitmap 位图

- 每个用户一年 365 个 bit，按 bit 对齐约 46 字节。
- 10 亿用户总计约 43GB，可分布式存储（如 Redis Cluster 分片）。
- Key 设计：`sign:user:{userId}:2026` → 365bit 位图。签到第 N 天置位：`SETBIT key N 1`。

## 判断连续 30 天：五次移位

```python
x = 用户的年度签到位图整数
x &= x >> 1   # 标记连续≥2天的起点
x &= x >> 2   # 连续≥4天
x &= x >> 4   # 连续≥8天
x &= x >> 8   # 连续≥16天
x &= x >> 14  # 连续≥30天
return x != 0 # True 表示存在连续 30 天
```

原理：`x &= x>>k` 后位 i 为 1 ⟺ 位 i..i+k 全部为 1（即存在长度为 k+1 的连续段）。通过 1→2→4→8→16→30 的倍增组合，5 次位运算完成检测，O(1) 时间。

## 复杂度

- 空间：O(N × 365bit)，N=用户数，约 43GB。
- 单用户检测：O(1)（固定 5 次位运算）。
- 全量统计：并行按分片扫描，MapReduce 风格聚合。

## 扩展讨论

- 跨年：按年份分段存储，查询时拼接多个位图。
- 多平台（App/小程序）：按平台分 key，合并统计。
- 只算工作日：构造工作日掩码，与位图相与后再检测。
