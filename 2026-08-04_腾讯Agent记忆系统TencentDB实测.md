---
title: 腾讯把Agent记忆系统开源了，TencentDB Agent Memory实测
date: 2026-08-04
category: AI技术
depth: 深度
layer: layer2
tags: [腾讯, Agent, 记忆系统, TencentDB, AgentMemory, 记忆金字塔, RAG, 语义召回, 任务地图, 冲突仲裁, 开源]
summary: 腾讯开源 TencentDB Agent Memory——面向 Agent 的托管记忆系统。核心是记忆金字塔 L0-L3 四层架构（L0原始上下文/L1记忆块/L2语义记忆/L3知识图谱），写入/提炼/检索/更新四步闭环；语义召回默认关闭（避免噪声），冲突仲裁处理记忆矛盾；实测任务地图场景下 Token 消耗从 2.21 亿降到 8500 万（-61.5%）。对自建 Agent 记忆体系的启示：记忆不是"越多越好"，而是分层+按需召回+主动更新的系统工程。
source_url: https://mp.weixin.qq.com/s/Rwf1z89_0uUt1pixe_ohwg
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** 腾讯云开源 TencentDB Agent Memory，把"Agent 的记忆"做成托管服务。核心设计是**记忆金字塔**：L0 原始上下文（对话/工具调用流水）→ L1 记忆块（LLM 从 L0 提炼的结构化片段）→ L2 语义记忆（向量化检索层）→ L3 知识图谱（实体关系）。四步闭环：**写入**（事件进 L0）→ **提炼**（LLM 总结成 L1，升级到 L2）→ **检索**（语义召回+关键词混合）→ **更新**（遗忘/合并/冲突仲裁）。两个值得注意的工程决策：语义召回默认关闭（实测语义检索噪声大，关键词更稳）——"先关键词后语义"；冲突仲裁——新旧记忆矛盾时按置信度+时间戳裁决。实测收益：任务地图场景 Token 从 2.21 亿降至 8500 万，成本降 61.5%。金句：Agent 的长期记忆不是数据库加个向量字段，而是"分层存储+按需召回+主动更新"的系统工程。

## 我的理解

> 由小林生成，供小涵审阅修改

1. **"记忆金字塔"是对 Context Engineering 的工程化落地**：L0-L3 的分层本质上就是把"上下文工程"从 prompt 技巧变成系统架构——原始数据、提炼知识、向量索引、图谱关系各归其位。这与我们知识管线的"文章→KF→概念"分层是同构的：原文是 L0，KF 是 L1/L2，概念连接是 L3。Agent 记忆系统的设计者最终都走向同一套分层逻辑。
2. **"语义召回默认关闭"是反直觉但正确的工程决策**：向量检索在 Agent 记忆场景的痛点——检索出的片段上下文不完整、噪声高、召回准度不稳。先关键词、后语义（语义作为兜底）符合"宁可少不可错"的原则。这提醒我们：RAG 系统不要迷信向量，混合检索 + 可配置开关才是生产级做法。
3. **冲突仲裁是记忆系统最容易被忽视的组件**：Agent 长期运行必然出现"昨天的记忆"和"今天的记忆"矛盾。没有仲裁机制，记忆系统会变成"错误的放大器"。置信度+时间戳裁决是一个简单可行的启发式。
4. **Token 降 61.5% 的数据值得关注**：记忆系统的核心 ROI 不是"记得更久"而是"花更少 token 记得更久"。任务地图场景把"每轮全量塞上下文"变成"只塞相关记忆块"，这才是 Agent 成本优化的真正杠杆。

## 📌 关键要点

1. **记忆金字塔 L0-L3**：L0 原始上下文（完整事件流）→ L1 记忆块（LLM 提炼的结构化摘要）→ L2 语义记忆（向量检索层）→ L3 知识图谱（实体/关系网络）。层级越高越精炼、越接近长期知识。
2. **四步闭环**：写入（事件追加进 L0）→ 提炼（异步 LLM 将 L0 压缩为 L1 记忆块，升级进 L2）→ 检索（关键词+语义混合，语义默认关闭）→ 更新（遗忘、合并、冲突仲裁）。
3. **冲突仲裁规则**：新旧记忆矛盾时按"置信度 × 时间戳"裁决——高置信度新记忆胜出，低置信度记忆降级或合并。防止记忆自我矛盾导致行为漂移。
4. **实测数据**：任务地图场景（多轮复杂任务）Token 消耗 2.21 亿 → 8500 万，降幅 61.5%，直接转化为成本下降。
5. **设计启示**：① 记忆要分层不要全量塞；② 召回默认保守（关键词优先）；③ 必须有更新/遗忘机制，只写不删的记忆是负债；④ 冲突要有仲裁，否则记忆自相矛盾。

## 原文

（微信公众号「Datawhale」2026-08-03 原创文章，内容为整理版）

# 腾讯把 Agent 记忆系统开源了，TencentDB Agent Memory 实测！

Agent 的记忆一直是落地难题：把全部历史塞进上下文，token 爆炸；只保留最后几轮，Agent 失忆。腾讯云把 Agent 记忆做成托管服务 TencentDB Agent Memory 并开源，本文实测其架构与效果。

## 记忆金字塔：L0-L3 四层架构

- **L0 原始上下文**：完整记录对话、工具调用、事件流水，不加工不丢失，是记忆的"原料层"。
- **L1 记忆块**：LLM 定期从 L0 提炼的结构化记忆片段（事件摘要、决策记录），比原始流水精炼。
- **L2 语义记忆**：L1 记忆块向量化后进入语义索引，支持语义检索。
- **L3 知识图谱**：从记忆中抽取实体与关系（人物、项目、偏好），形成可查询的知识网络。

## 四步闭环：写入 → 提炼 → 检索 → 更新

1. **写入**：每次交互追加到 L0，不丢原始数据。
2. **提炼**：异步任务将 L0 压缩为 L1 记忆块，升级到 L2 语义层。
3. **检索**：混合检索（关键词 + 语义召回）。实测语义召回默认关闭——语义检索噪声大、召回片段上下文不全，关键词检索更稳，语义作为增强手段按需开启。
4. **更新**：遗忘过期记忆、合并相似记忆、冲突仲裁（置信度 + 时间戳裁决矛盾）。

## 实测结果

任务地图场景（长链路多轮任务）：使用记忆系统前每轮全量塞上下文，Token 消耗 2.21 亿；使用后仅召回相关记忆块，Token 降至 8500 万，**降幅 61.5%**，成本同步下降。

## 工程启示

Agent 长期记忆不是"数据库加个向量字段"，而是分层存储 + 按需召回 + 主动更新的系统工程。开源后社区可基于此构建自己的记忆层。
