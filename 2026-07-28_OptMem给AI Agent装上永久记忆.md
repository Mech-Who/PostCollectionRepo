---
title: OptMem：426个Token提示词给AI Agent装上永久记忆
date: 2026-07-28
category: AI技术
depth: 深度
layer: layer2
tags: [AI Agent, 记忆系统, OptMem, 上下文工程, 追加式日志, 二叉合并树, 开源项目, 提示词工程]
summary: OptMem 是一个 AI Agent 永久记忆系统：426 token 的 Markdown 提示词 + 859 行零依赖 Python 脚本，让任何 Agent（Claude Code/Codex/Cursor）拥有跨会话、跨模型、跨供应商的持久记忆。核心设计理念是"记忆不是检索问题，而是压缩问题"：追加式日志（固定320字节宽度记录，O(1) seek 定位）+ 二叉合并树压缩（最近记忆保持原文，久远记忆逐层折叠为摘要）+ 固定预算唤醒（默认208行≈16K token，适配各 harness 上下文限制）。9 个 CLI 命令（init/wake/note/nap/recall/zoom/forget/config/import），Agent 驱动压缩（nap），零依赖零基础设施纯文件存储。
source_url: https://mp.weixin.qq.com/s/YlCoBiQxyZM9MUPaD1irXQ
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** OptMem 是 VictorTaelin 开源的 AI Agent 永久记忆工具：一个 426 token 的提示词块（粘贴进 AGENTS.md）+ 一个 859 行的零依赖 Python 脚本（memo）。核心洞察：**记忆不是检索问题，而是压缩问题**——所有记忆存为追加式日志（永不修改永不删除），用二叉合并树自动压缩（最近的记忆保持原文，久远记忆逐层折叠为一行摘要），每次会话启动时 `memo wake` 在固定预算（208 行 ≈ 16K token）内输出一份「记忆文档」。与 Mem0/LangChain Memory 等向量数据库方案相比，OptMem 零依赖、零基础设施、纯文件存储、不绑定任何 Agent 框架。9 个命令：init/wake/note/nap/recall/zoom/forget/config/import。

## 📌 关键要点

1. **核心理念：记忆是压缩问题，不是检索问题**。传统方案（Mem0/LangChain）用向量数据库+嵌入模型做检索；OptMem 把记忆做成追加式日志 + 二叉摘要树，用固定预算渲染"记忆文档"，让 Agent 醒来时像人类回忆"我是谁、昨天做了什么"。
2. **三大数据结构**：
   - `LOG.txt`：追加式原始记忆，每条固定 320 字节宽度（≤280 字符），永不修改、永不删除，**位置即身份**（第 i 条记忆在 i*320 偏移处，O(1) seek 定位，不需要索引）
   - `TREE/`：二叉合并树摘要缓存（固定 288 字节宽度记录），相邻两条合并为一行摘要，逐层向上折叠；细节随时间衰减，可从 LOG.txt 完全重建
   - `config`：尺寸参数（WAKE_LINES=208 / ENTRY_CHARS=280 / PART_CHARS=20000 / PART_LINES=500）
3. **Agent 驱动压缩（关键设计）**：工具本身不内置任何 AI/LLM 调用。`note` 记录记忆后，如果有待压缩的块，会输出一条 `Run: memo nap ...` 指令，**Agent 自己执行该指令完成压缩**（用 Agent 的智能做压缩器），回传结果。这使得工具零依赖。
4. **集成方式**：`install.sh` 一行安装 → `memo init` 输出 `## Memory` 提示词块 → 粘贴到项目 `AGENTS.md`/`CLAUDE.md` 顶部 → 之后 Agent 每次会话自动先跑 `memo wake`、学到新东西 `memo note`、需要压缩 `memo nap`。**注意：不会自动记忆，需要告诉 Agent 调用 note 才记录**（作者自己也指出这是坑点）。
5. **健壮性设计**：固定宽度记录可检测部分写入（torn write）并截断修复；`fcntl.flock`（Linux/macOS）/`msvcrt` 自旋锁（Windows）并发安全；`forget` 丢弃错误摘要（截断树文件，日志永不动）；输出自动分页适配各 harness 限制（Claude Code 30K 字符 / pi 50KB / Codex 10K token）。
6. **9 个 CLI 命令**：`init`（初始化+输出提示词）、`wake`（唤醒读记忆，每个会话第一个命令，支持分页/快照时间点）、`note`（记录一行 ≤280 字符记忆）、`nap`（执行待处理压缩）、`recall`（正则全文检索，流式扫描）、`zoom`（树导航下钻原始记忆）、`forget`（丢弃错误摘要）、`config`（调尺寸参数）、`import`（批量导入历史记忆初始化身份）。
7. **适用边界**：适合终端 Agent（Claude Code/Codex）跨会话保持项目上下文；不适合需要语义相似度检索的场景（recall 是正则匹配非向量检索）；跨模型迁移时记忆文档是纯文本，天然可移植。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇对小涵（正在研究 AI Agent 架构 + 知识管理双系统）有双重价值：既是**工具**（可以给 WorkBuddy/Codex 装上跨会话记忆），更是**设计思想**（记忆系统的架构范式）。

1. **「记忆是压缩问题而非检索问题」是一次认知反转**：向量数据库方案把记忆当作"语义检索"（存嵌入、相似度匹配），OptMem 反其道——把记忆当作"时间衰减的日志"（最近的最详细、久远的折叠成摘要），用固定预算渲染。这更接近人脑的遗忘曲线，且零基础设施。这个认知可以直接用于评估/设计自己的记忆体系。
2. **「位置即身份」是极优雅的工程决策**：固定宽度记录 → 第 i 条记忆直接 seek 到 i*320 偏移，不需要索引文件，崩溃恢复也简单（截断 torn write）。以空间换 O(1) 查找，以小代价（每条最多浪费 40 字节填充）换巨大简单性。
3. **「Agent 是压缩器」把 LLM 成本外部化**：工具不调用 LLM，压缩指令输出给 Agent 执行——设计上避免了自己变成"又一个依赖 API 的服务"。这也是 Skill/工具设计的好范例：**让宿主智能体干活，自己只做确定性计算**。
4. **对小涵自己的知识管线启发**：OptMem 的"固定预算唤醒文档"思想，和 WorkBuddy 的 `MEMORY.md` 截断机制（工作记忆超限被截断）问题完全对应——我们的 memory 体系当前是"截断丢失"，OptMem 是"分层折叠"。如果未来做记忆优化，二叉合并树 + 固定预算渲染是可以借鉴的进化方向。
5. **坑点提示**：不自动记忆（需 Agent 主动 note）+ recall 仅正则匹配（非语义检索），这两个边界决定了它适合"项目上下文记忆"而非"知识库问答"。

**关联知识**：可挂到「AI Agent 架构」相关概念（记忆系统是 Agent 核心组件之一），与已入库的 AI Agent 设计模式映射（Controller→Planner 等）并列。

## 原文

大家好，这里是 `AI开源提效指南`！

你有没有遇到过这样的场景：和 `Claude Code` 聊了半小时，上下文被压缩，之前讨论的技术决策全丢了；换了一个模型供应商，Agent 对你一无所知；开了两个并行会话，它们互相不知道对方做了什么。

今天要介绍的 `OptMem`，正是为了解决这类问题而设计的。

它是一款 **AI Agent 永久记忆系统**，通过一个 426 token 的 `Markdown` 提示词 + 一个零依赖的 `Python` 脚本，帮助开发者让任何 AI Agent 拥有跨会话、跨模型、跨供应商的持久记忆。

与常见的向量数据库记忆方案（如 `Mem0`、`LangChain Memory`）相比，`OptMem` 的特点是 **零依赖、零基础设施、纯文件存储**——不需要数据库、不需要嵌入模型、不需要 API Key，一个 859 行的 `Python` 文件就是全部。

我也会在文中贴出效果截图，大家可以看一下！

## 🔥 推荐原因

AI Agent 的记忆问题，本质上是一个 **身份连续性** 问题。

其实为了解决记忆问题已经出现了很多成熟的开源项目了，但是我个人感觉这个项目确实让人耳目一新，而且超级简单，还没有依赖包。

`OptMem` 的切入点完全不同：**记忆不是检索问题，而是压缩问题**。它把 Agent 的所有记忆存为一个追加式日志，然后用二叉合并树自动压缩——最近的记忆保持原文，久远的记忆逐层折叠为一行摘要。Agent 每次启动时读取一份固定预算的「记忆文档」，就像人类醒来时回忆「我是谁、昨天做了什么」。

这个设计意味着：

- 不需要嵌入模型，压缩由 Agent 自己完成（`nap` 命令）
- 不需要数据库，固定宽度记录 + 文件偏移 = O(1) 查找
- 不需要网络，所有数据在本地文件系统
- 不绑定任何 Agent 框架，一个 `Markdown` 提示词就是全部集成

## 📦 项目介绍

`OptMem` 是一个面向 AI Agent 的永久记忆工具。

它的核心理念是：**一个 426 token 的提示词 + 一个零依赖脚本 = 即插即用的 Agent 记忆**。整个项目只有一个 859 行的 `Python` 文件（`memo`），不依赖任何第三方库，支持 `Linux`、`macOS` 和原生 `Windows`。

### 核心能力

✅ **追加式记忆日志**：每条记忆是一行不超过 280 字符的文本，以固定 320 字节宽度追加到 `LOG.txt`，永不修改、永不删除，位置即身份，任何一条记忆都可以通过一次 `seek` 定位

✅ **二叉合并树压缩**：记忆自动形成二叉树结构——相邻两条合并为一行摘要，摘要再两两合并，逐层向上。最近的记忆保持原文，久远的记忆折叠为粗粒度摘要，细节随时间衰减

✅ **固定预算唤醒**：`wake` 命令在固定行数预算（默认 208 行 ≈ 16K token）内输出一份「记忆文档」，自动在原文和摘要之间分配空间，确保不超出任何 Agent harness 的上下文限制

✅ **Agent 驱动压缩**：压缩不是后台自动运行的——`note` 命令在记录记忆后，如果有待压缩的块，会输出一条 `Run: memo nap ...` 指令，Agent 执行该指令完成压缩。整个过程对 Agent 透明

✅ **正则全文检索**：`recall` 命令对全部历史记忆做正则匹配，流式扫描不加载全量数据到内存，结果按时间倒序截断到输出预算内

✅ **树导航**：`zoom` 命令打开任意树节点，展示其两个子半区——摘要或原文。Agent 可以从 `wake` 输出的任意 `#a-b` 行开始，逐层下钻到原始记忆

✅ **跨平台并发安全**：`Linux`/`macOS` 使用 `fcntl.flock` 文件锁，`Windows` 降级为 `msvcrt` 自旋锁（含退避策略），16 个并行进程写入不会冲突

✅ **崩溃恢复**：固定宽度记录意味着部分写入（torn write）可以被检测和截断——下次追加前自动修复对齐，不会污染后续记录

✅ **多 Harness 输出适配**：输出自动分页，每页不超过 20000 字符 / 500 行，兼容 `Claude Code`（30K 字符截断）、`pi`（50KB 头部截断）、`Codex`（10K token 预算）等主流 Agent 运行环境

## 🚀 快速开始

### 1. 环境要求

- `Python 3.7+`（唯一依赖，无第三方库）
- `Linux`、`macOS` 或原生 `Windows`（无需 WSL）
- 任意支持 shell 命令执行的 AI Agent（`Claude Code`、`Codex CLI`、`Cursor` 等）

### 2. 安装方式

```
# 一行安装（下载 memo 脚本到 ~/.optmem/，执行 init）
curl -fsSL https://raw.githubusercontent.com/VictorTaelin/OptMem/main/install.sh | sh
```

安装完成后会输出一个 `## Memory` 块，将其粘贴到你自己项目根目录的 `AGENTS.md`（或 `CLAUDE.md`）文件顶部即可。

你不需要手动执行 `memo wake`命令，可以看看效果！

```
整个设计的逻辑是这样的：

你启动 Codex
    ↓
Codex 自动读取项目里的 AGENTS.md
    ↓
看到 "Run ~/.optmem/memo wake before any other tool call"
    ↓
Codex 自己执行 memo wake（作为它第一个动作）
    ↓
开始正常工作

memo wake 是 Codex 自己跑的，不是你跑的。你粘贴到 AGENTS.md 里的那段提示词，就是给 AI 看的指令，告诉它：

> "每次会话开始，先运行 memo wake；学到新东西就 memo note；需要压缩就 memo nap。"
```

所以你的操作就是：

1. ✅ 安装
2. ✅ 把输出的提示词粘贴到 `AGENTS.md`
3. ✅ 以后直接 `codex` 进入对话就行（注意：不会自动记忆，需要告诉 Agent调用 memo note 记一下才行， 说实话这点有点坑， 后面研究一下有没有好办法）

自动记忆可以看看我这个方法，但是不能保证 100%

可选：将 `~/.optmem` 加入 `PATH` 以直接使用 `memo` 命令。

### 3. 使用示例

```
# 初始化（幂等，可重复执行）
~/.optmem/memo init

# 每个会话开始时
~/.optmem/memo wake

# 记录记忆
~/.optmem/memo note "项目使用 monorepo 结构，pnpm workspace"

# 按提示执行压缩
~/.optmem/memo nap 0-1 "monorepo + pnpm workspace"

# 搜索
~/.optmem/memo recall "pnpm"

# 树导航
~/.optmem/memo zoom 0-3

# 调整唤醒预算
~/.optmem/memo config WAKE_LINES=300
```

## 🧩 功能概览

`OptMem` 的全部功能通过 9 个 CLI 命令暴露，没有配置文件（除了可选的 `config` 尺寸调节），没有 API，没有插件系统——这是有意为之的极简设计。

### 完整命令列表

| 命令 | 描述 |
|------|------|
| `memo init` | 创建记忆存储，输出需要粘贴到 `AGENTS.md` 的 `## Memory` 提示词块 |
| `memo wake [part [T]]` | 读取记忆——每个会话的第一个命令。输出固定预算的记忆文档，支持分页和快照时间点 |
| `memo note "..."` | 记录一条记忆：一行文本，最多 280 字符。如果有待压缩的块，输出压缩指令 |
| `memo nap [id "..."]` | 执行待处理的压缩。无参数时显示下一个待压缩块的提示；带参数时提交压缩结果 |
| `memo recall <regex>` | 正则搜索全部历史记忆，逐字匹配，结果按时间倒序，自动截断到输出预算 |
| `memo zoom <lo>-<hi>` | 打开一个树节点，展示其两个子半区（摘要或原文） |
| `memo forget <lo>-<hi>` | 丢弃一个错误的摘要及其所有上层块，下次 `nap` 会重建。日志不受影响 |
| `memo config [NAME=N]` | 查看或修改记忆尺寸参数。空值恢复默认。修改即时生效，无需重算 |
| `memo import <file>` | 批量导入历史记忆（`YYYY-MM-DD <text>` 格式），用于初始化身份。仅用一次 |

### 可配置参数

| 参数 | 默认值 | 含义 |
|------|--------|------|
| `WAKE_LINES` | 208 | `wake` 输出的行数预算（208 行 ≈ 16K token） |
| `ENTRY_CHARS` | 280 | 单条记忆的最大字节数 |
| `PART_CHARS` | 20000 | 输出分页：每页最大字节数 |
| `PART_LINES` | 500 | 输出分页：每页最大行数 |

### 核心使用语法

```
# 安装（一行命令）
curl -fsSL https://raw.githubusercontent.com/VictorTaelin/OptMem/main/install.sh | sh

# 初始化记忆（输出提示词块，粘贴到 AGENTS.md）
~/.optmem/memo init

# 每个会话开始时唤醒
~/.optmem/memo wake

# 记录一条记忆
~/.optmem/memo note "用户偏好 TypeScript strict 模式，项目用 pnpm"

# 执行压缩（Agent 按 note 输出的指令执行）
~/.optmem/memo nap 0-1 "用户偏好 TS strict + pnpm"

# 搜索历史记忆
~/.optmem/memo recall "pnpm"

# 树导航：从粗到细
~/.optmem/memo zoom 0-1023
~/.optmem/memo zoom 0-511
~/.optmem/memo zoom 0-255

# 丢弃错误摘要
~/.optmem/memo forget 16-31
```

## 🏗️ 整体架构

`OptMem` 的架构可以用一句话概括：**一个追加式日志 + 一棵二叉摘要树 + 一个固定预算的渲染器**。没有服务器、没有数据库、没有网络调用——所有状态都在文件系统中，所有计算都在一个 `Python` 进程内完成。

研究原理的可以去看看`~/.optmem/memo` 这个脚本实现！

### 1. 架构概览

整个系统分为三层：

- **存储层**：`LOG.txt`（追加式原始记忆）+ `TREE/` 目录（按块大小分文件的摘要缓存）+ `config`（尺寸参数）
- **算法层**：`cover()` 函数（细节衰减覆盖算法）+ `pending()` 函数（待压缩块发现）+ 固定宽度记录的 O(1) 寻址
- **交互层**：9 个 CLI 命令，每个命令读取存储、执行算法、输出文本指令给 Agent

关键设计决策：

- **位置即身份**：记录是固定宽度的（`LOG_REC=320` 字节，`TREE_REC=288` 字节），所以第 i 条记忆在 `i*320` 偏移处，块 `[k*s, (k+1)*s)` 在 `TREE/s` 文件的 `k*288` 偏移处。查找是 O(1) 的 `seek`，不需要索引
- **摘要是缓存**：`TREE/` 目录可以从 `LOG.txt` 完全重建，`forget` 只是截断树文件，日志永远不动
- **Agent 是压缩器**：工具不内置任何 AI/LLM 调用，压缩指令输出给 Agent，Agent 执行后回传结果。这使得工具本身零依赖

### 2. 模块职责

| 模块 | 职责 |
|------|------|
| 存储管理 | 固定宽度记录的读写、文件锁（`fcntl`/`msvcrt`）、崩溃修复（torn write 截断）、`fsync` 持久化 |
| 覆盖算法 | `cover(T, budget)` 二叉搜索 + 细节衰减：在固定行数预算内，最近记忆保持原文，久远记忆折叠为摘要 |
| 压缩调度 | `pending()` 发现待压缩块、`nap_prompt()` 生成压缩指令、`tree_put()` 按序写入摘要 |
| 唤醒渲染 | `cmd_wake()` 组装记忆文档、`paginate()` 分页适配各 harness 输出限制、快照时间点 T 保证一致性 |
| 检索与导航 | `recall` 流式正则扫描、`zoom` 树节点展开、`forget` 摘要丢弃与级联截断 |
| 配置管理 | `KNOBS` 字典定义 4 个可调参数、`config` 文件覆盖、`memo config` 命令即时生效 |
| 安装与集成 | 执行 `install.sh` 会下载、安装脚本、`cmd_init()` 幂等初始化、`TEMPLATE` 426 token 提示词模板 |

### 3. 技术架构图

（原文含架构图，此处省略）

## 🎯 总结

`OptMem` 是一款极简主义的 AI Agent 永久记忆工具，用 859 行零依赖 `Python` 实现了追加式日志 + 二叉摘要树 + 固定预算渲染的完整记忆系统。

**一句话推荐**：一个文件，永久记忆

**适合场景**：

- ✅ 使用 `Claude Code` / `Codex CLI` 等终端 Agent，需要跨会话保持项目上下文
- ✅ 不想部署向量数据库或额外服务，偏好纯文件方案
- ✅ 对 Agent 记忆机制感兴趣，想阅读一个完整、可测试的极简实现

## 📚 参考资料

```
项目仓库：https://github.com/VictorTaelin/OptMem
Windows 支持文档：WINDOWS.md
```

* * *

免责声明：本文内容仅供学习交流，所述工具/方法请遵守相关平台服务条款及法律法规。如涉及第三方服务，请以官方最新政策为准。

* * *

**🎯 觉得这份工具干货有用？希望收到您的支持：**

- ⭐ 星标 / 置顶公众号，**第一时间解锁最新工具分享！**
- ✅ **点赞**「**推荐**」，让更多技术伙伴发现优质干货！
- 🔗 **转发**给团队小伙伴，一起高效提效！
- 💬 **底部留言区**，告诉我您想找的工具/项目方向！

**📬 长期追踪优质开源工具**

- 关注「**AI 开源提效指南**」｜日更开源神器，玩转技术提效！
- 回复 **【容器加速器】**，即刻开启你的高效探索之旅～
