---
title: C#开发小技巧之垃圾回收机制深度解读：世代、模式与性能影响
date: 2026-07-18
category: 游戏开发
depth: 深度
layer: layer2
tags: [C#, GC, Unity, 内存优化, 垃圾回收, 对象池, StringBuilder]
summary: 系统介绍 .NET GC 的分代回收策略（Gen0/1/2）、工作站/服务器两种回收模式，以及游戏开发中的三个实战优化技巧：减少堆分配（struct优先 + 对象池）、用 StringBuilder 替代字符串拼接、在关卡切换时手动触发 GC——配合 Unity Profiler 定位 GC 瓶颈。
source_url: https://mp.weixin.qq.com/s/DupWHiip1uTNSrlUpasxDA
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** 系统介绍 .NET GC 的分代回收策略（Gen0/1/2）、工作站/服务器两种回收模式，以及游戏开发中的三个实战优化技巧：减少堆分配（struct优先 + 对象池）、用 StringBuilder 替代字符串拼接、在关卡切换时手动触发 GC——配合 Unity Profiler 定位 GC 瓶颈。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇文章最核心的价值不是讲 GC 原理本身，而是把 GC 行为翻译成了游戏开发者的直觉语言——「清洁工」隐喻非常有效：你扔的垃圾越多，清洁工越累；扔的方式越乱，收拾越费劲。

三个实用洞察：
1. **分代回收的本质是「热数据假设」**——大部分对象短命。这和 CPU 缓存的 LRU 策略同构：最近创建的最可能被回收，活得久的大概率继续活。理解了这一点，就知道为什么 struct 和对象池是 GC 友好的——它们让对象要么不存在于堆上，要么长期存活在 Gen2 不被扫描。
2. **强制 GC 的双 Collect 模式**（Collect → WaitForPendingFinalizers → Collect）值得记住。第一次回收触发 finalizer，第二次才真正释放。这和 C++ 的两阶段析构有异曲同工之处，但自动化了。
3. **技能系统的优化案例暴露了一个常见误区**：用临时 List 做批量移除只是转移了问题，Remove 还是 O(n)。真正的解法应该是对象池或标记-清扫，而不是在 Update 里做任何增删。

这篇文章和 2026-07-03 的 ArrayPool 优化技巧是互补关系——ArrayPool 讲的是数组层面的复用，这篇讲的是 GC 全局视角。

## 📌 关键要点

- **三代分代回收**：Gen0（新对象，回收最频繁）→ Gen1（缓冲区/中转）→ Gen2（长期存活对象）。回收频率逐代递减，Gen2 很少触发
- **两种 GC 模式**：Workstation GC（默认，适合桌面/游戏，不阻塞主线程）vs Server GC（多线程高吞吐，适合服务器）。Unity IL2CPP 下有差异但原理相通
- **减少堆分配（核心优化）**：优先用 struct（Vector3/Quaternion/Color），class 对象用对象池（子弹/粒子/特效），避免 Update 中 new
- **字符串拼接**：String 是 immutable，高频拼接产生大量 GC 垃圾 → 用 StringBuilder。日志系统尤其注意
- **强制 GC 时机**：场景切换时 `Resources.UnloadUnusedAssets()` + `GC.Collect()`（连续两次 Collect + WaitForPendingFinalizers）
- **性能评估工具**：dotnet-counters 监控 Gen0/1/2 回收频率；Unity Profiler Memory 面板定位 allocation 热点
- **Update 中避免增删**：频繁 RemoveAt 是 O(n) 操作，改用对象池或标记-清扫模式

## 原文

> 你有没有过这种感觉：明明代码写得挺规矩，但游戏跑着跑着就开始卡顿，内存占用像吹气球一样往上涨？
>
> 别急着怪 Unity 版本，也别急着骂电脑配置。这种"跑着跑着就变慢"的毛病，十有八九是 GC 在背后捣鬼。
>
> 今天咱们就来好好聊聊 C# 的垃圾回收机制，看看这位"清洁工"到底是怎么干活的，以及怎么让它少给你添乱。

### GC 是什么

咱们写的 C# 代码，对象都是 new 出来的。每次 new 一个对象，计算机会在内存里给它分配一块空间。等你用完了，这块空间得有人收回去，否则内存迟早被塞满。

手动管理内存的时代，程序员得自己操心这件事——写完一个对象就得记得 free 掉。忘了吗？内存泄漏。请错了？程序崩溃。当年 C/C++ 时代的"野指针"和"双份释放"，是多少程序员的噩梦。

后来有了自动内存管理，GC（Garbage Collection）就成了你的贴心小管家。你只管 new，对象用完不用管，GC 会自动识别哪些对象已经没人用了，然后把它们清理掉。

C# 和 Java 这类语言都是走的这条路。听起来很美好对吧？但问题在于——GC 不是免费的，它干活的时候，你的程序得暂停。

### 三代垃圾回收

.NET 的 GC 采用的是分代回收策略，把内存分成三代：第 0 代、第 1 代、第 2 代。

为什么要分代？因为经过大量实践发现：大部分对象都是短命的。

所以 GC 的策略是：**新创建的对象放在第 0 代，用得久的对象慢慢晋升到第 1 代、第 2 代。**

- **第 0 代**：存放新创建的对象，回收最频繁。每次 GC 首先检查这里，存活的对象晋升到下一代。
- **第 1 代**：存放经历过一次回收但还存活的对象。这是"缓冲区"——有些对象用完马上就没用了，有些会用久一点，第 1 代就像个中转站。
- **第 2 代**：存放长期存活的对象，比如游戏里的静态配置、场景资源管理器、音频系统等。

回收频率差异：第 0 代回收最频繁，第 1 代次之，第 2 代很少动。

### GC 的回收模式

**工作站模式（Workstation GC）**：默认模式，适合桌面应用和游戏。GC 回收时尽量不影响主线程，利用专门的线程来做清洁工作，程序可以继续跑。

**服务器模式（Server GC）**：多线程回收，吞吐量更高，但占用资源也更多。

在 Unity 的 IL2CPP 环境下，GC 行为和你直接写的 .NET 有些差异，但基本原理是相通的。

切换模式：
```csharp
GC_SETTINGS mode = GC_SETTINGS.GC_SETTINGS_SERVER_GC;
System.GC.TryStartNoGCRegion(1024 * 1024);
```

### 小技巧一：减少堆分配

**能用栈就别用堆**：

Vector3、Quaternion、Color 都是结构体（struct），栈上分配，不用 GC。但用 `new` 创建 class 对象就是堆分配了。

**对象池**：游戏里的子弹、粒子、特效，用对象池管理太重要了。

```csharp
public class ProjectilePool
{
    private readonly Stack<Projectile> _pool = new Stack<Projectile>();
    private readonly Projectile _prefab;

    public Projectile Get()
    {
        if (_pool.Count > 0)
            return _pool.Pop();
        return Object.Instantiate(_prefab);
    }

    public void Return(Projectile projectile)
    {
        projectile.gameObject.SetActive(false);
        _pool.Push(projectile);
    }
}
```

### 小技巧二：关注字符串拼接

字符串在 C# 里是 immutable 的，每次拼接都会创建新字符串。

```csharp
// ❌ 每次循环都 new 新字符串
string info = "";
for (int i = 0; i < 100; i++)
    info += "item" + i + ",";

// ✅ 用 StringBuilder
var sb = new StringBuilder();
for (int i = 0; i < 100; i++)
{
    sb.Append("item");
    sb.Append(i);
    sb.Append(",");
}
string info = sb.ToString();
```

日志系统特别容易犯这个毛病。高频循环里拼接会产生大量 GC 垃圾。

### 小技巧三：知道何时强制 GC

正常情况下不建议手动调用 `GC.Collect()`。但场景切换时有用：

```csharp
void OnLevelWasLoaded(int level)
{
    Resources.UnloadUnusedAssets();
    GC.Collect();
    GC.WaitForPendingFinalizers();
    GC.Collect();  // 第二次才真正释放 finalizer 相关的对象
}
```

连续调用两次 Collect 是因为有些对象需要两次回收才能彻底清理：第一次触发 finalizer，第二次才真正释放。

### 性能评估

- `dotnet-counters monitor --process-id <pid> System.Runtime` — 观察 Gen 0/1/2 回收频率和暂停时间
- Unity Profiler Memory 面板 — 查看 `GC Allocated` 随时间变化的曲线，定位 allocation 热点

### 实战例子：技能系统优化

```csharp
// ❌ Update 中频繁 RemoveAt（O(n)）
void Update()
{
    for (int i = _activeEffects.Count - 1; i >= 0; i--)
    {
        if (!_activeEffects[i].IsAlive())
        {
            Destroy(_activeEffects[i].gameObject);
            _activeEffects.RemoveAt(i);
        }
    }
}

// ✅ 批量移除 + 对象池（减少 GC 压力）
private readonly List<ParticleSystem> _toRemove = new List<ParticleSystem>();
void Update()
{
    foreach (var effect in _activeEffects)
        if (!effect.IsAlive())
            _toRemove.Add(effect);

    foreach (var effect in _toRemove)
    {
        _activeEffects.Remove(effect);
        Destroy(effect.gameObject);
    }
    _toRemove.Clear();
}
```

关键是：**不要在每帧执行的 Update 里做频繁的增删操作**。

## 相关笔记

- [[2026-07-03_CSharp ArrayPool数组池优化技巧]] — ArrayPool 作为 GC 友好的数组复用方案，与本文对象池思路互补
- [[2026-07-07_gpp-object-pool]] — 游戏编程模式：对象池模式（设计模式的 GC 友好实践）
- [[2026-05-18_ThreadLocalWeakReference内存泄漏]] — ThreadLocal 与弱引用的 GC 交互陷阱
