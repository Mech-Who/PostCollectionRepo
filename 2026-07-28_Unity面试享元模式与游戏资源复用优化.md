---
title: Unity面试系列：享元模式与游戏资源复用优化
date: 2026-07-28
category: 游戏开发
depth: 深度
layer: layer2
tags: [Unity, 享元模式, 设计模式, 内存优化, 资源复用, 对象池, 面试, 游戏开发]
summary: 享元模式解决的核心问题：当大量对象共享相同的"内在状态"（模型/贴图/动画），只有"外在状态"（位置/血量/状态）各不同时，如何高效复用资源。10000个士兵若每人独立加载资源需约340MB内存、40000次加载；用享元模式（内在状态提取为共享资源 + SharedResourceManager 单例缓存 + 引用计数）可压到约14KB、4次预加载。实际项目常配合对象池使用，粒子系统、字符串驻留（intern）是内置的享元应用。
source_url: https://mp.weixin.qq.com/s/wdhAa0ERet15keFjoopF9g
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** 享元模式（Flyweight）针对"大量对象共享相同内在状态、外在状态各异"的场景：把可共享的 3D 模型/贴图/骨骼动画提取为共享资源（内在状态），把位置/血量/等级/buff 等独有数据留在实例里（外在状态），由 SharedResourceManager 单例预加载并缓存，配合引用计数管理生命周期。对比反模式（每士兵独立 Resources.Load）：10000 士兵从 ~340MB/40000 次加载优化到 ~14KB/4 次预加载。实际项目必须配合对象池（士兵创建/复用），粒子系统、C# 字符串驻留都是享元的经典应用。

## 📌 关键要点

1. **内在状态 vs 外在状态**：内在状态=可共享、不随环境变化（模型、贴图、骨骼动画、特效、建筑几何）；外在状态=每实例独有、会变化（位置、朝向、血量、等级、buff、敌我阵营）。**内在状态存享元对象，外在状态由客户端管理**——这是享元模式的定义性切分。
2. **反模式识别**：每个 Soldier 实例自己 `Resources.Load` 4 次 → 10000 士兵 = 40000 次加载 + 10000 份相同资源，灾难级内存浪费（估算 ~340MB）。
3. **共享资源管理器三件套**：`Dictionary<string, SoldierSharedData> _sharedDataCache`（缓存）+ `Dictionary<string, int> _referenceCount`（引用计数）+ 预加载方法（`PreloadSharedResources`，游戏启动时调用一次）。获取时计数++，释放时计数--。
4. **渲染层关键 API**：必须用 `MeshFilter.sharedMesh` / `MeshRenderer.sharedMaterial`（共享实例），而不是 `mesh` / `material`（会实例化副本，享元失效）。
5. **享元 + 对象池是标准组合**：对象池解决"实例创建/销毁的 GC 压力"，享元解决"资源重复加载"，二者互补。SoldierFactory 从池中取实例 → 注入共享资源 → 设置外在状态。
6. **粒子系统是天然享元场景**：ParticlePrefab/材质/纹理共享，实例只改 startColor/startSize/duration/startDelay 等外在参数。
7. **字符串驻留（intern）是语言内置享元**：相同字面量字符串共享引用（`ReferenceEquals(a,b)==true`），new 出来的字符串则不复用。可预生成常用伤害数字文本数组做缓存。
8. **面试应答模板**：适用条件（大量共享内在数据+内存紧张+可分离状态）、三结构（Flyweight/FlyweightFactory/Client）、优缺点（省内存/少加载/支持更多对象 vs 增加复杂度/需分离状态/需工厂管理）、项目经验话术。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇是小涵正在学的 GameDev101 设计模式体系（游戏循环/组件/命令/对象池等已归档）的又一环——**享元模式**，且正好和已入库的《对象池》模式形成姊妹篇：

1. **享元是"静态维度"的资源复用，对象池是"动态维度"的实例复用**：享元砍的是"重复资源"（同一份模型被加载 10000 次），对象池砍的是"反复创建销毁"（GameObject 频繁 Instantiate/Destroy 的 GC 压力）。两者组合才是完整的"百万单位"方案——这解释了为什么文中强调"享元通常配合对象池使用"。
2. **「共享资源必须用 sharedMesh/sharedMaterial」是全文最容易被忽略的技术点**：如果写成 `renderer.material`，Unity 会自动实例化一份材质副本，享元立即失效且产生隐藏内存泄漏。这是实战中 90% 的人踩的坑，面试答出来是加分项。
3. **对 ECS/自研框架的启发**：小涵正在推进自定义 ECS 架构——ECS 本身就是享元思想的极致版：组件数据（外在状态）连续存储，共享的 Mesh/Material 是内在状态，系统只处理数据。可以把这个认识串起来：**面向数据设计（DOD）= 享元模式在架构层的升级**。
4. **数字锚点很有说服力**：340MB → 14KB（约 24000 倍差距）、40000 次加载 → 4 次。面试时能给具体数字的候选者明显更专业。

**关联知识**：可挂到「game-内容架构设计工程」概念（设计模式语言体系），与已入库的命令/观察者/对象池/空间分区等模式并列。

## 原文

# Unity面试系列之享元模式与游戏资源复用优化

# 面试系列041：享元模式与游戏资源复用优化

面试的时候，我问候选者："一个SLG游戏里，有10000个士兵单位，每个士兵都有模型、贴图、动画。这些资源会占用多少内存？怎么优化？"

有的候选者会说："每个士兵占10MB，那10000个就是100GB，太夸张了。"

我再问："但实际上10000个士兵的模型是一样的，区别只是位置、血量、状态。你怎么利用这一点来节省内存？"

他想了一会儿，说："所有士兵共用同一个模型对象？"

我说："对，但怎么做？要注意士兵的位置、血量这些数据是各不相同的。"

他开始犯难。

这就是享元模式要解决的问题——**当大量对象共享相同的"内在状态"（Intrinsic State），只有"外在状态"（Extrinsic State）各不同时，如何高效地复用资源。**

## 区分内在状态和外在状态

在谈享元模式之前，先理解两种状态的区分：

- **内在状态（Intrinsic）**：可以共享的、不随环境变化的
  - 士兵的3D模型、贴图、骨骼动画
  - 武器的外观、特效
  - 建筑的几何数据
- **外在状态（Extrinsic）**：每个对象独有的、会变化的
  - 士兵的位置、朝向
  - 士兵的血量、等级、buff
  - 士兵是友军还是敌军

**内在状态存储在享元对象里，外在状态由客户端管理。**

## 先看常见的资源加载方式

```
// 反模式：每个士兵都加载自己的资源
public class Soldier : MonoBehaviour
{
    // 每个Soldier实例都持有一份资源
    public GameObject ModelPrefab;
    public Material BodyMaterial;
    public Material ArmorMaterial;
    public RuntimeAnimatorController AnimController;
    
    // 这些是外在状态
    public int Health;
    public Vector3 Position;
    public bool IsEnemy;
    
    void Start()
    {
        // 加载自己的资源
        ModelPrefab = Resources.Load<GameObject>("Prefabs/Soldier");
        BodyMaterial = Resources.Load<Material>("Materials/SoldierBody");
        ArmorMaterial = Resources.Load<Material>("Materials/SoldierArmor");
        AnimController = Resources.Load<RuntimeAnimatorController>(
            "Animations/SoldierController");
        
        // 实例化模型
        var model = Instantiate(ModelPrefab, transform);
        model.GetComponent<MeshRenderer>().material = BodyMaterial;
        model.GetComponent<Animator>().runtimeAnimatorController = AnimController;
    }
}
```

问题在哪？

假设有10000个士兵：

- 每个士兵都调用 `Resources.Load` 4次 = 40000次资源加载
- 每个士兵都实例化一套资源对象
- 内存中可能有10000份相同的模型、贴图、动画
- 这简直是灾难级别的内存浪费

## 享元模式的实现

### 第一步：提取内在状态为共享资源

```
/// <summary>
/// 士兵的共享资源（内在状态）
/// </summary>
[System.Serializable]
public class SoldierSharedData
{
    // 可以直接引用的预制体
    public GameObject Prefab;
    
    // 可以复用的组件
    public Mesh BodyMesh;
    public Material BodyMaterial;
    public Material ArmorMaterial;
    public RuntimeAnimatorController AnimController;
    
    // 动画数据
    public AnimationClip IdleClip;
    public AnimationClip WalkClip;
    public AnimationClip AttackClip;
    public AnimationClip DeathClip;
}
```

### 第二步：资源预加载管理器

```
/// <summary>
/// 资源池管理器：负责预加载和共享资源
/// </summary>
public class SharedResourceManager
{
    // 单例
    public static SharedResourceManager Instance { get; private set; }
    
    // 共享资源字典
    private readonly Dictionary<string, SoldierSharedData> _sharedDataCache = new();
    
    // 引用计数
    private readonly Dictionary<string, int> _referenceCount = new();
    
    public SharedResourceManager()
    {
        Instance = this;
    }
    
    /// <summary>
    /// 预加载共享资源（游戏启动时调用）
    /// </summary>
    public void PreloadSharedResources()
    {
        LoadSoldierSharedData("Soldier");
        LoadSoldierSharedData("EliteSoldier");
        LoadSoldierSharedData("Cavalry");
        // ...
    }
    
    private void LoadSoldierSharedData(string soldierType)
    {
        var data = new SoldierSharedData();
        
        // 只加载一次
        data.Prefab = Resources.Load<GameObject>($"Prefabs/{soldierType}");
        data.BodyMaterial = Resources.Load<Material>($"Materials/{soldierType}Body");
        data.ArmorMaterial = Resources.Load<Material>($"Materials/{soldierType}Armor");
        data.AnimController = Resources.Load<RuntimeAnimatorController>(
            $"Animations/{soldierType}Controller");
        
        // 动画片段
        data.IdleClip = Resources.Load<AnimationClip>($"Animations/{soldierType}_Idle");
        data.WalkClip = Resources.Load<AnimationClip>($"Animations/{soldierType}_Walk");
        data.AttackClip = Resources.Load<AnimationClip>($"Animations/{soldierType}_Attack");
        data.DeathClip = Resources.Load<AnimationClip>($"Animations/{soldierType}_Death");
        
        _sharedDataCache[soldierType] = data;
        _referenceCount[soldierType] = 0;
        
        Debug.Log($"预加载 {soldierType} 共享资源完成");
    }
    
    /// <summary>
    /// 获取共享数据
    /// </summary>
    public SoldierSharedData GetSharedData(string soldierType)
    {
        if (_sharedDataCache.TryGetValue(soldierType, out var data))
        {
            _referenceCount[soldierType]++;
            return data;
        }
        
        Debug.LogError($"未找到共享数据: {soldierType}");
        return null;
    }
    
    /// <summary>
    /// 释放引用
    /// </summary>
    public void ReleaseReference(string soldierType)
    {
        if (_referenceCount.ContainsKey(soldierType))
        {
            _referenceCount[soldierType]--;
        }
    }
}
```

### 第三步：优化后的士兵类

```
/// <summary>
/// 士兵实例（外在状态）
/// </summary>
public class SoldierInstance : MonoBehaviour
{
    // 引用共享资源（所有实例共享）
    private SoldierSharedData _sharedData;
    
    // 外在状态：每个士兵都不同
    public int InstanceId { get; set; }
    public int Health { get; set; }
    public int MaxHealth { get; set; }
    public Vector3 Position { get; set; }
    public Vector3 Forward { get; set; }
    public bool IsEnemy { get; set; }
    public int Level { get; set; }
    public List<BuffData> ActiveBuffs { get; set; } = new();
    
    // 渲染组件（每个实例都需要）
    private MeshFilter _meshFilter;
    private MeshRenderer _meshRenderer;
    private Animator _animator;
    
    /// <summary>
    /// 初始化（传入共享资源）
    /// </summary>
    public void Initialize(SoldierSharedData sharedData)
    {
        _sharedData = sharedData;
        
        // 获取或添加渲染组件
        _meshFilter = GetComponent<MeshFilter>();
        if (_meshFilter == null)
            _meshFilter = gameObject.AddComponent<MeshFilter>();
            
        _meshRenderer = GetComponent<MeshRenderer>();
        if (_meshRenderer == null)
            _meshRenderer = gameObject.AddComponent<MeshRenderer>();
            
        _animator = GetComponent<Animator>();
        if (_animator == null)
            _animator = gameObject.AddComponent<Animator>();
        
        // 共享资源赋值（Mesh、Material、Controller都是共享的）
        _meshFilter.sharedMesh = _sharedData.BodyMesh;
        _meshRenderer.sharedMaterial = _sharedData.BodyMaterial;
        _animator.runtimeAnimatorController = _sharedData.AnimController;
    }
    
    void Update()
    {
        // 应用外在状态到渲染
        transform.position = Position;
        transform.forward = Forward;
        
        // 播放对应动画
        UpdateAnimation();
    }
    
    private void UpdateAnimation()
    {
        if (Health <= 0)
        {
            _animator.Play(_sharedData.DeathClip.name);
        }
        else if (Velocity.magnitude > 0.1f)
        {
            _animator.Play(_sharedData.WalkClip.name);
        }
        else
        {
            _animator.Play(_sharedData.IdleClip.name);
        }
    }
    
    public void TakeDamage(int damage)
    {
        Health -= damage;
        // ...
    }
}
```

## 对象池 + 享元模式

在实际项目中，享元模式通常配合对象池使用：

```
/// <summary>
/// 士兵工厂：创建和复用士兵实例
/// </summary>
public class SoldierFactory
{
    private readonly SharedResourceManager _resourceManager;
    private readonly ObjectPool<SoldierInstance> _soldierPool;
    private int _instanceIdCounter = 0;
    
    public SoldierFactory(SharedResourceManager resourceManager)
    {
        _resourceManager = resourceManager;
        
        // 创建对象池
        _soldierPool = new ObjectPool<SoldierInstance>(
            createFunc: () => CreateNewSoldier(),
            actionOnGet: soldier => soldier.gameObject.SetActive(true),
            actionOnRelease: soldier => soldier.gameObject.SetActive(false)
        );
    }
    
    private SoldierInstance CreateNewSoldier()
    {
        var go = new GameObject("Soldier");
        var soldier = go.AddComponent<SoldierInstance>();
        return soldier;
    }
    
    /// <summary>
    /// 从池中获取士兵
    /// </summary>
    public SoldierInstance GetSoldier(string soldierType, Vector3 position, bool isEnemy)
    {
        var soldier = _soldierPool.Get();
        
        // 获取共享资源
        var sharedData = _resourceManager.GetSharedData(soldierType);
        soldier.Initialize(sharedData);
        
        // 设置外在状态
        soldier.InstanceId = ++_instanceIdCounter;
        soldier.Position = position;
        soldier.IsEnemy = isEnemy;
        soldier.Health = soldier.MaxHealth;
        
        return soldier;
    }
    
    /// <summary>
    /// 归还士兵到池中
    /// </summary>
    public void ReleaseSoldier(SoldierInstance soldier)
    {
        // 清理外在状态
        soldier.ActiveBuffs.Clear();
        
        // 标记为不活跃
        _soldierPool.Release(soldier);
    }
}
```

## 粒子系统的享元优化

粒子系统是享元模式的经典应用场景：

```
/// <summary>
/// 粒子效果的共享配置
/// </summary>
[System.Serializable]
public class ParticleSharedConfig
{
    // 共享的粒子预设
    public ParticleSystem ParticlePrefab;
    
    // 共享的渲染材质
    public Material ParticleMaterial;
    
    // 共享的纹理
    public Texture2D ParticleTexture;
}

/// <summary>
/// 粒子实例：只管理外在状态
/// </summary>
public class ParticleInstance
{
    private ParticleSharedConfig _sharedConfig;
    private ParticleSystem _particleSystem;
    
    // 外在状态
    public Vector3 Position { get; set; }
    public float StartDelay { get; set; }
    public float Duration { get; set; }
    public Color StartColor { get; set; }
    public float StartSize { get; set; }
    
    public void Initialize(ParticleSharedConfig config)
    {
        _sharedConfig = config;
        
        // 实例化粒子系统（但材质、纹理是共享的）
        _particleSystem = Object.Instantiate(config.ParticlePrefab, Position, Quaternion.identity);
        
        // 只修改粒子系统的不共享参数
        var main = _particleSystem.main;
        main.startColor = StartColor;
        main.startSize = StartSize;
        main.duration = Duration;
        main.startDelay = StartDelay;
    }
    
    public void Play()
    {
        _particleSystem.Play();
    }
    
    public void Stop()
    {
        _particleSystem.Stop();
    }
}
```

## 字符串驻留：内置的享元模式

C#的字符串也是享元模式的典型应用：

```
// 字符串驻留：相同内容的字符串共享内存
string a = "Hello";
string b = "Hello";
string c = new string(new[] { 'H', 'e', 'l', 'l', 'o' });

Console.WriteLine(a == b);  // True（引用相同）
Console.WriteLine(a == c);  // True（值相等）
Console.WriteLine(ReferenceEquals(a, b));  // True（同一引用）
Console.WriteLine(ReferenceEquals(a, c));  // False（c是新分配的）

// 在游戏开发中的应用
public class DamageNumber
{
    // 使用字符串驻留来复用"伤害"文本对象
    private static readonly string[] _cachedDamageTexts = new string[1000];
    
    static DamageNumber()
    {
        // 预生成常用的伤害数字
        for (int i = 0; i < 1000; i++)
        {
            _cachedDamageTexts[i] = i.ToString();
        }
    }
    
    public static string GetDamageText(int damage)
    {
        if (damage >= 0 && damage < 1000)
        {
            return _cachedDamageTexts[damage];
        }
        return damage.ToString();
    }
}
```

## 内存优化效果对比

| 项目 | 反模式 | 享元模式 |
|------|--------|----------|
| 10000士兵的Mesh | 10000份（~200MB） | 1份（~20KB） |
| 10000士兵的Material | 10000份（~40MB） | 1份（~4KB） |
| 10000士兵的Animation | 10000份（~100MB） | 1份（~10KB） |
| 总计 | ~340MB | ~14KB |
| 资源加载次数 | 40000次 | 4次（预加载） |

## 面试应答要点

**1. 什么时候用享元模式？**

- 大量对象共享相同的内在数据
- 对象数量非常大，内存紧张
- 可以分离内在状态和外在状态
- 需要配合对象池使用

**2. 享元模式的核心结构？**

- Flyweight（享元）：包含内在状态的类
- FlyweightFactory（享元工厂）：管理和复用享元对象
- Client（客户端）：管理外在状态

**3. 内在状态 vs 外在状态？**

- 内在状态：可共享、不随环境变化（模型、贴图、配置）
- 外在状态：每个实例独有、随环境变化（位置、血量、buff）

**4. 享元模式的优缺点？**

- 优点：大幅节省内存、减少资源加载次数、支持更多对象
- 缺点：增加代码复杂度、需要分离状态、增加工厂管理

**5. 实际项目经验怎么说？**

- "我们SLG游戏的单位系统用了享元模式，10000个同屏单位共用一套模型贴图"
- "粒子系统也是享元模式，同一个特效的材质和纹理是共享的"
- "UI图集用的也是类似思想，把小图标合并到大图上复用"

* * *

**好的享元模式设计让你在有限的内存里装下无限的可能。**
