---
title: CRTP奇异递归模板模式与编译期多态
date: 2026-08-02
category: 游戏开发
depth: 深度
layer: layer2
tags: [CRTP, C++, 设计模式, 模板元编程, 静态多态, 性能优化, Mixin]
source_article: 2026-08-02_Cpp聊聊模板模式CRTP奇异递归模板模式
---

# K-Fragment: CRTP奇异递归模板模式与编译期多态

## A. Applicable — 适用场景

- C++ 热路径性能优化：确定不需要运行时动态绑定时，用 CRTP 干掉虚函数（vtable 查找 + 间接寻址 + 缓存未命中）
- 游戏引擎/底层库中"固定流程 + 局部变化"的场景——模板方法模式的编译期版本（基类管骨架，派生类管细节）
- 需要"静态接口约束"：派生类漏实现时编译期直接报错，而非运行时崩溃（插件系统、接口契约）
- 策略模式改模板参数注入：编译期确定策略，免指针、免内存分配（排序/渲染后端等底层库）
- 每个派生类独立静态计数（对象池、资源管理、实例统计）
- Mixin 能力组合：绕开多重继承菱形问题，像搭积木一样组合 Serializable/Cloneable 等能力
- Builder 链式调用保持子类类型：`builder.setName("x").setAge(25)` 不丢失类型（DSL 设计神器）

## B. Boundary — 边界与注意事项

- **滥用代价**：模板报错会变得像天书一样难懂，调试复杂度显著上升——编译期确定性的代价
- **只适用编译期已知类型**：需要运行时多态（容器存异质对象、插件动态加载）时不能替代虚函数
- **代码膨胀**：每个派生类实例化一份基类代码，模板实例数量多时二进制体积上升
- **性能收益有前提**：只有在高频调用/热路径上，消除 vtable 开销才有意义；低频调用场景收益可忽略
- **与虚函数的关系**：CRTP 消除的是"运行时动态绑定"这一层抽象，如果确实需要运行时灵活性（插件、动态库、异质集合），虚函数仍然是正确选择——先判断"是否需要动态绑定"再选
- 类型安全与性能是"手术刀"不是"万能锤"：`static_cast<Derived*>` 依赖派生类正确传参，用错类型会 UB

## C. Core — 核心要点

1. **核心一句**：派生类把自己作为模板参数传给基类——`class Child : public Base<Child>`，基类通过 `static_cast<Derived*>(this)` 向下转型调用派生类实现
2. **本质**：把运行时多态（vtable）搬到编译期——static_cast 在编译期完成类型转换，零运行时开销
3. **静态接口约束**：派生类漏实现 run() 时编译期直接报错——把错误的发现时间从运行时提前到编译期
4. **七大场景速记**：①榨性能（去 vtable）②骨架复用（编译期模板方法）③接口强制（编译期报错）④策略注入（模板参数免指针）⑤独立计数（模板类型不同→静态成员独立）⑥Mixin 组合（避开菱形继承）⑦Builder 类型安全（链式返回 Derived&）
5. **取舍本质**：用运行时的灵活性换编译期的确定性——确定不需要动态绑定时才用，性能敏感 + 类型安全要求高的底层架构最受益

## D. Data — 可复用代码骨架

```cpp
// ① 核心骨架：CRTP 基类（静态多态）
template <typename Derived>
class Base {
public:
    void log(const std::string& msg) {
        // 编译期向下转型，零开销（无 virtual）
        static_cast<Derived*>(this)->logImpl(msg);
    }
};
class FileLogger : public Base<FileLogger> {
public:
    void logImpl(const std::string& msg) { /* 写文件 */ }
};

// ② 编译期模板方法（骨架复用）
template <typename Derived>
class DataProcessor {
public:
    void process() {
        static_cast<Derived*>(this)->validate();
        static_cast<Derived*>(this)->transform();
        static_cast<Derived*>(this)->save();
    }
};

// ③ 静态接口约束（漏实现编译报错）
template <typename Derived>
class Plugin {
public:
    void execute() { static_cast<Derived*>(this)->run(); } // 没实现 run() 编译不过
};
class MyPlugin : public Plugin<MyPlugin> {
public:
    void run() { /* 插件逻辑 */ }
};

// ④ 策略模板参数注入（免指针免分配）
template <typename T, typename SortStrategy>
class Container {
    std::vector<T> data_;
    SortStrategy sorter_;
public:
    void sort() { sorter_.sort(data_); }
};
Container<int, QuickSort<int>> c;  // 策略编译期确定

// ⑤ 派生类独立计数（对象池/资源管理）
template <typename Derived>
class Counter {
    static int count_;
public:
    Counter() { ++count_; }
    ~Counter() { --count_; }
    static int getCount() { return count_; }
};
template <typename Derived> int Counter<Derived>::count_ = 0;
class User : public Counter<User> {};
class Order : public Counter<Order> {};
// User::getCount() 与 Order::getCount() 完全独立

// ⑥ Mixin 能力组合（避开菱形继承）
template <typename Derived>
class Serializable {
public:
    std::string serialize() { return static_cast<Derived*>(this)->toJson(); }
};
class Entity : public Serializable<Entity>, public Cloneable<Entity> {
public:
    std::string toJson() { return "{}"; }
};

// ⑦ Builder 链式调用保类型（DSL 设计）
template <typename Derived>
class BuilderBase {
public:
    Derived& setName(const std::string& name) {
        return static_cast<Derived&>(*this);  // 返回派生类类型而非 BuilderBase
    }
};
class UserBuilder : public BuilderBase<UserBuilder> {
public:
    UserBuilder& setAge(int age) { return *this; }
};
UserBuilder b;
b.setName("Alice").setAge(25);  // 类型不丢失
```
