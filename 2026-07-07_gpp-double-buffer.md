---
title: "双缓冲模式 — 让世界看起来同步更新"
date: 2026-07-07
category: 游戏开发
depth: 深度
layer: layer2
tags: [双缓冲, 渲染, 帧缓冲, 图形API, 状态同步, GPU交换链]
summary: "双缓冲模式通过维护'当前'和'下一'两个缓冲区，在完成修改后原子交换，使外部观察者看到的是完整一致的快照。在图形渲染中是标配，也适用于需要同步更新的游戏逻辑。"
source_url: https://gpp.tkchu.me/double-buffer.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 双缓冲是每个游戏引擎的底层基础。从最经典的图形帧缓冲（避免画面撕裂），到游戏逻辑中的"扇巴掌"问题（保证所有角色同时看到同帧状态），双缓冲用最简单的"两个缓冲区+一次交换"解决了顺序计算机上的同步更新问题。

## 我的理解

> 双缓冲模式展示了如何用极简的机制解决根本性的并发问题。计算机是顺序执行的，但游戏世界需要"同时发生"——渲染需要整帧一起出现，角色需要同时互相同步反应。双缓冲的答案优雅得令人惊叹：写到一个缓冲区，读到另一个，交换就是原子操作。

> "扇巴掌"喜剧演员的例子是全书最精妙的教学案例之一。三个角色环形互扇，更新顺序不同导致完全不同的结果——先更新的人看到的是旧状态，后更新的人看到的是新状态。双缓冲通过"先全更新，再全交换"完美解决了这个顺序依赖问题。

> 两种交换策略的讨论非常实用：交换指针（O(1)、不能被外部持有指针）vs 拷贝数据（O(n)、数据只差一帧）。在图形渲染中交换指针是标配（GPU直接读显存地址），而在游戏逻辑中拷贝布尔状态更合适（数据量小）。

> 那个巧妙的静态偏移量优化（current_从0/1切换，所有角色共享同一个current_索引）展示了批量状态交换的极致优雅——一次静态变量修改，所有实例的状态全部切换。

## 📌 关键要点
- **核心机制**：两个缓冲区（当前读、下一写）→ 修改完成 → 原子交换
- **图形渲染**：避免画面撕裂——GPU始终读完整帧，渲染器写另一缓冲
- **逻辑同步**：先更新所有对象，再统一交换状态 → 消除更新顺序依赖
- **交换策略**：指针交换（O(1)，经典图形方案）vs 数据拷贝（O(n)，数据只差一帧）
- **静态偏移量**：所有实例共享current_索引 → 一次修改切换所有对象状态
- **使用场景**：状态被增量修改 AND 修改中途可能被外部访问 AND 想隐藏内部工作

## 原文

### 图形双缓冲
帧缓冲A用于显示（GPU读取），渲染代码写入帧缓冲B → 渲染完成 → swap交换指针。GPU永远只读到完整的一帧。

### 逻辑双缓冲（扇巴掌例子）
每个角色有两个slapped_状态：currentSlapped_（读）和nextSlapped_（写）。先update()所有角色（写next），再swap()所有角色（next→current）。消除角色在数组中的排列顺序对结果的影响。
