---
title: 美末2贴墙检测系统复刻 — UE5.8 Actor组件化实现拆解
date: 2026-08-21
category: 游戏开发
depth: 标准
layer: layer2
tags: [UE5, 贴墙检测, 角色移动, ActorComponent, 蓝图, 组件化, TheLastOfUs, 游戏手感]
summary: 国外博主 Hydra 复刻《最后生还者2》的贴墙检测系统：面向墙中心时移动输入归零（停止），看向两侧越过墙中心限制时自动平滑沿墙滑行，同时支持 W+S / A+D 相反输入抵消停止。核心是 Actor Component + Enum 数据分离的可插拔架构，参数外露到角色蓝图，一个组件即可迁移到 CMC 和 Mover 两种角色。
source_url: https://www.bilibili.com/video/BV1ngKj6AEGo
source: bilibili
video_duration: 12:08
status: 📥已采集
sync_si: ✅已同步(2026-08-21)
---

> **摘要：** 博主 Hydra 观察《最后生还者2》的贴墙手感后，用 UE5.8 复刻了这套系统。核心逻辑三层：(1) 角色正对墙中心 → 移动输入归零，完全停止；(2) 视角略微偏斜（越过墙中心限制）→ 对应方向移动自动开启，平滑沿墙滑行；(3) 相反输入同时按下（W+S / A+D）→ 停止移动。工程上用一个 Actor Component + Enum 数据分离的文件夹结构实现 plug-and-play，参数外露到角色蓝图，同一组件即可驱动 CMC（CharacterMovementComponent）角色和 Mover 角色。

## 我的理解
> 由小林生成，供小涵审阅修改

这是**"手感机制"如何被工程化封装**的一个好样本。贴墙检测看似是移动逻辑，Hydra 把它做成"组件 + 数据枚举"的可插拔结构，触及了几个可复用的设计原则：

1. **手感 = 状态机，不是数值调参**：贴墙滑行不是"摩擦系数调大点"能出来的。它的本质是"朝向 vs 墙中心"的三态判定（停止 / 左滑 / 右滑），外加"相反输入抵消"这种独立规则。手感机制要先抽象成明确的状态转换，再谈实现。

2. **可插拔 = 数据与逻辑分离**：文件夹结构只有两个东西——Actor Component（逻辑）和 Enum 数据（配置）。参数（trace 距离、中心限制角度等）外露到角色蓝图的 Details 面板，改参数不用打开组件。这是依赖注入/配置外置思想在蓝图里的朴素落地。

3. **迁移成本是最小化的设计目标**：整套系统"选中主文件夹 → Migrate → 勾依赖 → 落到新项目 Content"即可复用，连迁移路径都被设计进去了。这提醒我们：**组件的价值 = 逻辑正确性 × 迁移便捷性**，后者往往被忽略。

与既有知识的连接：
- `软件设计架构`：组件化 + 数据逻辑分离，是"单一职责 + 配置外置"的 UE 落地
- `game-内容架构设计工程`：手感机制作为内容原子，其"验证标准"是"贴墙滑动是否丝滑、是否可随意集成到任意角色"

## 视频内容要点

### 1. 贴墙检测核心逻辑（三态）
| 状态 | 触发条件 | 结果 |
|------|---------|------|
| 停止 | 角色正对墙中心 | 移动输入归零 |
| 左/右滑 | 视角越过墙中心限制（偏斜） | 对应方向移动开启，平滑沿墙滑 |
| 抵消停止 | 相反输入同时按下（W+S / A+D） | 停止移动 |

- 支持 **strafe（侧移）**：capsule trace 基于输入 + 控制旋转，横向移动同样生效

### 2. 工程结构（可插拔）
```
WallDetectionSystem/
├── ActorComponent/   # 逻辑：SetMovementInputFunction + capsule trace
└── Enum/             # 数据：参数枚举
```
- **参数外露**：trace 距离、中心限制角度等放到角色蓝图 Details 面板可调，无需打开组件
- **迁移**：选中主文件夹 → Migrate → 勾依赖 → 落到新项目 Content 即可

### 3. 接入两种角色
| 角色类型 | 接入方式 |
|---------|---------|
| CMC 角色 | 删旧移动逻辑 → 挂组件 → MoveInput 节点 → 调 SetMovementInputFunction |
| Mover 角色 | getMoveInput（纯函数）内删逻辑保留 IEMoveNode → 挂组件 → 函数返回值直连 return 节点 |

### 4. Debug 可视化
- capsule trace 可视化：输入移动即 casting，trace 从角色出发，方向由输入+控制旋转决定

## 提炼给开发者的行动点
1. **手感机制先画状态图**：贴墙/攀爬/冲刺取消这类手感，先用"状态 + 触发条件"画清楚，再实现
2. **组件即产品**：做功能时默认"它未来要迁移到别的项目"，把逻辑与数据分文件夹、参数外露
3. **相反输入抵消**是独立规则，可单独拆出用于任何移动系统（防误触）
4. **复用别人成熟手感**：与其自己调参，不如拆解成熟游戏（美末2/塞尔达）的手感状态机

## 局限与提醒
- 视频为 UE 复刻教程，非官方方案；生产项目需自行适配碰撞/墙角特殊情况
- 参数枚举外露是好实践，但枚举过多会退回"改参数要翻文档"的老路，需克制

---
transcript_file: videos/BV1ngKj6AEGo/transcript.txt
whisper_model: medium
language: English
