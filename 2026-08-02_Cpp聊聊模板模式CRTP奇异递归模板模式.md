---
title: Cpp聊聊模板模式CRTP奇异递归模板模式
date: 2026-08-02
category: 游戏开发
depth: 深度
layer: layer2
tags: [CRTP, 设计模式, C++, 模板元编程, 静态多态, 性能优化]
summary: CRTP（奇异递归模板模式）通过「派生类把自己作为模板参数传给基类」，把运行时多态搬到编译期——零虚函数开销、静态接口约束、策略/能力以模板参数注入。文章用 7 个实战场景（性能、骨架复用、接口强制、策略注入、独立计数、Mixin、Builder 类型安全）展示了它在性能敏感底层架构中的价值，核心取舍是「用运行时的灵活性换编译期的确定性」。
source_url: https://mp.weixin.qq.com/s/aZdPj6r8hX8VWkn9C0eGaw
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** CRTP（奇异递归模板模式）让派生类把自身作为模板参数传给基类，将运行时多态（vtable 间接调用）搬移到编译期（static_cast 零开销）。文章通过 7 个实战场景——榨干性能、骨架复用、静态接口约束、策略注入、独立计数、Mixin 组合、Builder 类型安全——展示其作为"编译期手术刀"的价值；代价是模板报错如天书，适用面是性能敏感、类型安全要求高的底层架构。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇文章表面在讲 CRTP 的 7 个玩法，实质在揭示一个更深的规律：**设计模式是语言能力边界的脚手架，语言越弱、模式越厚**（与「软件设计架构」概念洞察八完全同构）。

CRTP 在 C++ 中解决的是其他语言"内建"的能力缺口：
- **静态接口约束**（场景三）→ 其他语言有 interface/trait/协议，C++ 只能用模板 + 编译错误模拟"编译期鸭子类型"
- **Mixin 组合**（场景六）→ 其他语言有 trait 组合，C++ 用它绕开多重继承的菱形问题
- **策略注入**（场景四）→ 其他语言闭包/一等函数天然支持，C++ 把策略做成模板参数

这正好回应了 Q177 的疑问——装饰器模式在 C# 中弥补 trait/组合缺失，而 CRTP 是 C++ 世界同一种"补偿"思路的编译期版本。两者的分界不是"哪个模式更好"，而是**语言的虚函数成本决定了多态应该发生在运行时还是编译期**。

另一个洞察：CRTP 的"编译期确定性"本质上是**把错误的发现时间从运行时提前到编译期**。虚函数漏实现是运行时崩，CRTP 漏实现是编译报错——这与架构三定律的"故障驱动"思想呼应：能早暴露的错误绝不放行到线上。

## 📌 关键要点
- **核心方法**：`template <typename Derived> class Base` + 派生类 `class Child : public Base<Child>`，基类通过 `static_cast<Derived*>(this)` 向下转型调用派生类实现——类型转换在编译期完成，无 vtable 查找、无指针间接寻址
- **性能收益**：高频调用场景下虚函数开销（vtable 查找 + 间接寻址 + CPU 缓存未命中）被消除，这是热路径优化的经典手段
- **静态接口约束**：派生类漏实现时编译期直接报错，把错误拦截在编译期，避免运行时崩溃/上线事故
- **适用场景**：性能敏感（游戏引擎热路径、底层库）、类型安全要求高、需要 Mixin 组合避开菱形继承、需要 Builder 链式调用保持子类类型
- **滥用代价**：模板报错会变得像天书一样难懂，编译期确定性换来的是调试复杂度上升
- **取舍本质**：用运行时的灵活性换编译期的确定性——确定不需要动态绑定时才用

## 原文

**聊聊C++ 里"模板模式"**

每次做性能压测，看到虚函数表（vtable）的开销，我都忍不住想吐槽。

明明逻辑很清晰，但为了那点"运行时多态"的灵活性，硬生生插进了一层间接调用。在热路径上，这玩意儿简直是性能杀手。

这时候，老架构师往往会拍拍你的肩膀："试试 CRTP 吧。"

CRTP，全称 Curiously Recurring Template Pattern（奇异递归模板模式）。名字听着唬人，其实核心就一句话：**派生类把自己作为模板参数传给基类**。

```cpp
template <typename Derived>
class Base {
    // 基类可以通过 Derived 类型直接访问派生类
};
class Child : public Base<Child> {
    // 把自己传上去
};
```

就这几行代码，把运行时多态搬到了编译期。今天咱们不扯那些干巴巴的理论，直接看 7 个实战场景，看看这招到底能怎么玩。

### 场景一：干掉虚函数，榨干性能

这是 CRTP 最经典的用法。如果你确定不需要运行时的动态绑定，那就把虚函数干掉。

```cpp
// 基类定义接口骨架
template <typename Derived>
class Logger {
public:
    // 注意这里没有 virtual
    void log(const std::string& msg) {
        // 编译期直接向下转型，零开销
        static_cast<Derived*>(this)->logImpl(msg);
    }
};

// 派生类实现具体逻辑
class FileLogger : public Logger<FileLogger> {
public:
    void logImpl(const std::string& msg) {
        // 写入文件...
    }
};
```

妙在哪？static_cast在编译期就完成了类型转换。没有 vtable 查找，没有指针间接寻址，CPU 缓存命中率飙升。在高频调用的场景下，这比虚函数快得不是一星半点。

### 场景二：把通用逻辑"抽"到基类

有时候，一堆派生类都有相同的流程，只是中间某一步不一样。用CRTP，你可以把流程固化在基类里。

```cpp
template <typename Derived>
class DataProcessor {
public:
    void process() {
        // 固定的处理骨架
        static_cast<Derived*>(this)->validate();
        static_cast<Derived*>(this)->transform();
        static_cast<Derived*>(this)->save();
    }
};

class JsonProcessor : public DataProcessor<JsonProcessor> {
public:
    void validate() { /* 校验 JSON 格式 */ }
    void transform() { /* 转换数据结构 */ }
    void save() { /* 写入数据库 */ }
};
```

这其实就是"模板方法模式"的编译期版本。基类管流程，派生类管细节，代码复用得干干净净。

### 场景三：编译期"强制"实现接口

用虚函数，你忘了重写，编译器可能只是给个警告，或者运行时直接崩。用CRTP，忘了实现？编译直接报错。

```cpp
template <typename Derived>
class Plugin {
public:
    void execute() {
        // 如果 Derived 没实现 run()，这里编译期直接报错
        static_cast<Derived*>(this)->run();
    }
};

class MyPlugin : public Plugin<MyPlugin> {
public:
    // 必须实现 run，否则编译不过
    void run() {
        // 插件逻辑
    }
};
```

这就是所谓的"静态接口约束"。把错误拦截在编译期，总比上线后半夜被电话叫醒强。

### 场景四：把策略当参数传进来

策略模式大家都会写，但通常是传个指针或引用。CRTP 可以把策略作为模板参数注入，连指针都省了。

```cpp
// 排序策略
template <typename T>
struct QuickSort {
    void sort(std::vector<T>& v) { /* 快排逻辑 */ }
};

// 容器接受策略作为模板参数
template <typename T, typename SortStrategy>
class Container {
    std::vector<T> data_;
    SortStrategy sorter_;
public:
    void sort() {
        sorter_.sort(data_);
    }
};

// 使用时，策略在编译期就确定了
Container<int, QuickSort<int>> c;
```

没有虚函数开销，没有内存分配。策略的切换完全在编译期完成，这对底层库来说太香了。

### 场景五：每个派生类独立计数

想统计每个类的实例数量？用CRTP，每个派生类自动拥有独立的计数器，互不干扰。

```cpp
template <typename Derived>
class Counter {
    static int count_;
public:
    Counter() { ++count_; }
    ~Counter() { --count_; }
    static int getCount() { return count_; }
};

// 静态成员需要在类外定义
template <typename Derived>
int Counter<Derived>::count_ = 0;

class User : public Counter<User> {};
class Order : public Counter<Order> {};

// User::getCount() 和 Order::getCount() 完全独立
```

基类是个模板，所以Counter<User>和Counter<Order>是两个完全不同的类型，静态成员自然也是独立的。这招在对象池、资源管理里特别好用。

### 场景六：Mixin 模式，像搭积木一样组合功能

不想用多重继承？CRTP 的 Mixin 模式可以让你优雅地组合功能。

```cpp
// 序列化能力
template <typename Derived>
class Serializable {
public:
    std::string serialize() {
        return static_cast<Derived*>(this)->toJson();
    }
};

// 克隆能力
template <typename Derived>
class Cloneable {
public:
    Derived clone() {
        return static_cast<const Derived&>(*this);
    }
};

// 像搭积木一样组合
class Entity : public Serializable<Entity>,
               public Cloneable<Entity> {
public:
    std::string toJson() { return "{}"; }
};
```

不用多重继承的菱形问题，不用虚函数的开销。想要什么功能，继承一下就行，干净利落。

### 场景七：Builder 模式的类型安全

写过 Builder 的都知道，继承之后链式调用返回的是基类类型，子类的方法就丢了。CRTP 完美解决。

```cpp
template <typename Derived>
class BuilderBase {
public:
    // 返回派生类类型，而不是 BuilderBase
    Derived& setName(const std::string& name) {
        // 设置名字...
        return static_cast<Derived&>(*this);
    }
};

class UserBuilder : public BuilderBase<UserBuilder> {
public:
    UserBuilder& setAge(int age) {
        // 设置年龄...
        return *this;
    }
};

// 现在可以无缝链式调用
UserBuilder builder;
builder.setName("Alice").setAge(25); // 不会丢失类型
```

这招在DSL（领域特定语言）设计里简直是神器。

=====================================

CRTP 说白了，就是把运行时的灵活性换成了编译期的确定性。

滥用会让模板报错变得像天书一样难懂。但在那些对性能敏感、对类型安全要求高的底层架构里，它确实是把锋利的手术刀。

用不用，看场景。但不知道它，那就是你的损失了：）

## 相关笔记
- 2026-08-02_Unity面试装饰器模式与游戏道具效果叠加（同批设计模式实证，Q177 关联）
- 2026-07-06_GameProgrammingPatterns_Command模式（设计模式本质论：语言能力边界脚手架）
- 2026-07-28_Unity面试享元模式与游戏资源复用优化
