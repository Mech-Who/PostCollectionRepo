---
title: AI BI 契约约束架构（Omega）
date: 2026-08-04
category: AI应用
depth: 深度
layer: layer2
tags: [腾讯, Omega, ABI, ChatBI, QueryRegistry, DTBridge, SemanticQL, 数据契约, 运行时契约, Agent治理]
source_article: 2026-08-04_腾讯Omega下一代AI BI架构深度拆解
---

# K-Fragment: AI BI 契约约束架构（Omega）

## A. Applicable — 适用场景

- 构建 AI 查询/分析类应用（BI、报表、数据问答）——防止 AI 幻觉 SQL
- 任何"LLM 自由生成内容"需要可信治理的系统（查询、代码生成、文档生成）
- Agent 生产系统上线检查：可治理性（有界/可取消/可观测/可自纠）验收清单
- 数据产品"可审计性"设计：每个 AI 产出可溯源（证据链）

## B. Boundary — 边界与注意事项

- **契约库需要维护**：QueryRegistry 的指标定义/口径会变，需要版本管理与人工审核，否则契约本身成为新的错误源
- **语义层建设成本高**：SemanticQL 需要先建设语义模型（指标层），适合指标规模有限的企业场景；指标爆炸时维护成本失控
- **运行时契约是兜底不是修复**：有界/可自纠只防"跑飞"，不能解决"口径错误"——口径对错靠契约定义与证据链
- **成本与模型强相关**：BI 高频场景必须选低成本模型（混元 Hy3 0.47 元 vs Claude 4.6 14.2 元，约 30 倍差）

## C. Core — 核心要点

1. **核心思想**：ChatBI 的失败不是模型不够聪明，而是缺少契约与运行时约束——**AI 需要的是"有边界的自由"**
2. **QueryRegistry（数据契约）**：指标口径/维度/粒度/权限/SQL 模板的注册中心；流程=意图识别→契约匹配→契约内实例化查询——幻觉 SQL 从源头掐断
3. **DTBridge 四个运行时契约**：① 有界（资源/行数/耗时上限）② 可取消（用户随时终止）③ 可观测（逐步追踪）④ 可自纠（失败自动降级/改写）——**这四条是"生产级 Agent"的验收清单**
4. **指标证据链**：结果数字可回溯到"指标定义 + 数据血缘 + 计算 SQL"——可审计是可信度的前提
5. **SemanticQL 语义层**：面向业务语义而非物理表，屏蔽表结构复杂度
6. **三层安全边界**：数据权限（行/列级）+ 查询边界（契约限额熔断）+ 输出审计（脱敏+日志）

## D. Data — 架构蓝图

```
用户提问 → 意图识别 → QueryRegistry 契约匹配
                              ↓
                   契约内实例化查询（含权限/限额）
                              ↓
                    DTBridge 运行时（四契约护航）
                    ├─ 有界: 行数/耗时/资源上限
                    ├─ 可取消: 用户随时终止
                    ├─ 可观测: 每步可追踪
                    └─ 可自纠: 失败降级/改写
                              ↓
                    结果 + 证据链（指标定义/血缘/SQL）
                    输出（脱敏 + 审计日志）

生产级 Agent 验收清单（迁移到任何 Agent 系统）:
  □ 有界: 所有外部调用有资源/时间上限
  □ 可取消: 用户可随时终止任务
  □ 可观测: 执行过程可追踪定位
  □ 可自纠: 失败可降级或自动重试
```
