---
title: 装饰器模式在游戏道具系统中的应用
date: 2026-08-02
category: 游戏开发
depth: 深度
layer: layer2
tags: [Unity, 装饰器模式, 设计模式, 道具系统, 附魔, Buff叠加, C#]
source_article: 2026-08-02_Unity面试装饰器模式与游戏道具效果叠加
---

# K-Fragment: 装饰器模式在游戏道具系统中的应用

## A. Applicable — 适用场景

- RPG 道具/装备需要支持多种可叠加效果（附魔、Buff、技能组合），且玩家要能在运行时自由组合
- 存在"效果间交叉反应"（如冰火蒸发伤害）时，需要把组合逻辑与效果定义分离
- 需要在不修改已有类的前提下为对象动态添加职责（开闭原则）
- Unity 面试「设计模式」环节的经典考题：道具附魔系统设计

## B. Boundary — 边界与注意事项

- **装饰器顺序敏感**：包装顺序可能影响结果，必须做幂等性设计（同一效果重复添加结果一致）
- **不适合**：效果数量少且固定、无交叉反应、无需运行时组合的简单场景——直接字段/枚举更简单
- **效果列表收集链**：GetEffects() 逐层递归收集，装饰器层数过多时注意性能与调试复杂度（链深=附魔数）
- **交叉反应勿塞进装饰器**：装饰器之间不应互相感知，交叉逻辑必须集中到 EffectProcessor，否则退化为耦合的继承
- 与享元模式区分：享元解决"大量对象共享资源"，装饰器解决"单个对象动态叠加功能"，两者可组合使用（享元共享资源 + 装饰器叠加效果）

## C. Core — 核心要点

1. **四层结构**：组件接口 IWeapon → 具体组件 Weapon（裸对象）→ 装饰器基类 WeaponDecorator（持有引用+转发调用）→ 具体装饰器（每个效果一个，带 Level）
2. **继承 vs 装饰器**：继承编译期固化组合（5 效果 = 32 子类，类爆炸）；装饰器运行期动态包装，新效果只写一个类，所有组合自动支持
3. **交叉反应隔离**：装饰器只收集效果（GetEffects 返回 List<Effect>），冰火蒸发伤害、眩晕等交叉规则全部在 EffectProcessor 统一计算
4. **叠加规则**：持续伤害累加、同类状态效果（减速）取最大值、交叉反应按等级公式计算
5. **面试应答模板**：核心=动态附加职责优于继承扩展；场景=附魔/Buff/技能组合；优势=运行时组合/避免类爆炸/单一职责/可单测；坑=顺序影响需幂等

## D. Data — 可复用代码骨架

```csharp
// 1. 组件接口
public interface IWeapon
{
    string Name { get; }
    int CalculateDamage();
    List<Effect> GetEffects();
}

// 2. 具体组件
public class Weapon : IWeapon
{
    public string Name { get; private set; }
    public int BaseDamage { get; private set; }
    public Weapon(string name, int baseDamage) { Name = name; BaseDamage = baseDamage; }
    public virtual int CalculateDamage() => BaseDamage;
    public virtual List<Effect> GetEffects() => new List<Effect>();
}

// 3. 装饰器基类（持有引用 + 转发）
public abstract class WeaponDecorator : IWeapon
{
    protected IWeapon _weapon;
    public WeaponDecorator(IWeapon weapon) { _weapon = weapon; }
    public virtual string Name => _weapon.Name;
    public virtual int CalculateDamage() => _weapon.CalculateDamage();
    public virtual List<Effect> GetEffects() => _weapon.GetEffects();
}

// 4. 具体装饰器（每效果一个，转发+附加）
public class FireEnchantment : WeaponDecorator
{
    public int Level { get; private set; }
    public FireEnchantment(IWeapon weapon, int level = 1) : base(weapon) { Level = level; }
    public override string Name => $"🔥{_weapon.Name}";
    public override int CalculateDamage() => _weapon.CalculateDamage() + 10 * Level;
    public override List<Effect> GetEffects()
    {
        var effects = _weapon.GetEffects();
        effects.Add(new Effect { Type = EffectType.Fire, Level = Level, DamagePerTick = 5 * Level, Duration = 3f });
        return effects;
    }
}

// 5. 交叉反应隔离到处理器（勿塞进装饰器）
public class EffectProcessor
{
    public int CalculateTotalDamage(IWeapon weapon)
    {
        int damage = weapon.CalculateDamage();
        foreach (var e in weapon.GetEffects())
            if (e.DamagePerTick > 0) damage += (int)(e.DamagePerTick * e.Duration);
        return damage + CalculateIceFireReaction(weapon.GetEffects()); // 冰火蒸发 = 等级和×10
    }
}

// 6. 组合使用：逐层包装
IWeapon sword = new Weapon("铁剑", 50);
sword = new FireEnchantment(sword, 2);   // 🔥铁剑
sword = new IceEnchantment(sword, 1);    // 🔥❄️铁剑 → 总伤害 50+20+8+30=93
```
