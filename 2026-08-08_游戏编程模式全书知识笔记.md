---
title: 《游戏编程模式》全书知识笔记（Game Programming Patterns）
date: 2026-08-08
category: 游戏开发
depth: 深度
layer: layer2
tags: [游戏编程模式, 设计模式, RobertNystrom, 架构, 游戏开发, GPP, 数据局部性]
summary: Robert Nystrom《游戏编程模式》全书中译版系统笔记：19 个模式按 5 组组织（重访设计模式/序列/行为/解耦/优化），贯穿"解耦与取舍"主线。核心洞见：架构=最小化修改前需了解的信息；单例是安慰剂；命令是具现化方法调用；享元是唯一有硬件支持的GoF模式；数据局部性比算法更重要（缓存不命中慢50倍）；行为模式主线是"让非程序员也能定义行为"。
source_url: https://github.com/ccyr119/Game-Programming-Patterns-CN/tree/master/book
source: github
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** 《游戏编程模式》是 Robert Nystrom（Game Programming Patterns 作者，前 EA/Google 工程师）的游戏开发设计模式专著，本笔记基于 GitHub 中译本书稿（ccyr119/Game-Programming-Patterns-CN，27 章 510KB）系统提取。全书 19 个模式分 5 组：重访设计模式（命令/享元/观察者/原型/单例/状态）、序列模式（游戏循环/更新方法/双缓冲）、行为模式（字节码/子类沙盒/类型对象）、解耦模式（组件/事件队列/服务定位器）、优化模式（数据局部性/脏标记/对象池/空间分区）。贯穿主线是"解耦与取舍"——好架构=最小化修改前需了解的信息；作者对经典模式有鲜明立场：单例是安慰剂、命令被冷落、原型常非最优解、享元是唯一有硬件支持的 GoF 模式。

## 我的理解
> 由小林生成，供小涵审阅修改

1. **这本书与"GPP 设计模式×游戏开发知识线"的关系**：本库已有 Command(07-06)、ServiceLocator(07-07)、享元(07-28)、装饰器(08-02)、CRTP(08-02)、组合(08-08) 六篇实证，均源自这本书的模式体系。本次全书提取把散点串成完整地图——19 个模式各自的定位、取舍、与其他模式的关联一览无余，为后续"模式驱动开发"提供总索引。

2. **"架构=最小化修改前需了解的信息"是全书的元公式**：好架构不是漂亮的分层，而是让改动发生时"程序好像正等着这种改动"。解耦（组件/事件队列/服务定位器）都是这个公式的实例化——组件解耦空间、事件队列解耦时间、服务定位器解耦实现。这与本库「软件设计架构」概念的"要素+结构+连接"框架互补：Nystrom 给了判断标准（改动成本），要素-结构-连接给了操作定义。

3. **作者对 GoF 模式的"翻案"立场极具启发**：单例是安慰剂（"真朋友不会让你用单例"）、原型"找不到一种场景它是最佳方案"、命令"被冷落了"、享元"唯一有硬件支持"、观察者"如果设计今日版本我会基于函数"。这印证本库 Q156 的核心猜想——设计模式的本质是"语言能力边界的手动补全"，随语言/硬件演进价值会重估。C# 有闭包所以命令可用 lambda、GPU 有实例化所以享元有硬件支持——模式价值随环境变化。

4. **数据局部性章节是全书最具颠覆性的认知**："我们被欺骗了"——CPU 每年翻倍 RAM 却跟不上，缓存不命中多的程序慢 50 倍；"我一直从代码角度想性能，而不是数据"。"减少 -> 操作符"（摆脱指针追逐）、冷/热分割、按领域拆分组件数组（ECS 雏形）——这对 C#/Unity 的性能优化（本库 08-04 CSharpSLG 存储 IO 优化的理论印证）和 ECS 架构（小涵正在推进的 IDT 框架）都是直接支撑。

5. **行为模式组的主线是"让非程序员定义行为"**：类型对象（类型数据化）→ 子类沙盒（安全原语封装）→ 字节码（行为本身变数据），层层递进、越往后行为离代码越远、灵活性越大。这与本库「ai-体验生产分界线」概念共振：游戏设计中的"行为生产"正从程序员垄断走向工具化/数据化——字节码 VM + 可视化脚本（Henry Hatsworth）就是游戏行业的"低代码平台"。

## 📌 关键要点

### 全书框架（19 模式 5 组）
| 组 | 模式 | 一句话定位 |
|----|------|-----------|
| 重访设计模式 | 命令 | 具现化方法调用：可存储/传递/延迟/撤销 |
| | 享元 | 共享固有状态省内存（唯一有硬件支持的 GoF 模式） |
| | 观察者 | 一对多广播：让代码宣称"有趣的事发生了" |
| | 原型 | 对象克隆自己；作者：常非最优解，委托用于数据模型极佳 |
| | 单例 | 安慰剂：全局变量封装进类，作者教如何避免 |
| | 状态 | FSM：状态集合+转移表，扩展并发/分层/下推自动机 |
| 序列模式 | 游戏循环 | 引擎与库的分水岭：固定时间步长更新+动态渲染 |
| | 更新方法 | 每实体封装 update()，游戏循环每帧遍历调用 |
| | 双缓冲 | 当前只读/下一只写，原子交换实现同步错觉 |
| 行为模式 | 字节码 | 行为变数据：栈式 VM + 中间值栈 |
| | 子类沙盒 | 基类提供 protected 操作，子类只调这些操作 |
| | 类型对象 | 类型数据化：Breed 类替代继承层次 |
| 解耦模式 | 组件 | 按领域拆类，实体退化为薄壳（ECS 雏形） |
| | 事件队列 | 发送/处理解耦，环状缓存实现，及时性可调 |
| | 服务定位器 | 电话簿式服务查询；作者建议少用，优先依赖注入 |
| 优化模式 | 数据局部性 | 按处理顺序连续排列数据（缓存不命中慢50倍） |
| | 脏标记 | 置脏位延期重算，需要时才检查 |
| | 对象池 | 预分配复用对象，避开 GC/堆碎片 |
| | 空间分区 | 网格/四叉树降 O(n²)，以内存换速度 |

### 架构原理（引言+架构章节）
- **好架构标准**："当我作出改动，整个程序就好像正等着这种改动"
- **解耦本质**：两块代码可被单独理解、改动不互相波及
- **最小化认知负担**：架构的关键目标是"最小化在编写代码前需要了解的信息"
- **YAGNI**："每次添加抽象或扩展支持，就是在赌以后需要灵活性"
- **三速角力**：长期开发/运行性能/短期实现速度，没有简单答案
- **金句**："让有趣的游戏变得高效比让高效的游戏变有趣简单得多"；"好的解决方案不是往代码中注水，而是蒸干代码"

### 独特洞见精选（跨模式）
- **单例**："它不是解药，它是安慰剂"；"通过控制对实例的访问，你控制了耦合"
- **命令**："真朋友不会让你用单例"（指用单例实现命令）；有闭包的语言直接用函数闭包
- **状态**：并发状态机把 n×m 状态降为 n+m；下推自动机用状态栈解决 FSM 无历史问题
- **原型**："我不能说找到了一种情景，原型设计模式是最好的方案"；JS"在实践中更像基于类的语言"
- **观察者**：观察者是同步的、会阻塞被观察者；GC 语言有失效监听者问题；"如果设计今日的观察者模式，我会让它基于函数而不是基于类"
- **享元**："享元对象几乎总是不可变的"；无形共享（地形指针网格）比清晰身份共享更显功力
- **游戏循环**：动态时间步长让游戏"不确定且不稳定"（浮点舍入+物理阻尼调校）；移动端固定帧率让 CPU 休眠省电
- **更新方法**："虚拟游戏核心其实是回合制的，只是完整回合只有一帧那么长"；更新时增删对象需小心（缓存长度/倒序遍历/标记延迟清理）
- **字节码**：解释器模式"怎么念？S-L-O-W"；"是人都会犯错误，但错误同时也是创作的固有基础"
- **类型对象**：C++ 的虚函数表就是一个品种对象；类型对象作构造器可在分配前插入对象池
- **子类沙盒**：与模板方法正相反；基类会膨胀形成"脆弱基类问题"
- **组件**："继承是重锤"，"用拥有代替继承"；菜单比喻——继承是订套餐、组件是照单点菜
- **事件队列**："中心事件队列是一个全局变量"；避免在处理事件的代码中发送事件（异步环路无声运行）
- **服务定位器**："依赖注入不过是简单思路的复杂行话"；空服务让"故意停用"变成零成本特性
- **数据局部性**："我一直从代码角度想性能，而不是数据"；"没有银弹，只有挑战性的权衡"
- **脏标记**："计算机科学中只有两件难事：缓存一致性和命名"（Phil Karlton）
- **对象池**："复用对象不自动清除"——失去 0xdeadbeef 安全网，未初始化 bug 极难发现
- **空间分区**：四叉树=固定划分+自适应划分两利；"人生苦短，不要浪费在编写链表上"

### 模式间关键关联（跨组）
- **状态↔享元**：无字段状态对象用静态实例共享，正是享元
- **观察者→事件队列**：观察者同步、事件队列异步，队列是观察者的异步实现
- **组件→数据局部性**：组件把实体按领域拆开，数据局部性才能按领域排列连续数组
- **事件队列↔数据局部性**：环状缓存替代链表正因缓存友好
- **子类沙盒↔更新方法**：update() 本身就是沙箱方法
- **组件→ECS**：极致是实体只是一串 ID，组件反向知晓实体（ECS 雏形）
- **数据局部性→ECS**：多态处理三策：别用（类型对象替代）/每种类型分数组/指针集合；极端形态实体退化为纯 ID

## 原文
（GitHub 仓库 ccyr119/Game-Programming-Patterns-CN，book 目录 27 个 markdown 章节。已 clone 至本地 _gpp_book_tmp/ 供参考，核心内容已在上文提取）

## 相关笔记
- [[软件设计架构]]
- [[GameProgrammingPatterns_Command模式]]
- [[Unity面试享元模式与游戏资源复用优化]]
- [[Unity面试装饰器模式与游戏道具效果叠加]]
- [[Unity面试组合模式与游戏UI层级管理]]
- [[CSharpSLG存储性能优化文件系统与IO调优]]
