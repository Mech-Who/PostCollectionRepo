---
title: 老板想部署一套Kimi K3要花多少钱？
date: 2026-07-28
category: AI技术
depth: 标准
layer: layer2
tags: [Kimi K3, 大模型部署, MoE, 成本分析, 算力, H100, 量化, 开源模型]
summary: Kimi K3（2.8T 参数 MoE，MoonshotAI 开源）本地部署成本拆解：即使 4bit 量化（MXFP4）后权重仍约 1.4TB 显存，实际需求破 2TB。官方推荐 64 卡 H100 超节点：硬件 1800-2500 万元（显卡 1150-1400 万 + 互联/存储/机柜配套 50%-100%）、月电费 4.3-5.8 万元、3-5 人专职团队年成本约 200 万。卖 API 按官方定价（输出每百万 token 100 元）月收入约 14.46 万元，单算硬件 20 个月才能回本。替代方案：deltafin 项目可在 M1 MacBook 上跑完整 K3（64GB 内存 M1 Max 吞吐 0.0687 token/秒）。
source_url: https://mp.weixin.qq.com/s/cy_X_rN-ek9BC4fpA7AgGQ
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** Kimi K3（2.8T 参数 MoE 开源模型）自建部署成本全景：①显存需求——4bit 量化（MXFP4）后权重约 1.4TB，加上 KV 缓存等实际破 2TB；换算 H100(80GB) 至少 19 张，更靠谱估算是 8 节点×8 卡=64 张 80GB 显卡（官方推荐的超节点配置）；②成本——64 张 H100 单卡 18-22 万，光显卡 1150-1400 万，加服务器/高速互联/存储/机柜约 1800-2500 万；月电费 4.3-5.8 万（75-90kW 功耗）；3-5 人专职团队月成本 18-25 万、一年约 200 万；③回本测算——按官方 API 定价（输出 100 元/百万 token）月收入约 14.46 万元，单算硬件 20 个月才回本，和加盟蜜雪冰城差不多；④穷人方案——deltafin 项目在一台 M1 MacBook 上跑完整 K3（64GB M1 Max 实测 0.0687 token/秒 ≈ 14.6 秒一个 token）。结论：普通人老老实实调官方 API。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇是罕见的"开源模型部署成本"硬核科普——大部分开源新闻只讲参数和 benchmark，这篇把账算到了老板能听懂的层面。几个值得沉淀的认知：

1. **「开源≠免费」的算力真相**：K3 开源的是权重，不是推理成本。2.8T 参数 MoE 即使在 4bit 量化下也要 1.4TB 显存——这解释了为什么"开源大模型"的实际使用者依然主要是大厂，个人/小团队只能走 API。**开源降低了使用权，但没有降低算力门槛**。
2. **成本结构教育意义**：硬件（1800-2500万）> 人工（200万/年）> 电费（60万/年）。电费在硬件面前"忽略不计"这句话本身很能说明问题——真正的隐性成本是分布式推理调优的工程师人力。
3. **回本测算的粗糙但有效**：500 token/s 稳定输出 × 30 天 ≈ 13 亿 token ≈ 13 万元/月 + 输入部分 ≈ 14.46 万元/月，硬件 20 个月回本。这个"用 API 定价反推自建回本周期"的测算框架，可以直接复用到任何大模型部署决策。
4. **deltafin 的 M1 MacBook 方案是"极限压缩"的范例**：0.0687 token/秒（14.6 秒一个 token）在商业上毫无价值，但作为"能不能跑起来"的可行性证明很有意义。这提示：**"能跑"和"能用"之间隔着几个数量级的工程优化**。

**关联知识**：可挂到「AI技术」分类的大模型部署与成本主题，与小涵关注的模型动态（Claude/GLM/DeepSeek）对比形成成本坐标系。

## 原文

这几天经常刷到 Kimi K3 开源了的帖子，然后肯定有老板在想，都开源了，那我是不是在公司 Mac mini 装一个，这样就不用花钱买 ChatGPT、Claude 的 Token 了？

然后甩给我一个开源地址：https://github.com/MoonshotAI/Kimi-K3

我们先看下参数：

参数信息：

# 看这参数整个最低配置，能跑起来就行那种，光是把模型权重塞进显存，就已经是个体力活。

# K3 用了 4bit 量化（MXFP4）来压缩体积，即便这样，**光权重就要占约 1.4TB 显存**，这还没算上下文、KV 缓存这些隐藏消费，实际需求轻松破 2TB。

换算成显卡数量大概是这样：

- 用 H100（80GB）：至少 **19 张**
- 用 H200（141GB）：至少 **11 张**
- 用 B200（192GB）：至少 **8 张**

以上这些还只是刚好装得下模型，完全没考虑并发用户、长上下文这些实际使用场景。

更靠谱一点的估算是：**8 节点 × 每节点 8 卡 = 64 张 80GB 显卡**打底，聚合显存超过 5TB，这才算勉强够用。

### 官方推荐配置：**64 张卡起步的超节点（Supernode）集群**，而且是为了吃满卡间高速互联带宽专门设计的配置。

咱们就按官方推荐的 64 卡 H100 超节点来算一算到底需要多少钱：

### 1、硬件

64 张 H100 80GB，单卡采购价约 18-22 万元，光显卡就要 **1150-1400 万元，再加上服务器整机、高速互联网络（NVLink/InfiniBand）、存储、机柜改造……这些配套通常还要再加 50%-100%**

总硬件投入：约 1800-2500 万元

### 2、电费

H100 单卡满载功耗约 700W，64 张卡光 GPU 本身就要 45kW，再加上网络、存储、制冷设备，整套系统实际功耗大概在 **75-90kW**。

24 小时连轴转，一个月电费大概是 **4.3-5.8 万元**，这种费用在硬件面前忽略不计了。

### 3、人工

伺候一个 2.8T 参数的 MoE 怪兽，不是随便找个运维就能搞定的，至少得配 3-5 人的专职团队（分布式推理调优 + SRE 运维），月薪加社保福利，团队月成本大概 **18-25 万元，一年怎么的要 200 个 W**。

* * *

## 如果拿去卖 API，多久能回本？

当然如果真买了这些设备，肯定有人想：那我搭起来之后转卖 API 呢？总能回点本吧？

咱们按官方定价（国内站）算算：输入未命中每百万 token 20 元，输出每百万 token 100 元，缓存命中的输入价格能低到 2 元。

假设这套 64 卡集群能稳定输出 500 token/秒，一个月下来：

月输出量约 13 亿 token，收入约 **13 万元，加上输入部分（假设 80% 缓存命中，再加约 1.46 万元，月收入合计约 14.46 万元。**

**然后不算人工与电费等，单单硬件就得 20 个月左右才能回本～～这么看好像跟加盟蜜雪冰城的店回本周期差不多吧。**

**不过对于大多数普通人来说，还是老老实实调用官方 API，自己买设备卖 API 这种事情留给真正有钱的大厂去实现～～～**

* * *

当然 如果老板一定要让你本地装部署上 K3，不然就让你滚蛋，办法也是有，有人就开源了个项目 -- deltafin，在一台 M1 MacBook 上运行完整的 Kimi K3（2.8T 参数）。

开源地址：https://github.com/gavamedia/deltafin

测试结果：**搭载 64GB 内存的 M1 Max 普通工作站，吞吐 0.0687 token / 秒，也就是 14.6 秒生成一个 token**：
