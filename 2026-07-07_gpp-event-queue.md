---
title: "事件队列 — 解耦消息的发送与处理时机"
date: 2026-07-07
category: 游戏开发
depth: 深度
layer: layer2
tags: [事件队列, 消息队列, 环形缓冲区, 解耦, 音频系统, 线程安全]
summary: "事件队列模式通过队列解耦消息的发送和处理时机。以音频系统为例，展示了如何用环形缓冲区实现零动态分配的消息队列，并讨论了请求合并、线程分离和全局事件总线的风险。"
source_url: https://gpp.tkchu.me/event-queue.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 事件队列将"发送请求"和"处理请求"在时间上解耦。以音频系统从同步阻塞API到异步队列的重构为例，展示了用环形缓冲区实现零动态分配、支持请求合并、线程安全的消息队列。也讨论了中心事件总线作为全局变量的风险。

## 我的理解

> 事件队列是观察者模式的异步升级版——观察者让你不知道"谁在听"，事件队列让你不知道"什么时候处理"。这个"时间解耦"带来了三大超能力：请求合并（两个playSound合并为一个大音量）、延迟处理（等音频通道空闲）和线程分离（发送者在游戏线程，处理在音频线程）。

> 环形缓冲区的实现是全书最优雅的数据结构案例。不需要动态分配、不需要数据拷贝、对缓存友好——只用模运算就实现了一个完整的FIFO队列。这种简洁性正是游戏编程的魅力所在：在约束中找到最精简的解决方案。

> 请求合并的时机选择很有讲究：在入队时合并（O(n)扫描队列检查重复）vs在出队时合并。作者选择了入队时合并，因为可以在浪费资源处理重复请求之前消除它们。但这也意味着每个playSound()调用都要遍历队列——在队列很长时可能成为瓶颈。使用哈希表替代线性扫描是一个自然的优化方向。

> 关于中心事件总线作为全局变量的警告非常重要。全局变量的问题不会因为有了一层"事件协议"的包装就消失——任何代码都能向全局队列发消息，导致难以追踪的隐式耦合和反馈循环。作者建议把事件队列限制在类或领域内使用，这比建立全局消息系统更安全可控。

## 📌 关键要点
- **核心机制**：发送者入队请求即返回，处理者在方便时从队列取出处理
- **环形缓冲区**：head_/tail_指针+模运算=零动态分配FIFO队列
- **请求合并**：遍历队列检查重复soundId，合并为取最大音量
- **线程分离**：playSound()在游戏线程入队，update()在音频线程出队处理
- **三种队列**：事件（已发生的事情，多监听者）vs 消息（要求做的事，单监听者）vs 工作队列（负载均衡）
- **全局vs局部**：全局事件总线=全局变量的危险；建议限制在领域内使用

## 原文

### 音频系统重构
同步playSound() → 改为向队列添加PlayMessage立即返回 → update()中逐个处理。解决了同步阻塞、多个同时播放相同声音和跨线程调用三个问题。

### 环形缓冲区实现
head_指向读取位置，tail_指向写入位置。入队：`tail_ = (tail_ + 1) % MAX_PENDING`。出队：`head_ = (head_ + 1) % MAX_PENDING`。

### 发送者/接收者模型
- 单播队列：一个接收者（如Audio类），发送者只知道playSound() API
- 广播队列：多个监听者，事件可能无人接收
- 工作队列：多监听者，每个消息只发给其中一个（负载均衡）
