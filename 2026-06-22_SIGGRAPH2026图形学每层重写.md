---
title: 拆解SIGGRAPH 2026：从芯片到像素，图形学的每一层都在被重写
date: 2026-06-22
category: AI技术
depth: 深度
layer: layer2
tags: [SIGGRAPH, 神经渲染, 路径追踪, 图形API, Shader Model 6.10, 3D生成, 物理仿真, GPU架构, 游戏引擎, 图形学趋势]
summary: SIGGRAPH 2026 全景拆解：从 Zeus 路径追踪专用 GPU、Shader Model 6.10 将神经渲染写入 DirectX 标准，到 Neural Materials 将着色复杂度从 O(n) 降为 O(1)、8DNA 八维光传输函数、3D 生成从"能生成"到"能用"的跨越——图形学从芯片到像素的每一层都被神经网络重写，路径追踪正在成为下一代图形管线的默认选项。
source_url: https://mp.weixin.qq.com/s/gyZUQpTzd4VSntghCNAG0g
source: weixin
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** SIGGRAPH 2026 的议程揭示了一个根本性转变：从芯片指令集到屏幕上的最后一个像素，图形学的每一层都在被神经网络重写。自底向上逐层拆解——硬件层 Zeus GPU 为路径追踪从零设计芯片、API 层 SM 6.10 将矩阵运算设为 DirectX 强制要求、渲染算法层 Neural Materials 使着色复杂度从 O(n) 降为 O(1)、几何表示层神经网络替代 CAD 内核数学层、内容生成层 3D 生成进入实用化阶段、动画层 15,000fps 零样本动作合成、物理仿真层追求"同样真实但快得多"。核心信号：路径追踪正从高端显卡炫技功能变成下一代图形管线的默认选项，神经渲染正从厂商特性变成图形 API 标准。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇文章的价值不在于罗列论文，而在于它构建了一个**从底层硬件到顶层应用的全景说服链**：当 GPU 厂商为路径追踪从零造芯、API 标准将神经网络写进强制规范、渲染论文实现 O(n)→O(1) 的复杂度跃迁同时发生时，结论不再是"AI 能辅助图形学"，而是"图形学的默认范式正在被替换"。

对游戏开发者而言，有三个信号值得特别关注：

**1. 路径追踪的"默认化"不可逆。** Zeus GPU（225W、384GB、路径追踪第一）和 Enhanced ReSTIR（Cyberpunk 2077 全路径追踪 34→89fps）是同一方向的两条腿——硬件和算法同时在推。这意味着未来 3-5 年内，新引擎如果不把路径追踪作为默认光照方案，会像当年不用 PBR 一样落后。对 Unity 开发者来说，需要关注 HDRP 的路径追踪成熟度；对 Unreal 开发者来说，UE5 Path Tracer 从实验到生产的时间线在加速。

**2. SM 6.10 打破了 NVIDIA 的神经渲染护城河。** Neural Texture Compression 的 6.7 倍压缩率、AI 降噪不再绑定 DLSS、材质上采样全平台化——这些对独立游戏和小团队是利好。之前只有 NVIDIA 高端卡能用的技术，未来 AMD/Intel 也能跑。但注意：老 GPU 基本无缘，DX12 Ultimate 是门槛。

**3. 3D 生成进入"能用"阶段的关键信号。** Meshy AI 1 亿模型、Tripo 在 SIGGRAPH 做 Keynote、Mobile3DGS³ 让移动端跑 3DGS——这些都是"质量够了，开始想怎么用"的标志。对 PostCollection 已有的 3D 生成相关概念（如 `ai-体验生产分界线`）形成有力补充。

**一个被低估的元变化：** Conference 轨（7 页、验证可不全面）的出现，意味着 SIGGRAPH 从"图形学的 Nature"变成了"图形学的 Nature + ICML"。引擎开发者能更早看到不成熟但方向对的早期研究——这些才是影响未来 2-3 年技术路线的真正信号。

## 📌 关键要点

- **硬件范式转换**：Bolt Graphics Zeus GPU——自研 SIMD 架构、路径追踪优先、最大 384GB 显存（RTX 5090 的 12 倍）、225W 功耗、集成 400/800 Gbps 网络。路径追踪从"嫁接功能"变成"芯片设计起点"。
- **API 标准化**：Shader Model 6.10 将 Cooperative Vector（矩阵运算）设为 DirectX 强制要求。神经纹理压缩（NTC，6.7x 压缩）、AI 降噪、材质上采样从此跨厂商可用。
- **渲染复杂度跃迁**：Neural Materials 将着色计算复杂度从 O(n)（n=材质节点数）降为 O(1)——不管 Shader Graph 多复杂，神经网络推理时间固定。实测性能提升 1.4-24 倍。
- **光传输突破**：8DNA 学习完整八维光传输函数（含近场照明位置），室内间接光照从"实时开销大"变"预计算+神经网络解码"。方差比路径追踪更低。
- **几何表示革新**：NeuBase 用神经网络替代 CAD 内核的数学表示层（B-splines/NURBS），NEO 将特征分解从迭代求解变为神经网络单次前向传播。
- **3D 生成实用化**：Tripo Keynote 提出"交互式媒体需要由用户想象力实时生成的世界"，DeG 可学习密度控制 3DGS，Mobile3DGS³ 让移动端跑 3DGS，Meshy AI 已服务 1000 万用户。
- **动画与仿真**：MotionBricks 15,000fps 零样本动作合成（比实时快 250 倍），MPM Lite 求解器复杂度不再与粒子数量挂钩，JGS2-GQ 免训练的物理仿真。
- **制度变革**：SIGGRAPH 首次双轨制——Journal 轨（完整验证）+ Conference 轨（新颖即可），加速 AI 时代研究迭代。

## 原文

> 以下原文转自微信公众号「哲思哲言」（2026-06-22），作者哲思哲言。

---

如果你在 2026 年做游戏引擎，SIGGRAPH 今年的议程将会告诉你一件事：**从芯片指令集到屏幕上的最后一个像素，图形学的每一层都在被重写。** 不是某一层，是全部。

今年 SIGGRAPH 的主题是 "The Creative Complexities of Translation Practices, Artifacts, and Stories"——翻译的创造性复杂性：实践·物·叙事。

### 第零层：制度——连论文怎么投都被重写了

SIGGRAPH 2026 首次将 Technical Papers 分为两条轨道：

- **Journal 轨（TOG）**：ACM Transactions on Graphics，无长度限制，要求"完整、全面、充分验证"，周期 12-18 个月。
- **Conference 轨**：SIGGRAPH Conference Proceedings，正文 ≤7 页，**新颖即可，验证可以不全面**。

为什么？AI 时代研究迭代以月为单位。一篇神经渲染新方法在审稿周期中等 12 个月，发表时可能已被 3 篇 arXiv 预印本超越。

### 第一层：硬件——有人为路径追踪从零造了一颗 GPU

**Bolt Graphics Zeus GPU**：

- 自研 SIMD 架构，RISC-V 命令处理器同时充当 CPU——不需要插在主板上
- 内存：LPDDR5X + DDR5 SODIMM，最大 **384 GB**（RTX 5090 是 32GB）
- 网络：集成 400/800 Gbps 企业级网络接口 + BMC/IPMI
- 功耗：225W，单 8-pin PCIe（RTX 5090 是 575W）
- 性能声明：路径追踪性能是 RTX 5090 的 2.5 倍

**NVIDIA Enhanced ReSTIR**：

1. 自适应光线足迹阈值：空间复用时只考虑光线锥真正重叠的像素对，贡献约 30% 性能提升
2. 样本重复映射（Sample Duplication Map）：追踪热门样本，去重重新分配
3. 智能可见性重用：世界空间位置+法线接近时跳过 shadow ray，漫反射表面准确率 >95%

综合效果：RTX 5090 上 Cyberpunk 2077 全路径追踪 34→89fps，PSNR 提高 1.8dB。

### 第二层：API——神经渲染正在写进图形标准

**Shader Model 6.10**：矩阵运算成为 DirectX 强制合规要求。

```hlsl
// SM 6.10 Cooperative Vector 伪代码
CooperativeVector<float, 16, 16> input;   // 16×16 输入矩阵
CooperativeVector<float, 16, 64> weight;  // 16×64 权重矩阵
CooperativeVector<float, 64, 16> output;  // 64×16 输出矩阵
output = CooperativeVectorMultiplyAdd(input, weight, bias);
```

底层映射：NVIDIA→Tensor Core，AMD→RDNA 4 AI Accelerator，Intel→XMX 引擎。

这意味着：(1) 神经纹理压缩（NTC）可跨厂商部署，6.7 倍压缩几乎无损；(2) AI 降噪不再绑定 DLSS；(3) 材质上采样全平台可用。

### 第三层：渲染算法——神经网络正在替代传统 Shader

**Neural Materials**：整个材质行为编码进紧凑潜在表示，运行时小型神经网络一次性输出所有着色结果。计算复杂度从 O(n) 变成 O(1)——n 是材质节点数。实测性能提升 1.4-24 倍。

**8DNA**：学习完整八维光传输函数（额外两维编码近场照明位置）。近场光源的光传输可预计算并实时重建，方差比路径追踪更低。

**VideoNeuMat**：从视频扩散模型提取材质先验，文本→材质，文本到材质的生成。

**BodyReLux**：基于视频扩散模型的动态光照控制，逐帧重打光时间一致（不闪烁）。

### 第四层：几何表示——神经网络正在替代 CAD 内核的数学层

**NeuBase**：神经基函数样条，用神经网络隐式学习曲面基函数，替代传统 CAD 内核的数学表示层。

**NEO**（中科大+清华+上海期智研究院）：特征分解从迭代求解变为神经网络单次前向传播。

**SuperSDF**（Caltech）：从稀疏有向距离场恢复高分辨率表面。

**实时 GPU 矢量图形 SDF**（厦门大学）：GPU 上实时生成矢量图形有向距离场。

### 第五层：内容生成——3D 生成从"能生成"到"能用"

**Tripo AI Keynote**（7 月 22 日）：交互式媒体需要从手工搭建转向由用户想象力实时生成的世界。

- **DeG**（VAST+清华）：可学习密度控制的 3D 高斯生成，复杂区域多放高斯球。
- **Pixal3D**：像素对齐 3D 生成，单图/多视图重建高保真 3D。
- **ArtiFixer**（NVIDIA）：自回归扩散增强 3D 重建，PSNR +3dB。
- **Mobile3DGS³**（中科大 GCL）：移动端 3DGS 渲染优化。
- **OmniRoam**（UC+Adobe）：单图输入生成 360° 全景视频，支持无限漫游。

Meshy AI：1000 万+用户，1 亿+ 3D 模型。Roblox：AI 驱动游戏目标 2K@60fps。

### 第六层：动画与运动——生成式 AI 接管动作控制

**MotionBricks**：15,000fps 零样本动作合成，比实时快 250 倍。

**ReActor**：强化学习物理感知动作重定向，双层优化框架，消除脚部滑动和自身穿透。

**MUSIC**：分层架构让骨骼肌肉手弹奏全新乐谱。

### 第七层：物理仿真——从"更真实"到"同样真实，但快得多"

**MPM Lite**：粒子仅作为运动学状态载体，力的组装和时间积分完全不需要访问粒子数据。求解器复杂度不再与粒子数量挂钩。

**JGS2-GQ**：高斯求积替代数据驱动体积积分，**不需要训练**，泛化性更强。

**Mixwell**：基于势流理论的 2D 流体笔刷，零数值耗散。

**纱线级针织物仿真**：嵌套 Douglas-Rachford 分裂算法，还原每根线的拉伸、弯曲、摩擦。

**沙-水-气多相流耦合**：LBM 流体 + MPM 颗粒沙统一耦合，内置体积守恒。

### 回到顶部：Lanny Smoot 和迪士尼的 107 项专利

迪士尼研究院 Research Fellow Lanny Smoot 做开幕 Keynote，持有 107 项专利（超过华特·迪士尼本人），发明了可伸缩光剑、HoloTile Floor 全向跑步机等。

> 图形学最根本的驱动力：不是为了发论文，而是为了造出让人"哇"出来的体验。从芯片到像素，从 API 到物理仿真，SIGGRAPH 2026 的每一层技术栈，最终都指向这个目标。

## 相关笔记

- [[ai-体验生产分界线]] — 3D 生成从"能生成"到"能用"的跨越与此概念直接相关
- [[ai-工程基础设施壁垒]] — SM 6.10 打破 NVIDIA 独占的神经渲染壁垒
- 待建立概念：「神经渲染范式转换」— 从硬件到 API 到算法的全栈重写
- 关联：游戏引擎开发趋势、Unity HDRP/UE5 路径追踪路线图
