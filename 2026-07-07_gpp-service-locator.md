---
title: "服务定位器 — 更灵活的单例替代品"
date: 2026-07-07
category: 游戏开发
depth: 深度
layer: layer2
tags: [服务定位器, 依赖注入, 空对象模式, 装饰器模式, 解耦, 单例替代]
summary: "服务定位器为服务提供全局接入点，但通过接口抽象让调用者不依赖具体实现。支持编译时/运行时/外部注册三种定位方式，空服务回退和日志装饰器是亮点。建议少用——先考虑传参。"
source_url: https://gpp.tkchu.me/service-locator.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 服务定位器是单例模式的"更灵活版本"——通过接口抽象和外部注册，调用者不依赖具体实现类。空服务模式和日志装饰器展示了其强大的可扩展性。作者的建议简洁有力：**少用**，先考虑把服务当参数传递。

## 我的理解

> 服务定位器本质上是在"全局访问的便利性"和"依赖的显式性"之间的折中。它比单例更灵活（可以在运行时切换实现），但仍然是全局状态。作者开头就强调"少用"——先考虑传参——这种诚实的态度值得赞赏。

> 空服务模式（Null Object）是最被低估的防御性编程技巧。与其到处检查null或让游戏崩溃，不如返回一个"什么都不做"的服务实现。这不仅让调用代码更简洁（不需要null检查），还提供了优雅的功能开关——想关闭音频？不注册真正的音频服务就行了，不需要到处加if(audioEnabled)。

> 日志装饰器是装饰器模式在游戏开发中的典范应用。不需要修改任何使用音频的代码，就能为所有音频调用添加日志。enableAudioLogging()用LoggedAudio包装现有服务再替换——这种非侵入式的功能注入是面向对象设计中组合优于继承的又一个例证。

> 关于编译时绑定的讨论也值得注意：`#if DEBUG DebugAudio #else ReleaseAudio #endif` 在主机游戏开发中很常见，因为运行时灵活性不如性能和确定性重要。企业级偏好运行时反射配置，游戏开发偏好编译时决策——这是两种不同约束下的不同最优解。

## 📌 关键要点
- **模式定位**：单例的灵活替代——通过接口和外部注册解耦具体实现
- **三种定位方式**：外部注册（最灵活）> 编译时绑定（最快）> 运行时反射（最复杂）
- **空服务模式**：返回什么也不做的NullAudio → 避免null检查，提供优雅的功能开关
- **日志装饰器**：LoggedAudio包装现有服务 → 非侵入式添加日志，任意开关
- **使用原则**：先传参 → 不够用才考虑服务定位器 → 尽量限制在类层级
- **依赖注入本质**：外部代码向定位器注入服务，而非定位器自己创建

## 原文

### 基本实现
Locator::provide(audio)注册服务 → Locator::getAudio()获取服务。调用者只知道Audio接口，不知道ConsoleAudio具体实现。

### 空服务
NullAudio实现所有方法但什么都不做。getAudio()永不返回NULL，调用代码无需null检查。

### 装饰器扩展
LoggedAudio包装真实服务，在转发前记录日志。enableAudioLogging()用LoggedAudio替换当前服务，全程不修改使用服务的代码。

### 服务范围
全局可访问 vs 限制在特定基类中。日志等广泛使用的服务适合全局，网络接口等特定领域服务应限制范围。
