---
title: "空间分区 — 把世界切分成可管理的格子"
date: 2026-07-07
category: 游戏开发
depth: 深度
layer: layer2
tags: [空间分区, 网格, 四叉树, 碰撞检测, 邻近查询, BSP, k-d树]
summary: "空间分区模式将对象按位置存储在空间数据结构中，将O(n²)的邻近查询降为可控复杂度。从最简单的固定网格到自适应的四叉树，讨论了层次vs平面、固定vs自适应分区的设计选择。"
source_url: https://gpp.tkchu.me/spatial-partition.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 空间分区是解决"哪些对象在附近"这一游戏核心查询的模式。通过将世界划分为空间区域，将O(n²)的双重循环比较降为只检查同区域内的对象。固定网格是最简单的实现，四叉树/BSP/k-d树等层次结构提供了自适应划分。

## 我的理解

> 空间分区是"数据结构即算法"的经典案例。同样的碰撞检测逻辑，放在没有空间结构的数组中就是O(n²)，放在网格中就是O(n)——差别不在于代码怎么写，而在于数据怎么组织。这个洞察比任何具体的数据结构都重要。

> 作者选择固定网格作为示例非常明智——它是最简单、最直观的空间分区，也是实践中使用最广泛的。网格的实现核心在于双向链表操作：在格子间移动单位需要正确的从旧格子移除+添加到新格子。如果每个格子维护的是vector而非链表，移动的开销会更大。

> 邻近格子检查的半边扫描技巧是一个我见过的最优雅的"避免重复比较"方案。检查所有8个邻居会每对单位比较两次，只检查4个（左上、上、右上、左）恰好覆盖所有邻近格子对且不重复。这和我之前在RTS游戏中实现的笨拙方法形成了鲜明对比。

> 四叉树的讨论揭示了一个重要的设计权衡：平面分区（固定网格）简单但在对象分布不均时效率差，层次分区（四叉树）自适应但在对象频繁移动时更新开销大。游戏中的静态场景（地形、建筑）适合层次结构，动态对象（单位、子弹）适合固定网格——实践中通常会用两套系统分别处理。

> 将空间分区数据结构映射到经典CS数据结构（网格=桶排序、BSP/k-d树=二叉搜索树、四叉树=多叉树）这个视角非常有助于建立知识连接，让这些看似神秘的"游戏专用技术"回归到它们的基础数据结构本质。

## 📌 关键要点
- **核心机制**：将空间划分为区域 → 对象按位置存入 → 查询时只检查同区域/邻近区域
- **复杂度降维**：O(n²)配对检查 → O(n)区域检查（当对象均匀分布时）
- **网格实现**：双向链表存储每个格子中的对象 → 移动时从旧格子移除+添加到新格子
- **半边扫描**：只检查4个邻近格子（非全部8个）→ 避免每对比较两次
- **层次结构**：四叉树递归分割密集区域 → 自适应平衡，但移动更新成本更高
- **数据结构映射**：网格=桶排序、BSP/k-d树=二叉搜索树、四叉树=多叉树

## 原文

### 战场上的单位（RTS例子）
双重循环检查所有单位对 → O(n²)。改为网格分区后，只检查同格子内的单位对。

### 固定网格实现
Grid类维护cells_[N][N]网格，每个格子是Unit的双向链表。handleMelee()遍历所有格子调用handleCell()。单位移动时检查是否跨格子边界，是则移出旧格子、加入新格子。

### 处理邻近格子
攻击有范围时，还需检查邻近8个格子中的4个（半边扫描避免重复比较）。

### 常见空间分区结构
Grid（网格）、Quadtree（四叉树）、BSP（二叉空间分割）、k-d tree、BVH（包围盒层次）
