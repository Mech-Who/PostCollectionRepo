---
title: 游戏存储 IO 性能优化四方案
date: 2026-08-04
category: 游戏开发
depth: 深度
layer: layer2
tags: [C#, SLG, 存储优化, MemoryMappedFile, LRU, 缓冲存档, 异步IO, Unity, GZip, 冷热分离]
source_article: 2026-08-04_CSharpSLG存储性能优化文件系统与IO调优
---

# K-Fragment: 游戏存储 IO 性能优化四方案

## A. Applicable — 适用场景

- SLG/大世界等数据量大的游戏：几十 MB 存档、频繁随机读写
- 任何需要高性能文件读写的 C#/Unity 项目（配置表、地图、存档、战报）
- 存档系统设计（防损坏、防卡顿）
- Unity 异步加载与资源管理优化

## B. Boundary — 边界与注意事项

- **内存映射 ≠ 无成本**：映射大文件占用虚拟地址空间，32 位进程注意地址空间耗尽；映射区不宜过大
- **LRU 淘汰策略要匹配访问模式**：顺序读场景 LRU 效果差（应为预读/流式），随机读场景 LRU 收益最大
- **原子写入的代价**：临时文件+rename 每次写盘多一次 IO——高频小改动应走缓冲合并，低频全量写才直接原子写
- **异步 IO 必须可取消**：无 CancellationToken 的异步是"失控的异步"——用户退游戏/切场景时任务仍占资源
- 压缩与解压有 CPU 成本：GZip 适合冷数据（读写少），热数据压缩反而拖慢访问

## C. Core — 核心要点

1. **MemoryMappedFile + LRU 缓存**：读文件=读内存（系统按页加载），LRU 缓存热数据——随机小文件读取性能数量级提升；核心思路是**用系统能力替代自研复杂度**
2. **BufferedSaveSystem 缓冲存档**：写操作进内存缓冲，按策略（定时/数量/脏标记）批量刷新；**原子写入**（临时文件+rename）防断电损坏；异步队列剥离主线程——**减少 IO 次数、合并 IO 请求**
3. **AsyncFileIO 三件套**：CancellationToken（可取消）+ 超时（超时降级）+ 进度回调（可感知）——异步 IO 可控化
4. **Unity 落地**：异步加载（Addressables/自定义 Loader）+ 冷热分离（热数据内存常驻/冷数据压缩落盘）+ GZip 压缩（约 3-5 倍）
5. **核心原则**：IO 是慢速外设——减少 IO 次数（缓冲/合并）、合并 IO 请求（批量）、让 IO 异步化（不阻塞主线程）
6. **红线**：存档写入必须原子（临时文件 + rename），防止断电/崩溃损坏存档——**宁可多一次 IO，不可损失数据完整性**

## D. Data — 可复用代码骨架

```csharp
// 1. 内存映射 + LRU（骨架）
public class MappedFileCache<T> // T = 反序列化后的数据块
{
    private readonly Dictionary<string, T> _lru = new();      // 热数据缓存
    private readonly Dictionary<string, MemoryMappedFile> _maps = new();
    public T Read(string path, long offset, long size)
    {
        if (_lru.TryGetValue(path, out var hit)) return hit;   // LRU 命中
        var mmf = MemoryMappedFile.CreateFromFile(path, FileMode.Open);
        using var acc = mmf.CreateViewAccessor(offset, size);
        var data = Deserialize(acc);                            // 系统按页惰性加载
        _lru[path] = data;                                      // 写回缓存
        return data;
    }
}

// 2. 缓冲存档（原子写入）
public class BufferedSaveSystem
{
    private MemoryStream _buffer = new();
    public void Enqueue(string key, byte[] data) { /* 合并进 _buffer + 脏标记 */ }
    public async Task FlushAsync(string mainPath, CancellationToken ct)
    {
        var tmp = mainPath + ".tmp";
        await File.WriteAllBytesAsync(tmp, _buffer.ToArray(), ct);
        File.Move(tmp, mainPath, overwrite: true);   // 原子替换，防断电损坏
    }
}

// 3. 异步 IO 三件套
async Task<byte[]> ReadWithTimeoutAsync(string path, CancellationToken ct,
                                        int timeoutMs, IProgress<float> progress)
{
    using var cts = CancellationTokenSource.CreateLinkedTokenSource(ct);
    cts.CancelAfter(timeoutMs);                    // 超时兜底
    var bytes = await File.ReadAllBytesAsync(path, cts.Token);
    progress?.Report(1f);                          // 进度可感知
    return bytes;
}
```
