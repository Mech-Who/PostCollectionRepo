---
url: https://mp.weixin.qq.com/s/JqMTxub7f355xbNUyYuXVA
title: Unity3D入门教程之预制件（Prefab）：游戏开发的"复制粘贴"神器
date: 2026-05-25
source: weixin
status: ok
category: 游戏开发
depth: 🟡标准
tags: [Unity, Prefab, 游戏开发, 教程, 入门, 预制件]
author: 
layer: layer1
sync_si: true
---

## 摘要

Unity预制件（Prefab）是将游戏对象全套配置保存为独立资源文件的机制，实现一次创建、无限复制、统一管理。文章通过从创建到代码动态生成的完整教程，讲解Prefab的核心用法，包括变体、嵌套预制件、Instantiate方法等进阶内容。

---

## 预制件概念

预制件就是一个游戏对象的模板或蓝图。将配置好的对象（模型结构、组件挂载、参数设置、子对象关系）保存成独立资源文件。所有从同一预制件生成的实例都与母版保持连接，修改母版则所有实例自动更新。

## 创建第一个预制件

手把手教程：在场景中搭好"样板间"（创建立方体、调整参数、添加组件）→从Hierarchy拖拽到Project窗口→预制件自动创建。改母版全生效，再也不用一个一个修改。

## 代码动态生成

运行时用Instantiate()方法动态生成预制件实例。支持指定位置和旋转，结合循环可批量生成。基本语法：Instantiate(prefab, position, rotation)。可对生成的实例重命名以便管理。

## 进阶特性

嵌套预制件：预制件里还能包含其他预制件，比如车轮→车身→汽车，模块化开发效率拉满。预制件变体（Variant）可在继承原版的基础上做微调。Apply和Revert用于实例与母版之间的同步。创建预制件后Hierarchy原始实例可删除。

---

## 金句

> 1. "预制件不是什么高深技术，就是个朴实无华但超级好用的效率工具。一次创建，无限复制。改母版，全生效。"
> 2. "嵌套预制件让复杂对象变成乐高积木，拼装组合随心所欲。"
> 3. "最牛的是，这玩意儿不是简单的'复制粘贴'。所有从同一个预制件生成的实例，都跟这个'母版'保持连接。"

---

## 我的理解

这是一篇面向 Unity 初学者的 Prefab 入门教程，核心价值不在于"教你怎么拖拽创建预制件"，而在于帮助建立**"模板驱动"的游戏开发思维**——一次创建、无限复制、统一管理的理念，是从"每个对象单独配置"到"用模板驱动批量生产"的思维转变。嵌套预制件的模块化设计（车轮→车身→汽车）是值得关注的高级用法：将复杂对象拆解为可独立修改的组装单元，既是架构设计也是团队协作的基础。对于已有 Unity 经验的开发者，可重点关注 Prefab Variant 的设计模式——在继承原版的基础上做微调，这是在大型项目中最实用的扩展方式。

---

## 原文

我猜你也遇到过这种头疼事儿——

想做个塔防游戏，辛辛苦苦花了一个多小时，总算捏出来一座像模像样的防御塔。模型拼接得挺漂亮，特效也加了，脚本里的攻击逻辑写得明明白白。

然后你发现……一个塔哪儿够啊！地图上起码得放四五个吧？

行，复制粘贴。Ctrl+D，一个变两个，两个变四个。忙活半天总算摆好了，结果策划突然说："哎，咱们是不是该在塔顶加个火焰特效？看起来更霸气！"

你盯着屏幕上那五个塔，瞬间感觉血压上来了。一个一个点开，找到模型，添加粒子系统，调整位置……重复五遍，手指头都快抽筋了。

要是地图上有一百个塔呢？光是想想就头皮发麻。

还好，Unity早就想到了这茬。他们搞了个神器，专门解决这种"批量复制还得统一管理"的麻烦——预制件，Prefab。

## 一、预制件到底是什么玩意儿？

讲得太官方你肯定听不进去，我用大白话解释一下。

预制件就是个**游戏对象的模板**，或者叫"蓝图"。

你费老大劲配置好一个游戏对象（GameObject），比如那个防御塔，然后把它的**全套配置**——模型结构、组件挂载、参数设置、子对象关系——全都保存成一个独立的资源文件。

这个文件，就是预制件。

下次你需要同样的塔，不用再从头搭一遍。直接从资源库里把这个预制件拖进场景，Unity就自动给你生成一个**一模一样**的实例。

最牛的是，这玩意儿不是简单的"复制粘贴"。所有从同一个预制件生成的实例，都跟这个"母版"保持连接。

你改一下母版（比如给塔顶加火焰特效），所有用这个母版生成的实例，统统跟着变！

是不是感觉游戏开发的效率能翻好几倍？别急，咱们一步步来。

## 二、手把手教你创建第一个预制件

理论说再多不如实际动手。咱们这次不用复杂的防御塔，先从最简单的立方体开始，把这个流程走通。

### 第一步：在场景里搭个"样板间"

1.  打开Unity，新建个3D项目。
2.  在Hierarchy窗口右键，选`3D Object > Cube`，创建一个立方体。
3.  给它改个有辨识度的名字，比如"MyFirstPrefabCube"。
4.  稍微调一下参数，让它跟默认的立方体不一样。比如：
    - Inspector里找到Transform，把Scale改成(2, 1, 1)，让它变成个长方形。
    - 加个刚体组件：点击`Add Component`，搜索"Rigidbody"，加上。
    - 改个颜色：在Materials里换个新材质，或者直接用红色。

现在，你的场景里有个独一无二的长方体了。这就是我们的"样板"。

### 第二步：拖一拖，预制件诞生！

1.  在Project窗口，找个合适的位置（建议专门建个"Prefabs"文件夹）。
2.  直接从Hierarchy窗口，把"MyFirstPrefabCube"这个对象**拖拽**到Project窗口的空白区域（或者文件夹里）。

看到没？Project窗口里瞬间出现了一个蓝色立方体图标的新文件，名字就是你的对象名。

恭喜你，第一个预制件创建成功！

这时候你再看Hierarchy里的原对象，名字是不是变成蓝色了？这就是预制件实例的标志。

### 第三步：试试批量"生产"

现在好玩了。在Project窗口找到刚生成的预制件，直接把它**再拖回Scene视图**。

拖一次，生成一个。随便拖，想拖几个拖几个。

每个新生成的立方体，都跟最初那个"样板"长得一样——同样的形状、同样的刚体、同样的颜色。

## 三、让代码也学会"复制粘贴"

光在编辑器里拖来拖去多没劲。游戏里敌人是一波波刷出来的，道具是随机掉落的，这些都得靠代码在**运行时**动态生成。

这就需要用到`Instantiate()`方法。

### 最简单的实例化

新建个C#脚本，就叫`PrefabSpawner.cs`吧，挂到场景里任意一个空对象上。

    using UnityEngine;

    public class PrefabSpawner : MonoBehaviour
    {
        public GameObject myPrefab;

        void Start()
        {
            Instantiate(myPrefab);
        }
    }

保存脚本，回到Unity编辑器。

你会看到挂载脚本的对象Inspector里，多出来一个"My Prefab"的字段，还是个空槽。

把Project窗口里那个蓝色图标的预制件，**拖到这个空槽里**。

运行游戏，啪嗒一下，一个新立方体就在世界原点(0,0,0)出现了。

### 控制生成的位置和旋转

`Instantiate()`其实有三个主要参数：预制件、位置、旋转。

    void Start()
    {
        Instantiate(myPrefab, new Vector3(5f, 0f, 0f), Quaternion.identity);
    }

### 来点高级的：一次性生成一排

咱们结合循环，搞个有点用的功能。

    public class PrefabSpawner : MonoBehaviour
    {
        public GameObject myPrefab;
        public int numberOfCubes = 10;
        public float spacing = 2f;

        void Start()
        {
            for (int i = 0; i < numberOfCubes; i++)
            {
                float xPos = i * spacing;
                Vector3 spawnPosition = new Vector3(xPos, 0, 0);
                GameObject newCube = Instantiate(myPrefab, spawnPosition, Quaternion.identity);
                newCube.name = "SpawnedCube_" + i;
            }
        }
    }

运行一下，是不是看到一排整齐的红方块？

## 四、"套娃"进阶：嵌套预制件

Unity支持**嵌套预制件**——预制件里还能包含其他预制件。

举个例子：
1. 先做"车轮"预制件（WheelPrefab）
2. 再做"车身"预制件（BodyPrefab）
3. 然后创建"汽车"预制件（CarPrefab），把前两个拖进去

以后生成一辆车，直接实例化CarPrefab就行。

而且修改WheelPrefab，所有用到这个车轮的汽车都会自动更新。模块化开发，效率拉满。

## 五、几个必须知道的"坑"

1. **预制件变体（Prefab Variant）**：基于某个预制件做微调时使用，能继承原版所有修改
2. **Apply和Revert**：对实例的修改可用Apply同步到所有实例，或Revert恢复默认
3. **别乱删原始对象**：创建预制件后，Hierarchy里的原始实例删了也没事

## 六、总结

预制件不是什么高深技术，就是个朴实无华但超级好用的**效率工具**。一次创建，无限复制。改母版，全生效。嵌套预制件让复杂对象变成乐高积木，拼装组合随心所欲。
