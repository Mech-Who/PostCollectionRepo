---
title: "单例模式 — 何时使用，更重要的何时不用"
date: 2026-07-07
category: 游戏开发
depth: 深度
layer: layer2
tags: [单例模式, 设计模式, 反模式, 全局变量, 依赖注入, 服务定位器]
summary: "全书唯一'教你如何避免使用'的章节。单例解决两个问题（唯一实例+全局访问），但全局访问带来的耦合、测试困难和并行不友好远大于收益。提出了多种替代方案：传参、基类获取、全局对象携带、服务定位器。"
source_url: https://gpp.tkchu.me/singleton.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 这是全书唯一教你**避免**使用某个模式的章节。单例模式同时解决两个问题——保证唯一实例和提供全局访问——但后者带来的耦合、测试困难和并行不友好远大于收益。作者提供了多层次替代方案，从最简单的"传参"到服务定位器模式。

## 我的理解

> 这章在游戏开发社区引起了广泛共鸣。作者直言不讳："我从来没有在游戏中使用完整的GoF单例模式。"单例的问题不是技术上的——它确实能工作——而是架构上的：它让全局状态看起来像是"设计好的"，从而鼓励了全局耦合。

> 最精彩的洞察是"管理器类"的反模式。很多游戏代码中充斥着MonsterManager、ParticleManager、SoundManager等单例"管理器"——但它们的存在往往只是因为对象没有被设计好管理自己。如果一个BulletManager的所有方法都可以移到Bullet类中，那这个管理器就是多余的。

> 惰性初始化的陷阱也值得深思。在PC应用中惰性初始化（首次使用时才创建）是好习惯，但在游戏中恰恰相反——你需要在加载画面时控制初始化时机，而不是在紧张的boss战中突然触发几百毫秒的初始化卡顿。

> 替代方案的分层策略很有实践价值：先考虑传参（最简单）→ 不够用就基类提供 → 再不行从已有的全局对象获取 → 最后才用服务定位器。这体现了一个重要原则：全局访问的便利性是有代价的，应该尽可能推迟使用。

## 📌 关键要点
- **双问题陷阱**：单例同时解决"唯一实例"和"全局访问"——前者往往是合理需求，后者才是问题根源
- **管理器反模式**：很多"XXManager"单例只是对象的职责没放对地方
- **惰性初始化陷阱**：游戏中需要控制初始化时机，不能等到首次使用才创建
- **替代方案优先级**：传参 > 基类protected方法 > 已有全局对象携带 > 服务定位器
- **断言单例**：构造器中assert(!instantiated_)可保证唯一实例而不暴露全局访问
- **静态类 vs 单例**：如果只是静态方法集合，直接用静态类，去掉instance()中间层

## 原文

### 为什么后悔
1. 它是全局变量——理解代码更困难、促进耦合、对并行不友好
2. 它解决两个问题但你可能只需要一个
3. 惰性初始化剥夺了内存分配时机的控制权

### 替代方案
1. **传参**：最简单的依赖注入
2. **基类获取**：GameObject基类提供getLog()等protected方法
3. **全局对象携带**：让唯一的Game对象持有Log/FileSystem/AudioPlayer
4. **服务定位器**：定义专门的全局访问类

### 何时真正需要
作者坦诚：从未在游戏中使用完整GoF单例。通常用静态类或运行时断言保证唯一实例。
