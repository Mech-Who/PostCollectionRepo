---
title: "平平无奇的源码，竟藏着Agent的核心秘密？"
date: 2026-05-26
category: AI技术
depth: 深度
tags: [Agent架构, OpenClaw, System Prompt, Skill机制, Agent Loop, 上下文工程, Prompt Engineering, 源码分析]
summary: 基于OpenClaw开源项目源码，深入拆解其Agent架构的三大核心设计：分层组装的System Prompt（像乐高积木）、按需加载的Skill机制（防token浪费）、SkillSnapshot防上下文压缩失忆。文章将Agent架构抽象为"System Prompt是剧本、Skill是扩展包、Agent Loop是演员按剧本行动"。
source_url: https://mp.weixin.qq.com/s/LEzv1rji5RZX889fkZQrqw
source: weixin
status: 📥已采集
sync_status: ✅已同步
---

> **摘要：** 文章从OpenClaw开源项目源码出发，逐层拆解Agent架构的核心设计。三个精华：① System Prompt采用分层乐高积木式组装（含身份/工具/规则/Skill等多模块，按场景深度拼装）；② Skill按需加载机制（资格过滤→路径压缩→token截断→生成SkillSnapshot），避免一次性加载全部内容的token浪费；③ SkillSnapshot防上下文压缩失忆——在上下文窗口满时保持Skill目录不被丢弃。此外还介绍了心跳机制（Agent主动巡逻）、子Agent机制、Silent Reply等设计。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇文章对我的工作有直接参考价值。OpenClaw 的架构设计思路和我目前实践的「三层Skill加载（L1/L2/L3）」本质上是一回事——都在解决同一个问题：Agent如何在有限的上下文窗口里，只加载当前任务需要的知识，而不是把所有东西都塞进去。

几个让我印象深刻的点：

1. **Skill的"资格过滤"**——不是所有Skill在当前环境下都能用。按OS、二进制存在性、环境变量做过滤。这比我目前只是靠名称匹配更严谨。比如macOS的Skill在Windows环境下直接隐藏，不需要等到调用时才出错。

2. **SkillSnapshot机制**——Agent在收到用户消息时，把加载的Skill拍个快照生成到System Prompt里。这样即使后续上下文窗口满了、历史被压缩，Skill指令依然在。这解决了多轮对话中"Agent忘记自己能做什么"的问题。

3. **PromptMode的三档深度**——不同场景用不同深度的System Prompt组合，避免了轻量对话也要带全套工具描述的开销。这和SOUL.md里的三层加载一脉相承。

4. **心跳机制**——Agent能主动"巡逻"而不只是被动等用户消息。这对自动化任务非常关键。

## 📌 关键要点
- **System Prompt分层乐高**：由多个独立模块拼接（身份/工具/规则/Skill/安全），按PromptMode深度组合
- **Skill按需加载流水线**：发现来源 → 资格过滤(OS/二进制/环境变量) → 路径压缩 → token截断 → 生成SkillSnapshot
- **SkillSnapshot防失忆**：在上下文窗口满历史被压缩时，保持Skill目录在System Prompt中不被丢弃
- **心跳机制**：Agent可定期主动执行任务（如08:00自动发送"今日事务"）
- **子Agent机制**：主Agent可派生子Agent执行子任务，子Agent有独立上下文
- **Silent Reply**：Agent判断"没什么要说的"时用特定token代替空回复，避免无意义消耗
- **本质结论**："大模型就一个输入变量。再怎么封装都是在拼string。"

## 原文
（全文见微信公众号「腾讯云开发者」）

## 01 整体架构：三件套
- 用户消息
- System Prompt：是剧本
- Skill：是剧本的扩展包
- Agent Loop：是演员按剧本行动

## 02 System Prompt 是什么？怎么组装？

### 2.1 简化理解
System Prompt = 给 LLM 的"岗前培训手册"，在每次对话开始前注入，告诉模型：
- 你叫什么（身份）
- 你能用什么工具
- 你要遵守什么规则
- 你有哪些 Skill 可以用
- 你的工作目录在哪

### 2.2 OpenClaw 的 System Prompt 分层结构
从 `src/agents/system-prompt.ts` 的 `buildAgentSystemPrompt()` 函数可以看到，System Prompt 由以下模块拼接而成（按顺序）：
- You are a personal assistant running inside OpenClaw.

### 2.3 关键源码：buildAgentSystemPrompt() 函数签名
- `src/agents/system-prompt.ts`

### 2.4 三种 PromptMode：不同场景用不同深度
- promptMode 控制哪些模块被包含

（以下为完整原文…引用腾讯云开发者微信公众号发布的完整内容）
