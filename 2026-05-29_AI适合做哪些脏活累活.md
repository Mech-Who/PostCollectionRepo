---
title: AI适合做哪些脏活累活
date: 2026-05-29
category: AI应用
depth: 深度
tags: [AI游戏开发, Unity, 代码生成, 编辑器工具, 配置管理, 代码审查, 文档生成]
summary: 文章系统梳理了AI在游戏开发中适合处理的"脏活累活"：生成重复代码、写配置导入工具、开发Unity编辑器小工具、命名整理、代码扫描、文档生成、测试清单、重构前分析、胶水代码等。核心原则是任务要小、边界要清楚、输入输出要明确，不要一上来就让AI接管整个项目。
source_url: https://mp.weixin.qq.com/s/e6K0eHFFrVM3Y6OJykeGQg
source: weixin
status: 📥已采集
sync_status: ✅已同步
---

> **摘要：** 文章系统梳理了AI在游戏开发中适合处理的"脏活累活"：生成重复代码、写配置导入工具、开发Unity编辑器小工具、命名整理、代码扫描、文档生成、测试清单、重构前分析、胶水代码等。核心原则是任务要小、边界要清楚、输入输出要明确，不要一上来就让AI接管整个项目。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇文章讲的虽然是"AI做游戏"，但它的核心观点其实超越了游戏开发——它揭示了一个普遍规律：**AI最适合的不是做顶层决策，而是做规则明确的执行层工作。** 这和团队协作很像：你让一个实习生设计架构会很恐怖，但让他按规范生成100行数据类就很稳。

对小涵这种在用Unity做游戏开发的场景来说，这篇文章给出了非常实用的切入点。特别有价值的几个点：

1. **"先扫描不修改"原则**：让AI做检查和整理可以大胆用，但批量修改时一定要先只读检查再决定是否执行。这和我们WorkBuddy中`.done`信号文件防撕裂的边界保障是同一类思想。
2. **文档/任务说明生成**：游戏开发者普遍不喜欢写文档，但项目大了之后AI的上下文依赖文档——让AI给自己擦屁股，这是个有趣的闭环。
3. **胶水代码**：按钮绑定、事件转发这些确实烦，AI写初稿、人审阅，效率最高。

一个值得讨论的点：文章说"不要一上来就让AI直接做大规模重构"，但实际操作中，开发者往往会在"先试试看"和"必须稳妥"之间摇摆。更实际的建议可能是：重构前让AI列步骤和风险点，重构时分批次小范围执行，每批验证。

## 📌 关键要点

- **核心原则**：让AI做"规则明确、边界清楚、输入输出可验证"的任务，不要让它做顶层设计决策
- **适合AI的9类脏活**：
  1. **重复代码生成** — 数据类、配置类等结构相似但数量多的代码
  2. **配置表/数据转换工具** — CSV导入ScriptableObject等编辑器工具
  3. **Unity Editor小工具** — 批量检查Prefab、资源命名校验等
  4. **命名整理** — AI先出建议表格，人确认后再执行改名
  5. **代码扫描** — 事件未解绑、Update中UI刷新、GameObject.Find等常见问题
  6. **文档/任务说明生成** — 模块说明、依赖关系、事件列表
  7. **测试清单生成** — 分正常情况/边界情况/回归测试三个维度
  8. **重构前分析** — 按职责分组方法、标出可拆模块、标注高风险字段
  9. **胶水代码初稿** — UI绑定、事件转发、状态切换等
- **安全红线**（不适合完全交给AI）：
  - 大规模自动改全项目代码
  - 批量删除资源
  - 直接修改存档格式
  - 一次性重构核心战斗系统
  - 批量迁移场景对象
- **最佳实践**：让AI做"第一轮扫描工具"而不是"最终执行者"；批量操作前先只读检查、先备份、先小范围测试
- **代码审查的限制**：AI适合扫常见小问题，但要让AI"只输出问题列表不重写代码"，防止引入新问题

## 原文

在小说阅读器读本章

去阅读

在小说阅读器中沉浸阅读

**这是欧米的第443篇游戏开发原创分享。**

点击关注↓ 解锁价值百万的「灵感生产线」

* * *

前面几篇，我们一直在讲一个观点：AI 做游戏，不要一上来就指望它直接把整个项目做完。先拆需求，再拆模块，再说清楚 Unity 项目的上下文。这些步骤看起来慢，其实是为了让 AI 后面真正帮得上忙。

因为 AI 最适合的地方，往往不是替你做最高层的设计决定，也不是替你拍板一个战斗系统到底好不好玩。它更适合做一些开发里很烦、很碎、很耗时间，但规则又比较明确的事情。

也就是平时开发里那些不难但很烦的活。

比如整理配置、生成重复代码、批量改命名、写导入工具、补编辑器脚本、生成测试清单、检查空引用、把一段逻辑拆成更清楚的结构。这些工作不一定难，但很占时间，也很容易打断思路。做多了以后，人会明显觉得烦。

而 AI 在这些地方，反而很有价值。

#### AI不一定适合拍板，但很适合执行

很多人刚开始用 AI 做游戏，最容易犯的一个错误，是直接把最难的问题丢给它。

比如：

> "帮我设计一个好玩的战斗系统。" "帮我做一个完整的塔防游戏。" "帮我设计一套成熟的数值体系。" "帮我把这个游戏做到商业化水平。"

这些问题不是不能问。AI 往往会给你一份看起来很完整的回答，有系统、有模块、有数值、有流程，还有一些听起来很合理的建议。

可问题是，游戏好不好玩，数值顺不顺，战斗节奏对不对，这些东西最后还是要靠人判断。AI 可以给你很多方案，但最后选哪一个，还是要人来判断。

反过来，如果你给它一个边界清楚的小任务，它通常会做得更稳定。

比如：

> "把这 20 个敌人配置整理成 CSV。" "根据这个字段生成 C# 数据类。" "帮我写一个 CSV 导入 ScriptableObject 的编辑器工具。" "把这段重复的 UI 绑定代码整理成通用方法。" "检查这段代码里有没有事件订阅没解绑。" "根据这个功能写一份测试清单。"

这些事情不一定很高级，但真的能省时间。这就是 AI 比较现实的用法。它不是替你做主程，而是先把那些重复、琐碎、容易出错的活接过去。

#### 重复代码可以先交给 AI 生成

游戏开发里有很多重复代码。

比如你有一堆数据类：

    public class EnemyData
    {
        public string enemyId;
        public string name;
        public int hp;
        public int attack;
        public float moveSpeed;
    }

后面又要做 `TowerData`、`BulletData`、`StageData`、`WaveData`、`SkillData`、`BuffData`。

这些类结构不完全一样，但写法很像。一个一个手写当然可以，但很容易写着写着就烦了。这种活就可以先交给 AI。

你可以直接给它一份字段表，让它生成数据类：

> 请根据下面字段生成 Unity C# 数据类。
> 要求：
> 1. 使用 [System.Serializable]。
> 2. 字段使用 public。
> 3. 不写复杂逻辑。
> 4. 字段名保持 camelCase。
> 5. 只生成数据结构，不要生成读取逻辑。
> TowerData 字段：
> towerId:string
> name:string
> damage:int
> attackRange:float
> attackInterval:float
> bulletId:string
> buildCost:int
> upgradeCost:int

AI 很快就能给你一版：

    [System.Serializable]
    public class TowerData
    {
        public string towerId;
        public string name;
        public int damage;
        public float attackRange;
        public float attackInterval;
        public string bulletId;
        public int buildCost;
        public int upgradeCost;
    }

这类代码没什么技术含量，但开发里经常要写。如果后面字段变多，AI 也可以帮你同步生成构造方法、字段注释、CSV 表头、默认配置示例。

这类任务的好处是规则清楚，检查起来也简单。AI 先生成一版，人再过一遍，一般比从零手写轻松很多。

#### 配置表和数据转换，AI 也能省不少时间

游戏项目一旦开始做内容，就会有各种配置。

敌人配置、塔配置、子弹配置、关卡配置、波次配置、技能配置、掉落配置、本地化文本……这些东西很少是一次写完的。经常是你今天加一个字段，明天改一个命名，后天把几个表拆开，过几天又要从 CSV 转成 ScriptableObject 资源。

这种时候，AI 很适合帮你写小工具。

比如你可以让它写一个编辑器导入器：

> 请帮我写一个 Unity Editor 工具，用于把 TowerData.csv 导入成对应的 ScriptableObject 资源。
> 要求：
> 1. 工具放在 Editor 文件夹下。
> 2. 使用 UnityEditor API。
> 3.  CSV 第一行是表头。
> 4. 每一行生成一个 TowerData.asset。
> 5. asset 命名为 towerId。
> 6. 输出目录为 Assets/GameData/Towers。
> 7. 如果已有同名 asset，则更新，不重复创建。
> 8. 导入完成后调用 AssetDatabase.SaveAssets 和 Refresh。

这种任务的输入和输出都很明确：读什么文件，生成什么资源，放到哪个目录，怎么命名。这些条件说清楚以后，AI 通常能把工具框架搭出来。

当然，AI 写出来的 CSV 解析器未必完美。比如遇到逗号、引号、换行、多语言文本时，简单 `Split(',')` 就可能出问题。所以这类工具还是要审，尤其是项目后面要长期用的时候。

但即使这样，它也能帮你省掉很多重复搭框架的时间。你再根据项目情况改一改，就能变成一个长期可用的小工具。

#### Unity 编辑器小工具，很适合让 AI 先写一版

很多 Unity 项目里，其实缺的不是复杂系统，而是一些小工具。

比如一键清理场景里的测试对象、批量给 Prefab 挂组件、批量重命名资源、把选中的图片生成 Sprite 配置、把场景里的障碍物保存到关卡数据里、把关卡数据重新加载回场景、批量截图关卡预览图、检查某个目录下资源命名是否符合规则。

这些工具对项目很有用，但很多开发者不愿意写。因为它们不是正式玩法，不是上线功能，写起来也不算难，只是细节很多。

比如你想做一个"批量检查 Prefab 是否缺少组件"的工具，可以这样让 AI 写：

> 请写一个 Unity EditorWindow，用于检查指定目录下所有 Prefab 是否缺少 TowerBase 组件。
> 要求：
> 1. 可以选择一个 Assets 下的目录。
> 2. 扫描该目录下所有 .prefab 文件。
> 3. 加载 Prefab 后检查根节点或子节点是否包含 TowerBase。
> 4. 在窗口中列出缺失组件的 Prefab 路径。
> 5. 不要自动修改资源，只做扫描检查。

这个任务边界很清楚：只扫描，不修改。即使第一版不完美，也可以很快迭代。

更重要的是，它能减少很多人工检查。有时候项目里真正浪费时间的，不是写主逻辑，而是在一堆资源、Prefab、场景对象里来回找问题。这些地方，让 AI 写工具，比让人手动点半天要舒服很多。

#### 命名整理这类杂活，AI 比人更有耐心

项目做久了，命名一定会乱。

有的脚本叫 `EnemyController`，有的叫 `EnemyBase`，有的叫 `EnemyManager`，有的叫 `MonsterData`，有的又叫 `EnemyConfig`。刚开始不觉得有什么，但等项目变大，AI 也接进来以后，命名混乱会变成很大的问题。

因为 AI 会根据名字猜职责。

你叫 `EnemyManager`，它可能以为这个类管理所有敌人。你叫 `EnemyBase`，它可能以为这是敌人基类。你叫 `EnemyConfig`，它可能以为它是配置。如果项目里这些名字本来就混用，AI 后面写代码就更容易猜错。

所以命名整理本身，就是一种可以交给 AI 辅助的杂活。

比如你可以让它先整理命名表：

> 我现在项目里有这些类名：
> EnemyBase
> EnemyManager
> EnemyConfig
> MonsterData
> EnemyRuntime
> EnemyView
> EnemySpawner
> 请帮我整理一套更清楚的命名建议。
> 要求：
> 1. 区分配置、运行时实例、表现层、生成器、管理器。
> 2. 不要直接改代码。
> 3. 只输出建议表格。
> 4. 对每个旧名字说明是否建议保留、改名或合并。

这类任务不需要 AI 直接动项目。先让它整理建议，开发者再判断。

等命名方案定下来以后，也不要一上来就让 AI 全项目重命名。更稳的做法，是先让 AI 列出需要修改的文件和引用关系。真正批量改之前，最好先小范围测试，确认不会影响 Unity 的资源引用和序列化字段。

这类整理工作，AI 往往比人更有耐心。但越是批量修改，越不能只图快。

#### 代码里的小问题，可以先让 AI 扫一遍

AI 不一定能替你判断所有架构问题，但它很适合帮你扫一些常见小问题。

比如事件订阅有没有解绑，`Update()` 里有没有不必要的 UI 刷新，有没有直接 `GameObject.Find`，运行时代码有没有引用 `UnityEditor`，有没有把配置数据和运行时状态写在一起，有没有明显空引用风险，有没有重复代码，有没有公共接口被顺手改了。

这些问题人当然也能查，但人工查很累，而且容易漏。

你可以把某个脚本发给 AI，让它只做有限范围的检查：

> 请审查下面这段 Unity C# 代码。只检查这些问题：
> 1. 是否有事件订阅未解绑。
> 2. 是否在 Update 中做了不必要的 UI 刷新。
> 3. 是否使用了 GameObject.Find 或 FindObjectOfType。
> 4. 是否有明显空引用风险。
> 5. 是否使用了 UnityEditor API。
> 不要重写代码。只输出问题列表和修改建议。

这里最重要的是"不要重写代码"。

很多时候你只是想让它帮你看问题，不是想让它直接改。它一旦直接上手改代码，也可能顺手引入新的问题。把它当"第一轮扫描工具"，会比把它当"最终代码审查人"更合适。

#### 文档和任务说明，也可以让 AI 先打底

很多开发者不喜欢写文档。

不是不知道文档有用，而是写起来太麻烦。尤其是一个系统刚写完，脑子里还很清楚，你会觉得"这有什么好写的，我自己知道"。但过两周再回来，就不一定记得了。

如果还要让 AI 继续接着开发，文档就更重要。因为 AI 不知道你的项目历史。你不给它上下文，它就会自己猜。

所以一些简单文档，很适合交给 AI 辅助生成。

比如：

> "根据这段代码，帮我写一个模块说明。" "帮我整理这个系统的输入、输出和依赖。" "帮我把这段功能说明改成任务单。" "帮我生成 Inspector 字段说明。" "帮我写一份测试步骤。"

这些不是很难，但很有用。

比如你写完一个 `WaveSystem`，可以让 AI 帮你整理：

> 请根据下面代码，整理一份 WaveSystem 模块说明。
> 要求：
> 1. 说明它负责什么。
> 2. 说明它不负责什么。
> 3. 列出它依赖哪些模块。
> 4. 列出它会发出哪些事件。
> 5. 列出外部调用它时需要注意什么。
> 6. 不要改代码。

这类文档不需要写得多漂亮。只要能让你自己和 AI 后面都看懂，就已经有价值。

#### 测试清单，可以让 AI 先列一版

游戏开发里，很多 bug 都不是因为代码写不出来，而是因为没有想清楚怎么验收。

一个功能看起来完成了，但到底怎么证明它完成了？比如"减速塔完成了"，那要测什么？

> 塔能不能索敌。子弹能不能命中。敌人速度有没有降低。减速时间到了会不会恢复。重复命中会不会刷新时间。敌人死亡后 Buff 会不会清理。普通塔有没有受影响。

这些测试点，如果每次都靠人临时想，很容易漏。AI 很适合先帮你列一版。

> 请根据下面功能说明，生成测试清单。
> 功能：减速塔子弹命中敌人后，给敌人添加 2 秒减速效果，减速比例为 30%。
> 要求：
> 1. 分正常情况、边界情况、回归测试。
> 2. 每条测试写清楚操作步骤和预期结果。
> 3. 不要写自动化测试代码，只写人工测试清单。

它可能给你列出十几条。你不一定全部照做，但至少有个起点。

尤其是 AI 写的代码，更应该配一份验收清单。否则你只看它"能不能编译"，其实是不够的。

#### 重构前的整理工作，也适合交给AI

AI 可以帮忙重构代码，但直接让它大规模重构很危险。

更稳的做法，是先让它做重构前的整理工作。比如整理类职责、找重复代码、列出公共接口、分析哪些脚本依赖这个类、标出哪些方法可以拆、哪些方法不能动、生成重构步骤。

这类工作比直接改代码安全很多。

比如你有一个很大的 `GameManager`，可以先问：

> 请审查这个 GameManager，但不要重写代码。只做三件事：
> 1. 按职责把现有方法分组。
> 2. 标出哪些职责可以拆成独立模块。
> 3. 标出拆分时风险最大的公共字段和方法。

这样 AI 不会直接动你的项目，只是先帮你把问题摊开。你再决定要不要拆、怎么拆、先拆哪一块。

重构最怕一上来就动刀。让 AI 先做整理和分析，风险会小很多。

#### 胶水代码，适合让 AI 做初稿

游戏项目里有很多"胶水代码"。

它们不复杂，但到处都有。比如按钮点击绑定、UI 文本刷新、配置 ID 查找、对象池取出和回收、事件转发、状态枚举切换、资源路径拼接、简单的编辑器菜单。

这些事情单独看都不难，但数量一多，就会很花时间。AI 可以帮你写这种代码的初稿。

比如 UI 绑定：

> 请根据下面 UI 字段，生成一个简单的绑定脚本。
> 要求：
> 1. 不写业务逻辑。
> 2. 只负责把按钮点击事件转发给外部 Action。
> 3. OnEnable 绑定，OnDisable 解绑。
> 4. 字段为空时打印明确错误。

这种代码不应该让 AI 自由发挥。你要告诉它：只做胶水，不做业务。

这样它就不会顺手把战斗逻辑写进 UI 脚本。胶水代码写得清楚，项目会舒服很多。但它确实没什么创造性，很适合交给 AI 做第一版。

#### 哪些脏活不适合完全交给AI

当然，不是所有麻烦活都适合直接交给 AI。

比如大规模自动改全项目代码、批量删除资源、直接修改存档格式、一次性重构核心战斗系统、自动改大量 Prefab、批量迁移场景对象。这些事情也算"脏活累活"，但风险很高。

AI 可以帮你写工具，可以帮你生成步骤，可以帮你做检查，但最好不要一上来就让它直接执行。尤其是会影响大量资源和已有数据的操作，一定要先备份、先小范围测试。

比如批量改 Prefab，可以先要求：

> 请先写一个只扫描、不修改的版本。列出将要修改的 Prefab 和字段。确认后再写真正修改版本。

这类流程很重要。AI 很快，但它不一定知道什么东西不能碰。

越是批量操作，越要先只读检查，再决定是否真的改。

#### 写在最后

AI 适合做的，不一定都是听起来很厉害的事情。有时候它最有用的地方，反而是那些开发里很烦、很碎、很重复的工作。

整理配置、生成数据类、写导入工具、做编辑器窗口、扫一遍常见代码问题、整理模块说明、生成测试清单、把任务说明改得更清楚、写一些不复杂但数量很多的胶水代码，这些事情以前都要自己慢慢做，现在可以交给 AI 做一部分。

但前提还是一样：任务要小，边界要清楚，输入输出要明确。

不要一上来就让 AI 接管整个项目。先让它把这些琐碎工作做好。

它能省下来的，不只是敲代码的时间。更多时候，是让你少被这些琐碎工作打断，把精力留给真正需要人判断的地方。

## 相关笔记
- [[工程三次进化：Prompt→Context→Harness]] — 同为AI应用实践，可对比AI在编程中的角色演变
