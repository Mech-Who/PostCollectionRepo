---
title: Codex 5.6 Subagents 玩法 — 官方建议把探索、测试、日志分析丢给子 Agent
date: 2026-08-21
category: AI技术
depth: 深度
layer: layer2
tags: [Codex, Subagents, OpenAI, 多Agent架构, Agent协作, AI编码工具, Harness, 上下文有界化]
summary: Codex 5.6 正式支持 Subagents。官方建议把"探索代码库 / 测试 / 日志分析"这种独立工作丢给子 Agent，主线 Agent 保持简洁上下文。这是 OpenAI 在产品层面对 Agent 上下文治理的明确信号——独立工作应隔离，独立验证应自动化。
source_url: https://www.xiaoheihe.cn/app/bbs/link/2594724dee39
source: xiaoheihe
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** Codex 5.6 正式支持 Subagents（子 Agent 模式）。官方甚至建议把"探索代码库 / 跑测试 / 分析日志"这种独立工作流丢给子 Agent，让主线 Agent 的上下文保持简洁。这与 `harness-engineering` 中"上下文有界化"原则完全同构——独立工作流单独隔离，主线只接收"已完成的结果"。

## 我的理解
> 由小林生成，供小涵审阅修改

Codex 引入 Subagents 是 **大厂 AI 编码工具对"上下文爆炸"的明确回应**——主线 Agent 上下文承载不了全部探索/测试/日志细节，必须把"独立工作流" 隔离为子 Agent。这与以下趋势完全同构：

1. **OpenRSI（08-09）**：按算子按需检索"有界证据"上下文
2. **Graft（08-09）**：确定性代码地图让 Agent 跳过盲目搜索
3. **DeepSeek Harness PTC 模式（08-16）**：工具链程序化、上下文不回传
4. **Codex 5.6 Subagents**：把探索/测试/日志工作流外包子 Agent

**四种手段收敛到同一原则**：**只给 Agent 完成任务所需的最小上下文**，精准供给比模型原始探索更决定效率。这是 `harness-engineering`（洞察 16-17）+ `context-engineering`（洞察五）的官方产品级印证。

**Subagents 的工程含义**：
- **上下文隔离**：每个子 Agent 有自己的上下文窗口，主线不接收"探索过程"原始数据，只接收"结论"
- **任务并行**：独立子 Agent 可以并行启动，大任务变成许多小任务并发执行
- **责任清晰**：子 Agent 失败的职责明确，不会污染主线思维
- **可观测性增强**：每个子 Agent 有独立任务日志，调试与审计能力提升

**这与 `master-slave-orchestrator`（主从 Agent 编排器）Skill 的设计哲学同构**——主 Agent 不读子 Agent 的细节内容，只持有路径与状态。

## 📌 关键要点

### Codex Subagents 核心机制
- **官方建议的子 Agent 用途**：
  - **代码库探索**（找模块结构 / 接口定义 / 变更历史）
  - **测试运行**（单元测试 / 集成测试 / 验收测试）
  - **日志分析**（错误日志 / 性能日志 / 业务日志）
- **主线 Agent 收益**：
  - 上下文保持简洁（不被海量探索结果污染）
  - 决策更快（只接收"已完成"的结果而非过程）
  - 调试更容易（每个子 Agent 失败独立诊断）
- **典型工作流**：主线 "修复这个 bug" → 派子 Agent 1 探索相关模块 → 派子 Agent 2 跑测试复现 → 派子 Agent 3 分析错误日志 → 主线综合判断并修改

### 与已有产品对比
| 工具 | Subagent 机制 | 状态 |
|------|--------------|------|
| Claude Code | Sub-agent（任务派发） | 已支持 |
| Codex 5.6 | Subagents（独立工作隔离） | 已支持 |
| Gemini Code Assist | 多任务并行 | 部分支持 |
| DeepSeek Harness | PTC 模式 + 任务轨迹 | 已支持 |

### 上下文有界化的四种范式收敛
| 方法 | 核心机制 | 代表 |
|------|---------|------|
| 按算子检索 | 按需给证据（不预先给全部） | OpenRSI |
| 静态索引 | 确定性代码地图（TF-IDF/BM25） | Graft |
| 程序化工具链 | 工具结果不回传上下文 | dsh PTC |
| 子 Agent 隔离 | 独立工作流单独验证 | Codex 5.6 |

## 适用场景
- 大型项目（>10 万行代码）维护
- 多人协作项目（每个模块独立验证）
- CI/CD 流程与 AI 编码工具集成
- Agent 工程化落地的标准化模式

## 边界与注意
- **Subagent 不是万能**：高度耦合的任务（如修改+测试紧依赖）放子 Agent 反而割裂上下文
- **任务划分**：必须能清晰切分"独立工作"与"主线决策"的边界
- **调试成本**：子 Agent 失败时定位问题需要登录子 Agent 的对话历史
- **权限控制**：子 Agent 应只分配"完成任务所需的最小权限"（沙箱原则）

## 原文摘要（来自小黑盒 Codex 5.6 玩法）

> 以前一个复杂任务扔给 Sol。找代码、翻文件、看日志、跑测试，最后还要自己作判断。什么都让它干。项目一大，主线工程很快就被各种中间结果塞满。

> 现在 Codex 已经正式支持 Subagents。官方甚至建议把探索、测试、日志分析这种独立工作丢给子 Age...

## 相关笔记
- `harness-engineering` — Subagents 是 Harness 的工程化产品实现
- `context-engineering` — 上下文有界化四种范式的收敛
- `2026-08-09_清华开源OpenRSI递归自我进化工程化` — 同样范式的另一实现
- `2026-08-09_Graft给代码建立档案让Agent更省Token` — 同样范式的另一实现
- `2026-08-16_DeepSeekHarness实测一天夯爆了还是拉完了` — 同样范式的另一实现
- `Q184` — 上下文有界化的边界追问
- `Q185` — "一切皆插件" + Subagents 的架构对照
- `master-slave-orchestrator` — 主从 Agent 编排的工程化方法论
