---
title: "子类沙箱 — 用受限操作定义无限行为"
date: 2026-07-07
category: 游戏开发
depth: 标准
layer: layer2
tags: [子类沙箱, 模板方法, 外观模式, 继承, 代码复用, 超能力系统]
summary: "子类沙箱模式通过基类提供受限的protected操作集合，子类在沙箱方法中使用这些操作定义行为。适用于有大量子类需要共享基础能力但又不希望直接耦合底层系统的场景。"
source_url: https://gpp.tkchu.me/subclass-sandbox.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 子类沙箱将子类需要的能力封装在基类的protected方法中，让子类程序员在"安全沙箱"内自由定义行为，无需了解游戏引擎的底层细节。这是外观模式+模板方法模式的结合体。

## 我的理解

> 子类沙箱是一个"软"模式——它的价值不在于实现复杂度，而在于意图的清晰性。其核心洞察是：当你有很多子类（如超能力系统），让子类程序员直接调用游戏引擎的各种API会导致两个问题：冗余代码散布在各处且难以约束、底层修改会随机破坏超能力代码。子类沙箱通过在基类中集中提供move()、playSound()、spawnParticles()等操作解决了这两个问题。

> 这其实是"依赖倒置"的一个变体：子类不依赖底层系统，而是依赖基类提供的抽象操作。基类承担了与底层系统耦合的"债务"，换来的是数百个子类的独立性。这种"债务集中化"策略在大型代码库中非常有效。

> 服务定位器集成的讨论（基类通过Locator::getParticles()获取粒子系统）展示了模式间的协同：子类沙箱决定了"谁提供操作"，服务定位器决定了"操作如何获取依赖"。两者的组合为子类程序员创建了一个完全屏蔽底层复杂性的开发环境。

> 与模板方法模式的正反对照很有意思：子类沙箱是"基类提供操作、子类定义算法"（自底向上），模板方法是"基类定义算法框架、子类提供具体步骤"（自顶向下）。同一个继承结构，谁控制流程决定了模式的名称。

## 📌 关键要点
- **核心结构**：基类提供protected操作（move/playSound/spawnParticles）+ 抽象沙箱方法activate()
- **子类约束**：子类只能通过基类提供的操作与游戏世界交互 → 不直接调用底层API
- **代码复用**：发现的重复模式可提升到基类作为新操作 → 所有子类受益
- **债务集中**：基类与底层系统耦合 → 数百子类保持独立 → 底层修改只需改基类
- **初始化方式**：构造器传参 / 两阶初始化 / 静态状态 / 服务定位器（按需选择）
- **与模板方法关系**：子类沙箱=子类定义流程，基类提供操作；模板方法=基类定义流程，子类提供步骤

## 原文

### 超能力系统
Superpower基类提供move()/playSound()/spawnParticles()/getHeroX()等protected操作。SkyLaunch子类在activate()沙箱方法中调用这些操作实现三段跳劈攻击。

### 操作提供策略
- 直接提供方法：最简单，但基类会变大
- 包在对象中提供：getSoundPlayer().playSound() → 基类保持精简
