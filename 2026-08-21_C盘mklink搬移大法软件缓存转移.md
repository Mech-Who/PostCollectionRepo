---
title: 小众高效的 C 盘清理方法 — mklink 搬移大法（顽固缓存克星）
date: 2026-08-21
category: 工具推荐
depth: 标准
layer: layer2
tags: [C盘清理, mklink, 软链接, Windows技巧, 缓存管理, 符号链接]
summary: 对不允许迁移缓存位置的顽固软件（如浏览器/开发工具缓存），使用 `mklink` 创建符号链接把缓存指向其他盘——软件生成的缓存文件仍认为在 C 盘，物理位置却在目标盘。零侵入式解决 C 盘顽固缓存占用。
source_url: https://www.xiaoheihe.cn/app/bbs/link/c98b72524832
source: xiaoheihe
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 大多数软件都能设置缓存迁移到其他盘，但对一些 **不允许你迁移缓存位置** 的顽固软件（cache 路径硬编码），标准的"设置改路径"办法无效。`mklink /D` 创建目录符号链接是这类情形的杀手锏——在原 C 盘位置建一个"虚拟目录"，实际指向其他盘的物理目录，软件以为缓存还在 C 盘，实际写在目标盘。

## 我的理解
> 由小林生成，供小涵审阅修改

这是一个**零侵入**的解决方案。优点是不需要修改软件本身、不需要破解、不需要找隐藏设置项；缺点是需要命令行操作+理解符号链接的概念。适合"已经用尽常规清理手段但 C 盘仍吃紧"的用户——尤其是开发工具链（IDE 索引/编译器缓存/包管理器缓存）和游戏平台（Steam/Epic 不允许迁移的位置）的顽固占用。

**关键 trick 是"先转移、后链接"**：必须先把原缓存文件 **移动** 到目标盘（用 `robocopy /MOVE` 或资源管理器剪切），再删空原目录，最后用 `mklink /D 原路径 新路径` 创建软链。否则会在原位置残留真目录导致 `mklink` 报错"目录已存在"。

**与同类工具对比的边界**：
- 简单迁移（设置改路径）→ 软件支持时优先用
- mklink 软链接 → 软件不支持设置迁移时用；可以挪动大型缓存但软件完全无感知
- `MoveFile` 资源管理器剪切 → 仅支持普通目录；符号链接保留软件兼容性

**风险点**：
1. mklink /D 是软链接（符号链接），软件如果自己做了"判断是否在系统盘"的逻辑可能异常（实际非常少见）
2. 链接被删除时，**只会删除链接不删除目标**——但如果误删真目录后建立的链接，可能会删到目标
3. 不适合云同步盘（如 OneDrive/坚果云）目标位置——符号链接可能不跨设备同步

## 📌 关键要点

- **核心命令**：`mklink /D "<原C盘路径>" "<目标其他盘路径>"`
  - 例：`mklink /D "C:\Users\xxx\AppData\Local\SoftwareCache" "D:\AppCache\Software"`
- **必做前置**：先把原目录的缓存文件**实际移动**到目标盘，**清空原 C 盘位置**（否则 mklink 报"目录已存在"）
- **有效场景**：浏览器缓存（部分版本）、Steam 下载缓存（部分）、IDE 索引目录（VS/JetBrains userHome）、npm/yarn/pip 缓存
- **无效场景**：C 盘系统文件（Windows、Drivers、Program Files 系统目录）、需要管理员权限才能操作的位置
- **验证方法**：在 C 盘原位置看到 `<JUNCTION>` 标记（dir 命令），或写入测试文件后到目标盘能看到

## 操作步骤

```powershell
# 步骤 1: 移动原目录内容到目标盘
robocopy "C:\原路径" "D:\目标路径" /E /MOVE
# /E = 包含空子目录, /MOVE = 移动而非复制

# 步骤 2: 确认 C 盘原位置已清空
dir "C:\原路径"
# 应显示 "系统找不到指定的文件"

# 步骤 3: 创建符号链接
mklink /D "C:\原路径" "D:\目标路径"

# 步骤 4: 验证
dir "C:\原路径"
# 应显示 <SYMLINKD> 标记
```

## 适用场景
- 已经清空回收站、temp、下载文件夹但 C 盘仍不够用
- 顽固软件缓存（硬编码路径）如浏览器 developer tools 缓存
- 想保留 C 盘 50% 以上可用空间的强迫症用户
- 系统盘在 M.2 小容量 SSD（如 256GB/512GB），数据在机械盘场景

## 边界与注意
- **不适用云同步盘作为目标**：符号链接在 OneDrive 等可能不会跨设备同步
- **备份重要数据**：移动操作前先复制一份到目标盘
- **不要对系统目录使用**：Program Files、Windows、System32 等用 mklink 可能触发系统保护
- **移动前关闭软件**：避免文件占用导致 `robocopy /MOVE` 失败或部分文件未移动

## 原文（来自小黑盒 cube_开心 推荐）

**适用情况**：对一些比较顽固的软件缓存。

**核心 trick**：一些软件它不允许你去迁移它的缓存位置，你就可以使用 `mklink` 来迁移它的缓存，往后软件生成的缓存也会指向迁移目标，而非 C 盘。可以有效的清理 C 盘空间。

## 相关笔记
- `windows-磁盘管理`（待建）— 磁盘清理工具盘点
- `windows-符号链接`（待建）— mklink 完整用法（硬链接 /H、软链接 /D、目录联接 /J 区别）
