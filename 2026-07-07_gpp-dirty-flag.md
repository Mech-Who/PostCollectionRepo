---
title: "脏标识模式 — 把工作推迟到真正需要时"
date: 2026-07-07
category: 游戏开发
depth: 标准
layer: layer2
tags: [脏标识, 缓存一致性, 延迟计算, 场景图, 世界变换, 性能优化]
summary: "脏标识模式通过一个布尔标记追踪'导出数据是否过期'，在原始数据变化时设置标记，在需要导出数据时检查并重新计算。适用于原始数据变化远快于导出数据使用频率的场景。"
source_url: https://gpp.tkchu.me/dirty-flag.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 脏标识是游戏开发中最常见的微观优化模式。场景图中用一位脏标识追踪世界变换是否需要重算，避免每帧对所有对象做矩阵乘法。作者给出了父子脏标识传递的精妙实现，并讨论了延迟计算的代价。

## 我的理解

> 脏标识模式揭示了"缓存"这个计算机科学中最难问题的微观版本。它的核心是两句话：变化时设脏，使用时检查并清理。听起来简单得不像一个"模式"，但作者通过场景图的例子展示了它在实践中的微妙之处。

> 父子脏标识传递是本章最精巧的设计。当父节点移动时，它的所有子节点的世界变换也变了——但逐个递归设置子节点的脏标识太昂贵。解决方案是在render()遍历时传递一个dirty参数（dirty |= dirty_），让子节点在需要时才判断是否需要重算。这体现了"推迟决策"原则的极致应用。

> "每次状态改变都必须设置脏标识"这个看似简单的规则，在实践中是最大的bug来源。如果有一处修改了原始数据但忘了设脏，就会导致使用过期的导出数据——这类bug极难追踪。将数据修改隐藏在接口后面（setter自动设脏）是标准防御策略。

> 脏标识的代价——延期太久——在文件保存场景中最为明显。文本编辑器的"未保存"星号就是脏标识，但如果在保存前断电，所有工作都会丢失。解决方案是增设自动保存的中间频率——这与增量GC回收vs标记-清除GC的权衡是同构的：频繁处理代价小但频繁，集中处理代价大但发生少。

## 📌 关键要点
- **核心机制**：原始数据变化 → 设置脏标识 → 需要导出数据时检查 → 重算并清除
- **场景图应用**：节点的自身变换改变 → 设脏 → 渲染时才重算世界变换
- **父子传递**：通过render(parentWorld, dirty)参数传递脏状态，避免递归设置子节点
- **缓存一致性**：任何修改原始数据的代码都必须设脏——遗漏=过期数据=难追踪bug
- **防御措施**：将数据修改隐藏在setter接口后，自动设置脏标识
- **延期代价**：推迟到需要时才计算 → 可能导致可察觉卡顿（需考虑后台预计算）
- **典型应用**：场景图变换、文本编辑器"未保存"标记、Angular脏检查、物理引擎休眠检测

## 原文

### 场景图世界变换延迟计算
每帧为所有对象计算世界变换（矩阵乘法）浪费CPU。添加dirty_标识：自身变换改变时设脏，渲染时检查并只重算脏节点。通过parentWorld世界变换和父链脏传递，避免递归设置子节点的脏标识。

### 脏粒度的权衡
- 细粒度（每个属性单独脏位）：只处理真正改变的数据，但内存开销大
- 粗粒度（整个对象一个脏位）：内存省，但可能处理未改变的数据
