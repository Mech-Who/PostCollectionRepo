---
title: "类型对象 — 用数据定义新类型而无需新建类"
date: 2026-07-07
category: 游戏开发
depth: 深度
layer: layer2
tags: [类型对象, 数据驱动, 继承替代, 品种系统, 工厂方法, JSON配置]
summary: "类型对象模式用Breed类的实例代表不同的怪物'品种'，替代传统的Dragon/Troll子类继承。通过数据配置、属性继承和工厂方法，让设计师不修改代码就能创建和调整新类型。"
source_url: https://gpp.tkchu.me/type-object.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 类型对象是"组合优于继承"在类型系统层面的应用。不再为每种怪物创建子类，而是用一个Breed类实例描述品种属性。通过JSON配置+原型继承+工厂方法，设计师可以自行创建数百种怪物变体而无需程序员介入。

## 我的理解

> 类型对象模式解决了一个我曾在工作中反复遇到的痛点：产品/策划想调整一个数值（比如巨魔血量从48改到52），需要走"改代码→编译→测试"的循环，整个流程耗时可能是"改数据文件→重载"的100倍。类型对象模式将"类型"从编译时世界迁移到运行时世界，让数值调整变得像改配置文件一样简单。

> 品种继承的"复制继承"vs"动态委托"是一个经典的空间换时间决策。复制继承在构造时就将父品种的属性拷贝到子品种（不需要存储父品种引用，查找O(1)），适合属性不变的场景。动态委托每次查找都回溯继承链（查找O(h)，h为继承深度），但允许运行时修改。游戏数据通常是不变的——所以复制继承几乎是默认的正确选择。

> 将类型对象的品种继承与JSON配置的prototype字段联系起来，展示了数据驱动设计的完整图景。Troll Archer的health设为0表示"继承父品种Troll的值"——这种用特殊值（0=继承，NULL=继承）表示"不覆盖"的约定比显式标记更简洁，但需要文档说明。

> 与C++虚函数表的类比尤其精彩。Breed对象本质上就是手写的vtable——它存储了类型级别的数据（health/attack等字段），Monster持有指向Breed的引用就像对象持有vptr。这个类比让"类型对象"这个概念在C++程序员脑海中立即清晰起来。

## 📌 关键要点
- **核心转换**：Monster子类体系 → Monster + Breed（类型对象）——类变成了数据
- **构造函数工厂**：Breed::newMonster()替代直接new → 集中控制内存分配（对象池/自定义分配器）
- **品种继承**：通过parent_指针实现单继承 → 子品种未设置的属性从父品种继承
- **复制vs委托**：构造时拷贝父属性（快，O(1)） vs 运行时动态查找（灵活，O(h)）
- **JSON配置**：Troll Wizard {parent: Troll, health: 0, attack: "..."} → 0表示继承
- **行为定义**：类型对象擅长数据类型，但行为需要额外机制（函数指针/策略模式/字节码）
- **与vtable类比**：Breed就是手写的C++虚函数表——数据层面的类型信息

## 原文

### 从子类到类型对象
Dragon/Troll extends Monster → Monster持有Breed引用。Breed存储初始血量、攻击字符串等共享数据。添加新品种只需创建新的Breed实例而非新类。

### 工厂方法
Breed::newMonster()封装对象创建 → 可控制内存分配策略（从对象池取、使用特定分配器）

### 品种继承
Breed的parent_字段实现单继承。构造时的复制语义：如果子品种的health==0且parent!=NULL，则health=parent->getHealth()。
