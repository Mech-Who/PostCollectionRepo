---
title: 游戏开发：聊聊AOI
date: 2026-05-27
category: 游戏开发
depth: 🔴深度
tags: [AOI, 空间分区, 九宫格, 游戏服务器, MMO, SLG, 网络同步, 性能优化]
summary: 游戏服务器端AOI（感兴趣区域）管理的硬核技术文章，系统介绍AOI定义、11种实现方案及其设计思想，澄清常见误区。
source_url: https://mp.weixin.qq.com/s/crxn3hYD4FiPgBIOA9-wGg
source: weixin
status: 📥已采集
sync_status: ✅已同步
---

> **摘要：** 游戏服务器端AOI（Area Of Interest）管理的系统级技术文章。作者从AOI定义澄清入手，详细讲解了全场景广播、空间分区、九宫格、分层AOI、网格订阅、LOD、分组AOI、AOI代理、AOI订阅者、AOI线程和更新频率等11个技术方案的设计思路与适用场景，覆盖MMO和SLG两大类游戏的需求差异。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇AOI文章质量很高，适合做游戏服务器开发的同学深入阅读。几个让我印象深刻的地方：

1. **AOI ≠ 空间分区**：作者明确指出了很多开发者的常见误区——把四叉树、八叉树当成AOI算法本身。实际上空间分区只是底层基础设施，AOI是上层的业务逻辑。这个区分很关键，能帮助初学者避免概念混淆。

2. **九宫格的本质是"以格子为中心"的订阅**：传统理解中容易把九宫格想成"玩家为中心"的遍历，但作者指出实际上MMO中可以将订阅转化为以格子为中心，实现内存共享。这个视角转变很有价值。

3. **SLG与MMO的AOI差异**：SLG没有"角色"只有"视角"，这决定了它的AOI设计完全不同（网格订阅 + AOI订阅者 + AOI线程），这种分类思维对做不同品类的开发者很有参考意义。

4. **分层AOI的设计取舍**：作者建议MMO通常只需要两层（近处+全局），避免过度设计。这种"够用就好"的思路在游戏开发中值得提倡。

5. **AOI代理是复杂场景的终极方案**：把非GameObject数据伪装成实体同步，同时能优雅解决分组AOI问题，抽象层次高但灵活性也最高。

作为一个做Unity/游戏开发的读者，这篇文章对理解服务器端设计非常有帮助——虽然客户端不直接涉及AOI，但了解同步机制的原理有助于和服务器端同学对齐认知。

## 📌 关键要点

- **核心定义**：AOI指的是维护客户端屏幕内的可见实体（网络同步实体范围），不是AI角色的可见范围
- **九宫格粒度公式**：以屏幕四周各预留25%缓冲带来计算格子大小
- **MMO推荐方案**：九宫格 + 分层AOI（近处+全局两层）
- **SLG推荐方案**：网格订阅 + AOI订阅者抽象 + AOI线程
- **更新频率**：每秒1~10帧，不宜过高（客户端频繁加载卸载实体）也不宜过低（影响碰撞检测）
- **性能优化**：通过"订阅制"解决网格迭代成本；高密度场景可用AOI线程分担广播压力
- **空间分区选型**：网格、四叉树/八叉树、BSP、KD树等，以额外内存换取查询速度

## 原文

### 游戏开发：聊聊AOI

本文是一篇关于游戏服务器端AOI（Area Of Interest，感兴趣区域）管理知识的硬核技术文章。作者系统地介绍了AOI的定义、各种实现方案及其背后的设计思想，并澄清了常见误区。

#### 一、AOI的定义

- **标准定义**: 实体（Entity）只与感兴趣区域内的其它实体交互。
- **常见误解澄清**: 作者指出，AOI在行业惯例中通常指的是**维护客户端屏幕内的可见实体（网络同步实体范围）**，而非指维护每个实体（如AI角色）可见的所有实体。后者虽然底层实现相似（依赖空间分区），但通常不属于AOI的核心范畴。

#### 二、AOI实现方案

**1. 全场景广播**
- **适用场景**: 每个场景限定为少量玩家和对象的房间类游戏。
- **不适用场景**: MMORPG，因为一个场景中可能有数百个玩家，全场景广播会产生广播风暴，压垮服务器和客户端。
- **核心思想**: 不能为了减少广播量而牺牲游戏体验。

**2. 空间分区（基石）**
- **核心作用**: 优化基于位置的查询，将全局O(n)或O(n²)的查询复杂度降低到局部小规模量级。
- **实现方式**: 网格、四叉树/八叉树、二分空间分割（BSP）、KD树等。
- **特点**: 以额外内存换速度。
- **关键澄清**: **空间分区 != AOI**。作者强调，许多开发者将空间分区算法（如四叉树、八叉树）称为AOI算法是错误的。空间分区是底层算法，为AOI、碰撞检测等上层业务提供支持。AOI是更上层的业务。

**3. 经典九宫格（MMO）**
- **核心逻辑**: 将场景切分为固定大小的网格。玩家所在格子及其周围8个相邻格子为玩家的可见区域。事件也以此范围进行广播。
- **数据存储**: 作者解释了为何九宫格AOI中很少看到显式的订阅数据。因为MMO中，同一格子玩家的可见范围相同，可以将以玩家为中心的订阅转换为以格子为中心的订阅，实现内存共享。
- **网格粒度**: 不能太大（同步不必要的实体），也不能太小（导致进出频繁）。行业经验是**以屏幕四周各预留25%的缓冲带来计算格子大小**。
- **命名误区**: "九宫格"不一定是9格，也可以是25格（5\*5）或49格（7\*7）。高密度、小碰撞范围的场景适合更小的网格。
- **性能优化**: 可以通过"订阅制"解决迭代网格的成本问题，即玩家主动注册到需要接受消息的网格中。

**4. 分层AOI（MMO）**
- **解决需求**: 玩家除近处实体，还能看见远处的特殊实体或全局实体（如大Boss）。
- **实现原理**: 将AOI划分为多个视距层。每层仍使用九宫格算法。玩家同时监听所有层级，广播消息时只广播给实体所在层的监听者。
- **业务示例**: `AoiLayer` 枚举包含 `Near`, `Far`, `Global`。
- **性能建议**: 避免过多层级划分，MMO通常只需两层（近处 + 全局）。

**5. 网格订阅（SLG）**
- **需求差异**: SLG类游戏要求客户端镜头范围内（动态变化）的所有实体，而非MMO的"内圈所有+外圈部分"。
- **解决方案**: 玩家主动注册到其看到的网格中。消息广播时，只广播给订阅了该格子的玩家。
- **示例场景**: 玩家镜头拉升的过程，就是9宫格 => 25格 => 49格的过程。

**6. 层次细节（LOD）**
- **核心思想**: 玩家镜头越远，同步的实体数据越少，类似于客户端渲染的LOD。
- **实现**: 在构建同步信息时，根据LOD等级（如 `FULL`, `DETAIL`, `ROUGH`）决定填充的数据粒度。
- **应用**: 广播消息的接口需要指定LOD参数，以控制广播范围。

**7. 分组AOI**
- **解决需求**: 以组为单位进行AOI管理，如：玩家队伍的可见性由队长决定，或一组NPC由领队决定。
- **实现方案**:
    1.  **推荐方案**: 将计算AOI用的坐标与场景真实坐标解耦。在更新AOI前，按业务规则（如设置队长坐标）更新队员或NPC的AOI坐标，保持AOI核心逻辑简单。
    2.  **备选方案**: 将队员设为无需AOI管理，在队长AOI事件中修改队员网格（事后弥补型，适用性有限）。

**8. AOI代理**
- **解决需求**: 将非GameObject的数据（例如某个技能效果、标记点）伪装成GameObject同步给客户端。
- **解决方案**: 引入独立的、抽象的AOI层。将GameObject和非GameObject都映射到这个AOI世界中的实体（Agent）上。
- **优势**: 灵活性最高，是复杂AOI的终极方案。也能轻松解决分组AOI问题（多个GameObject映射到同一个Agent）。

**9. AOI订阅者**
- **解决需求（SLG）**: 玩家在场景中并不存在一个可见的GameObject，如何维护客户端的订阅数据？
- **解决方案**:
    1.  **方案一**: 为玩家创建一个不可见的GameObject，像MMO一样管理AOI。
    2.  **方案二**: 维护独立的订阅者抽象（Subscriber），解除与GameObject的依赖。更灵活、更轻量。
- **注意**: 两种方案都能实现多视角AOI。

**10. AOI线程（SLG）**
- **解决需求**: 高密度玩家场景下，主线程广播消息时迭代列表消耗过多CPU时间。
- **解决方案**: 引入独立的AOI线程，负责最终的广播实现。
- **工作机制**:
    1.  主线程同步AOI数据和连接信息到子线程。
    2.  主线程发包时，通知消息所在格子和LOD，子线程进行精确广播。
    3.  所有玩家数据包都通过子线程发送，避免时序错误。
- **适用范围**: 主要适用于SLG这类单世界场景，不推荐用于MMORPG（维护镜像AOI过于复杂）。

**11. AOI更新频率**
- **频率范围**: 每秒 1 ~ 10 帧。
- **不宜太高**: 频繁更新可能导致客户端频繁加载/卸载实体。
- **不宜太低**: 影响基于AOI的实体搜索和碰撞检测。
- **指导原则**: AOI的更新频率不应影响游戏逻辑的正确性。如果无法协调，可以维护两套空间分区（AOI与碰撞检测）。

#### 三、结语

-   作者没有贴大量代码，而是提供解决问题的思路。
-   建议初学者先从MMO项目的九宫格实现学起。
