---
title: "对象池模式 — 放弃new和delete的自由"
date: 2026-07-07
category: 游戏开发
depth: 深度
layer: layer2
tags: [对象池, 内存管理, 碎片化, 空闲列表, 粒子系统, 性能优化]
summary: "对象池模式通过预分配固定大小的对象数组并重用，避免了频繁new/delete导致的内存碎片和分配开销。空闲列表(free list)技巧利用未使用对象自身的内存存储链表指针，实现O(1)分配和回收。"
source_url: https://gpp.tkchu.me/object-pool.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 对象池是游戏开发中与内存管理器"夺权"的模式。通过预分配对象数组并按需重用，避免了堆分配的两个噩梦：内存碎片和分配延迟。空闲列表技巧利用union复用未使用对象的内存存储链表指针——零额外内存实现了O(1)分配。

## 我的理解

> 对象池在主机游戏开发中是标配，在PC/移动游戏中则需要权衡。主机游戏强制"浸泡测试"（demo模式运行数天不崩溃），碎片导致的内存分配失败是主要失败原因。但对象池也带来了新负担：池大小需要精心设置（太小崩溃、太大浪费），对象重用时的状态清理需要额外谨慎。

> 空闲列表（free list）是对象池的点睛之笔。利用union让"未使用的粒子"和"指向下一个空闲粒子的指针"共享同一块内存——这是一个精巧的零成本抽象。这让我想起了Rust的enum和Option优化：当编译器知道某些位模式不可能出现时，就可以复用那块空间。

> O(1)创建的实现尤其精彩：直接从空闲列表头部取一个 → 不需要遍历查找空闲槽。但这也意味着粒子的回收顺序是LIFO（后进先出），可能导致缓存行为的变化——最新回收的粒子内存可能还在缓存中，但它的重用会导致该cache line被重新写入。

> 与享元模式的关键区别值得反复强调：享元是**同时**共享（多个上下文引用同一对象），对象池是**先后**重用（前一个用完、后一个再用）。两者都管理对象生命周期，但适用场景完全不同。

## 📌 关键要点
- **核心机制**：预分配对象数组 → 使用时标记"使用中"并初始化 → 用完标记"未使用"
- **空闲列表**：union复用未使用对象的内存存储next指针 → O(1)分配+零额外内存
- **内存碎片**：频繁new/delete造成堆碎片化 → 对象池预分配一块连续内存避免
- **池大小权衡**：太小=崩溃，太大=浪费 → 特定场景可能需要动态调整
- **重用陷阱**：重用对象不会自动清零 → 必须完整重新初始化 → 建议debug模式清零
- **与享元区别**：享元=同时共享（森林的树共享TreeModel），对象池=先后重用（粒子死亡后重用）
- **GC语言中**：对象池在GC语言中仍有价值——避免构造/析构开销和减少GC压力

## 原文

### 粒子系统实现
ParticlePool维护Particle数组和firstAvailable_空闲列表头。create()从列表头取一个粒子，animate()中粒子死亡后放回列表头。

### 空闲列表原理
```cpp
union { struct { double x, y, xVel, yVel; } live; Particle* next; } state_;
```
活跃粒子使用live字段，未使用粒子使用next指针——两者共享同一块内存。
