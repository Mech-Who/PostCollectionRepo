---
title: "更新方法 — 让每个对象活起来"
date: 2026-07-07
category: 游戏开发
depth: 标准
layer: layer2
tags: [更新方法, 游戏循环, 实体系统, 组件模式, 状态机, Unity MonoBehaviour]
summary: "更新方法模式让每个游戏实体通过实现update()方法封装自己的逐帧行为，游戏循环统一调用。这是构建游戏实体系统的基础模式，解决了行为代码散落在主循环中的混乱问题。"
source_url: https://gpp.tkchu.me/update-method.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 更新方法是最简单的游戏编程模式之一——每个游戏实体实现update()方法，游戏循环每帧遍历调用。它解决了"所有对象的行为代码混在主循环中"的问题，将行为封装到各自对象内。

## 我的理解

> 这个模式简单到几乎不值得单独成章——但正因如此，它揭示了游戏编程中最基本也最重要的设计原则：每个对象管理好自己的行为。对比书中开头的"骷髅巡逻"代码从for循环版本到update()版本的转变，可以看到将连续行为切分为逐帧步骤带来的复杂性（需要显式状态变量追踪方向），以及封装带来的收益（每个实体自带全部所需状态）。

> 子类化Entity来定义行为的方法在现代游戏开发中已被组件模式取代，但update()方法本身——无论是放在Entity子类、Component类还是委托对象中——仍然是游戏引擎的基础原语。Unity的MonoBehaviour.Update()、Unreal的Tick()都是这个模式的直接体现。

> 可变时间步长的处理展示了简单模式在真实场景中的复杂性：传入elapsed时间意味着需要考虑"步长太大导致越界"等边界情况——原本简单的`x += 1`变成了需要越界回弹的`x += elapsed; if (x >= 100) x = 100 - (x - 100)`。

> 隐藏对象管理的讨论也很有实践价值：大量不活跃对象遍历检查浪费CPU，分离活跃/不活跃集合是常见优化——但这引入了保持同步的额外复杂度。

## 📌 关键要点
- **核心循环**：游戏主循环遍历对象集合 → 逐一调用update() → 每个对象封装自己的逐帧行为
- **状态保存**：切分为逐帧步骤需要显式状态变量（如patrollingLeft_追踪方向）
- **演变路径**：Entity子类 → 组件模式 → 委托对象（状态模式/类型对象）
- **时间处理**：可变步长需传入elapsed并处理越界 → 复杂性随灵活性增长
- **隐藏对象**：分离活跃/不活跃集合可节省CPU，但需保持两者同步
- **Unity对照**：MonoBehaviour.Update()就是这个模式的标准实现

## 原文

### 动机
游戏中的所有敌人、道具、效果都需要每帧更新。最简单的方式是每个实体封装update()，游戏循环统一调用。这取代了将所有实体代码散落在主循环中的混乱方案。

### 子类化Entity
通过继承Entity并重写update()来定义不同行为。Skeleton子类实现巡逻逻辑，Statue子类实现定时射击。随着实体种类增多，子类化变得难以维护。

### 组件化替代
现代方案是用组件模式——update()在组件中而非Entity中，通过组合而非继承来构建实体行为。
