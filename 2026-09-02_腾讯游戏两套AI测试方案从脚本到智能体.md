---
title: "腾讯游戏拆解：两套全新 AI 测试方案，从固定脚本到自主智能体"
date: 2026-09-02
category: 游戏开发
depth: 深度
layer: layer2
tags: [腾讯, WeTest, AI测试, 游戏测试, AI Agent, 证据链, 强化学习, 黑神话悟空, 可复用架构, 视觉导航]
summary: 腾讯游戏 WeTest 在 gamescom dev 首次公布两套互补的游戏测试智能体：①Acorn AI 测试智能体（叶桂生）——"目标驱动与状态闭环"，把测试逻辑从"脚本跑完了吗"转向"游戏状态是否在预期内且可被验证"，以 Agent Loop 为中心、外围知识层/运行时可观测层/分析学习层三层支撑，靠"证据链"实现从发现掉帧到定位根因的闭环；②可复用 Agent 架构（高文）——"纯视觉、零接入、架构跨品类复用"，Brain/Cerebellum/Master Agent 三级分工，用《黑神话：悟空》做评估环境（约 9 小时自主通第一章、120h 轨迹 SFT + 50h 战斗 RL、Boss 胜率地狼 90%/沙国王 80%）。核心结论：不是用 AI 盲目取代脚本，而是让"AI 决策 + 脚本稳定执行 + 证据闭环验证"三者协同。
source_url: https://mp.weixin.qq.com/s/p61fZyaMBHi7DDIxIsrpPw
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** 腾讯 WeTest 首次登上 Devcom 舞台，公布两套互为补充的 AI 测试智能体方案。Acorn 主打"目标驱动 + 状态闭环"，将测试从"脚本跑完"转向"状态是否在预期内且可验证"；其架构中心是 Agent Loop，外围由知识层（Game Graph/Skill Library/State Model）、运行时可观测层（Action Trace/UI 截图/日志/遥测，毫秒级对齐成证据链）、分析与学习层（验证→归因→反哺）三层支撑。可复用 Agent 架构主打"纯视觉、零接入"，用 Brain（System 2 视觉理解与长程规划）/Cerebellum（System 1 低延迟帧级执行）/Master Agent（编排）三级分工，以《黑神话：悟空》为评估环境：约 9 小时自主通第一章、120 小时轨迹微调 + 50 小时战斗强化学习、Boss 战胜率泛化到未训练 Boss（地狼 90%、沙国王 80%）。两条路线的共同结论：AI 不是取代脚本，而是"AI 决策 + 脚本执行 + 证据验证"协同。

## 我的理解
> 由小林生成，供小涵审阅修改

这场分享最值得小涵吸收的，是**两套方案共同指向的一个架构原则：把"决策"和"执行"分开，把"执行"和"验证"闭环**。Acorn 的感知-操作-判断三模块，和可复用架构的 Brain-Cerebellum-Master Agent，其实是同一套分层思想在两个粒度上的实现——都是"慢的想清楚、快的做稳定、最后拿证据确认"。

这里有几个可以直接迁移到 Unity 游戏开发的判断：

一是**"结构化原生状态优先于纯视觉"**。Acorn 明确说：视觉信息直观但不够精确，结构化游戏原生数据才能让 Agent 最可靠地掌握游戏内真况。翻译到游戏开发，就是不要迷信"纯画面 + 大模型"这条路，能拿到确定性数据（坐标、状态、事件）就优先用数据，视觉只是兜底。这对小涵做战斗系统事件驱动重构是个印证——解耦 damage 与 hit reaction、引入 intensity 等级和 batch GUID，本质上就是在为系统提供"结构化、可验证的状态"，而不是靠表现层去猜。

二是**"证据链"这条线的价值被严重低估了**。Acorn 的杀招不是让 AI 跑测试，而是把所有数据（状态、事件、日志、遥测、截图）以毫秒级精度对齐到同一时间节点，让"多源数据瞬间聚合、完整还原现场"。它把自动化测试从"发现现象（掉帧）"升级到"定位根因（哪个函数/资源调用导致）"——因为"知道卡顿只是第一步，回答为什么卡顿才耗研发精力"。小涵做受击反馈重构、做寻路调优时，也值得给系统加上这种"时间线对齐 + 证据留痕"的能力，Debug 效率会质变。

三是**"可复用的是架构，不是策略"**。高文反复强调：他们复用架构、Agent 接口、训练管线和评估方法，但每款游戏仍需专门 SFT 和 RL。这是对"通用 Agent"幻想的一次清醒纠偏——跨游戏迁移不能全自动完成。落到小涵的自研 ECS 框架、NPC 系统、寻路系统，同样的道理：**把框架、接口、评估方法做成可复用，把具体游戏逻辑做成可插拔**，而不是妄想一套代码吃所有项目。

四是**RL 的泛化证据很有说服力**：只用第一章 3 个 Boss 训练，策略能泛化到未训练的 Boss（地狼 90%、沙国王 80%）。这说明"学到了可迁移的战斗能力，而非记忆轨迹"。对想做 AI 战斗/行为的团队，这是"小样本 + RL"可行性的一个强实证。

## 📌 关键要点
- **Acorn（叶桂生）三能力**：感知（可靠信息：静态游戏知识 + 动态运行时状态）→ 操作（稳定执行：Agent 只决策何时调用脚本/Skill，微观操作仍由脚本完成）→ 判断（明确验证：靠系统日志/性能数据/状态帧等硬证据校验）。
- **Acorn 四层架构**：中心 Agent Loop + 知识层（Game Graph/Skill Library/State Model）+ 运行时可观测层（Action Trace/UI 截图/日志/遥测，毫秒级对齐成证据链）+ 分析与学习层（验证→归因→反哺知识层与 Skill 库）。
- **证据链（Evidence Chain）**：多源数据毫秒级强对齐到同一时间节点，事件发生时可瞬间聚合还原现场；支持"发现现象→定位根因"（如掉帧归因到"特定区域特效资源加载过载"或"特定技能逻辑死循环"）。
- **性能测试四阶闭环**：探索（30 次运行聚类出反复性能尖峰 → 收窄到 Area B 热点）→ 发现 → 调查（定向复现任务 + utrace/深层 Trace 定位关键路径函数）→ 证明。
- **可复用架构（高文）**：Brain（System 2：视觉理解/长程规划/导航/GUI）+ Cerebellum（System 1：紧凑 VLM、纯像素、输出可变长键鼠 Action Chunk、三阶段训练预训练+SFT+RL）+ Master Agent（编排/状态记忆/切换控制权）；多 Agent 拆 Navigation/Combat/GUI。
- **黑神话悟空评估**：约 9 小时自主通第一章；120h 轨迹微调 + 50h 战斗 RL；Boss 胜率地狼 90%、沙国王 80%（高于业内可比公开结果）；导航用 VPR（视觉位置识别）+ VO（视觉里程计）+ 拓扑记忆地图。
- **四条共同原则**：优先依赖结构化原生状态；验证深度融入执行（每步动作伴随强验证）；沉淀失败经验实现自愈；终极目标不是 AI 取代脚本，而是"AI 决策 + 脚本稳定执行 + 证据闭环验证"融合。
- **四个待解难题**：跨游戏迁移不能全自动（需按类型建基础模型）；机制感知推理（结构化游戏知识 + RAG）；长程执行误差积累（Checkpoint/状态管理/恢复策略）；多模态测试判定器（Multimodal Test Oracle 组合规则/遥测/视觉/时序做 Pass/Fail）。

## 原文
（游戏那点事整理的 gamescom dev 现场分享实录，叶桂生 + 高文两位技术专家主讲）

**背景**：今年 gamescom dev 上 AI 话题升温，设 AI in Gameplay 与 AI in Production 两大专题赛道，32 场 AI 议题中落地应用占比过半。腾讯 WeTest 首次登台，公布两套测试智能体方案。

**01 AI 不只是"会玩游戏"（叶桂生/Acorn）**：测试用例核心目标固定（如"进入 Area B，第二波战斗存活、消灭所有敌人完成任务"），但执行过程充满动态变化（敌人随机刷新、路线被挡、战斗走向不同）。传统两条路线各有局限——固定脚本：稳定但缺应变、机制越复杂越难维护、且"每条指令成功执行"不等于"目标达成"；通用视觉模型：灵活但信息不精准、难跟上高频战斗与复杂导航。因此把智能体能力归纳为感知/操作/判断三模块。四层架构中心是 Agent Loop：知识层（从规则/功能清单/文档提取组装 Game Graph、Skill Library、State Model）赋能感知；运行时可观测层（采集状态/动作/日志，全量追踪 Action Trace/UI 截图/日志/遥测，毫秒级对齐成证据链）驱动决策与留痕；分析与学习层（据证据链客观验证目标达成，归因 Bug 生成报告，反哺知识层与 Skill 库）实现"任务执行→结果验证→归因分析→知识与技能反哺"闭环。演示中监控面板实时抓玩家位置/HP/装备/技能、受击/击杀/死亡/治疗事件、系统日志/Widget/性能指标。

**02 AI 不仅发现掉帧还能找根因（叶桂生）**：传统性能测试是线性"运行脚本→采集数据→分析报告→交付开发者"，报告即终点、根因压力全在开发者。AI 闭环让基础数据成为下一轮探索起点：自主交叉分析多次运行沉淀数据、识别跨场次隐蔽瓶颈；发现异常后自主创建针对性衍生测试任务。以 30 次重复运行为例，Agent 把每次超 200ms 性能尖峰与当时上下文（坐标/敌人类型/动作序列）关联、聚类分析，收窄为"集中在 Area B"的排查假说；再定向前往 Area B 召唤 Enemy A、复现动作序列反复校验，超阈值时触发深采样调取 utrace/深层 Trace，定位帧耗时关键路径到具体函数。三条核心原则：优先依赖结构化原生状态；验证深度融入执行；沉淀失败经验实现复原。

**03 从"会打 Boss"到完整游戏执行（高文）**：目标是复用架构/接口/训练管线/评估方法，每款游戏再调模型与 Agent。传统脚本依赖游戏 Hook、版本变化维护成本高；VLM 纯像素策略可按自然语言指令输出标准键鼠、不读内部状态。选《黑神话：悟空》作评估（实时战斗/长程穿越/成长系统/UI 交互，难度高），目标自主完成第一章。系统输入原始 RGB + 自然语言指令，输出标准键鼠动作，不调用私有 API、不改客户端（仅受控研究/QA 环境，不代表与游戏科学合作）。三级分工：Cerebellum（System 1，紧凑 VLM、低延迟高频、指令跟随/战斗/逐帧决策，三阶段训练：预训练通用视觉-动作理解 + SFT 对齐游戏指令 + RL 提升高难交互，纯像素输入标准动作输出、可变长 Action Chunk、必要时先短推理再执行，三项基础能力视觉定位/指令对齐/反应式控制）；Brain（System 2，视觉理解/长程规划/导航/GUI）；Master Agent（编排、维护任务状态与记忆、协调专用 Agent、切换控制权）。Boss 战自动化 RL 管线：精选专家示范冷启动→SFT 稳定策略→采集 Boss 战片段用 VLM 打分→RL 更新策略→可重复条件评估；训练集仅第一章 3 Boss（幽魂/黑风大王/黑熊精），泛化到未训练 Boss：地狼 90%、沙国王 80%。长程导航用 VPR + VO + 拓扑记忆地图（不几何重建、把重要位置抽象成节点、边表可通行关系；VO 从连续帧估短程位移姿态；VPR 匹配当前画面在地图中定位）。多 Agent：Master 拆解目标分发，Navigation Agent 长距移动、Combat Agent 帧级战斗、GUI Agent 界面交互。与公开研究关系：SIMA/SIMA 2 广谱指令跟随、字节 Lumine 长程自主跨世界迁移，本工作互补（商业游戏可靠帧级控制 + RL 战斗 + 长程执行）。四种能力三类型验证：指令跟随/GUI 已三类验证，战斗/导航已在 Action RPG 验证、正扩向开放世界 ARPG 与 FPS。数据：约 120h 轨迹微调 + 50h 战斗 RL，多轮实验每轮约两天，整项工作约三个月。待解难题：跨游戏迁移、机制感知推理（结构化知识 + RAG）、长程误差积累（Checkpoint/状态管理/恢复）、多模态测试判定器。

**04 现场问答**：不会修改模型（通用模型升级快、架构可换模型），持续更新的是项目空间经验与 Skill；会用 Feature Map 扫描游戏内容、版本更新后脚本+AI 逐项走查、判断差异来自版本变化还是 Skill 失效/动作未执行/模型问题；AI 测试生成报告但最终由人把关；两套系统会结合（执行模块 + 从用例生成到验证的完整系统）；约 9 小时自主运行能减少一部分人工、支持长时稳定性/性能测试；整套架构处于研究阶段、已在腾讯内部多个项目验证。

## 相关笔记
- [[2026-08-27_腾讯云TDSQL-Nexa为Agent服务的数据库]]
- [[2026-08-27_米哈游源初之结主创对话多人共斗神话战斗]]
- `ai-agent-架构`、`harness-engineering`、`游戏工程-AI辅助开发工具链` 概念
