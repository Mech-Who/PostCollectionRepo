---
title: Graft——给代码建立档案，让 AI Agent 工作更稳、更省 Token
date: 2026-08-09
category: AI应用
depth: 深度
layer: layer2
tags: [Graft, AI编程, 代码地图, Tree-sitter, Token优化, Claude Code, Codex, 上下文工程]
summary: 开源项目 Graft（NanoNets 团队）用 Tree-sitter 静态分析毫秒级生成代码地图（.graft/ 目录的 Markdown），让 Claude Code/Codex 优先读取地图而非盲目 grep 搜索。基准测试：162 次控制变量测试减少 46% 工具调用和 42% Token 消耗；SWE-bench Verified 通过率从 65% 提升到 75%。但能力上限锁死在已索引的本地代码库内——纯确定性词汇重叠（TF-IDF/BM25）+ 严格阈值拦截，涉及库外新技术/未拉取依赖时帮不上忙。
source_url: https://mp.weixin.qq.com/s/KvePCTm5GUhEQ_LQ4FgdIQ
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** 秋哥实测 Graft——一个给 AI 编程 Agent 建立"代码档案"的开源工具。它用 Tree-sitter 静态解析出代码中的函数、类与依赖关系，生成 `.graft/` 目录下的 Markdown 本地地图，Agent 开启新会话时优先读地图，跳过盲目的试探性搜索。官方基准：工具调用减 46%、Token 减 42%、SWE-bench 通过率 65%→75%。核心边界：检索是纯确定性的 TF-IDF/BM25 词汇重叠（无向量嵌入、无数据库），匹配分低于阈值时直接放弃注入——所以它只对已索引的本地代码库有效，探索新业务/新 SDK 时帮不上忙。

## 我的理解

这篇文章直击 AI 编程的一个**隐性成本**：Agent 每次新会话都像"严重失忆的实习生"，靠反复 grep、挨个打开无关文件来重建上下文——这些试探性探索既烧 Token 又污染上下文（真正改代码时漏掉关联文件）。

Graft 的思路值得深挖两点：

1. **用"确定性地图"替代"概率性搜索"**：它刻意不用向量嵌入、不用后台数据库，只用 Tree-sitter 符号提取 + TF-IDF/BM25 词频匹配。这看似"简陋"，实际是刻意的工程取舍——地图是确定性的、毫秒级的、可增量维护的，成本极低。AI Agent 的最大开销不是"读地图"，而是"盲搜"，Graft 把前者压缩到极致。

2. **严格的阈值拦截是特性不是缺陷**：匹配分低于阈值时直接放弃注入，避免引入噪声。这跟"上下文宁缺毋滥"的原则一致——注入错误上下文比不注入更糟。边界清晰：它是一本"纯本地代码字典与导航图"，不是通用大脑。

这与上一篇 OpenRSI 的"按算子按需检索有界证据"（token 少 41.7% 有效发现高 84.3%）是同一个底层逻辑：**长程 Agent 任务中，精准的上下文供给比模型的原始探索能力更决定效率**。这也再次印证 harness-engineering 的核心判断——Agent 的上限由"如何管理它的工作记忆"决定。

## 📌 关键要点

- **问题背景**：AI Agent 每次开新会话都从零开始，反复 grep、打开无关文件、追踪导入关系——白白烧 Token、拉长等待，还常因上下文被无关垃圾挤满导致改代码时漏改关联文件。
- **Graft 方案**：用 Tree-sitter 静态语法分析，毫秒级解析函数/类/依赖关系，生成 `.graft/` 目录下的 Markdown 本地地图；Claude Code/Codex 安排任务时优先读取地图，跳过盲目试探。
- **实测数据**：162 次控制变量测试减少 46% 工具调用、42% Token 消耗；SWE-bench Verified 通过率 65%→75%（提升主要来自多文件联动修改时能沿依赖链找全"改了 A 必须改 B"的文件）。
- **底层设计**（src/ask/ask.ts）：① 纯确定性词汇重叠计算——无向量嵌入、无后台数据库，基于 Tree-sitter 符号名+注释+提问词汇做 TF-IDF/BM25 词频匹配；② 严格阈值拦截——提问词汇在索引里找不到重叠，得分降为 0，低于阈值直接放弃注入，避免噪声。
- **使用边界**：能力上限锁死在已索引的本地代码库内。涉及库外新技术、外部 SDK、未拉取依赖、未开始的架构方案时完全帮不上忙——它是"本地代码字典与导航图"，不是推演未知知识的通用大脑。
- **安装使用**：`npm install -g @nanonets/graft` + 项目根目录 `graft init`，自动构建图谱、写入 Hook 到 `.claude/`、把 `graft/` 加入 `.gitignore`；改代码后台毫秒级静默更新，无需人工维护。
- **适用场景**：已有复杂多模块项目里做重构、修跨文件 Bug → 大幅省钱减少漏改；摸索全新业务/探讨库内不存在的新东西 → 不必期待帮助。

## 原文

（全文见 source_url 原文链接——秋哥聊成长 2026-08-09）

## 相关笔记

- （待连接：harness-engineering / 上下文工程 / AI 编程工具链相关概念）
