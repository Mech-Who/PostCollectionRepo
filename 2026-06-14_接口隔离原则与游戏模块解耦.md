---
title: Unity面试系列之接口隔离原则与游戏模块解耦
date: 2026-06-14
category: 游戏开发
depth: 深度
layer: layer2
tags: [接口隔离原则, ISP, C#, Unity, 游戏开发, 模块解耦, SLG, 代码重构, 设计模式]
summary: 深入讲解接口隔离原则（ISP）在游戏开发中的应用，通过技能系统重构和SLG经济/战斗系统拆分实例，展示从"耦合地狱"到"模块天堂"的完整路径，含面试技巧和实用建议。
source_url: https://mp.weixin.qq.com/s/z3vB9MopH6ez9GqgB7q5kg
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** 深度讲解接口隔离原则（ISP）在游戏开发中的实战应用。从"万能接口"的坏设计出发，通过技能系统拆分、SLG经济/战斗系统解耦两个真实案例，展示如何通过细粒度接口设计降低耦合度。文章还包含面试技巧、ISP与SRP的对比，以及从"耦合地狱"到"模块天堂"的团队经验总结。

## 我的理解
> 由小林生成，供小涵审阅修改

这是我目前看到过的最接地气的ISP讲解之一。作者没有停留在概念层面，而是用游戏开发中最常见的两个场景——技能系统和SLG经济战斗系统——完整演示了从"万能接口"到"细粒度接口"的拆解过程。

**三个特别有价值的点**：

1. **接口拆分粒度指南**：作者提到"超过30%的方法都是空实现，就该拆了"这个经验阈值非常实用。接口设计最怕的不是拆分过多，而是拆得不够——过早优化是万恶之源，但接口拆分是"为未来留余地"。

2. **接口组合 + 依赖注入的协同**：文章中的GameManager通过构造函数注入多个细粒度接口（IEconomySystem、IBattleSystem等），这个模式在Unity项目中可以极大简化单元测试。值得注意的是，文章提到甚至可以传null——这在测试场景下非常爽。

3. **SLG系统的"离婚协议"**：经济系统和战斗系统互相干扰是SLG项目的经典痛点。作者给出的按领域拆分的接口方案，实际上是在实践DDD（领域驱动设计）的有界上下文概念。

**关联已有知识**：这和近期关于游戏架构设计的内容高度相关，可以连接到游戏设计模式体系中的SOLID原则部分。ISP是SOLID中最容易被忽略但实际影响最大的一个原则。

## 📌 关键要点

- **核心方法**：采用"使用驱动"设计——只有两个以上调用方需要某个功能时，才提取成接口；接口方法超过30%空实现即为拆分信号
- **经验规则**：先小范围重构，不要一次性动整个系统；C# 8.0默认接口方法和C# 10接口静态成员是好帮手
- **面试加分**：将ISP与SRP对比展示互补关系，用具体项目经历说明（比背定义更有说服力）
- **踩坑注意**：过度拆分会导致接口爆炸；已发布接口不要修改，通过继承扩展；依赖注入配置随接口数量增加而复杂化

## 原文

> "这代码简直是一团乱麻！我就想加个新技能效果，结果发现要改UI系统、战斗系统、甚至存档系统……这还让不让人活了？"

这是上个月我们团队新来的小张在工位上的哀嚎。你猜怎么着？两天后他提交了离职申请，理由写着"代码耦合度太高，心脏受不了"。

说实话，这场景我太熟悉了。SLG游戏开发到中期，模块间那种剪不断理还乱的依赖关系，简直像一屋子人在玩"绑腿跑"——一个人摔跤，所有人都得跟着倒。

但今天我要告诉你一个秘密武器：**接口隔离原则**。听起来高大上？其实就是"别让不该说话的人互相说话"那么简单。

#### 一、什么是接口隔离原则？不就是"别管闲事"嘛

官方定义是：客户端不应该被迫依赖它不使用的方法。但讲真，这翻译太拗口了。

我更喜欢这么理解：**每个接口都应该像个小圈子，只聊自己感兴趣的话题**。

想象一下游戏里的NPC系统。如果所有NPC都继承一个`ICharacter`接口，这个接口包含移动、攻击、对话、交易、任务发布等二十几个方法，会出现什么情况？

*   场景装饰用的树（也是NPC？）得实现"攻击"方法，只能抛异常
*   只会走路的巡逻兵得实现"对话"方法，还是抛异常
*   任务发布者NPC得实现"移动"方法，继续抛异常

结果就是代码里到处是`throw new NotImplementedException()`，看着都头疼。

这就是违反了接口隔离原则的典型症状：

1.  **空方法满天飞**：实现类里一大堆啥也不做的方法
2.  **异常当饭吃**：到处是`NotImplementedException`或`NotSupportedException`
3.  **改一处伤全身**：接口加个新方法，所有实现类都得跟着改
4.  **依赖莫名其妙**：调用方明明只需要读取功能，却被迫依赖整个大接口

#### 二、游戏技能系统的"改造手术"

##### 反面教材：那个"万能"的技能接口

看看这个早期的设计，我脸都红了：
```csharp
public interface ISkill
{
    string Name { get; }
    SkillType Type { get; }
    float Cooldown { get; }
    void ApplyEffect(Character target);
    void RemoveEffect(Character target);
    Sprite Icon { get; }
    string Description { get; }
    void SaveData(SkillData data);
    void LoadData(SkillData data);
    byte[] SerializeForNetwork();
    void DeserializeFromNetwork(byte[] data);
    void DebugLogInfo();
    void DrawDebugGizmos();
}
```

然后呢？所有技能类都得硬着头皮实现这堆方法。火球术要实现网络序列化？治疗术要画调试Gizmo？

##### 正面案例：拆！拆得越细越好

后来做的"拆分手术"——拆成`ISkillEffect`、`ICooldownManager`、`ISkillUI`、`ISkillPersistence`、`INetworkSync`五个小接口，每个只关注一个维度。火球术只需实现`ISkillEffect`、`ICooldownManager`、`ISkillUI`三个接口。

#### 三、SLG游戏中的模块解耦实战

##### 案例：经济系统与战斗系统的"离婚协议"

从`IGameSystem`大接口拆分为`IEconomySystem`、`IBattleSystem`、`IBuildingSystem`、`ITechSystem`四个专注接口，通过`GameManager`依赖注入组合。

```csharp
public class GameManager
{
    private readonly IEconomySystem _economy;
    private readonly IBattleSystem _battle;
    private readonly IBuildingSystem _building;
    private readonly ITechSystem _tech;
    
    public GameManager(IEconomySystem economy, IBattleSystem battle, IBuildingSystem building, ITechSystem tech)
    { /* ... */ }
}
```

#### 四、面试时怎么聊接口隔离原则？

1.  **"解释ISP"** — 不要背定义，用自己项目的具体经历，说明怎么从万能接口拆出小接口
2.  **"ISP和SRP区别"** — 展示互补关系：SRP是"一个类只干一件事"，ISP是"一个接口只提供一类能力"
3.  **"怎么判断违规"** — 四看：看实现类空方法、看调用方只用部分方法、看变更影响面、看接口命名

#### 五、从"耦合地狱"到"模块天堂"

改之前：加功能改10+文件，测试覆盖率<30%，新人上手两周
改之后：改1-2个文件，覆盖率85%+，新人第一天跑通核心

经验教训：**适度拆分，按需设计**。接口不是越细越好，而是边界清晰就好。

#### 六、给游戏开发者的实用建议

1.  从小处着手
2.  遵循"使用驱动"——两个以上调用方需要才提取
3.  善用C# 8.0默认接口方法、C# 10接口静态成员
4.  保持接口稳定，通过继承扩展
5.  重视命名：`IDamageable`比`ICombatTarget`更好

**传播金句**：好的代码不是没有依赖，而是依赖得明明白白。
