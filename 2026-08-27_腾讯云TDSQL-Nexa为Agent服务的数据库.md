---
title: "腾讯云 TDSQL Nexa：为 Agent 服务的数据库来了"
date: 2026-08-27
category: AI技术
depth: 深度
layer: layer2
tags: [TDSQL Nexa, Agent数据库, 腾讯云, 数据底座, 多模数据库, Agent Memory, RAG, 数据库自治, DatabaseClaw, 向量检索]
summary: 腾讯云数据库在 DTCC 大会上发布面向 AI Agent 时代的数据库应用架构与新产品 TDSQL Nexa。核心判断：数据库正从「以人为主体的数据管理系统」走向「Agent 的数据底座」，Agent 时代数据访问由模型自主生成、时间/范围/资源不可预测。生产级 Agent 面临三类数据问题——失忆、资源窒息、自我进化困难，对应四类能力——多模统一承载、Agent 原生架构、语义与记忆、治理与自治。Nexa 用开放引擎架构（事务/搜索/AI计算/分析四类专用引擎）提供统一数据平面，一张多模表+一套 SQL；数据可逻辑纳管联邦计算（不复制）或旁路传输到对象存储；Nexa Knowledge 构建业务语义（解决「销量怎么算」的口径问题），Nexa Catalog 做细粒度权限。配套 TDSQL-C（Branch 秒级克隆试错+Serverless+多 Agent 沙箱）、TencentDB Agent Memory（渐进式记忆+团队记忆，SWE-Pro 成功率 60%→80% 成本降 66.3%）、DatabaseClaw（数据库自治，L1-L4 分权，DROP/TRUNCATE 不开放给 Agent）。结语：「模型决定 Agent 的下限，数据决定它的上限。」
source_url: https://mp.weixin.qq.com/s/bIAehwHtX2eEHZnrE8Gabg
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** 腾讯云数据库发布 TDSQL Nexa，把数据库从「服务人和预设流程」推进到「服务 Agent」——Agent 时代数据请求由模型自主生成，调用时机、访问范围、资源需求都不可预测。针对生产级 Agent 的「失忆、资源窒息、自我进化困难」三类问题，Nexa 提供开放引擎架构（事务/搜索/AI计算/分析四引擎）+ 统一数据平面（一张多模表、一套 SQL，内部路由编排）。两大现实难题的解法：老数据可逻辑纳管联邦计算（不复制）或旁路传输到对象存储；Nexa Knowledge 扫描数据资产做建模+业务语义（解决统计口径），Nexa Catalog 做细粒度权限。配套产品补齐「存储/试错/记忆/自治」：TDSQL Boundless 多模态统一存储、TDSQL-C 用 Branch 秒级克隆+Serverless+多 Agent 沙箱把试错控制在独立环境、Agent Memory 渐进式记忆+团队记忆、DatabaseClaw 数据库自治（权限隔离+行为护栏，不可逆操作不开放给 Agent）。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇文章的洞见不在「又一个数据库新品」，而在它点出的那个范式转移：**数据库的服务对象从「人」变成了「Agent」**。过去的数据库是「事实记录系统」——工程师写 SQL、业务看报表、应用沿固定路径增删改查，一切可预测。Agent 时代，数据请求由模型自主生成，什么时候查、查什么、吃多少资源都不可预测。这一个变化，把过去几十年数据库的隐含假设全部动摇了。

「失忆、资源窒息、自我进化困难」这三个词是我认为全文最值钱的地方，因为它把「Agent 上生产」这个模糊目标拆成了可工程化的三个坑。失忆——对话越长，全量历史塞给模型既慢又贵还干扰当前任务；资源窒息——Agent 并发访问让运维没法逐条排查；自我进化困难——任务成功率上不去、经验留不下来。小涵现在追 AI Agent 架构（Controller→Planner、Service→Executor 的映射），这篇正好补上了她一直缺的那块拼图：**Agent 的「记忆」和「数据」这一层**——之前关注的多是编排逻辑，这篇告诉你编排之下还得有数据底座。

有一个细节特别值得记：Nexa Knowledge 解决的不是「会不会写 SQL」，而是「写对了语法也答不对」的口径问题——字段名只能告诉 Agent「数据是什么」，不会告诉它「企业怎么用这些数据」。「销量」算已支付、已完成、还是扣退款？「复购率」按用户还是按订单？这些业务规则才是 Agent 落地的真正壁垒。这和我之前理解的「向量检索就够了」完全不是一回事——向量检索只解决「找资料」这一个问题，Agent 上真实业务要解决的是「接数据 + 懂业务 + 控权限 + 留记忆 + 自治」一整条链。

最后「模型决定下限、数据决定上限」这句收得漂亮，和小涵的知识体系里「数据驱动」的认知（李飞飞讲 ImageNet 赌数据）形成呼应。模型能力是公用的、趋于同质化的下限，谁的数据质量、规模、流动方式更好，谁就能拉开上限。

## 📌 关键要点
- **核心方法**：开放引擎架构——事务引擎（高并发在线）、搜索引擎（RAG/语义）、AI 计算引擎（多模转换+AI Function）、分析引擎（聚合）四类专用引擎，入口是一张多模表+一套 SQL，路由编排由核心引擎内部处理。
- **数据接入两路径**：老系统不推倒——外部数据库可逻辑纳管加入联邦计算（数据不复制）；离线分析数据旁路传输到对象存储并转开放格式。新数据接进来，老系统留原处。
- **语义与权限分层**：Nexa Knowledge 扫描纳管数据资产→数据建模→构建业务语义（解决统计口径，如「销量」是否扣退款）；Nexa Catalog 做细粒度权限，划定 Agent 可访问的数据范围。
- **试错隔离**：TDSQL-C 用 Branch（PB 级秒级克隆、不干扰主库）+ Serverless（闲时缩容至零）+ 多 Agent 沙箱（隔离租户），解决「敢不敢让 Agent 碰生产数据」与「最坏情况能否被控制」。
- **记忆能力**：TencentDB Agent Memory 渐进式记忆+团队记忆（SWE-Pro 成功率 60%→80%、成本降 66.3%，开源 80 天 GitHub Stars 破 2 万），把企业认知/经验转化为组织智能。
- **自治边界**：DatabaseClaw 持续感知状态→分析异常→定位 SQL，长期走向自愈自治；通过权限隔离、行为护栏、架构安全、全链路审计 + L1-L4 细粒度分权，DROP/TRUNCATE 等不可逆操作不向 Agent 开放，生产修改必须人工确认，数据不出客户 VPC。

## 原文
（来源：Datawhale，作者：李秀奇）

**新品发布：TDSQL Nexa**——做一个 Agent 到底要接多少套数据库？做 Demo 时，准备文档→向量数据库→接大模型，客服机器人就能跑起来，数据库似乎只负责从知识库找资料。但 Agent 进入真实业务后完全不同：要读企业数据、理解业务口径、连续执行任务、保存运行状态、在多 Agent 并发和突发流量下保持稳定。向量检索只能解决其中一个问题。腾讯云副总裁王义成将生产级 Agent 的数据问题归纳为三类：失忆、资源窒息、自我进化困难，对应需要四类能力：多模统一承载、Agent 原生架构、语义与记忆、治理与自治。

**从人到 Agent，服务对象变了**：过去数据库服务人和预设流程（工程师写 SQL、业务看报表、应用沿固定路径增删改查），是「事实记录系统」。Agent 时代，数据请求由模型自主生成，调用时机/访问范围/资源需求更难预测。

**给 Agent 一张统一数据平面**：企业数据散落（MySQL 订单、数仓复购率、知识库资料），Agent 想多回答一个问题就得加接口、加同步、加运维。Nexa 用开放引擎架构，核心引擎之下四类专用引擎，入口一张多模表+一套 SQL。

**数据可接进来也可留原处**：核心数据还在老数据库里，不能搬。Nexa 对外部数据库逻辑纳管加入联邦计算（不复制），离线数据旁路传输到对象存储转开放格式。

**接上数据还要理解业务**：字段名不能告诉 Agent 业务规则（「销量」算已支付/已完成/扣退款？「复购率」按用户还是订单？）。Nexa Knowledge 扫描数据资产、建模、构建业务语义、引入腾讯云海量客户专家经验；Nexa Catalog 负责细粒度权限。

**一张数据平面的收益**：Langfuse 官方架构通常需 PostgreSQL+ClickHouse+Redis+S3，采用 Nexa 后 Agent Trace 场景性能提升 10 倍以上；中等规模应用 Nexa Knowledge 生成 SQL 基本「人工零介入」，更广场景 Agent 业务性能平均提升 50%+。

**Agent 进生产还需要的能力**：TDSQL Boundless（多模态统一存储：行存关系/文档/键值、列存实时分析、向量索引语义召回、全文索引关键词、冷热分层）；TDSQL-C（试错：Branch 秒级克隆 PB 级、Serverless 缩容至零、多 Agent 沙箱隔离）；Agent Memory（渐进式记忆+团队记忆，SWE-Pro 60%→80% 成本降 66.3%）；DatabaseClaw（数据库诊断自治：感知状态、分析异常、定位 SQL、走向自愈，L1-L4 分权、不可逆操作不开放、生产修改人工确认、数据不出域）。

**写在最后**：模型决定 Agent 的下限，数据决定它的上限。腾讯云数据库稳定运营 15 年+、50 万+客户、千万核+千 PB，为 Nexa 面向生产提供基础。

## 相关笔记
- [[2026-08-26_李飞飞苏格拉底是最好的Prompter]]
- [[2026-08-26_读懂Pi你就是AI应用之王agent-loop源码]]
