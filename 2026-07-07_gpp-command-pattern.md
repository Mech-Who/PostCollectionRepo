---
title: "命令模式 — 具现化的方法调用"
date: 2026-07-07
category: 游戏开发
depth: 深度
layer: layer2
tags: [命令模式, 设计模式, 输入配置, 撤销重做, 游戏AI, 网络同步]
summary: "命令模式将方法调用封装为对象，实现行为与执行对象的解耦。在游戏中的应用：可配置按键绑定、AI指令生成、撤销/重做系统、网络同步和游戏回放。"
source_url: https://gpp.tkchu.me/command.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 命令模式是GoF设计模式中对游戏开发最有价值的模式之一。它将方法调用"具现化"为对象——让行为变成可以存储、传递、排队的数据。应用于可配置按键绑定、AI指令队列、多级撤销/重做和游戏录像回放系统。

## 我的理解

> 命令模式的本质是"将行为变成数据"——方法调用从动词变成了可以存储、传递、排队的名词。这与函数式编程中一等公民函数的概念高度对应，本质上是为没有闭包的语言模拟闭包。

> 三个递进的应用场景特别有启发性：① 简单的按键映射（可配置输入绑定）→ ② 将命令与执行对象解耦（AI控制任意角色，不再硬编码jump/fireGun等具体函数）→ ③ 通过命令队列实现生产者和消费者的时间解耦（网络同步和游戏回放）。

> 撤销实现的讨论尤为精彩：给命令添加undo()方法+维护历史列表+当前指针，就能实现完整的多级撤销。这比我见过的任何撤销实现都简洁优雅。关键洞察是命令需要保存执行前的状态（xBefore_, yBefore_），这是备忘录模式的轻量替代。

> 作者指出了一个深刻的代码可读性洞察：在支持闭包的语言中可以简化命令为实现函数，但"显式类能帮助读者看清命令包含了什么数据"——闭包自动包装了状态，但过于自动化反而让状态变得不可见。

## 📌 关键要点
- **核心定义**：命令 = 具现化(reify)的方法调用，将"做什么"封装为对象
- **三层解耦**：输入绑定↔行为 → 行为↔执行对象 → 生产者↔消费者
- **撤销系统**：execute() + undo() + 历史列表 + 当前指针 → 多级撤销
- **网络同步**：命令序列化 → 网络传输 → 远端重现（多人游戏基础）
- **回放系统**：记录每帧命令序列 → 重放时重新执行（比记录全量状态省内存）
- **与闭包关系**：命令模式是为没有闭包的语言模拟闭包；有闭包的语言仍可能需要显式类以提高可读性

## 原文

命令模式将一个请求封装为一个对象，从而可用不同的请求对客户进行参数化、对请求排队或记录请求日志，以及支持可撤销的操作。它的精简定义是：**命令是具现化的方法调用**。

### 配置输入
用Command基类和JumpCommand/FireCommand等子类替代硬编码的按键判断，实现可配置按键绑定。每个按键存储一个Command指针，按下时调用execute()。

### 角色说明
将execute()的参数从无参改为接受GameActor引用，实现命令与具体角色的解耦。AI系统也生成Command对象，统一由调度器执行。

### 撤销和重做
给Command添加undo()方法，执行前保存状态（如移动前的位置）。维护命令历史列表和"当前"指针，支持多级撤销/重做。

### 类还是函数
在支持闭包的语言中，命令模式可以用函数/闭包实现。但显式类能帮助"看清命令包含的数据"。
