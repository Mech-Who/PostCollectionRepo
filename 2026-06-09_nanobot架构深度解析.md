---
title: 4000行代码撑起一个Agent框架？nanobot架构深度解析
date: 2026-06-09
category: AI技术
depth: 深度
layer: layer2
tags: [nanobot, Agent框架, 架构设计, ReAct, HKUDS, 开源, 极简主义, Skill系统, MCP]
summary: nanobot 是港大数据科学实验室（HKUDS）开源的极简 AI Agent 框架，核心仅3935行代码（对比 LangChain 43万+行），30天获28500+ Star。核心设计：ReAct循环约20行、Tool最小接口（execute返回str）、Skill系统用Markdown文档教LLM使用CLI工具、用grep代替RAG做记忆检索、Subagent通过消息总线重注入通知。
source_url: https://mp.weixin.qq.com/s/6m2ezyi119r8NLMBjsARDQ
source: weixin
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** nanobot 是香港大学数据科学实验室（HKUDS）开源的极简 AI Agent 框架，核心仅3935行代码，30天获28500+ GitHub Star。五大设计亮点：①ReAct循环约20行，全部决策穿过同一个while循环；②Tool最小接口（execute强制返回str）；③Skill系统用Markdown文档而非Python代码扩展能力（懒加载、确定性、零成本）；④记忆系统用grep文件代替向量RAG（"grep beats RAG"）；⑤Subagent通过消息总线重注入完成通知，无需轮询。

## 我的理解
> 由小林生成，供小涵审阅修改

nanobot 是我见过"最诚实"的 Agent 框架——4000行代码和它的功能声明完全匹配，没有用复杂抽象隐藏简单实现。它代表了一种与 LangChain 截然相反的设计哲学：**不试图解决所有问题，只把最核心的事做到极致简单**。

几个值得迁移到任何 Agent 项目的设计原则：
1. **Markdown-as-Config 的 Skill 系统**：用文件系统管理 prompt 工程，非工程师也能扩展 agent 能力。这是对"Skill"概念最优雅的实现——Skill 不是代码，是教 LLM 如何使用已有 CLI 工具的说明书。
2. **"grep beats RAG"**：在个人规模（数百条历史）下，文件 grep 确实比向量检索更可靠——确定性、可审计、零成本。但规模天花板明显。
3. **错误恢复委托给 LLM**：框架层不做重试/回退，全交给 LLM 判断。这在强模型上有效，体现了"信任模型能力"的设计哲学。
4. **消息总线解耦子任务通知**：子 Agent 完成后不通过回调通知，而是重新注入到输入流——消除了"结果传递协议"。

## 📌 关键要点

- **核心架构**：控制面完全集中在 AgentLoop 的单个 while 循环中，无 Chain/Runnable/DAG/PLAN 等编排层。错误响应不持久化到 session history（防止"400中毒循环"），工具结果在 history 中截断为500字符控制上下文增长。
- **Skill系统（最独特设计）**：Skill 不是 Python 代码而是 Markdown 文档，教 LLM 如何使用已有 CLI 工具。通过 `shutil.which` 检查前置 CLI 是否存在标记可用性。System prompt 放置 XML 索引，LLM 自主决定何时用 `read_file` 加载具体 SKILL.md——文件系统作为懒加载机制。优势：确定性（无相似度阈值不确定性）、可审计、零额外成本。局限：skill 数量极大时 XML 索引占满 context window。
- **Skill vs Tool 分工**：Tool=执行Python逻辑/直接调API；Skill=包装已有CLI工具/包装有完善文档的服务。非工程师也能扩展能力。
- **记忆系统**：两个 Markdown 文件——MEMORY.md 每次注入 system prompt，HISTORY.md 用 `exec grep` 按需搜索。累计100条未整合消息时异步触发记忆整合（独立 asyncio.Task）。设计论据："grep beats RAG for agent memory — deterministic, auditable, zero-cost, composable"
- **Subagent 系统**：spawn 工具委托后台 asyncio.Task，能力受限（无message工具、无spawn递归、最多15次迭代、无memory）。完成后通过消息总线重新注入 InboundMessage，主 agent 像处理普通用户消息一样处理——无需特殊结果传递协议。
- **MCP集成**：工具自动包装为原生 Tool 对象，支持 stdio 和 streamable-http，工具名带 `mcp_{server}_` 前缀做命名空间隔离。
- **适用场景**：个人自托管AI助手、快速原型、学习Agent框架最小实现。不适合：精确多Agent协调、大规模历史记忆、细粒度行为控制、生产环境安全隔离。

## 原文
（原文由腾讯云开发者发布，作者俞孟凡）

文章从8个维度深度拆解 nanobot 架构：整体架构（控制面集中化）→ ReAct循环极简实现（约20行）→ Tool系统（最小接口，execute返回str）→ Skill系统（Markdown文档扩展能力，懒加载机制）→ 记忆系统（grep代替RAG）→ Subagent系统（消息总线重注入）→ MCP集成（标准协议桥接）→ 可借鉴的架构模式（5条迁移原则）。

GitHub：https://github.com/HKUDS/nanobot

## 相关笔记
- [[2026-05-26_平平无奇的源码藏着Agent核心秘密]] — Agent 框架源码分析
- [[2026-06-08_Agent三种常见思路ReAct Reflection规划执行]] — Agent 范式对比
- [[Harness Engineering]] — Agent 控制系统的设计哲学
- [[2026-05-27_OmniWork一人AI工作室平台体验]] — AI Agent 平台实践
