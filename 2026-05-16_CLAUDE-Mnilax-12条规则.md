---
title: "Mnilax：CLAUDE.md 规则从 Karpathy 的 4 条增加到 12 条，错误率从 41% 降到 3%"
date: 2026-05-16
tags: [AI应用, AI编程, CLAUDE.md, 最佳实践, Agent]
summary: Mnilax 在 Karpathy 原版4条规则基础上，通过30个代码库的实测新增8条规则，形成12条 CLAUDE.md 规则模板，将 Claude Code 错误率从41%降至3%，覆盖了长任务Agent、测试质量等此前被忽略的60%失败模式。
category: AI应用
status: 📥已采集
---

> **摘要：** Mnilax 在 Karpathy 原版4条规则基础上，通过30个代码库的实测新增8条规则，形成12条 CLAUDE.md 规则模板，将 Claude Code 错误率从41%降至3%，覆盖了长任务Agent、测试质量等此前被忽略的60%失败模式。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇的价值不在于12条规则本身，而在于它揭示了一个关键方法论：CLAUDE.md 不是"许愿清单"，而是"行为合约"——每一条规则都对应一个实际踩过的坑。Karpathy 的4条规则（先想后写、简单至上、外科手术式修改、目标驱动）覆盖约40%失败模式，而 Mnilax 新增的8条补齐了剩下60%——其中"模型只做判断不做决策"和"显性失败不要静默"两条尤其深刻，直指当前 Agent 编程中最容易被忽视的边界。

另外，200行/4000 token的遵循率上限是一个重要约束，规则数量和质量之间需要在精不在多。

## 📌 关键要点
- **核心方法**：CLAUDE.md 是行为合约而非许愿清单，每条规则必须对应一个实际踩过的坑
- **量化效果**：无规则(41%错误率) → Karpathy 4条(11%) → Mnilax 12条(3%)
- **关键约束**：文件超过200行或4000 token，遵循率急剧下降
- **被放弃的尝试**：社交媒体特化规则、超12条、依赖特定工具的规则、空洞指令
- **新增8条中最值得关注**：模型只做判断不做决策、硬性Token预算、暴露冲突不要折中、先读后写、测试验证意图而非行为、长任务设检查点、约定胜于新奇、显性失败不要静默

## 原文

本文是一篇关于 AI 代码助手 Claude 的 `CLAUDE.md` 配置文件最佳实践的深度分析文章。文章核心内容是介绍开发者 Mnilax 在 Karpathy 原版 4 条规则的基础上，通过为期 6 周、在 30 个代码库上的实测，新增了 8 条规则，最终形成了一套更完善的 12 条规则模板，并将 Claude Code 的错误率从 41% 大幅降低至 3%。

### 背景
始于 Karpathy 2026年1月的吐槽帖，指出了 Claude 写代码的三大问题（静默错误、过度复杂、波及非目标代码）。Forrest Chang 将其提炼为 4 条行为规则，发布在 GitHub 上获得12万星。5个月后，Claude Code 生态发生了变化（Agent冲突、Hook级联等），Karpathy 的4条规则已不足以覆盖新的问题。

### 原版 4 条规则（底线）
1. **先想后写**：杜绝隐蔽假设，明确权衡
2. **简单至上**：用最少代码解决问题
3. **外科手术式修改**：只改必要部分
4. **目标驱动**：明确成功标准

### 补齐的 8 条新规则
5. **模型只做判断，不做决策**：分类、摘要交给模型；路由、重试用常规代码
6. **硬性Token预算**：单任务4K、单会话30K上限，超支重置
7. **暴露冲突，不要折中**：代码库中有矛盾模式时明确二选一
8. **先读后写**：新增代码前先通读相关导出和调用方
9. **测试验证意图，而非行为**：关注 WHY 而非 WHAT
10. **长任务设检查点**：每完成一个关键节点同步状态
11. **约定胜于新奇**：严格遵从既有规范
12. **显性失败，不要静默**：无法确信时必须明确声明

### 心智模型
CLAUDE.md 不是一个许愿清单，而是一个**行为合约**，用于关闭你已观察到的特定失效模式。宁缺毋滥：6条针对你真踩过的坑的规则，比12条里有6条永远用不上的更好。
