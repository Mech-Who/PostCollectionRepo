---
title: "Ruby 之父 DHH 彻底倒向 AI：两个月没亲手写代码，却看着 Vibe Coding 毁掉架构"
date: 2026-08-28
category: AI技术
depth: 深度
layer: layer2
tags: [DHH, Agent编程, Vibe Coding, AI编程, 架构审查, 代码审查, 并发Agent, Ruby on Rails, 程序员转型]
summary: Ruby on Rails 之父 DHH 在 Lex Fridman 访谈中自述：过去两个月上线的代码没有一行亲手写出，全部交给 Agent，日常规模已达 4-5 台机器、约 16 条 Agent 线程并发。核心观点是「实现权可以下放，判断权不能下放」——普通代码交给 Agent，架构方向、数据约束和关键边界仍逐行审查。最值得警惕的案例来自 Basecamp 5：设计团队用 Vibe Coding 加速提交 PR，每个 PR 单独看都合理，合在一起却把系统架构毁掉——局部完成快、整体债务却在累积。他同时宣告「代码行数是愚蠢的指标」「机械式编码这道工序正在受到威胁」，开发者价值向任务上游（定义问题、选方案、约束边界）和结果下游（验收、处理失败、维护系统）迁移。
source_url: https://mp.weixin.qq.com/s/zNoh0aWvbrWPL94eNxMI5g
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** DHH 从「13 个月前排斥 AI 补全」走到「新增代码 100% 交给 Agent」。他给出三条工程判断：①实现权下放、判断权不下放——模型层和关键业务逻辑仍逐行审查，Neovim 从写代码变成浏览 diff；②每个 PR 都说得通、系统却被改坏——Vibe Coding 让局部完成加速，但改动之间缺共同架构约束，重复逻辑/隐性依赖/风格分裂要到后续迭代才一起爆发，解法是把架构审查提前到任务拆分阶段；③代码行数是愚蠢的指标，衡量标准回归产品（问题是否解决、是否可维护、事故率、交付周期）。开发者价值向「定义问题、约束边界、验收结果」两端迁移，机械式编码工序正在被模型取代。

## 我的理解
> 由小林生成，供小涵审阅修改

DHH 这段话的分量，不在于「AI 能写 100% 代码」——这个结论过去半年已经被反复验证过。真正有价值的是他把一个工程师们正在经历、却很少有人讲清楚的分工变化，浓缩成了一句可以照着操作的原则：**实现权可以下放，判断权不能一起下放**。

这几乎就是给小涵当前 Unity ECS 重构、战斗系统事件驱动改造这些工作写的一条边界准则。Agent 可以帮你写 NPC 系统、写寻路、写受击反馈的分发逻辑，但「哪些模块是架构边界」「数据怎么流动」「关键状态由谁持有」这三件事不能交给它。DHH 说的「数据库模型、权限、计费、并发和数据迁移一旦出错，测试通过也未必能上线」——翻译到游戏开发就是：寻路数据、战斗状态机、ECS 的组件组合这些关键边界，测试绿了不代表对。

「每个 PR 都说得通，系统却被改坏了」这句最扎心，因为它精准命中 Agent 并发的本质陷阱。一个 Agent 改登录、一个改缓存、一个抽象公共组件，三个任务都过测试，但它们没有共享同一套边界。这和「16 条 Agent 线程并发」是一体两面：并发 Agent 只适合**边界独立**的任务（依赖升级、补测试、生成迁移脚本、清理重复代码、调查不同 Bug），一旦多个改动碰到同一片代码，线程越多冲突越难收拾。小涵如果以后用 Agent 并行改项目，先问的不是「能不能并」，而是「这些改动会不会碰到同一份状态」。

最后「代码行数是愚蠢的指标」和「机械式编码受威胁」指向一个更现实的判断：评价工程师（以及评价自己）的标尺得换。删 300 行旧逻辑让发布速度翻倍，比生成 5000 行没改善体验的代码更有价值。这对正在入职游戏开发的小涵是个提醒——别用「写了多少代码」证明自己，用「解决了什么问题」证明自己。

## 📌 关键要点
- **核心方法**：实现权下放、判断权不下放。Agent 接管完整任务（读仓库→找文件→提方案→改代码→跑测试→拿结果），但架构方向、数据约束、关键边界仍由人逐行审查。
- **并发规模**：4-5 台机器、约 16 条 Agent 线程；用 tmux 管不过来后自研 Herdr（tmux 会话+通知合并，需要确认/完成/报错时再叫人）。
- **踩坑经验**：Vibe Coding 的「局部正确、整体债务」——每份 PR 单独合理、合起来毁架构。解法是把架构审查提前到任务拆分阶段（写清哪些模块不能碰、哪些抽象必须复用、数据如何流动、改动由谁统筹），而不是等 PR 生成完再看。
- **并行边界**：并发 Agent 只适合边界独立的任务；涉及同一份状态/同一套架构的工作，线程越多冲突越难收拾。每个任务至少带回四样东西：改动摘要、测试结果、风险点、回滚办法。
- **评价口径**：代码行数是愚蠢的指标；换成「问题是否解决、变更是否可维护、线上指标、事故率、交付周期」。
- **就业判断**：机械式编码工序受威胁；喜欢「做出产品、解决问题」的人反而更快，价值向任务上游（定义问题/选方案/约束边界）和结果下游（验收/处理失败/维护系统）迁移。

## 原文
（内容根据 Lex Fridman 对 DHH 的访谈翻译整理，来源：51CTO技术栈）

DHH（David Heinemeier Hansson），Ruby on Rails 创始人、37signals 联合创始人兼 CTO。一年前他还不喜欢 AI 补全代码、不愿把聊天框嵌进编辑器；但在最近的 Lex Fridman 访谈中，他给出完全不同的答案：Omarchy Quattro 过去两个月的新增代码，已全部交给 Agent 完成。

**13 个月，从排斥走到「Or 100」**
AI 写代码占比从 5%、20% 一路走到 80%，DHH 补了一句「Or 100」。13 个月前 AI 编程体验让他提不起兴趣（补全打断思路、聊天框要反复搬运上下文），Agent 改变了交互：现在把问题和一个大致方向交出去，Agent 自己读仓库、找文件、提方案、改代码、跑测试、拿结果。变化重点不在模型会写多少语法，而在它开始接管完整任务。

**两个月没亲手写代码，还在逐行审什么**
Omarchy Quattro 过去两个月上线的代码没有一行由他亲手敲出——但这不等于「程序员不用写代码」。他看过所有改动的整体形状，模型层和关键业务逻辑仍逐行检查；Neovim 主要用来浏览项目、查看 diff、核对 Agent 做了什么。一句话：实现权可以下放，判断权不能一起下放。

**每个 PR 都说得通，系统却被改坏了**
最值得警惕的案例来自 Basecamp 5。设计团队用 Vibe Coding 后提交 PR 明显加快，每份 PR 单独都能解释为什么合理，问题出在改动之间没有共同架构约束——「这些改动合在一起，把系统架构毁掉了」。一个 Agent 改登录、一个改缓存、另一个抽象公共组件，都过测试，不代表采用同一套边界；重复逻辑、隐性依赖、风格分裂要到后续迭代才一起暴露。处理方式很工程化：让更多人获得实现能力，同时把架构审查提到更靠前——任务拆分时就要写清哪些模块不能碰、哪些抽象必须复用、数据如何流动、改动之间由谁统筹。

**一个人跑 16 条 Agent 线程，开发者成了总调度**
他最初用 tmux 管理并行 Agent，后来嫌切窗口、等通知麻烦，做了 Herdr（把 tmux 会话和通知接在一起，Agent 需要确认/完成任务/遇到错误时再叫人）。日常规模 4-5 台机器、约 16 条 Agent 线程。瓶颈在于：16 个 Agent 同时产出结果，人不可能逐个实时陪跑。并发 Agent 最适合边界独立的任务（依赖升级、补测试、生成迁移脚本、清理重复代码、调查不同 Bug）；涉及同一份状态/同一套架构的工作，线程越多冲突越难收拾。每条任务至少要带回四样东西：改动摘要、测试结果、风险点、回滚办法。

**别再比谁写得多，代码行数已经失效**
「lines of code is a stupid metric」。一个 Agent 生成 5000 行却没改善体验、没降低故障率，只会增加维护成本；另一个工程师删 300 行旧逻辑让发布速度翻倍，价值反而更高。更合适的指标：问题是否解决、变更是否可维护、线上指标是否改善、事故率是否下降、交付周期是否缩短。

**机械式编码受威胁，会做产品的人仍有机会**
如果明天重新找工作，DHH 认为很难再靠「手写代码」获得过去那种报酬——「the mechanical process is under threat」。他把「喜欢编程」拆成两件事：享受手工敲语法、记 API、亲自完成每个实现细节的人会很难适应；喜欢把东西做出来、解决问题、看产品落地的人，Agent 反而让这件事更快。开发者的价值向任务上游和结果下游移动。

## 相关笔记
- [[2026-08-26_读懂Pi你就是AI应用之王agent-loop源码]]
- [[2026-08-21_Codex5.6Subagents玩法把探索测试日志分析丢给子Agent]]
