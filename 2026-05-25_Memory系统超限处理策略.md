---
title: 在字节食堂打饭，我问同事 Memory 爆了怎么办，打饭阿姨头也没抬："你把'对花生过敏'压成'饮食偏好'，下次点外卖咋办？"
date: 2026-05-25
category: AI技术
depth: 深度
tags: [Memory管理, Agent系统, context窗口, 信息保鲜, 淘汰策略, 分层记忆, LLM工程]
summary: 深入拆解大模型 Memory 系统在 token 超限时的处理策略——从 FIFO 滑动窗口到重要性打分+时间衰减，再到三层分层记忆（短期/中期/长期），以及异步压缩+Coalescing 的工程实践。
source_url: https://mp.weixin.qq.com/s/dOkDqx3eJJVo0FT7QMageQ
source: weixin
status: 📥已采集
sync_status: ❌未同步
---

> **摘要：** 本文以面试场景为引，系统讲解了大模型 Memory 系统在 context 超限时的处理策略。核心观点是 Memory 的本质不是"无限存储"，而是在有限上下文下的信息保鲜。文章对比了三种淘汰策略（FIFO、LRU、重要性×时间衰减），提出了三层分层记忆架构（短期完整保留、中期摘要压缩、长期提取为结构化 fact 存外部），并讨论了触发时机的工程细节——70-80%异步触发+Coalescing。贯穿全文的核心场景是：用户30轮前说过的"我对花生过敏"，如何在压缩后仍然被系统记住。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇文章把 Memory 系统设计从概念到工程讲得很完整。对我最有价值的几个点：

1. **Memory 本质论的转变**：把"我能存多少条"转换为"我用什么策略让最重要的信息留在有限的上下文里"——这个框架性认知在面试中很有杀伤力。
2. **重要性打分的时间衰减系数 0.95^days**：很小的工程细节但很有说服力，说明作者真做过。
3. **三层分层 + 外部持久化的设计**：短期完整保留（6-12轮）、中期摘要压缩（5:1到10:1）、长期提取为结构化 fact 存外部存储——这是回答"30轮前的关键信息怎么办"的关键。
4. **异步压缩+Coalescing 策略（不是 Debounce）**：Coalescing 保证最终状态一定被处理，而不是 Debounce 可能漏掉中间请求——这个取舍很精妙。
5. **触发时机表（60-70%后台extraction → 70-85%后台Compact → 85-95%切换 → >95%强制同步）**：直接把昂贵的操作前置到用户感知不到的时机。

对做 Agent 系统的人来说，这就是一篇实用性很高的架构参考。目前我们 PostCollection 的知识体系里，概念连接层已经在 concepts/ 下有「Agent工程架构」相关概念，这篇可以作为 AI技术分类下的深度加工文章补充进去。

## 📌 关键要点

- **核心方法**：Memory 系统采用三层分层架构——短期（6-12轮对话完整保留）、中期（摘要压缩 5:1-10:1）、长期（关键 fact 提取为结构化数据存入外部存储，永不自动删除）
- **重要性打分**：LLM 评 0-10 分（硬约束 9-10分、项目背景 6-8分、闲聊 0-2分）+ 关键词正则自动判高分（过敏/禁忌/必须），配合时间衰减系数 0.95^days
- **踩坑经验**：FIFO 丢早期关键信息（"对花生过敏"）、LRU 误把高频当高价值；不要在 95% 才触发压缩，需在 70-80% 异步进行
- **适用场景**：长会话 Agent 系统、生产级的 Memory 管理、面试中 Memory 系统设计问答
- **触发时机表**：<60%不动 → 60-70%后台extraction → 70-85%后台Compact → 85-95%切入压缩版 → >95%强制同步

## 原文

在小说阅读器读本章

去阅读

在小说阅读器中沉浸阅读

大家好，我是吴师兄。

最近几个月，Memory 这道题在面试里出现的频率明显在涨，原本只问 RAG 的岗位现在也会问 Memory，原本 Memory 只问"做没做过"，现在开始追"怎么管"。

这一篇是这个系列的延伸，之前三篇拆了 Claude Code 源码层面 Memory 的类型、提取触发、注入位置，这一篇上升到所有 Memory 系统都绕不开的工程问题：context 装满了之后该怎么办。

继续来个段子。

上周中午在字节食堂打饭，前面排着一个做 Agent 的同事，我端着餐盘问他："你们 Memory 系统 context 爆了之后是怎么处理的？"

打饭阿姨手一停，抬头看了我一眼，慢悠悠把一勺红烧肉扣到我盘子里："老记忆是丢掉还是压缩？"

我愣了一下，下意识答："压缩吧，滑动窗口加摘要。"

阿姨没接话，转头给下一个人打菜。打了两三勺青菜的功夫，她头也没抬，又来了一句："压缩用什么算法？"

我硬着头皮答："调个小模型生成摘要。"

队伍往前挪了两步，阿姨递给我一份米饭，终于抬起头，平静地问了第三句："那用户 30 轮之前告诉你'我对花生过敏'，被你压缩成了一句'用户讨论了饮食偏好'，丢了细节，他下次让你点外卖，你怎么办？"

我端着餐盘站在原地，后面有人催我往前走。脑子里其实闪过几个词，"重要性""提取""结构化"，但拼不成一个完整的回答，所有术语都飘在空中没落地。

阿姨看我没动，朝我后面那位扬了扬下巴："让一让，回去再想想。"

我端着餐盘走到座位上，红烧肉还冒着热气，但已经没什么胃口。最难受的不是答不出来，是那个节奏，阿姨一边打菜一边问，每一次抬头都在告诉我"你想的还不够深"。字节食堂这帮阿姨的杀招就是日常，不需要冷笑，也不需要追问到底，慢慢问，让你自己意识到自己回答得有多薄。

今天这篇文章，就把 Memory 系统在 token 超限时的处理策略，从原理到工程实现完整拆开讲。如果你正在做或者准备做大模型相关的项目，这套思路面试时一定会被问到。

### 一、Memory 的本质不是"无限存储"，是有限上下文下的信息保鲜

先把这件事的物理基础说清楚。

大模型本质上是**无状态**的。它每一次推理，都需要你把当前对话的所有 context 一次性喂进去——历史消息、system prompt、tools 定义、当前 user input，全部加起来。模型并不"记得"上一轮你说过什么，是你的应用代码把上一轮重新拼到 context 里再喂给它，看起来才像在"对话"。

那 context 有多大？主流模型是 32K、128K、200K（Claude 4.x / GPT-5），少数模型有 1M。听起来很大，但分配下去就紧张了：

| 用途 | 典型占比 | 说明 |
| :--- | :--- | :--- |
| system prompt | 5-15% | 角色 + 规则 + 风格约定 |
| tools 定义 | 10-30% | 每个工具的 JSON schema |
| 历史对话 | 30-50% | 多轮交互累积 |
| memory / RAG 注入 | 10-20% | 检索到的相关知识 + 用户记忆 |
| 当前 user input | 5-10% | 这一轮的输入 |
| margin（输出预留） | 10-20% | 给模型输出留的空间 |

*Token 预算分配 · 200K context window 实际拆解*

一旦累积到 70-80%，你就必须开始管理，不然下一轮推理直接报 `context length exceeded`，整个对话崩掉。

所以 Memory 系统的本质，**不是"我能存多少条"，而是"我用什么策略，让最重要的信息留在有限的上下文里"**。这件事想清楚，后面所有的设计才有意义。

### 二、三种主流淘汰策略，以及它们各自的坑

最简单的策略是 **FIFO 滑动窗口，** 按时间顺序，超出窗口的最早消息直接丢掉，保留最近 N 轮。LangChain 的 `ConversationBufferWindowMemory` 就是这种实现。代码两行：

```
    def fifo_truncate(messages, window=10):     return messages[-window:]
```

但坑在哪？坑在**用户最重要的信息往往出现在对话开头**。"我是糖尿病人"、"我对花生过敏"、"我们公司的合规要求是…"——这些一次性、强约束性的信息，一旦超出窗口就被永远丢掉。Agent 后面给出的建议可能完全违背用户的真实约束，造成事故。

第二种是 **LRU（按访问频次）**——保留被频繁引用的记忆，长期不被访问的淘汰。这比 FIFO 进了一步，但有个新问题：**访问频次和重要性不等价**。用户问过一次"今天天气"，可能这条对话被检索引用了 10 次（关键词常见），但它对长期对话毫无价值；用户说过一次"我对花生过敏"，可能整个会话只检索到 1 次，但它至关重要。LRU 会保留前者、淘汰后者，**正好搞反**。

第三种是**重要性打分 + 时间衰减**，业界主流的做法。每条记忆有两个维度的分数：

*   **importance score**：记忆本身的重要程度，可以由 LLM 二次打分给出（"这条记忆对未来对话有多大价值，0-10 分"），或者用规则触发（包含"过敏""禁忌""偏好""不要""一定要"等关键词的自动高分）
*   **recency score**：时间衰减系数，常用 `0.95^days_ago` 或类似的指数衰减

最终分数 = importance × recency。淘汰时按最终分数从低到高排序，从最低分开始淘汰，直到 token 预算回到安全线下。

importance 打分这件事看起来虚，落到代码上其实就是一段简短的 prompt。我们在项目里用的版本大致是这样：

```
    SCORE_PROMPT = """请给下面这条对话内容打一个 0-10 的 importance 分数。 判断标准：   9-10 → 用户的硬约束、长期偏好、身份属性、合规要求（"我对花生过敏"、"必须用 Python 3.11"）   6-8  → 项目背景、关键决策、明确的目标（"我们这个项目主要是给老年用户用的"）   3-5  → 一般性事实、上下文说明   0-2  → 闲聊、客套、临时性问题（"今天天气不错"） 只返回一个整数,不要解释。 对话内容: {message} """
```

跑这一遍 LLM 评分有成本，所以工程上有两个常见优化：一是只对**新增的对话片段**评分而不是每轮全跑；二是先用关键词正则把明显的高分内容（过敏、禁忌、必须、千万不要）直接判 9 分，省下 LLM 调用。

Claude Code 用的就是这种思路的进化版，不是单纯按分数淘汰，而是**分层处理**。

*三种淘汰策略对比 · FIFO vs LRU vs 重要性打分*

| 策略 | 实现复杂度 | 关键风险 | 适用场景 |
| :--- | :--- | :--- | :--- |
| FIFO 滑动窗口 | 极低 | 丢失早期关键信息 | 短对话、低风险场景 |
| LRU 按访问频次 | 低 | 高频 ≠ 高价值 | 检索为主的场景 |
| importance × recency | 中 | 打分模型本身的成本 | 严肃业务、长会话 Agent |
| 分层 + 提取（Claude Code 路径） | 高 | 工程复杂度 | 生产级 Agent 系统 |

### 三、分层处理：压缩什么、丢弃什么、提取什么

分层是更进一步的工程思路。**不要把所有记忆当成同一种东西处理**，按"信息价值密度"分成三层，每层用不同的策略。

**短期记忆（最近 N 轮对话）：完整保留。** 这部分是模型理解当前对话脉络的基础，N 通常取 6-12 轮，根据 token 预算调。不做任何压缩，原文进 context。

**中期记忆（更早的对话历史）：摘要压缩。** 超过短期窗口的部分，按段落或按主题切块，用一个小模型（甚至同一个模型）生成摘要，把原始消息替换成 `[SUMMARY] ...` 占位。压缩比通常能做到 5:1 到 10:1。

**长期记忆（关键 fact）：提取为结构化数据，存到外部存储。** 这是最关键的一层。用一个 extraction 模型扫描历史对话，把"用户偏好""硬约束""项目背景""人物关系"这类长尾价值高的信息，提取成结构化 fact（`user_preference` / `hard_constraint` / `project_context`…）写入外部存储——SQLite、向量数据库、甚至 Markdown 文件都行。下次会话需要时按相关性检索回来。

*短/中/长期记忆 · 三层处理流水线*

Claude Code 的源码里就是这套思路。它把 **Compact（上下文压缩）** 和 **Memory（持久化记忆）** 做成两个独立但互补的机制。

Compact 是把较早的对话历史压成 `CompactBoundaryMessage` 占位，是有损的——工具执行细节、代码片段、中间推理都可能丢；Memory 是把关键决策提取成 `.md` 文件，永久保存在 `~/.claude/projects/{project_id}/memory/`，**写入文件系统后不会被任何自动机制删除或压缩**。索引文件 `MEMORY.md` 还有一个硬约束：`MAX_ENTRYPOINT_LINES = 200`，超过会截断并打 warning，也就是说连"什么值得被列入索引"这件事都受 token 预算管控。

这套机制的关键设计是：**压缩可以丢失实现细节（"怎么修的这个 bug"可以丢，因为代码和 git 历史还在），但 Memory 必须保留关键决策（"用户偏好方案 A 而不是方案 B"、"bug 的根因是竞态条件"）——后者无法从任何地方自动推导。**

这里还有一个容易被忽视的细节：**extraction 模型用什么模型**。生产里常见三种取法。第一种是用主模型（比如 Claude 4.x、GPT-5）同步做，质量最高但成本最贵、延迟最大；第二种是用小模型（Haiku、Mini）异步做，成本低延迟可接受，但漏召的概率上升；第三种是混合——规则先打捞高分内容、小模型扫一遍中等内容、关键场景再回退到主模型确认。Claude Code 走的是第二种思路，专门给提取这一步设了**低配 call\_model**，`max_tokens=1024` 直接限死，因为提取结果只是简短的 JSON，不需要长输出。这种"评分 / 提取用小模型，对话主流程用大模型"的分工，是把成本压下来同时保住效果的关键工程取舍。

回到面试官那个杀招："用户 30 轮前说过对花生过敏，被压缩成一句'讨论了饮食偏好'，怎么办？"

如果你只做了 Compact（摘要压缩），那确实没救，细节被压没了。但如果你做了 Memory 分层，extraction 模型在原始对话里就把"花生过敏"提取成 `hard_constraint: 花生过敏` 写进外部存储；后续生成菜谱建议时，先从 Memory store 里拉相关的约束注入 prompt，**这条信息根本不依赖 context 里的对话历史，永远不会被压缩丢失**。

这套从 importance 打分到分层提取到外部持久化的完整工程实现，是我们训练营企业培训 Agent 项目里专门的一章。学员不是只学概念，而是真的从 `ConversationBufferMemory` 起步，一步步加上重要性打分、加上时间衰减、加上 extraction、加上跨会话的 SQLite 持久化，把"用户 30 轮前的过敏信息还能召回"这件事用代码跑出来。做过一遍之后，再被面试官追问"压缩用什么算法"，能立刻讲出分层逻辑，而不是只会背 FIFO / LRU。

### 四、触发时机：不是 100% 满才动手

最后一个工程细节，也是面试时容易加分的点：**什么时候触发压缩/淘汰？**

错误做法是等 context 接近 100% 才动手。原因有两个：

第一，**压缩本身要消耗 token**。如果你 99% 的时候才触发，模型连"生成一段摘要"都没空间了，会直接报错。

第二，**压缩需要时间**。如果你前台用户正在等响应，这时候同步触发一次摘要生成，用户感知到的就是"agent 卡了几秒"。

主流做法是**在 70-80% 触发**。Claude Code 的 `/compact` 命令既支持手动触发，也支持基于 token 监测的自动触发。具体阈值可以根据业务调，但要给压缩自己留出 20% 左右的余量。

更精细的做法是**异步压缩 + Coalescing**——主对话线继续走，后台另起一个进程做摘要 + 提取，做完之后在下一轮对话开始时把新的状态切进去。Claude Code 的 `ExtractionCoordinator` 走的就是 Coalescing 策略：dirty 标记被设置过，提取完成后一定会重新跑一轮扫描所有新增消息。**为什么不是 Debounce？** 因为 Debounce 会丢中间请求，前一次提取快结束时的新对话可能永远不会被扫描到；Coalescing 保证的是最终状态一定被处理。这种"宁可多扫一遍、绝不漏一条"的取舍，正是生产级 Memory 系统和 demo 级 Memory 系统的区别。

整套触发逻辑可以总结成一个简单的判断表，面试时画在白板上一眼就能讲明白：

| 当前 token 占比 | 触发动作 | 用户感知 |
| :--- | :--- | :--- |
| < 60% | 不动 | 正常对话 |
| 60-70% | 后台启动 extraction，把新增关键信息提取到外部存储 | 无感 |
| 70-85% | 后台启动 Compact，准备压缩历史段落 | 无感 |
| 85-95% | 切入压缩后的版本，下一轮开始用新 context | 几乎无感 |
| > 95% | 强制同步压缩 | 用户能感觉到一次轻微卡顿 |

这张表的核心思路是：**把昂贵的操作前置到用户感知不到的时机。** 等到 95% 才动手，体验和稳定性都是最差的；提前到 60-70% 异步做，用户全程无感知，系统始终留有余量。

### 五、面试怎么答 Memory 超限处理？

如果时间倒流，回到蚂蚁那场面试，正确的答法分三步走。

**先讲 Memory 本质（30 秒）。** Memory 系统的本质不是无限存储，是在有限 context 下的信息保鲜。LLM 是无状态的，每次推理要把所有 context 喂进去，token 是硬约束，所以淘汰策略 + 压缩策略才是 Memory 的核心，不是"存了多少条"。

**再讲淘汰 + 压缩的设计（60 秒）。** 单一策略都有坑：FIFO 丢早期关键信息，LRU 误把高频当高价值。业界主流是 importance × recency 打分。更进一步是分层处理——短期完整保留、中期摘要压缩、长期提取成结构化 fact 存外部。**重要 fact 一旦提取到外部，就不依赖 context 历史了，永远不会被压缩丢失。** 这是回答"用户 30 轮前的关键信息怎么办"最有力的那一刀。

**最后讲触发时机（20 秒）。** 70-80% 就开始，不要等 100%。压缩自己要消耗 token、要消耗时间，所以要预留 buffer，最好做异步 + Coalescing。

这三段答完，加起来不到两分钟，每一段都在那个"沉默的三秒"里站得住。资深面试官听到 importance × recency、听到分层 + 外部提取、听到 Coalescing 异步触发，他就知道你不是在背 Memory 的几种类型，**是真做过这套机制**。

### 六、Memory 系列后续

这一篇是 Memory 系统的入口，重点讲了**为什么会爆**和**爆了怎么办**。再往深处走，就是具体某个生产系统是怎么实现这套机制的——Claude Code 的源码就是非常好的参考样本。

我之前用三篇文章拆了 Claude Code 源码里 Memory 模块的核心设计：

*   第一篇：四种 Memory 类型（user / feedback / project / reference）和它们各自的"什么值得被记住"
*   第二篇：自动提取机制——`ExtractionCoordinator` 的 Coalescing 触发策略
*   第三篇：注入位置——Memory 是什么时候、以什么格式被拼进 system\_prompt 的

加上今天这一篇关于淘汰与压缩的讨论，构成了 Memory 工程化的完整四块拼图。

今天这道题，只是大模型面试中 Memory 系统设计的一个切面。

真正的面试官不会只问这一问。他们会顺着你的回答追下去，追到你答不上来为止，判断的就是你到底做没做过这个系统。

背答案的人和真正做过的人，说话方式完全不一样。前者说"用 LangChain 的 Memory 模块"或者"做了滑动窗口截断"；后者说"我们在企业培训 Agent 项目里把 Memory 拆成短期、中期、长期三层，短期完整保留最近 8 轮，中期超过 70% 时摘要压缩比能到 8:1，长期用 extraction 提取成结构化 fact 存到 SQLite，importance 用 LLM 二次打分加时间衰减系数 0.95^days，30 轮之后用户的过敏信息还在 long-term store 里能稳定召回"。

面试官三句话就能听出来你是哪种人。

如果你想成为后者，欢迎了解我们的大模型训练营。

## 相关笔记
- concepts/Agent工程架构（Harness Engineering）— Agent系统设计规范相关
- concepts/工具使用与自我提升 — Memory系统作为Agent基础设施
