---
title: 游戏开发中的异常数据处理策略
date: 2026-06-14
category: 游戏开发
depth: 深度
layer: layer2
tags: [异常数据处理, 游戏服务端, Pydantic, 幂等设计, 数据校验, 安全策略, 游戏后端]
summary: 游戏服务端异常数据处理的系统方法论，从入口校验到数据库约束六层防护，涵盖拒绝/修正/隔离/补偿四种策略、幂等设计、异常日志和 dry-run 修复脚本。
source_url: https://mp.weixin.qq.com/s/3S0cXhNhyjJio-ptUEb5JQ
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** 游戏服务端异常数据处理根本不是单一层的防守，而是一个从入口校验到数据库约束的六层渐进体系。文章提出了四种处理策略（拒绝/修正/隔离/补偿）和具体实现方案，尤其强调玩家资产类数据不能"自动修复"，必须保留现场。

## 我的理解

> 由小林生成，供小涵审阅修改

这篇文章是少见的"落地导向"的游戏服务端安全文章。它不像很多教程只会说"做好校验"，而是给出了非常具体的分层策略和代码实现。几个值得划重点的洞察：

1. **异常≠错误**——错误可以直接报，异常是需要上下文判断的灰色地带。作者的四分法（拒绝/修正/隔离/补偿）把决策权交给了业务逻辑，而不是一刀切。这个框架本身就可以当做一个设计模式。

2. **幂等设计的重点是数据库约束而非代码判断**——很多人只在代码里写 `if exists`，但两个请求同时到达时全都会通过。文章用唯一索引作为兜底保障，这才是工业级的做法。

3. **客户端传来的值只能当"请求"不能当"结论"**——奖励内容必须服务端查配置表算出来，而不是客户端说"给我1000金币"就直接给。看似常识，但很多小型项目踩过这个坑。

4. **dry-run 脚本文化**——修数据脚本强制支持 dry-run，这条规则如果团队能坚持执行，能避免太多线上事故。

5. **埋点和资产数据要区别对待**——埋点可以"修"，资产只能"停"。这个分界线的意识很重要。

和 PostCollection 已有知识体系的关联：本文可以作为"游戏服务端安全"主题的核心参考，与游戏中其他设计类文章形成互补。

## 📌 关键要点

- **核心方法论**：异常数据处理的四策略——拒绝（明确违规）、修正（不影响核心资产）、隔离（有风险但不可丢）、补偿（已影响玩家数据）
- **六层防护体系**：入口校验 → 业务层状态判断 → 幂等设计 → 数据库约束 → 异常数据保留 → dry-run修复脚本
- **幂等设计的真正保障**：数据库唯一索引（`UNIQUE INDEX`）+ 事务，不是代码层的 `if exists` 判断
- **服务端权威原则**：奖励内容、任务状态、数量计算等必须由服务端决定，客户端的值最多当"请求"
- **异常表设计要点**：`user_id` 允许为空（避免统计误判）、必含 `scene/raw_data/reason/handled/created_at`
- **业务上限应从配置表来**：`MAX_BATTLE_DAMAGE` 等常量不应随手写，应来自当前版本的理论最大值估算
- **修数据脚本基本习惯**：支持 dry-run → 执行前备份 → 先在测试环境跑 → 限制影响范围 → 保留修复记录

## 原文

> 以下为公众号原文内容，由「欧米De游戏开发小屋」原创。

### 关于作者

这是欧米的第460篇游戏开发原创分享。

### 引言

接口报错时，问题通常好查。但真正麻烦的，是那些没有立刻报错、但已经悄悄写进数据库的数据。

比如某个玩家的背包里多了一件配置表里已经删掉的道具。接口当时没有报错，发奖流程也顺利走完。直到后面做活动结算、排行榜统计，或者客服查玩家数据时，才发现这条数据根本不该存在。

类似问题包括：任务奖励重复领取、金币扣成负数、支付回调重复发货、排行榜分数不合理、旧版本存档缺字段。

### 异常数据的处理方式

根据严重程度，可以分为四类：

| 处理方式 | 适合场景 | 例子 |
| :--- | :--- | :--- |
| **拒绝** | 明确违反业务规则 | 负数购买数量、活动结束后继续领取 |
| **修正** | 不影响核心资产和状态 | 昵称去空格、埋点时长小于 0 改成 0 |
| **隔离** | 有风险但不能直接丢 | 支付金额不一致、排行榜分数异常 |
| **补偿** | 已经影响玩家数据 | 少发奖励、重复扣款、邮件漏发 |

玩家资产、订单、排行榜、活动奖励这几类数据，最好不要轻易"自动修好"。修之前至少要把原始数据留下来。

### 先拦掉明显异常的请求

使用 Pydantic 做入口参数校验：

```python
from pydantic import BaseModel, Field
from typing import Literal

class ClaimRewardRequest(BaseModel):
    user_id: int = Field(gt=0)
    reward_id: int = Field(gt=0)
    source: Literal["daily_task", "mail", "event"]
```

参数校验只能说明"请求格式上像个正常请求"，不能说明它真的应该成功。

### 客户端传来的结果，最多只能当请求

```python
def claim_task_reward(user_id: int, task_id: int):
    task = db.get_task_progress(user_id, task_id)
    if task is None:
        return {"ok": False, "error": "任务不存在"}
    if not task.finished:
        return {"ok": False, "error": "任务未完成"}
    if task.reward_claimed:
        return {"ok": False, "error": "奖励已领取"}

    reward = config.get_task_reward(task_id)
    if reward is None:
        save_anomaly_data(...)
        return {"ok": False, "error": "奖励配置异常"}

    with db.transaction():
        db.add_item(user_id, reward.item_id, reward.count)
        db.mark_task_reward_claimed(user_id, task_id)
    return {"ok": True}
```

### 有些异常要留下现场

```python
def save_anomaly_data(user_id, scene, raw_data, reason):
    db.insert("anomaly_logs", {
        "user_id": user_id,
        "scene": scene,
        "raw_data": raw_data,
        "reason": reason,
        "handled": False,
        "created_at": now(),
    })
```

### 重复请求要靠幂等设计

```python
def claim_mail_reward(user_id, mail_id):
    mail = db.get_mail(user_id, mail_id)
    if mail is None:
        return {"ok": False, "error": "邮件不存在"}
    try:
        with db.transaction():
            db.insert("mail_claim_logs", {"user_id": user_id, "mail_id": mail_id, ...})
            db.add_item(user_id, mail.item_id, mail.count)
    except UniqueConstraintError:
        return {"ok": False, "error": "邮件奖励已领取"}
    return {"ok": True}
```

```sql
CREATE UNIQUE INDEX uniq_mail_claim
ON mail_claim_logs(user_id, mail_id);
```

### 数值异常要有业务上限

```python
MAX_BATTLE_DAMAGE = 500000
MAX_BATTLE_TIME_SECONDS = 1800
```

### 数据库约束

```sql
ALTER TABLE user_wallet
ADD CONSTRAINT check_gold_non_negative CHECK (gold >= 0);
```

### 日志要能帮人查问题

```python
def log_warning(scene, user_id, message, extra):
    logger.warning({
        "scene": scene,
        "user_id": user_id,
        "message": message,
        "extra": extra,
        "time": now().isoformat(),
    })
```

### 修数据脚本一定要支持 dry-run

```python
def fix_negative_gold(dry_run=True):
    rows = db.query("SELECT user_id, gold FROM user_wallet WHERE gold < 0")
    for row in rows:
        user_id, old_gold = row["user_id"], row["gold"]
        new_gold = 0
        if dry_run:
            continue
        with db.transaction():
            db.execute("UPDATE user_wallet SET gold=%s WHERE user_id=%s", [new_gold, user_id])
            db.insert("data_fix_logs", {...})
```

### 处理规则表

| 数据类型 | 常见异常 | 处理方式 |
| :--- | :--- | :--- |
| 登录参数 | token 缺失、格式错误 | 直接拒绝 |
| 购买请求 | 数量为负、商品不存在 | 拒绝并记录 |
| 任务奖励 | 重复领取、状态不一致 | 幂等处理，必要时记录异常 |
| 支付回调 | 重复通知、金额不一致 | 重复回调忽略，金额异常隔离 |
| 排行榜数据 | 分数超过合理范围 | 暂不入榜，记录异常 |
| 埋点数据 | 字段缺失、时间异常 | 核心字段缺失隔离，非核心字段修正 |
| 历史存档 | 字段缺失、旧版本格式 | 迁移或补默认值 |
| 玩家资产 | 负数、超大值 | 停止写入，记录并人工复查 |

### 总结

稳的做法，是在几个地方都留一层处理：
1. 入口挡掉明显错误
2. 业务层判断状态是否允许
3. 关键操作做幂等
4. 数据库加必要约束
5. 异常数据保留原始记录
6. 修数据脚本支持 dry-run

最后，尤其是玩家资产、活动奖励、排行榜、订单回调这类模块，功能本身写完只是第一步。真正决定系统是否稳定的，往往是那些异常路径有没有处理干净。
