---
title: "享元模式 — 渲染森林的秘密"
date: 2026-07-07
category: 游戏开发
depth: 深度
layer: layer2
tags: [享元模式, 设计模式, GPU渲染, 实例渲染, 内存优化, 地形系统]
summary: "享元模式将对象状态分为可共享的'固有状态'和实例独有的'变化状态'，在大量相似对象场景中大幅节省内存。GPU实例渲染是其硬件级实现。"
source_url: https://gpp.tkchu.me/flyweight.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 享元模式是唯一有GPU硬件支持的GoF设计模式。通过将对象数据分为"固有状态"（共享）和"变化状态"（实例独有），在渲染千万棵树或管理大量地形区块时大幅节省内存。关键实践：享元对象几乎总是不可变的。

## 我的理解

> 享元模式在游戏开发中的核心地位通过一个事实就能证明：它是唯一被GPU硬件直接支持的GoF模式（实例渲染）。作者用"森林中的树"和"地形区块"两个例子，演示了从内存共享到渲染优化的完整链条。

> 地形系统的例子最具启发性：用枚举表示地形类型会导致switch散落各处（getMovementCost、isWater等），每个函数都是一个巨大的switch语句。而用地形对象指针替代枚举——`tile.getMovementCost()`——不仅API更优雅，还将数据和行为内聚到对象中，完美体现了面向对象的设计哲学。

> 一个反直觉的性能发现：指针跳转虽然比枚举多一层间接访问，但由于享元对象的紧凑内存布局更有利于CPU缓存，作者测试发现享元实际比枚举更快。这再次印证了"不要凭直觉优化，要测量"的原则。

> 与类型对象模式的关键区别：类型对象是为了减少类定义数量（组织目的），享元纯粹为了内存效率。两者都涉及对象间共享状态，但意图不同。

## 📌 关键要点
- **两分状态**：固有状态（上下文无关，可共享）vs 变化状态（实例独有）
- **GPU实例渲染**：硬件级享元——发送一次共享模型，再分别发送每个实例的参数
- **枚举替代模式**：用地形对象指针替代枚举 → switch地狱消失，API更优雅
- **不可变性原则**：享元对象几乎总是不可变的——修改会影响所有引用处
- **工厂创建**：需要时创建，但先检查是否已有相同实例（配合对象池）
- **缓存友好**：紧凑的对象布局可能让指针跳转的开销被缓存命中率抵消

## 原文

享元模式通过将对象数据分为两种来解决共享问题：固有状态（没有特定指明是哪个对象实例的数据，如树的网格和纹理）和变化状态（每个实例独一无二的数据，如位置、大小、颜色）。

### 森林渲染
千千万万棵树使用相同的网格和纹理——这些共享数据只存一份（TreeModel），每棵树实例只存位置、颜色等独有数据和对共享模型的引用。

### 地形系统
用地形对象指针替代枚举：`Terrain* tiles_[WIDTH][HEIGHT]`。每个相同地形的区块指向同一Terrain实例。`world.getTile(2,3).getMovementCost()` 替代了switch地狱。

### 性能考虑
指针跳转vs枚举：一次间接访问的开销，但享元的对象布局更紧凑。作者测试表明享元反而更快——缓存行为是决定性因素。
