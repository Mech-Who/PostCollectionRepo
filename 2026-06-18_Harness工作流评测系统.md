---
title: 你的 Harness 工作流真的在进步吗？—— Harness Eval 评测系统
date: 2026-06-18
category: AI应用
depth: 深度
layer: layer2
tags: [Harness, 评测, AI工程, 工作流, 量化评估, Agent, 闭环系统]
summary: 腾讯技术工程团队开源了 Harness Eval 系统——一套面向 Harness 工作流的轻量可回归闭环评测体系。核心哲学从"测试"跃迁到"考试"，通过出题→答题→判卷的闭环，实现工作流质量的量化追踪和持续改进。
source_url: https://mp.weixin.qq.com/s/kAPx6ZxXxpTyRi-hYAbqtA
source: weixin
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** Harness Eval 是一套面向 AI Harness 工作流的评测系统。它解决了"主观 vibes 驱动工作流演进"的问题，通过考官（Examiner）+ 考生（Agent）+ 裁判（Judge）的三角色设计，实现了工作流的多维度量化评估。系统核心产出包括 pass/fail 判定、三档分数（流程遵循度、执行质量、综合分）、以及可归因的改进建议。整体通过率从 82.4% 提升到 100%。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇文章从根本上回答了我一直在思考的一个问题：**AI 工作流（Harness）的质量怎么衡量？**

过去我们评估一个 Skill 或 Workflow 好不好，基本靠"体感"——也就是文章说的"主观 vibes"。这在一个确定性系统里没问题（传统软件有 assertEqual），但在概率性系统里（同样的 prompt 跑两次可能不同结果），体感完全不够用。

**几个让我眼前一亮的洞察：**

1. **"测试"→"考试"的思维跃迁**：传统测试是二元判定（对/错），而 Harness 工作流的"正确"是一个光谱——同样的任务，优雅完成和笨拙完成都是"对"，但质量天差地别。考试模式用多维度打分+证据+改进建议来解决这个问题。这个思维转变本身就是一个值得记录的方法论。

2. **考官（Examiner）角色的引入**：这是最反直觉但最正确的设计。简单地把 task.md 丢给 Agent 执行，测的是"Agent 自嗨的能力"，不是"Agent 在真实场景下的表现"。真实用户会追问、纠偏、补充信息——考官就是模拟这个交互过程。这个设计直接点出了当前 AI 评测的核心缺陷：脱离交互场景的单项测试没有意义。

3. **判卷必须拥有"上帝视角"**：早期让考官"顺便"判分，结果误判率高——考官只能看到 Agent 说了什么，看不到 Agent 做了什么（tool call 内容）。最后不得不将判卷独立出来，用压缩后的完整交互记录（transcript.for-judge.txt）做依据。这个踩坑经验非常有价值。

4. **改进建议的三维度分类**：[workflow] / [eval] / [capability]——分别对应工作流规则、题目本身、Agent 能力。这样每次评测跑完，改进建议可以直接路由到不同团队/不同文件，不再是"成绩单"而是"迭代驱动器"。

5. **对 WorkBuddy 的启示**：这套系统虽然是为 Harness 工作流设计的，但方法论完全可以平移。小涵你现在也在构建各种 Skill 和自动化工作流，如果有一套类似的评测机制，就可以量化地回答"v0.2 比 v0.1 进步在哪"这样的问题。

## 📌 关键要点

- **核心问题**：Harness 工作流是"规则驱动的概率程序"，输出由 Prompt+Rules+Skills+Model 共同决定，传统 assertEqual 测试无效
- **三角色设计**：考官（Examiner）模拟用户多轮交互 → 考生（Agent）执行任务 → 裁判（Judge）独立判卷
- **题目结构**：每道题 = meta.yaml（身份信息）+ task.md（题面）+ rubric.md（阅卷标准）+ env.yaml（环境前提）
- **判卷输出**：score.yaml（pass/fail + 3 档分数）+ review.md（证据引用 + 三维度改进建议）
- **执行引擎**：Go 单一 CLI，git worktree 隔离，symlink 切换配置，transient 错误自动重试
- **效果**：通过率 82.4% → 100%，multi-run 追踪 workflow_rev 每个 commit 的影响
- **三个不可妥协原则**：可重复 > 精确、可归因 > 高分、闭环 > 单向

## 原文

### 一、一个让人不安的事实：你的Harness工作流可能一直在"裸奔"

你有没有这样的经历？

团队花了两周精心调教一套 Harness 研发工作流——写了十几条 Rules、打磨了 Skill——上线之后，评价体系是这样的：

"感觉这版稳了不少。"

"昨天那版好像更聪明？"

"我这边挺好用，你那边咋不行？"

这就是我们半年前面对的现实。 **整个团队在用"主观 vibes"驱动一个复杂系统的演进。**

说白了，我们连最基本的问题都回答不了：

- **改了一版 rule/skill，到底是进步还是退步？** 典型的"主观 vibes"驱动开发无法量化这个事情。
- **A 说好用，B 说难用——到底谁对？** 工作流问题、模型问题、还是 Prompt 本身有歧义？说不清。
- **工作流回归无从谈起。** 修一个 bad case，可能顺手破坏了三个 good case，但你不知道。
- **我们的 Harness 工作流和竞品比，到底差在哪？** 没有数据来衡量和佐证，都是各吹各的好。

**这不是一个"锦上添花"的问题。这是一个系统正在失控而你不自知的问题。**

### 二、核心哲学：为什么Harness工作流比传统软件更需要"考试"

#### 2.1 不可量化的东西，不可优化

这是整套系统的第一性原理。

传统软件是确定性的：同样的输入，同样的输出。你写一个 assertEqual，它的结果是二值的、稳定的、可复现的。

Harness 工作流完全不同。它本质上是一种"规则驱动的概率程序"——输出由 Prompt + Rules + Skills + Model 共同决定，任何一项的微调都可能引起蝴蝶效应般的行为变化。今天跑一遍是 A 结果，明天跑一遍可能是 B 结果。更要命的是，你甚至无法简单地判定"A 比 B 好"还是"B 比 A 好"，因为"好"本身就缺乏定义。

这种系统如果没有评测体系兜底，你面对的不是"缓慢退化"——而是**薛定谔式的退化**：你永远不知道它什么时候变差了。

#### 2.2 从"测试"到"考试"的思维跃迁

软件工程界用了几十年总结出"测试驱动开发"。但是，传统的单元测试、集成测试那套东西搬到 Harness 上是不够的。

为什么？因为 Harness 工作流的"正确"不是一个 boolean——它是一个**光谱**。一个 Harness 工作流完成任务的方式可以有无数种，有些优雅有些笨拙，有些主动有些被动，有些严谨有些草率。你不能简单地 assert output == expected。

所以我们需要的不是"测试"，而是"考试"：

- **测试**验证的是"对不对"——二值判定。
- **考试**评估的是"好不好"——多维度打分 + 证据 + 改进建议。

> 这个思维跃迁是整套系统的哲学基石：**我们不是在写测试用例，我们是在出考卷、组织考试、培养阅卷官。**

#### 2.3 三个不可妥协的原则

- **可重复 > 精确**：一道题跑多次，结果的分布比单次的分数更有价值
- **可归因 > 高分**：分数不带归因，就是垃圾数据
- **闭环 > 单向**：评测的终点不是"出一份成绩单"，而是"驱动下一次改进"

### 三、系统全景

整套系统只做三件事——**出题、答题、改卷**——但把它们串成了一个可以无人值守跑的闭环。

### 四、系统设计：出题→答题→改卷的闭环

#### 4.1 出题：如何设计一份AI考卷

一道题 = 题面 + 阅卷标准 + 环境前提 + 元信息。每道题在题库里就是一个独立目录，由 4 个文件构成：

- **meta.yaml**：题目身份（id、版本、类别、难度、考察目的）
- **task.md**：题面（给考生看的题目原话 + 给考官看的剧本）
- **rubric.md**：阅卷标准（硬性通过项 / 质量项 / 典型失分项）
- **env.yaml**：环境前提（前置条件 check 命令、关键工件）

新出题人只需按模板填 4 个文件，5 分钟就能新增一道题。

#### 4.2 答题：一场精心编排的"模拟考试"

引入**考官（Examiner）**——由 LLM 扮演的"用户"，按照 task.md 中预设的剧本和 Agent 进行多轮对话。

考试流程：
1. 考场布置（env.yaml 准备环境）
2. 考官开场（以自然语言向考生发出第一条消息）
3. 多轮交互（追问、纠偏、补充信息、关键节点做决策）
4. 考生完成（给出最终总结）
5. 全程录像（transcript.jsonl 记录每一轮对话和 tool call）

#### 4.3 判卷：多维度 + 证据 + 改进建议

**裁判（Judge）**是一个完全独立的进程，与答题对话没有任何共享上下文。输入只有两样东西：
- **rubric.md**：评分标准
- **transcript.for-judge.txt**：从原始对话记录精简的判卷专用版本

判卷流程：
1. 逐项核对硬性通过标准
2. 评估过程质量
3. 给出多维度分数（流程遵循度、执行质量、综合分）
4. 引用原文作为证据
5. 强制输出改进建议并分类归因：[workflow] / [eval] / [capability]

**改进建议的三维度分类**是反复打磨出来的关键设计：
- **[workflow]**——工作流本身该补什么 gate、该改什么提示
- **[eval]**——题目本身有没有问题
- **[capability]**——考生暴露的通用能力短板

#### 4.4 执行引擎

Go 单一 CLI，零环境部署，git worktree 隔离，symlink 软链接切换 IDE 配置，transient 错误自动重试。

#### 4.5 评测结果

产物：latest.md（汇总表）、latest-stats.yaml（结构化版本）、score-history.yaml（全历史追踪）、batch-insights.md（改进建议聚类汇总）。

workflow_rev 记录每次 run 用的工作流 git commit，可精确定位到某个 commit 的影响。

### 五、效果

整体通过率从 82.4%（14/17）提升到 100%（17/17）。通过 4 轮 workflow_rev 迭代、50+ 次自动化 run，系统性暴露了范围锁定、Critical 拦截、归档沉淀、提交规范、TDD 纪律等环节的缺陷。

### 六、结尾

> **给一个"行为不确定"的 Harness 工作流，建立可重复的、可归因的、闭环的质量度量体系。**

这套方法论**不绑定任意平台、不局限任意场景**。但凡存在行为效果难以直观判定优劣的 Harness 类工作流，均可直接使用这套评估体系的思想。

## 相关笔记
- [[2026-06-18_MattPocock技能Token成本降63%]] — 同样涉及 AI Agent/Skill 工程化
- Projects/PostCollection/Rules/Skill 开发 — 如何将 Skill 质量纳入评测
