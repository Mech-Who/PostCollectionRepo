---
title: Bitmap 位图与连续签到检测
date: 2026-08-04
category: AI技术
depth: 深度
layer: layer2
tags: [Bitmap, 位图, 连续签到, 算法, 面试, 数据压缩, 位运算, Redis]
source_article: 2026-08-04_腾讯面试10亿用户签到连续30天
---

# K-Fragment: Bitmap 位图与连续签到检测

## A. Applicable — 适用场景

- 海量用户/实体的布尔事件流存储与统计（签到、活跃、登录、在线、领奖）
- 需要"压缩存储 + 快速聚合"的场景：空间 O(N×365bit)、检测 O(1) 位运算
- 面试题"10 亿用户连续签到 30 天"的标准解法（腾讯高频）
- 游戏/产品场景：连续登录奖励、七日签到、留存分析、成就判定

## B. Boundary — 边界与注意事项

- **只适合布尔状态**：有状态值（等级、积分）需另存，不能硬塞位图
- **按用户维度分片**：key = `user_id → Bitmap`，因为查询天然是"单用户连续性"；跨用户聚合需并行分片扫描
- **跨年处理**：按年份分段存储，查询拼接多个位图；跨月无问题（位图连续）
- **多平台**：按平台分 key，合并统计时位或（OR）
- 只算工作日：构造工作日掩码与位图相与后再检测
- 面试注意：先澄清需求（连续=自然日？跨月？），再估算数据量，再给方案——不要一上来写代码

## C. Core — 核心要点

1. **存储模型**：1 bit = 1 天。每用户每年 365 bit ≈ 46 字节；10 亿用户 ≈ 43GB，Redis Cluster 可承载（`SETBIT/GETBIT/BITFIELD`）
2. **对比**：存日期字符串每用户每年几 KB → 位图压缩 99%+；聚合从"逐条统计"变为"位运算"
3. **连续 N 天检测（五次移位倍增）**：
   ```python
   x &= x >> 1   # 连续≥2天
   x &= x >> 2   # 连续≥4天
   x &= x >> 4   # 连续≥8天
   x &= x >> 8   # 连续≥16天
   x &= x >> 14  # 连续≥30天
   return x != 0
   ```
   原理：`x &= x>>k` 后位 i 为 1 ⟺ 位 i..i+k 全 1（存在长度 k+1 连续段），倍增覆盖 2→4→8→16→30
4. **底层洞察**：布尔事件流 → 用比特承载信息、用移位做聚合统计——"信息压缩 + 计算下推"是海量数据题通用思路

## D. Data — 可复用代码骨架

```python
def has_consecutive_days(bitmap: int, days: int = 30) -> bool:
    """判断位图中是否存在连续 days 天的签到段（倍增法）"""
    x = bitmap
    # 用 1,2,4,8,剩余 依次倍增覆盖目标长度
    shifts = [1, 2, 4, 8]
    covered = 0
    for s in shifts:
        covered += s + 1  # 当前覆盖长度（含起点）
        x &= x >> s
    # 还差 days - (covered-1) 再补一次移位
    remain = days - covered
    if remain > 0:
        x &= x >> remain
    return x != 0

# 存储侧（Redis 语义）:
# SETBIT sign:user:10001:2026 <day_index> 1   # 第 day_index 天签到
# 全量统计: 按用户分片并行扫描 → 位运算 → 聚合计数
```

```
面试五步模板:
① 澄清需求（连续定义/跨月跨年/工作日）
② 数据量估算（10亿×365 → 逐条存不可行）
③ 方案设计（Bitmap + 用户分片）
④ 复杂度分析（空间 O(N×365bit)，检测 O(1)）
⑤ 扩展性（跨年分段/多平台合并/冷数据压缩）
```
