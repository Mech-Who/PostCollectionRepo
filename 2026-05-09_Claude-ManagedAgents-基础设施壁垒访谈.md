---
title: "一年后Claude不需要Harness工程了？Anthropic产品&amp;工程负责人爆料：搭建Agent的最终难关是基础设施壁垒"
source: "微信公众号 - 51CTO技术栈"
url: "https://mp.weixin.qq.com/s/hYMj375l9Y29kOhehuOkZQ"
date: 2026-05-09
category: "AI技术"
depth: "🟡 标准"
tags: [Anthropic, Claude, Managed Agents, Harness工程, 基础设施, AI Agent, 多智能体编排, AI基础设施, 模型绑定, Angella Jiang, Katelyn Lesse]
---

## 一句话总结

Anthropic 产品负责人 Angela Jiang 和工程负责人 Katelyn Lesse 深度访谈：搭建 Agent 的真正难点不是 Harness 工程，而是**基础设施壁垒**；Harness 与模型正高度配对，通用 Harness 切换模型的做法正在失效；一年后的 Claude 将实现极简抽象，用户只需关心「结果」和「预算」。

## 关键判断（逐条提炼）

### 1. 核心矛盾：Harness 工程 vs 基础设施
- **人们普遍觉得** Harness 工程（工具构建、循环设计、提示词调优）是最难的部分
- **实际难关**：将原型投入生产时撞上"基础设施壁垒"——沙箱断连、内存丢失、异步处理、服务器常驻、对话记录存储、安全沙箱环境
- **Anthropic 的解法**：Managed Agents = 把文件系统、Skills、记忆、保险库（Vaults）都集成到云端，不再重复踩基础设施的坑

### 2. Harness 与模型正高度配对
- **历史阶段（几个月前）**：构建通用 Harness，随时切换模型 ~ STDI
- **当前趋势**：各实验室采取不同技术视角，用 Harness 工程榨取模型每一丝性能
- **内部证据**：同个模型在不同 Harness 方案下的评估（eval）表现差异极大
- **结论**：「极致通用」抽象层开始失效，Harness+模型成为捆绑组合

### 3. Managed Agents 的受众定位（两类）
1. **公司内部构建自动化工具的人**：法务审核营销文案、端到端开发平台、CI/CD 集成
2. **将 AI 构建进产品给客户的人**：需要大量定制，但不该把工程资源浪费在基础设施上

### 4. 多智能体编排的四种探索模式
| 模式 | 描述 | 适用场景 |
|------|------|----------|
| **顾问策略** | 执行与建议分离 | 需要独立审核的长流程任务 |
| **对抗模式** | 一个生成 + 一个对抗性审核 | 内容生成 + 合规审核 |
| **蜂群模式** | 多 Agent 并发找 Bug | 代码审查、漏洞扫描 |
| **N 选 1** | 多方案并行，选最优 | 高精度要求的决策场景 |

### 5. OpenClaw 是 Claude 的进化方向
- 两位负责人明确：**Claude 一定会向「容易部署的 Agent」方向进化**
- 起步重点在基础设施（因为那是最大痛点）
- 已迈出的步子：保险库（Vaults）组件——安全封装密钥、OAuth 令牌
- 未来愿景：一键集成 Slack，或直接对 Claude 说"添加 Slack"即可

### 6. 一年后 Claude 的极致简化愿景
- **用户关心的参数只剩两个**：✅ 结果  ✅ 预算
- **Claude 动态编写自己**：自动判断该用哪个模型、如何启动子智能体
- **现有创新会消失**：不需要 Harness 工程、不需要脚手架代码、不需要提示词工程
- **Agent 持续运行、不断自我重构**
- 玩笑话：「到那时候我们可能就不坐在这儿了，得求着它办事」

### 7. 组织内的 Agent 运维实践
- **自助修改模式**：业务团队（营销/法务）直接通过 Claude 对话调整 Agent 行为，无需触碰底层代码
- **架构本质**：套娃式 Managed Agents——一个托管 Claude 实例负责解释用户需求，多个后台 Claude 配合执行
- **Agent 生命周期**：新模型发布时使用专门的 Skill 做自动升级；狂热用户甚至让 Agent 监控其他 Agent 是否过时

## 我的理解

这篇访谈信息量极大，几个信号值得深入关注：

1. **「Harness 与模型高度配对」是 2026 年 Agent 生态最关键的转折信号**。几个月前大家都在做通用 Harness（LangChain、Vercel AI SDK 等），现在 Anthropic 官方说这条路在下一代模型时代走不通了。这不是 vendor lock-in 的营销话术——同个模型在不同 Harness 表现差异巨大是确实可复现的现象。**这意味着 AI Agent 的「中间件」价值在下行，模型本身的「原生 Harness」价值在上行。**

2. **基础设施壁垒的认知错位非常典型**。做原型的人觉得最难的是调 Harness、写工具、调提示词——这些是「可见的困难」。一旦要跑生产环境，沙箱稳定性、状态持久化、异步任务调度这些「不可见的困难」才是真正的杀手。**Managed Agents 本质上就是 Anthropic 把基础设施做成商品化服务（基础设施即服务）。** 这和 AWS 把服务器运维商品化的逻辑如出一辙。

3. **「结果 + 预算」双参数愿景值得思考**。这个表述非常极端——现在连选哪个模型都要人决策，一年后 Claude 要能自己决定。这意味着**模型需要有「自我认知」（知道自己擅长什么、什么场景用哪个策略），且能在运行时动态分配子智能体架构。** 如果真能实现，现在的 prompt engineer、agent builder 角色都会被重构。

4. **与 Greg Brockman 那篇访谈的对照**。Greg 说「算力才是硬通货」，Angela 说「基础设施才是最终壁垒」——一个在算力层找答案，一个在平台层。两者的共同点：**最稀缺的都不是 AI 能力本身，而是让 AI 能力「可用」的支撑系统。** 这对 WorkBuddy 生态的启示：上下文注入、技能管理、记忆持久化这些基础设施功能是真正的护城河。

5. **对个人开发者/小团队的影响**。如果一年后 Claude 真的能做到「你说结果和预算，剩下的交给机器」，那现在的很多 Agent 创业公司（做 Harness、做编排层、做 Agent 平台的）可能会被直接淘汰。同场发布的 Claude Managed Agents 四件套（Dreaming / 多智能体编排 / 结果验收）已经有 Harvey 任务完成率暴涨 6 倍的数据——**平台级 Agent 能力碾压第三方工具的时代可能真来了。**

---

*加工时间：2026-05-09 20:01*
