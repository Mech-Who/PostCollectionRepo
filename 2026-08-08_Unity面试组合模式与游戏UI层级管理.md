---
title: Unity面试系列：组合模式与游戏UI层级管理
date: 2026-08-08
category: 游戏开发
depth: 深度
layer: layer2
tags: [组合模式, 设计模式, Unity, UI层级管理, 递归, 软件设计架构]
summary: Unity面试系列第046期：用组合模式重构游戏UI层级管理——让叶子节点（按钮/文本）和容器节点（Panel/Window）实现统一接口，对根节点的操作自动递归到所有子节点。对比传统UIManager的耦合/漏关/难扩展三痛点，给出UIComponent抽象基类+UILayout叶子+UIContainer容器+UIManagerComposite四步完整代码，一次调用搞定整棵UI树的显示隐藏。
source_url: https://mp.weixin.qq.com/s/NNXMHdvERRDYrDNWuFOEew
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** 面试题切入：大型RPG的UI系统（几十个界面、每界面多个子界面）如何组织层级关系？答案是组合模式（Composite Pattern）——让叶子节点与容器节点实现同一接口，对根节点的操作自动递归到整棵树。文章用完整C#代码演示重构：UIComponent抽象基类（Add/Remove/Show/Hide/ShowRecursive/HideRecursive/GetDepth）→ UILayout叶子 → UIContainer容器（带depth层级）→ UIManagerComposite（Dictionary索引+递归构建UI树），最终OpenInventory()一行代码搞定整个背包界面显示。核心优势：递归操作零成本、新增UI零修改、统一接口可扩展（SetAlpha/PlayAnimation自动递归）、树形结构可排序/遍历/统计。

## 我的理解
> 由小林生成，供小涵审阅修改

1. **组合模式是"树形结构与统一接口"的完美结合**：它解决的核心问题是"整体-部分一致性"——让单个对象和组合对象拥有一致的接口，调用者对根节点的一次操作自动传播到整棵树。对游戏UI这种天然树形结构的系统（Canvas→Panel→Button）简直是量身定做。这与软件设计架构概念的"要素+结构+连接"框架呼应：组合模式就是在结构维度上用统一接口抹平了层次差异。

2. **组合模式与装饰器模式的分工是面试高频考点**：组合模式强调"整体与部分的一致性"（叶子与容器同接口，可统一处理）；装饰器模式强调"动态添加职责"（不改变原对象扩展功能）。两者可组合使用——装饰器包装组合树的节点来动态加特效。这印证了设计模式不是孤立的，而是可以嵌套的架构语言。

3. **组合模式的代价是"透明性vs安全性"的取舍**：把Add/Remove放在抽象基类里（透明设计）让叶子节点也能调Add，但运行时会报错；如果放在容器里（安全设计）接口就不统一。文章用基类虚方法（默认空实现）走透明路线，简单但需注意叶子误调Add的边界。这是面试官追问"缺点"时的深层考点。

4. **GPP设计模式×游戏开发知识线第5实证**：Command(07-06) → ServiceLocator(07-07) → Decorator(08-02) → CRTP(08-02) → **Composite(08-08)**。这条线的规律继续成立：设计模式本质是"语言能力脚手架"——C# 缺 trait/内建组合时，用组合模式在运行时组织树；而前作 CRTP 展示 C++ 编译期方案。组合模式进一步印证：**树形结构+递归是处理层级关系的通用原语**。

5. **面试答题模板价值**：这篇文章本身就是一份可复用的面试答题框架——痛点（传统写法三问题）→ 模式定义（统一接口+递归）→ 结构（Component/Leaf/Composite）→ 完整代码（四步）→ 优势 → 追问（对比/缺点/场景/实现）。这种"痛点-模式-代码-追问"结构可以直接迁移到其他设计模式面试题。

## 📌 关键要点
- **核心思想**：让单个对象和组合对象拥有一致的接口，用统一方式处理——叶子（按钮）和容器（界面）都实现同一接口，对根节点的操作自动递归到所有子节点
- **结构**：Component（抽象基类）├── Leaf（单个UI组件）└── Composite（容器 Panel/Window/UI Root）
- **传统写法三痛点**：①代码耦合严重（UIManager要知道所有UI引用）②递归操作麻烦（漏关一个子节点就出bug）③扩展性差（新增界面要加引用加方法）
- **四步重构**：
  1. `UIComponent`抽象基类：Add/Remove/GetChild + 抽象Show/Hide + 虚ShowRecursive/HideRecursive（递归遍历children）+ GetDepth
  2. `UILayout`叶子：重写Show/Hide，SetActive(true/false)
  3. `UIContainer`容器：带depth字段，重写GetDepth，IsContainer判断（childCount>0或Panel/Window/Container结尾）
  4. `UIManagerComposite`：Dictionary<string,UIComponent>索引 + BuildUITree递归构建 + ShowUI/HideUI/ToggleUI
- **使用效果**：`uiManager.ShowUI("InventoryPanel")` 一行代码显示整个界面树，自动递归
- **核心优势**：递归操作零成本 / 代码极度简洁（新增UI零修改）/ 统一接口灵活扩展（SetAlpha/PlayAnimation自动递归）/ 树形结构威力（排序/遍历/统计）
- **面试追问**：
  - Q1 组合vs装饰器：组合=整体与部分一致性；装饰器=动态添加职责
  - Q2 缺点：树形设计不合理→性能问题（递归过多）；组件耦合需仔细设计否则过度设计
  - Q3 场景：文件系统、组织架构、Unity场景管理、游戏对象层级
  - Q4 实现：抽象基类做MonoBehaviour、协程处理异步、ScriptableObject存UI配置
- **金句**："复杂的层级关系，用简单的递归解决。"

## 原文
（详见公众号原文，核心内容已在摘要与关键要点中覆盖）

## 相关笔记
- [[软件设计架构]]
- [[Unity面试装饰器模式与游戏道具效果叠加]]
- [[Cpp聊聊模板模式CRTP奇异递归模板模式]]
