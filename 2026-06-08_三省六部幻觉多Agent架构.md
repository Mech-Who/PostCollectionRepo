---
title: 三省六部幻觉：为什么"虚拟公司"式多Agent架构工程不行
date: 2026-06-08
tags: [AI, Agent, 多Agent, 架构设计, 工程实践]
summary: 犀利批判"虚拟公司"式多Agent设计模式——模仿人类部门分工让Agent协作，本质上是解决了人类的瓶颈而非AI的瓶颈。信息在流转中死亡的核心问题：传递结论而非推理过程，导致原始意图衰减、误差累积。对比Anthropic、OpenAI、Google的正确实践，给出Agent设计五原则。
category: AI技术
depth: 深度
source_url: https://mp.weixin.qq.com/s/0tbLEAH6VBPvOUtMd1ER9g
source: weixin
status: 📥已采集
sync_status: ✅已同步
---

> **摘要：** 文章尖锐批判了当前流行的"三省六部"式多Agent架构——用模仿人类公司的部门分工让AI Agent协作。核心论点：这个模式解决了人类的瓶颈（注意力有限、专业壁垒），而不是AI的瓶颈。LLM没有职业边界，强制角色分工反而限制了跨边界的有价值推理。信息在Agent之间传递结论而非推理过程，导致原始意图衰减和误差累积，最终整体结果漂移。文章对比了Anthropic、OpenAI、Google的正确做法，并提炼出Agent设计五原则。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇文章是近几个月读到的最有力的Agent架构批判。"三省六部"式的多Agent设计乍看很合理——给不同Agent贴角色标签（产品经理、工程师、设计师），让他们像公司一样协作。但文章指出这是把人类的组织形态强行套在AI上，犯了"类比谬误"。

**核心洞察：** LLM的瓶颈不在"广度"（他能同时处理多个领域），而在"深度"（推理链路越长越容易发散）。所以给LLM加角色标签不是帮他，是限制他。最有价值的推理往往发生在角色边界上——比如"这个需求虽然有技术难度，但从用户体验角度值得做"这种跨领域判断，在部门分工模式下会被丢弃。

**"信息在流转中死亡"这个观点非常深刻：** Agent之间传递的是"结论"（推理的终态），而不是"推理过程"。结论跨节点传递时，原始意图和隐含假设会逐级丢失，最终整体漂移。这其实就是分布式系统中的"信息损失级联"问题。

**与大厂方案的对照价值很高：**
- Anthropic的"外部状态文件"本质上是让Agent共享推理上下文而非结论
- OpenAI的Compaction是结构化压缩推理过程
- Google的1M上下文+CDD是直接给Agent尽可能多的原始信息
  三种思路虽不同，但都指向同一个方向：不要让信息在流转中衰减

## 📌 关键要点
- **核心问题：** "虚拟公司"式多Agent架构解决的是人类的瓶颈（注意力有限、专业壁垒），而不是AI的瓶颈（推理深度、信息完整性）
- **致命缺陷一：** 给Agent贴角色标签会限制能力——LLM没有职业边界，跨边界推理最有价值
- **致命缺陷二：** "信息在流转中死亡"——传递结论而非推理过程，导致意图衰减、假设丢失、误差累积
- **大厂对比：**
  - Anthropic：外部状态文件模式（共享推理上下文）
  - OpenAI：Compaction + Skills + 结构化Spec文件（压缩管理长任务）
  - Google：1M上下文 + Context-driven Development（原始信息最大化）
- **正确Agent设计五原则：**
  1. 推理链不能断，只能分叉再合并
  2. 依赖显式外部状态
  3. 多Agent价值是并行覆盖而非分工
  4. 验证Agent应是对抗性检验者
  5. 工具比角色标签更重要

## 原文

文章批判流行的"三省六部"多AI Agent设计模式（模仿人类公司部门分工让Agent协作），认为该模式存在根本性工程缺陷。

**核心论点：** 该模式解决了人类的瓶颈（注意力有限、专业壁垒、沟通成本），而非AI的瓶颈。LLM的特性（无职业边界、瓶颈在推理深度和信息完整性）与该模式不匹配。为Agent贴角色标签会限制能力，阻碍发生在边界上的有价值推理。"信息在流转中死亡"——传递结论而非推理过程，导致原始意图衰减、隐含假设丢失、误差累积，最终整体漂移。

**对比大厂设计：**
- Anthropic采用外部状态文件模式
- OpenAI通过Compaction、Skills和结构化Spec文件管理长任务
- Google采用1M上下文+Context-driven Development策略

**正确Agent设计五原则：**
1. 推理链不能断只能分叉再合并
2. 依赖显式外部状态
3. 多Agent价值是并行覆盖而非分工
4. 验证Agent应是对抗性检验者
5. 工具比角色标签更重要
