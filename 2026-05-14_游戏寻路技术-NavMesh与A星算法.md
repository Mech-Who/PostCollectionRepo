# 被石头挡路也能绕最优路线？揭秘游戏角色「聪明寻路术」

- **来源**：微信公众号「小商算法leet」（TableX）
- **日期**：2026-05-14 归档
- **分类**：游戏开发
- **加工深度**：🔴 深度（技术教程+代码实现+知识提炼）
- **标签**：#游戏开发 #寻路算法 #A星算法 #NavMesh #导航网格 #Unity #原神 #王者荣耀

---

## 摘要

以《原神》钟离自动绕路摘琉璃袋和《王者荣耀》韩信自动避开防御塔为引，深入拆解游戏角色智能寻路的两大核心技术：**导航网格（NavMesh）** 预烘焙可行走区域，**A\* 寻路算法** 在网格中计算最优路径（f = g + h）。文章提供完整 Python 代码实现，并关联《塞尔达》《CSGO》等跨游戏案例。

---

## 关键要点

### 1. 导航网格（NavMesh）
- 将复杂的 3D/2D 地图预烘焙成连续的可行走多边形区域
- 标记不可行走区域（悬崖、高墙、深水区）
- 相当于给角色划定「合法行走范围」
- 含高度信息，区分「平原可行走区」「山地不可走区」等

### 2. A\* 寻路算法
- 核心公式：**f = g + h**
  - `g`：起点到当前节点的实际距离
  - `h`：当前节点到终点的预估距离（启发函数，通常用直线距离）
  - `f`：「综合成本」，A\* 每次选 f 值最小的节点前进
- 本质：在 Dijkstra（保证最优但慢）和贪心最佳优先（快但不保证最优）之间取得平衡
- 只要 h 函数 ≤ 实际距离（可接受启发式），A\* 保证找到最优路径

### 3. 原神寻路完整流程
1. **预烘焙导航网格**：提瓦特大陆地形 → 带高度信息的 NavMesh，标记不可行走区
2. **触发寻路请求**：玩家点击目标 → 定位当前网格节点 + 目标网格节点
3. **A\* 计算最优路径**：从起点遍历周围可行走网格，筛选 f 最小节点，形成连续路径序列
4. **角色沿路径移动**：路径序列 → 移动指令 → 平滑移动

### 4. 其他游戏应用场景
- 《王者荣耀》：英雄自动避开防御塔、墙体，选最短安全路径
- 《塞尔达传说：旷野之息》：NPC 巡逻路径，自动避开悬崖和怪物领地
- 《CSGO》：Bot 移动自动绕开障碍物接近目标点

### 5. 代码实现（Python）

```python
class Node:
    def __init__(self, pos, walkable):
        self.pos = pos
        self.walkable = walkable
        self.g = 0          # 实际距离
        self.h = 0          # 预估距离
        self.f = 0          # 综合成本 g+h
        self.parent = None  # 父节点（用于回溯路径）

def astar(start, end, nodes):
    open_list = [start]
    closed_list = []

    while open_list:
        current = min(open_list, key=lambda n: n.f)
        open_list.remove(current)
        closed_list.append(current)

        if current == end:
            # 回溯路径
            path = []
            while current:
                path.append(current.pos)
                current = current.parent
            return path[::-1]

        for neighbor in get_neighbors(current, nodes):
            if not neighbor.walkable or neighbor in closed_list:
                continue
            temp_g = current.g + distance(current, neighbor)
            if neighbor not in open_list or temp_g < neighbor.g:
                neighbor.g = temp_g
                neighbor.h = distance(neighbor, end)
                neighbor.f = neighbor.g + neighbor.h
                neighbor.parent = current
                if neighbor not in open_list:
                    open_list.append(neighbor)

    return None  # 无可行路径
```

在 Unity 等引擎中，结合官方 NavMesh 组件，将此路径转换为角色移动动画与位置更新。

### 6. 拓展思考
- 动态障碍物（掉落的岩石、移动的敌人）如何实时更新路径？
- 常见方案：D\*（动态 A\*）、D\* Lite、LPA\* 等增量搜索算法，在环境变化时仅更新受影响的部分节点而非全量重算

---

## 我的理解

这篇文章把 A\* + NavMesh 讲得很清晰，适合作为游戏开发入门材料。但从实际工程角度看，有几点值得补充：

**1. NavMesh 的烘焙策略**：实际项目中，NavMesh 的粒度是性能 vs 精度的权衡。太细——A\* 搜索空间爆炸；太粗——路径不够平滑。常见优化包括 NavMesh 分层（粗粒度长距离 + 细粒度局部）、离线预计算 + 在线查询。

**2. A\* 的实际优化**：文中的实现是基础版，生产环境通常需要：
- **优先队列（二叉堆）替代 min()**：时间复杂度从 O(n) 降到 O(log n)
- **Jump Point Search (JPS)**：在均匀网格上大幅减少搜索节点
- **路径平滑**（如弦切法、漏斗算法）：A\* 输出的是网格路径，需要平滑处理才能自然移动
- **分层寻路（HPA\*）**：大地图先抽象为 high-level graph，大幅减少搜索空间

**3. 与 Unity 的集成**：Unity 的 NavMesh Agent 组件内置了 A\* 实现，一般不需要手写寻路代码。但在需要自定义行为（如动态障碍物、多目标优化、避开特定区域）时，理解底层原理就很重要。

**4. 游戏 AI 寻路的未来方向**：传统 NavMesh + A\* 适合静态环境。对于更复杂的动态场景，有两个值得关注的方向：
- **群体寻路（Crowd Simulation）**：RVO/ORCA 算法，处理角色之间互相避让
- **学习型寻路**：用 ML 学习隐式的导航策略（如导航网格之外的非几何约束），但目前在延迟和可控性上还不如传统方法

> 这篇文章是「小商算法leet」系列的第 100 期，专注游戏中数据结构与算法的科普，值得关注。
