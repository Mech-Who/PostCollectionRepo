---
title: "单元测试到底是什么？应该怎么做？"
date: 2026-06-10
source: "微信公众号：花宝宝 Dev"
author: "花宝宝 Dev"
category: "观点随笔"
depth: "🟡标准"
layer: "layer1"
processed_date: 2026-06-10
sync_si: "✅已同步"
mempalace: "❌未入库"
---

# 单元测试到底是什么？应该怎么做？

## 原文内容

单元测试不是"写一堆 test 文件"，也不是"把每个方法都测一遍"。

单元测试测的是一个很小的代码单元，在确定输入下是否产生确定输出。这个单元可以是一个函数、一个类里的核心方法，也可以是一段独立的业务逻辑。重点不是"单元"有多小，而是它能不能脱离复杂外部环境，被快速、稳定地验证。

比如价格计算、订单状态流转、参数校验、格式转换，这些都很适合写单元测试。因为输入和输出清楚，边界情况多，人工测容易漏。

但如果一个 Controller 只是转发请求，一个 Repository 只是调用 MyBatis/JPA 的 `findById`，这类东西硬写单元测试，往往只是在测框架，不是在测你的业务。

所以我不太赞成把单元测试分成"必须写"和"没必要写"两派。它值不值得写，取决于你在测什么。

### 高回报的测试场景

**纯函数和业务逻辑计算。** 输入确定、输出确定、没有外部依赖的代码，测试写起来最省力，收益最高：

```java
public class PriceCalculator {
    public BigDecimal calculate(BigDecimal originalPrice, List<Coupon> coupons) {
        // 折扣叠加、满减规则、上下限约束... 十几行逻辑
    }
}
```

这种方法的边界情况多（多个优惠券、价格为零、叠加超出上限），手工测每种组合成本很高，写测试反而快：

```java
@Test
void 满减叠加优惠券结果不低于1元() {
    BigDecimal price = calculator.calculate(
        new BigDecimal("50"),
        List.of(new Coupon(MINUS_20), new Coupon(MINUS_40))
    );
    assertThat(price).isEqualByComparingTo("1.00");
}
```

测试在这里的价值：逻辑改了，测试立刻告诉你哪里不对，不需要手动跑一遍所有组合。

**状态机和流程控制。** 订单状态流转、审批流、支付流程——状态转换规则复杂，非法转换的分支多，测试覆盖每种边界比靠 review 更可靠。

**数据解析和格式转换。** CSV 解析、JSON 映射、日期格式转换——边界情况非常多（空字段、特殊字符、时区问题），测试驱动开发这类代码能明显减少 bug。

### 低回报的测试场景

**只转发调用的 Controller。**

```java
@PostMapping("/orders")
public ResponseEntity<OrderVO> createOrder(@RequestBody CreateOrderRequest req) {
    return ResponseEntity.ok(orderService.createOrder(req));
}
```

Controller 里没有逻辑，只是调用 Service 并返回结果。测这个方法就是在测"Spring MVC 能不能转发调用"，回报极低。

**只做数据库 CRUD 的 Repository。** 测一个 `findById` 是否能查到数据，本质上是在测 JPA 或 MyBatis，不是在测业务逻辑。这类测试需要真实数据库（或内存数据库），维护成本高，价值低。

**大量 Mock 的测试。**

```java
when(orderRepository.findById(1L)).thenReturn(Optional.of(order));
when(paymentService.charge(any())).thenReturn(paymentResult);
when(notifyService.send(any())).thenReturn(true);
orderService.processOrder(1L);
verify(paymentService).charge(any());
```

这个测试 mock 了所有外部依赖，只验证"调了哪些方法"。它测的是实现细节，不是行为。方法内部重构（改了调用顺序、加了一层缓存），测试就失败了——但业务行为没有变化。这种测试是维护负担，不是保障。

### 测试的真实价值

单元测试最重要的价值不是发现 bug（集成测试和 e2e 测试在这方面更有效），而是**让重构有安全网**。

有测试覆盖的计算逻辑，你改的时候不会担心把边界情况搞坏；没有测试覆盖的历史代码，你每次改都是在裸奔，只能靠"小心点"。

另一个价值是**强迫设计**。代码难以测试，往往意味着设计有问题——依赖太多、职责不清晰、硬编码外部依赖。如果写测试需要 mock 七八个依赖，这个类本身就需要重构。

### 实际建议

不需要追求覆盖率数字。80% 覆盖率里可能全是低价值测试，30% 覆盖率里全是核心业务逻辑的测试——后者更有用。

从最复杂的业务规则开始写，让测试去覆盖容易出错的地方，而不是机械地给每个类配一个测试文件。

如果接手的代码没有测试，不要从零开始补全所有测试。在要修改或重构的地方加测试，改动之前先写失败的测试，改完让它通过——这是对时间最划算的投入方式。

### 测试金字塔：不同层的职责

测试金字塔的概念：底层大量单元测试（快、便宜），中间少量集成测试（中速、中等成本），顶层极少 E2E 测试（慢、贵）。

这个模型在大多数团队里是倒置的——大量 E2E 测试（Selenium/Cypress 跑 UI），少量或没有单元测试。倒置金字塔的问题：E2E 测试慢（一套跑完十几分钟），不稳定（网络抖动、异步时序），定位问题困难（失败了要人工排查是哪一层出的问题）。

对 Spring Boot 后端而言，实用的分层是：

**Service 层逻辑**：JUnit + Mockito，测业务规则，不启动 Spring 容器，毫秒级运行。

**Repository 层 SQL**：用 Testcontainers 起真实数据库容器，测查询逻辑，一次几十毫秒，换 H2 内存数据库的测试在 SQL 方言上会骗人。

**Controller 层 API**：`@WebMvcTest` + MockMvc，测 HTTP 层的参数校验、序列化、状态码，Service 用 Mock 代替，不需要完整启动。

**关键业务流程**：`@SpringBootTest` 启动完整上下文，配合 Testcontainers，跑 2-3 个最重要的端到端流程，覆盖核心 happy path。

### Testcontainers：真实数据库测试

H2 内存数据库是测数据库层的常见做法，但有个陷阱：H2 和 MySQL 的 SQL 方言不完全兼容。MySQL 特有的语法（`ON DUPLICATE KEY UPDATE`、`GROUP BY` 的 MySQL 宽松模式、`DATETIME` 精度行为）在 H2 下可能跑过但生产环境报错。

Testcontainers 启动真实的 Docker 容器来跑测试：

```xml
<dependency>
    <groupId>org.testcontainers</groupId>
    <artifactId>mysql</artifactId>
    <scope>test</scope>
</dependency>
```

```java
@SpringBootTest
@Testcontainers
class OrderRepositoryTest {
    @Container
    static MySQLContainer<?> mysql = new MySQLContainer<>("mysql:8.0")
        .withDatabaseName("testdb")
        .withUsername("test")
        .withPassword("test");

    @DynamicPropertySource
    static void configureProperties(DynamicPropertyRegistry registry) {
        registry.add("spring.datasource.url", mysql::getJdbcUrl);
        registry.add("spring.datasource.username", mysql::getUsername);
        registry.add("spring.datasource.password", mysql::getPassword);
    }

    @Autowired
    private OrderRepository orderRepository;

    @Test
    void 测试分页查询按时间倒序() {
        // 插数据，查，断言
    }
}
```

第一次启动会 pull 镜像，之后本地复用缓存，每次测试重用同一个容器（`@Container` + `static` 字段保证容器在所有测试方法间复用）。在 CI 里配好 Docker，Testcontainers 可以直接跑。

### Service 层测试的写法

Service 测试应该测业务行为，不测调用细节：

```java
@ExtendWith(MockitoExtension.class)
class OrderServiceTest {
    @Mock
    private OrderRepository orderRepository;
    @Mock
    private PaymentService paymentService;
    @InjectMocks
    private OrderService orderService;

    @Test
    void 余额不足时支付失败且订单状态不变() {
        Order order = Order.builder().id(1L).status(PENDING).amount(new BigDecimal("100")).build();
        given(orderRepository.findById(1L)).willReturn(Optional.of(order));
        given(paymentService.charge(any())).willThrow(new InsufficientBalanceException());
        assertThatThrownBy(() -> orderService.processPayment(1L))
            .isInstanceOf(InsufficientBalanceException.class);
        assertThat(order.getStatus()).isEqualTo(PENDING); // 状态没有变成 FAILED
        verify(orderRepository, never()).save(any());     // 没有持久化
    }
}
```

这里测的是"余额不足时业务状态的保证"，不是"调了哪些方法几次"。即使内部实现改了，只要行为约束还在，测试就通过。

---

## 摘要

**Key Takeaway**：单元测试的核心价值不是追求覆盖率数字，而是为重构提供安全网、倒逼代码设计改进；高层回报的测试从纯函数/业务逻辑开始写，低层回报的测试（转发Controller、CRUD Repository、大量Mock）应避免；用 Testcontainers 替代 H2 确保数据库层测试的真实性。

**Key Points**：
1. 高回报测试：纯函数计算、状态机流程、数据解析格式转换——输入输出清晰，边界情况多
2. 低回报测试：只转发调用的 Controller、只做 CRUD 的 Repository、大量 Mock 的实现细节测试
3. 单元测试真实价值：让重构有安全网 + 强迫好的代码设计（难以测试=设计有问题）
4. 不需要追求覆盖率数字，30%覆盖率的业务核心测试比80%的低价值测试更有用
5. 对 Spring Boot 后端的分层建议：Service用JUnit+Mockito、Repository用Testcontainers、Controller用@WebMvcTest

---

## 理解

这篇文章是我近期看到的关于单元测试最务实的一篇。作者不站在"覆盖率原教旨主义"也不站在"测试无用论"的极端立场，而是给出了明确的投资回报率判断标准：测什么比怎么测重要得多。最关键的一句话是"代码难以测试，往往意味着设计有问题"——这其实点出了 TDD 最有价值的副产品：不是测试本身，而是通过测试来反向驱动代码解耦。关于 Testcontainers 的推荐也很到位，用 H2 做数据库层测试在大型项目里埋坑太多了。对 Java/Spring 团队来说，这篇文章可以作为一份团队内部测试规范的参考模板。

---

## 元数据
- 分类：观点随笔
- 加工深度：🟡标准
- Layer：layer1
- K-Fragment：否
