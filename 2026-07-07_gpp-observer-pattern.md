---
title: "观察者模式 — 游戏中最常用的通知机制"
date: 2026-07-07
category: 游戏开发
depth: 深度
layer: layer2
tags: [观察者模式, 设计模式, 事件系统, 成就系统, 解耦, 数据绑定]
summary: "观察者模式让代码宣称'有趣的事发生了'而不必关心谁在接收。在游戏中的应用包括成就系统、UI更新、音频响应等。讨论了性能、内存管理和失效监听者问题，以及从类到函数/事件的演进。"
source_url: https://gpp.tkchu.me/observer.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 观察者模式是应用最广泛的GoF模式。通过"被观察者持有观察者列表、事件发生时遍历通知"的简单机制，实现代码间的松耦合通信。讨论了同步通知的性能与风险、链式观察者消除动态分配、失效监听者问题和数据绑定的未来趋势。

## 我的理解

> 观察者模式的精髓在于一个看似矛盾的平衡：它让两块代码能通信而不需要相互了解。物理引擎不知道成就系统的存在，成就系统也不直接依赖物理引擎——它们通过"事件"这个抽象接口对话。这是软件工程中"关注点分离"原则的完美体现。

> 最令我印象深刻的是作者对"观察者似乎很慢"这一常见误解的解构。同步观察者本质上只是一次虚函数调用——遍历列表、调用onNotify()——比静态调用慢一点，但远非"慢"。真正的问题在于使用不当：如果观察者在通知中做重活或者修改观察者列表，才会出问题。

> 链式观察者是一个精妙的优化：不维护独立的观察者列表，而是让观察者对象本身成为链表节点。这消除了动态内存分配，但代价是每个观察者一次只能观察一个被观察者。这种侵入式链表的设计在Linux内核中很常见。

> 失效监听者问题（lapsed listener problem）是实践中最大的坑：被观察者持有观察者引用导致GC无法回收。解决方案很简单：在观察者析构时取消注册——但关键是**记得做**。

> 最后关于数据绑定的讨论很有前瞻性。经典观察者中大部分代码都是"获知状态改变→更新UI"的重复模式，这正是数据绑定试图自动化的。但对于游戏引擎核心（非UI部分），经典观察者仍然是正确选择。

## 📌 关键要点
- **核心机制**：被观察者持有观察者列表 → 事件发生 → 遍历通知（同步调用）
- **性能误解**：同步观察者只是一次虚函数调用，并非"慢"；真正问题是观察者在通知中做重活
- **链式观察者**：Observer作为链表节点 → 消除动态内存分配（侵入式链表）
- **失效监听者**：被观察者持有引用 → GC无法回收 → 必须在析构时removeObserver
- **何时不用**：如果需要理解程序必须同时看两个模块 → 用显式耦合更清晰
- **数据绑定**：UI层面的"状态改变→更新显示"是观察者的自然演进方向

## 原文

### 成就解锁示例
物理代码调用notify(entity, EVENT_START_FALL)，成就系统注册为观察者接收通知，实现两者完全解耦。

### 链式观察者
避免动态内存分配：Observer类包含next_指针，所有观察者形成链表。addObserver插入头部，removeObserver遍历删除。

### 失效监听者问题
被观察者持有观察者的引用 → 即使UI关闭了，GC也无法回收被观察对象。解决方案：析构时取消注册。

### 现代演进
从基于类的Observer接口 → 基于函数/闭包/委托 → 数据绑定。游戏引擎核心仍适合经典观察者。
