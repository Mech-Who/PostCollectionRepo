---
title: 基于 Harness + SDD + 多仓管理模式的 AI 全栈开发实践｜得物技术
date: 2026-05-16
tags: [AI编程, 全栈开发, Harness, SDD, 多Agent, Cursor, ClaudeCode, 工程实践]
summary: 得物技术团队分享了AI全栈开发的完整实践方法论：Harness思维（让AI模仿而非凭空创造）、前后端多仓工作区+Codebase Indexing、SDD驱动的全栈代码生成、多Agent前后端并行开发、三阶段联调验证，以及SDD陷阱（隐性功能）的识别与防范。
category: AI应用
status: 📥已采集
---

> **摘要：** 本文提出了AI全栈开发的方法论体系——Harness思维（给AI模仿对象而非凭空创造）是全栈开发的起点，结合多仓工作区统一上下文、SDD驱动代码生成、多Agent并行开发和分阶段验证，形成完整闭环。特别指出SDD不等于需求文档，AI在模仿过程中会注入"隐性功能"，需要测试全面介入。

## 我的理解
> 由小林生成，供小涵审阅修改

这篇文章的实操价值很高，特别是"Harness思维"这个命名——以前我只是模糊地觉得"提示词里给参考代码效果更好"，但没有把它抽象成一个方法论。把"模仿式编程"作为一个自觉的策略来用，比一次次凭感觉试要高效得多。我特别关注的是文中对Cursor和Claude Code的对比——两者各有所长，Cursor在多Agent并行上更友好（Tab并行），Claude Code则在长链路复杂任务上有优势。另外"隐性功能"这个概念很值得警惕，我之前也遇到过AI自动加了一个没要求的逻辑，当时还以为是bug，现在看来是AI从参考代码里"学了不该学的东西"。方法论虽然写得不错，但我觉得这套体系对团队的要求比较高——没有统一的代码规范和参考代码库，Harness思维也很难发挥作用。

## 📌 关键要点
- **核心方法**: Harness思维（给AI参考代码约束生成范围）→ 多仓工作区（统一上下文） → SDD双文档（前后端各一份，接口契约对齐） → 多Agent并行 → 三阶段验证（前端Mock→后端编译→联调）
- **踩坑经验**: SDD不等于需求文档——AI在模仿参考代码时会注入"隐性功能"（如表单清除逻辑、数据格式转换、默认值补齐），这些不在SDD范围内但已被悄悄实现，必须通过测试全面介入来识别。
- **适用场景**: 适用于有一定代码规范基础的中大型全栈项目，团队有统一代码风格和参考代码库。个人项目或代码规范松散的项目，Harness的"约束"效果会打折扣。

## 原文

# 基于 Harness + SDD + 多仓管理模式的 AI 全栈开发实践｜得物技术

> **作者：** 盖伦
> **来源：** 得物技术

---

**目录**

一、核心理念：Harness 思维 — 让 AI 模仿，而不是凭空创造
二、全栈工作区搭建与 Codebase Indexing
三、SDD 驱动的全栈代码生成流程
四、多 Agent 协作：前后端并行开发
五、前后端联调：Mock 数据与分阶段验证
六、警惕 SDD 陷阱：测试如何介入全栈研发
七、综合效益与总结

---

### 一、核心理念：Harness 思维 — 让 AI 模仿，而不是凭空创造

#### 全栈 AI 开发最容易踩的坑

全栈 SDD 开发中，最常见也最致命的错误是：让 AI 从零开始写代码。AI 模型具备"通识能力"，给它一个需求描述，它确实能生成可运行的代码。但问题在于，这些代码往往是"外星代码"：**风格不一致**（命名规范、目录结构、分层方式与项目现有代码不同）、复用率低（没有利用项目已有的公共组件、工具函数、请求封装）、采纳率低（Code Review 时后端同学看到"外来风格"的代码，会产生大量修改意见）。结果就是：AI 生成了代码，但 Review 成本和返工成本反而更高了。

#### Harness 思维的核心：给 AI 一个"模仿对象"

Harness（约束）思维的本质是：给 AI 一个已有的实现作为参照，让它照着复刻一份，而不是凭空创造。就像给一个新入职的工程师说"你照着这个模块的风格，写一个类似的"，而不是"你自由发挥"——前者往往能更快产出符合团队规范的代码。

**原则与说明：**

| 原则 | 说明 |
| :--- | :--- |
| 找相似实现 | 在代码库中找到功能最相似的已有实现，作为参照 |
| 复用优先 | 能复用的组件、接口封装、数据结构，直接复用 |
| 模仿着复制 | 哪怕是"抄一份改一改"，也比用新方式写好 |
| 约束生成范围 | 在提示词中明确指定参考文件、参考接口 |

**举例：**

| 原则 | 举例 |
| :--- | :--- |
| 找相似实现 | "结束语"功能参照"场景化欢迎语"的前后端完整实现 |
| 复用优先 | 复用 `greetingExtendInfo` 的数据结构，新建 `closingExtendInfo` |
| 模仿着复制 | Controller / Service / Repository 按已有接口模仿 |
| 约束生成范围 | 前端修改入口参考 `@FeatureTable/index.tsx:53-58` |

#### 在提示词中体现 Harness

**不推荐（凭空创造）：**
> 请实现一个结束语管理的 CRUD 接口

**推荐（Harness 约束）：**
> 请参照现有"场景欢迎语"功能（后端接口 /api/v1/feature/list，... 前端组件 @FeatureTable...），为"结束语"功能实现一套完整的前后端 CRUD 接口。数据结构上参考 greetingExtendInfo，接口风格上参照已有的 FeatureController。

两者的差距不在于 AI 是否"聪明"，而在于你给了 AI 多少约束和上下文。约束越精准，生成代码的可用性越高。

---

### 二、全栈工作区搭建与 Codebase Indexing

#### 为什么要搭多仓工作区？

前后端代码通常分布在两个独立仓库。如果分开打开，AI 生成后端接口时看不到前端的调用方式，生成前端代码时看不到后端的返回结构，接口字段对不上是家常便饭。将前后端代码放在同一个工作区下，有三个核心价值：
- **Codebase Indexing**：Cursor 对工作区内所有代码进行向量化嵌入，建立语义索引。AI 能跨仓库理解代码关系，生成质量大幅提升。
- **上下文完整**：AI 同时能看到前后端代码，接口字段、命名风格自然对齐。
- **SDD 文档集中管理**：前后端 SDD 文档在同一工作区，便于接口契约对齐。

#### Codebase Indexing 的价值

Cursor 的 Codebase Indexing 会对工作区内的代码进行向量化嵌入，建立语义索引。**这意味着**：
- 当你问 AI "场景欢迎语是怎么实现的"，它不需要你手动指定文件，能通过语义检索自动找到相关的 Controller、Service、前端组件。
- 当你让 AI "照着欢迎语写结束语"，它会检索到欢迎语的前后端完整实现链路，而不只是单个文件。

前后端放在同一个工作区，Codebase Indexing 覆盖两侧代码。AI 生成后端接口时能参考前端的调用方式，生成前端代码时能参考后端的返回结构。

**Tips**：Cursor 打开工作区后，首次索引可能需要几分钟。可以在 Cursor 设置查看索引进度。确保索引完成后再开始让 AI 生成代码，效果会明显更好。

#### Cursor vs Claude Code：选哪个？

在全栈 AI 开发场景下，两款工具各有侧重，下表是实测对比：

| 功能维度 | Cursor | Claude Code |
| :--- | :--- | :--- |
| 代码库语义索引 | 支持 grep + 代码段语义相似度检索 | 仅支持 grep，准确度严重依赖模型能力 |
| 代码生成速度 | 极速（平均 1-3 分钟） | 中速（平均 3-30 分钟，5 分钟以上是常态） |
| 代码采纳率 | 两者相当 | 两者相当 |
| 文件/代码段引用 | 快捷键、拖拽即可引用 | 需手动 @文件路径，无法引用代码段 |
| 多 Agent | 默认开启（多 Tab 并行） | 需要手动注册子 Agent |
| 费率模型 | 失败任务不收费 | 失败任务耗时长，容易浪费大量 Token |
| 历史会话恢复 | 仅能查看当前项目会话记录 | 可查看全局会话记录 |
| 综合评价 | 快速迭代首选，推荐 Composer2 模式 | 长链路复杂任务可用，依赖卓越的基础模型 |

---

### 三、SDD 驱动的全栈代码生成流程

#### 全栈 SDD 的特殊之处

与纯前端/纯后端 SDD 不同，全栈 SDD 需要：
- **生成两份 SDD 文档**（前端一份、后端一份）
- **接口契约对齐**，前端 SDD 中的接口调用与后端 SDD 中的接口定义必须严格对应
- **字段映射一致**，前端 VO 中的字段名与后端返回的 JSON 字段名一一对应

#### 提示词编写范式

以下是经过实践验证的全栈 SDD 生成提示词模板：

> 这是一个前后端全栈开发工作区，需要你设计技术接口方案，同时开发前后端项目；
> 请先 `cd` 到对应目录，分析项目结构和 PRD 需求，生成前后端两份完整的 SDD 文档；
> 生成文档后，启动两个 agent 分别实现前后端代码；
> 过程中请先确认后生成，利用 @文件路径 引用相关参考文件。

---

### 四、多 Agent 协作：前后端并行开发

#### 为什么需要多 Agent

SDD 文档生成完毕后，前后端的代码生成工作是相互独立的——前端根据前端 SDD 生成组件和页面，后端根据后端 SDD 生成 Controller/Service/Repository。这天然适合并行执行。

#### Cursor 中的多 Agent 协作

Cursor 支持多个 AI 编程模式并行工作，这是其核心优势之一。**全栈开发场景下，Tab 1** 负责前端代码生成，**Tab 2** 负责后端代码生成，两个 Agent 同时运行、互不阻塞。

#### Claude Code 中的 Subagent 能力

Claude Code 内置了 Subagent（子代理）机制，适合命令行场景下的多任务并行。

Subagent 模式：只向主 Agent 汇报，主 Agent 管理一切，任务完成即结束。
Team 模式：队友之间可以直接沟通，共享任务列表，自我协调，队友保持空闲状态直到被关闭。

**Subagent 配置与使用**

Subagent 的核心配置项：
```json
{
  "subagent": {
    "maxAgents": 4,
    "agentTimeout": 300000,
    "defaultModel": "claude-sonnet-4-20250514"
  }
}
```

全栈开发场景中的应用：
- 主 Agent（你在对话的 Claude Code）负责统筹，生成 SDD 文档。
- 新建两个 Subagent：`agent-frontend`（根据前端 SDD 生成前端代码）、`agent-backend`（根据后端 SDD 生成后端代码）。
- 主 Agent 监控进度，处理异常。

---

### 五、前后端联调：Mock 数据与分阶段验证

#### 三阶段验证策略

直接联调往往是效率最低的验证方式，推荐采用三阶段分离验证：
> 阶段 1：前端 Mock 验证
> 阶段 2：后端独立构建验证
> 阶段 3：前后端联调

这样做的好处是：前后两端的问题可以提前发现、分别修复；避免在联调阶段才暴露；节省大量排查时间。

#### Mock 数据编写要点

Mock 数据质量直接决定前端自测的有效性，有三个关键要求：
- 字段名和字段类型必须与后端 SDD 中定义的完全一致
- 参考已有接口的真实返回数据作为模板，而不是随意构造
- 覆盖边界场景（空列表、单条数据、多条数据、各字段极值如空字符串、超长字符串、null 值等）

#### 后端独立构建验证

后端代码不需要在本地完整启动整个 Java 服务，只需编译通过即可验证大部分代码问题。
```shell
mvn clean compile -pl <module-name> -am
```
编译通过意味着：语法正确、依赖关系正确、类型兼容，是部署前最快速的验证手段。

#### 前后端联调步骤

1. 后端代码提交并部署到测试环境；
2. 前端本地开发服务通过代理配置，将 API 请求指向测试后端地址；
3. 前端请求携带功能路由标识，确保请求路由到对应的测试环境；
4. 逐接口验证，重点关注字段映射、状态处理、错误场景。

---

### 六、警惕 SDD 陷阱：测试如何介入全栈研发

#### SDD 不等于需求文档

这是 AI 全栈开发中最容易被忽视的问题。SDD 描述的是"技术上怎么实现"，而不是"业务上所有的行为"。AI 在模仿参考代码生成新代码时，会自动复刻很多**隐性功能**——这些功能在参考代码中存在，AI 认为是"理所当然"的，所以没有写进 SDD 文档，但实际上已经悄悄实现了。

#### 隐性功能示例

**示例 1：变量/表单清除（前端）**
```javascript
const handleClose = () => {
  setVisible(false);
  formRef.current?.resetFields(); // 隐性实现的清除逻辑
};
```

**示例 2：数据格式转换（后端）**
```java
if (StringUtils.isNotBlank(closingContent.getJsonContent())) {
    // 自动做了 JSON 解析和转换，SDD 中未定义
}
```

**示例 3：默认值补齐（后端）**
```java
closingExtendInfo.setPriority(lastPriority + 1);
```

这些隐性功能可能正是需要的，也可能完全不符合当前需求。问题在于你不知道它们的存在。

#### 测试介入建议

| 阶段 | 测试关注点 |
| :--- | :--- |
| SDD Review 阶段 | 关注接口契约是否完整，字段定义是否与需求对应。 |
| 代码 Review 阶段 | 重点：对照 SDD 文档和实际代码的差异，主动寻找隐性功能。 |
| 联调测试阶段 | 不要只测 SDD 中描述的 happy path，要覆盖边界场景和隐性行为。 |

---

### 七、综合效益与总结

#### 实践效益

通过本文介绍的"Harness + SDD + 多 Agent"全栈开发方法论，在实际项目中验证的效益如下：
- **采纳率提升