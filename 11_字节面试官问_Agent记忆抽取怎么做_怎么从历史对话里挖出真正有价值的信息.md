---
url: https://mp.weixin.qq.com/s/0R-8e6BLk6gwSUKNLt5J9A
title: 字节面试官问：Agent记忆抽取怎么做？怎么从历史对话里挖出真正有价值的信息？
date: 2026-05-25
source: weixin
status: ok
category: AI技术
depth: 🟡标准
tags: [Agent, 记忆抽取, LLM, 面试, Memory, 检索策略, 知识体系]
author: 
layer: layer2
sync_si: true
---

## 摘要

Memory已成为大模型方向高频面试考点，本文系统讲解Agent记忆从基础到进阶的完整知识体系。涵盖记忆分类（短期/长期/工作记忆）、写入策略、抽取四步流程（召回→过滤→排序→组装）、检索策略、压缩方法、时效性处理、一致性维护，以及工程落地最佳实践。

---

## Memory是什么与三大分类

LLM三大记忆缺陷：上下文窗口有限、跨会话失忆、无法积累经验。Memory的本质是将交互经验持久化存储并按需召回。三大分类：短期记忆（当前会话）、长期记忆（跨会话持久化：语义记忆/情景记忆/程序记忆）、工作记忆（当前任务临时信息）。

## 记忆的写入与抽取

信息价值判断四维度：持久性、复用性、事实性、独特性。三种写入策略：全量存储+后过滤（不丢信息但噪音多）、LLM实时抽取（主流做法）、摘要式存储。抽取四步流程：多路召回（语义+关键词+时间+主题）→相关性/时效性/置信度过滤→加权排序→按Token预算组装Top-K条。

## 检索策略与记忆压缩

记忆检索与RAG检索的区别在于时效性更敏感。混合检索推荐向量检索+BM25用RRF融合排名。四种压缩策略：滑动窗口、摘要压缩、关键信息抽取+摘要混合、分层压缩（最近5轮完整+5-20轮摘要+更早仅结构化记忆）。

## Agent应用与工程落地

MemGPT借鉴操作系统内存管理，Letta采用分层记忆架构（Core + Archival + Recall）。工程落地推荐PostgreSQL + pgvector，索引设计包括向量索引+时间索引+标签索引+用户索引。监控指标：召回准确率、延迟、记忆膨胀率、过期记忆占比。

---

## 金句

> 1. "很多人对 Memory 的理解，就停留在'把对话历史塞进 Prompt'这一步。面试官一追问底层设计就露馅。"
> 2. "记忆的抽取：怎么从历史信息里挖出有价值的东西？"
> 3. "今年同学们反馈最多的面试趋势就是：Memory 成了大模型方向的高频考点。"

---

## 我的理解

Agent Memory 不是一个数据库功能，而是一个**决策系统**——核心问题不是"怎么存"，而是"什么东西值得存、什么时机召回、怎么在有限的上下文窗口里做取舍"。本文提出的记忆抽取四步流程（召回→过滤→排序→组装）和记忆压缩四种策略（滑动窗口、摘要、关键信息+摘要混合、分层压缩）构成了完整的工程落地思路。工程落地建议中最有价值的几点：(1) 多路召回（语义+关键词+时间+主题）而非单一向量检索；(2) 记忆膨胀率作为关键监控指标——不控制膨胀会拖垮检索延迟；(3) MemGPT 的"LLM 自己管理记忆"设计理念，代表了从静态记忆到动态自我管理的发展方向。

---

## 原文

在小说阅读器读本章

去阅读

在小说阅读器中沉浸阅读

字节面试官问我："记忆的抽取怎么做的"，我说："就是存对话历史"，面试官："那你说说怎么从历史信息里挖出有价值的东西"，我。。。

今年同学们反馈最多的面试趋势就是：Memory 成了大模型方向的高频考点。

不管你投的是大模型应用开发、Agent 工程、还是 AI 后端，面试官都会问："你们 Agent 的记忆怎么做的？记忆抽取怎么设计？"

但很多人对 Memory 的理解，就停留在"把对话历史塞进 Prompt"这一步。面试官一追问底层设计就露馅。

短期记忆和长期记忆什么区别？记忆抽取的检索策略怎么设计？记忆压缩怎么做才能不丢关键信息？历史对话里哪些信息有价值、哪些是噪音？这些搞不清楚，面试官深挖两轮就原形毕露。

这篇文章把 Memory 记忆抽取从基础到进阶全部讲透，认真看完，面试不再怕被追问。

目录

1.  Memory 是什么？为什么大模型需要记忆？
2.  Memory 的三大分类：短期、长期、工作记忆
3.  记忆的写入：哪些信息值得存？
4.  记忆的抽取：怎么从历史信息里挖出有价值的东西？
5.  记忆检索策略：向量检索、关键词检索、混合检索怎么选？
6.  记忆压缩：对话历史太长怎么办？
7.  记忆的时效性：怎么处理信息过期和矛盾？
8.  记忆的一致性：多条记忆冲突怎么解决？
9.  记忆在 Agent 中的应用：MemGPT、Letta 怎么做的？
10. 记忆系统的工程落地：存储、索引、更新怎么做？
11. 大厂真实面试追问汇总

（全文内容详见原始文章，此处节选核心章节）

---

## Memory 记忆抽取从基础到进阶

### 1. Memory 是什么？为什么大模型需要记忆？

LLM 的三大记忆缺陷：上下文窗口有限、跨会话失忆、无法积累经验。

Memory 的本质：把 LLM 的交互经验持久化存储，在需要的时候精准召回。

### 2. Memory 的三大分类

- **短期记忆（Short-Term）**：当前会话的对话历史，会话结束即消失
- **长期记忆（Long-Term）**：跨会话的持久化信息，包括语义记忆（事实）、情景记忆（事件）、程序记忆（行为模式）
- **工作记忆（Working Memory）**：当前任务执行过程中的临时信息

### 3. 记忆的写入策略

信息价值判断四维度：持久性、复用性、事实性、独特性

三种写入策略：
- 全量存储 + 后过滤：不丢信息但噪音多
- LLM 实时抽取：每轮对话后用 LLM 判断是否值得存（主流做法）
- 摘要式存储：隔 N 轮做一次摘要

### 4. 记忆的抽取（核心）

记忆抽取四步流程：召回 → 过滤 → 排序 → 组装

**召回**：多路召回（语义召回 + 关键词召回 + 时间召回 + 主题召回）
**过滤**：相关性过滤、时效性过滤、置信度过滤
**排序**：按语义相关度、时效性、访问频率、置信度加权打分
**组装**：按 Token 预算控制，取 Top-K 条，结构化组织

### 5. 记忆检索策略

记忆检索 vs RAG 检索的区别：记忆对时效性更敏感

混合检索：向量检索 + BM25，用 RRF 融合排名

### 6. 记忆压缩

四种策略：
- 滑动窗口：只保留最近 N 轮
- 摘要压缩：LLM 做对话摘要
- 关键信息抽取 + 摘要混合
- 分层压缩：最近 5 轮完整、5-20 轮摘要、更早仅结构化记忆

### 7. 时效性与矛盾处理

三种处理方式：覆盖更新、时间衰减、矛盾检测（LLM 判断是更新还是补充）

### 8. 记忆一致性

冲突解决：时间优先、置信度优先、LLM 仲裁

### 9. 记忆在 Agent 中的应用

MemGPT：借鉴操作系统内存管理，LLM 自己管理记忆

Letta：分层记忆架构（Core + Archival + Recall）

### 10. 工程落地

存储选型：PostgreSQL + pgvector（推荐）
索引设计：向量索引 + 时间索引 + 标签索引 + 用户索引
更新策略：增量更新、批量更新、混合策略
监控指标：召回准确率、延迟、记忆膨胀率、过期记忆占比
