---
title: AAAI 2026 | 在LLM里发现了大脑功能分区
date: 2026-05-16
tags: [LLM, 功能分区, AAAI, 可解释性, 神经元模块]
summary: 港科大和华为的AAAI 2026论文，通过神经元-样本双重划分方法，首次系统揭示LLM内部存在类似大脑的功能分区——编程、数学、科学等模块在空间上聚集，语言类模块处于跨领域枢纽位置。
category: AI技术
status: 📥已采集
---

> **摘要：** 论文提出一种新的神经元-样本双重划分方法，将模型神经元和输入样本同时划分为K组，确保每组神经元对其对应样本的激活强度最大、交叉激活最低。在Qwen2.5系列上的实验验证了这种方法的有效性，并发现了三个重要特征：功能层级性（K增大时自然分裂出更细分子模块）、空间局部性（技术类模块相互聚集、语言类模块处于交汇处）、层深差异（复杂推理分布在更深层）。

## 我的理解
> 由小林生成，供小涵审阅修改

SAE（稀疏自编码器）在feature粒度做了解耦，但这篇论文在神经元层面直接做功能区块划分，是一个互补的视角。最让我印象深刻的是"功能层级性"的发现——K=10时只有一个大编程模块，K=20时自动分裂成代码调试和系统编程等子模块。这暗示LLM内部组织方式可能比我们想象得更有层次感，有点像生物学上"器官→组织→细胞"的层级。

"空间局部性"也很有趣——编程、数学、科学聚在一起形成"技术簇"，而语言类模块处于交汇处扮演"枢纽"。这和人类大脑的语言中枢位置在功能上形成了意想不到的对应关系。虽然我不确定这种类比的严谨性有多强，但至少说明LLM内部可能自发形成了类似人类认知的组织方式。

## 📌 关键要点
- **核心方法**：提出神经元-样本双重划分的优化框架——目标函数=最大化模块内平均激活强度×模块大小平衡系数（防退化），使用迭代算法交替优化神经元分配和样本分配直至收敛。
- **关键发现**：①功能具有层级嵌套结构（粗粒度模块可自然拆分为细粒度子模块）；②技术类功能（编程/数学/科学）在空间上聚集，语言类位于多个区块交汇处；③信息检索在前几层就可完成，复杂推理分布在更深层，且大模型的分工更清晰。
- **适用场景**：LLM可解释性研究、模型结构理解、针对特定能力做微调（如定位到编程模块做定向优化）。

## 原文
（原始推文内容）

大语言模型在做数学时和在写代码时，用的是同一批神经元吗？

还是说它们也像人类大脑的功能分区一样，各司其职？

这篇来自香港科技大学和华为的AAAI 2026 论文，是目前看到的第一个把这个问题形式化并给出系统答案的工作。

之前的研究也注意到LLM里存在"功能性神经元"，比如处理语言的、处理事实知识的。但核心问题是这些神经元集合之间大量重叠。所谓找到的编程神经元，同时也在数学任务里高度激活，谈不上真正的解耦。

作者把问题重新定义为一个神经元-样本双重划分：

即把模型里的全部神经元划分成K组，
同时把输入样本也对应地划分成K组，
要求每组神经元和对应那组样本之间的激活强度最大，
不同模块之间的交叉激活尽量低。

这样发现的模块才是功能专一的。

优化目标设计是：最大化模块内平均激活强度×模块大小平衡系数（后者是为了防止退化成一个大模块+多个空壳）。用一个迭代算法交替优化神经元分配和样本分配，直到收敛。

在Qwen2.5系列（1.5B / 3B / 7B）上的实验都验证了有效性，且在模块质量和下游分类任务上都超过K-Means等基线。

但真正有意思的在定性分析部分：

1️⃣ 一是功能层级性。K=10时出现一个大的编程模块；把K调到20，它自然分裂成代码调试和系统编程等更细粒度的子模块。说明功能结构是层级嵌套的（图下）。

2️⃣ 二是空间局部性。编程、数学、科学相关的模块在空间上聚在一起，形成技术簇；语言学和翻译类模块则恰好处于多个区块的交汇处，扮演跨领域枢纽的角色；这和人类大脑里语言中枢的位置有几分相似。

3️⃣ 三是层深差异。信息检索这类任务只需要前几层的浅层处理；数学和代码等复杂推理任务，则分布在更深的层；模型越大，这种分工似乎越清晰（图下）。

LLM可解释性研究里，SAE近两年比较火，已经在feature粒度做到了相当细的解析。但这篇论文走的是另一条路，直接在神经元层面进行功能区块的划分。

🔧可使用arXivSub查阅更多论文
每日自动总结 500+篇论文，并挑选与您兴趣相关的论文发送到您的邮箱；支持搜索arXiv论文和4w+篇顶级AI会议论文（CVPR、ICCV、ICML, ICLR, NeurlPS, AAAl, MICCAl）；arXiv趋势洞察；Claude Skill等更多功能。（注册即可获得七天全解锁体验）

📬arXivSub: arxivsub.comfyai.app
📬LLM全英文免费教程: comfyai.app

#arxiv# #大语言模型 #每日论文 #医学影像 #多模态 #agent #医学图像 #LLM #智能体 #benchmark #AI #文献 #arxivsub
