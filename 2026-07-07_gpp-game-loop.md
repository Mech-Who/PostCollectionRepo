---
title: "游戏循环 — 游戏编程模式的精髓"
date: 2026-07-07
category: 游戏开发
depth: 深度
layer: layer2
tags: [游戏循环, 帧率控制, 时间步长, 渲染插值, 固定更新, 性能自适应]
summary: "游戏循环是游戏编程模式中唯一不可或缺的模式。从最简单的不控制帧率循环，到固定时间步长+动态渲染的成熟方案，讨论了四种循环变体及其在帧率稳定性、确定性和性能上的权衡。"
source_url: https://gpp.tkchu.me/game-loop.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 游戏循环是游戏程序的心脏——非阻塞地处理输入、更新状态、渲染画面，同时自适应硬件差异保持固定游戏速度。从初代NES到现代多平台引擎，游戏循环的演进体现了对确定性、平滑性和性能的永恒追求。

## 我的理解

> 游戏循环是区分"游戏程序员"和"普通程序员"的标志性知识。大多数应用程序是事件驱动的——等待用户输入才做事；而游戏必须在没人输入时也继续运行。这个看似简单的差异导致了一整套关于时间管理、帧率控制和确定性的设计哲学。

> 四种循环变体的递进演进堪称经典教学案例：① 不受控循环（硬件决定速度）→ ② 固定帧率+sleep（快机器OK，慢机器无解）→ ③ 可变时间步长（能跟上真实时间但不稳定且非确定性）→ ④ 固定更新+动态渲染（最佳方案）。每个方案的取舍都清晰可辨。

> 可变时间步长的"确定性灾难"是最重要的教训。看似优雅的解决方案（根据耗时调整步长）因为浮点舍入误差的累积，导致同一颗子弹在Fred和George的机器上到达不同位置。这在网络游戏中是致命的——两个玩家看到的世界状态不一致。

> 固定更新+可变渲染的"追赶+插值"方案是对游戏引擎设计最有价值的贡献。核心思路是：物理/AI按固定时间步长更新（保证确定性和稳定性），渲染在任意时刻进行（保证视觉流畅），通过lag值在帧间插值位置。这是几乎所有现代游戏引擎的基础。

> Unity的FixedUpdate/Update分离就是这个模式的直接体现。理解了这个模式，你就理解了为什么物理要放在FixedUpdate里。

## 📌 关键要点
- **核心循环**：processInput() → update() → render()，永不阻塞等待输入
- **四种方案**：无控 → sleep同步 → 可变步长 → 固定更新+可变渲染
- **确定性陷阱**：可变时间步长因浮点误差累积导致非确定性行为（网络游戏致命）
- **追赶+插值**：固定步长更新追赶真实时间，lag值用于渲染帧间插值
- **Unity映射**：FixedUpdate = 固定步长更新，Update = 每帧调用，lag插值 = 平滑视觉
- **能量消耗**：移动端必须设置帧率上限并休眠，PC端通常跑满

## 原文

### 不受控循环
```cpp
while (true) { processInput(); update(); render(); }
```
问题：快机器太快，慢机器太慢。

### 固定帧率+sleep
每帧后sleep到目标时间。解决太快问题，但对太慢无能为力。

### 可变时间步长
update(elapsed)传入实际耗时。能跟上真实时间，但非确定性——浮点误差累积导致不同机器上同一颗子弹到达不同位置。

### 固定更新+可变渲染
固定MS_PER_UPDATE步长追赶真实时间，渲染在任意时刻进行。通过lag/MS_PER_UPDATE插值实现平滑移动。这是现代游戏引擎的标准方案。
