---
title: 大世界场景渲染模块开发笔记（一）
date: 2026-06-14
category: 游戏开发
depth: 深度
layer: layer1
tags: [游戏开发, 渲染技术, 大世界, GPU Instance, Cell Hash, Unity]
summary: 大世界MMO项目（10km×10km×1km）渲染模块的开发笔记，详细讲解技术选型（GPU Instance vs BRG）、空间划分（Cell Hash Mesh替代八叉树）、坐标存储压缩（ushort方案节省40%内存）、GPUBuffer管理（阈值清理策略）。
source_url: https://mp.weixin.qq.com/s/ENJcKLynDMiOZ_dvFVzbTA
source: weixin
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** Unity大世界（10km×10km）渲染方案详解：采用GPU Instance + Cell Hash Mesh空间划分 + 坐标压缩存储（精度0.39cm，内存节省40%），并探讨GPUBuffer三种管理策略（环形结构/双缓冲/阈值清理）。

## 我的理解

这篇文章的核心价值在于它展示了一个从零构建大世界渲染系统的完整决策过程。作者没有直接给答案，而是让读者理解"为什么选这个不选那个"——比如不用八叉树而用Cell Hash Mesh（O(1)哈希查询 vs O(logN)树遍历），不选Unity6的BRG（国内公司持观望态度）。坐标压缩方案（ushort替代float，精度0.39cm）对移动端大世界项目尤其有价值。

## 📌 关键要点

- **合批方案**：GPU Instance（DrawMeshInstancedIndirect），不选SRP Batch/BRG（Unity2022限制）
- **空间划分**：Cell Hash Mesh（256m×256m cell），O(1)直接计算哈希坐标，替代八叉树
- **坐标压缩**：float3×3（36字节）→ ushort×11（22字节），节省40%，精度xz轴0.39cm、y轴1.53cm
- **精度问题**：GPU传递相对坐标（worldPos - _WorldSpaceCameraPos）而非绝对坐标
- **GPUBuffer管理**：预分配大Buffer + Cell加载塞入尾部 + 失效不立即卸载 + 30%阈值触发清洗
- **1000万对象**：原方案224MB → 优化后160MB
- **模块化资产**：建筑拆分成N块基础块组装，单帧100个建筑只用20-30个不同mesh

## 原文

文章内容见 WebFetch 抓取结果。包含坐标压缩代码、Cell定位公式、GPUBatchEntry数据结构、PackedWorldTransform完整定义。
