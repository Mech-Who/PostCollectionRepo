---
title: "原型模式 — 克隆的艺术与数据建模"
date: 2026-07-07
category: 游戏开发
depth: 深度
layer: layer2
tags: [原型模式, 设计模式, Self语言, JavaScript, 数据驱动, JSON委托]
summary: "原型模式让对象通过克隆自身来生成新对象。虽然作为设计模式可用性有限，但作为语言范式（Self、JavaScript）和数据建模工具（JSON原型委托）有着更广泛的应用。"
source_url: https://gpp.tkchu.me/prototype.html
source: gpp-book
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 原型模式通过clone()方法让对象自己生成相似对象。作者诚实地指出：作为设计模式它可用场景有限，模板或工厂函数往往更简洁。但作为语言范式（Self语言的委托继承）和数据建模方法（JSON中的prototype字段），原型的思路极有价值。

## 我的理解

> 这章最独特的地方是作者的坦诚——他直言没找到原型设计模式是最好的方案的场景。这种"这个模式可能没那么有用"的诚实评价在全书中很罕见。

> 语言范式层面的讨论极其精彩。Self语言的委托机制——对象找不到属性时向父对象查找——本质上是用运行时灵活替代了编译时继承。但作者在自创Finch语言后的结论是：纯原型编程"是一团浆糊，难以阅读和使用"。JavaScript选择用class语法糖包裹原型机制是有道理的。

> 最实用的部分是JSON数据建模中的原型委托。与其在JSON中为每个哥布林变体重复生命值、抗性等属性，不如用`"prototype": "goblin grunt"`引用基础模板。这让游戏数据设计师可以像程序员一样用继承思维组织数据，大幅减少配置冗余。这实际上是类型对象模式的轻量数据版本。

> 深层拷贝vs浅层拷贝的语义陷阱提醒我们：clone()看似简单，实则充满设计决策。恶魔拿着叉子，克隆恶魔时要不要克隆叉子？这类问题在游戏实体系统中尤为常见。

## 📌 关键要点
- **诚实评价**：作为设计模式，原型很少是最佳选择；工厂函数或模板通常更简洁
- **Self语言**：纯原型OOP——对象同时包含状态和行为，通过委托实现继承
- **JavaScript真相**：虽然基于原型，但语法和惯用法更鼓励基于类的使用方式
- **JSON委托**：在数据文件中用prototype字段引用基础模板，避免配置冗余
- **深层vs浅层**：clone()的语义陷阱——需要明确哪些引用也应该被克隆
- **生产函数替代**：`Spawner* ghostSpawner = new Spawner(spawnGhost)` 比原型更简洁

## 原文

### GoF原型模式
Monster基类添加clone()虚方法，每个子类返回自身副本。Spawner持有原型Monster，调用clone()创建新怪物。

### Self语言与原型范式
对象直接包含状态和行为，通过父对象引用实现委托继承。克隆是创建新对象的唯一方式。JavaScript的new+constructor+prototype实际上是基于类的糖衣。

### JSON数据建模
```json
{"name": "goblin wizard", "prototype": "goblin grunt", "spells": ["fire ball"]}
```
通过原型委托减少游戏数据配置中的重复。
