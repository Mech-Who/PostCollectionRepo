---
title: 【技术分享】配置驱动能提高扩展性，也需要严格的数据校验
date: 2026-07-04
category: 游戏开发
depth: 深度
layer: layer1
tags: [配置驱动, 数据校验, Unity, 游戏开发, 项目管理, 配置系统, 游戏架构]
summary: 详细论述游戏开发中配置驱动的设计原则与数据校验的必要性。核心观点：配置驱动解决内容扩展问题（代码负责规则，配置负责内容），但只有读取没有校验的系统会导致后期排查成本剧增。提供了从字段合法性、枚举合法性、引用合法性到业务规则的渐进式校验策略，以及小项目的最低限度校验方案。
source_url: https://mp.weixin.qq.com/s/EyyZt1QLGoB6j6a5KE1GWw
source: weixin
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 详细论述游戏开发中配置驱动的设计原则与数据校验的必要性。核心观点：配置驱动解决内容扩展问题（代码负责规则，配置负责内容），但只有读取没有校验的系统会导致后期排查成本剧增。提供了一套从字段合法性、枚举合法性、引用合法性到业务规则的渐进式校验策略——校验应尽量提前（编辑器时 > 启动时 > 运行时 > 线上），以及小项目的最低限度校验方案。

## 我的理解

这篇文章非常务实。它没有讲大道理，而是用一个游戏项目从小到大的真实演进路径来展示问题：原型期硬编码没什么问题 → 内容多了必须拆出配置 → 配置只读不验 → 配置错误层出不穷 → 需要系统性的数据校验。核心洞察是"配置驱动只解决了一半问题"——让内容更容易扩展，但不会自动让项目更稳定。校验才是另一半。

最实用的部分是四类校验的渐进式框架：字段合法性（数值范围）→ 枚举合法性（防止拼写错误）→ 引用合法性（跨表引用检查）→ 业务规则合法性（项目特有逻辑）。以及关于错误提示的具体程度（"表名+字段名+Id+错误原因"）的提醒也很接地气。对于独立开发者或小团队，"从最容易出问题的几张表开始"的建议特别实用。

关联已有概念：game-内容架构设计工程（配置系统架构层面）

## 📌 关键要点
- **核心原则**：代码负责规则，配置负责内容。配置驱动本质是"把经常变化的内容从代码里拆出来"
- **四类校验（按复杂度递进）**：字段合法性（数值范围）→ 枚举合法性（防拼写错误）→ 引用合法性（跨表引用存在性）→ 业务规则合法性（项目特有逻辑）
- **错误发现时机成本顺序**：线上 < 测试 < 启动时 < 编辑器/打包前——越早发现越好
- **配置的边界感**：不要为了灵活把所有特殊逻辑塞进一张表，复杂技能应拆分为"基础信息"+"效果列表"
- **小项目最低校验**：Id 为空/重复/数值不合理/引用不存在/测试内容混入正式配置——从这五件事开始

## 原文

做游戏项目时，很多内容一开始都可以直接写在代码里。

比如怪物有多少血、攻击力是多少、掉落什么道具、技能冷却几秒。项目刚开始只有几种怪物、几把武器、几个技能时，直接写死也能跑，而且看起来还很直观。

```csharp
if (monsterId == "slime")
{
    hp = 100;
    attack = 12;
    prefabPath = "Monsters/Slime";
}
else if (monsterId == "goblin")
{
    hp = 160;
    attack = 18;
    prefabPath = "Monsters/Goblin";
}
```

这种写法在原型阶段很常见，也没什么问题。

但内容一多，问题就会慢慢出现。策划想加一个冰属性史莱姆，程序要改代码。想调整哥布林血量，程序要改代码。想让某个怪物换掉落，还是要改代码。

改得多了以后，怪物逻辑里会混进大量具体数据。一个本来负责"怪物怎么行动、怎么攻击、怎么死亡"的模块，里面开始出现各种怪物名字、数值和特殊判断。后面再加内容，就会越来越别扭。

这也是很多项目会引入配置驱动的原因。

### 配置驱动解决的是内容扩展问题

配置驱动可以简单理解成一句话：代码负责规则，配置负责内容。

怪物怎么被创建、攻击怎么计算、奖励怎么发放，这些仍然由代码控制。具体有哪些怪物、每个怪物多少血、用哪个预制体、掉什么东西，则尽量放到配置里。

Unity 项目中，配置可以来自 Excel、CSV、JSON、ScriptableObject，也可以来自团队内部的配置平台。具体格式不是重点，重点是把"经常变化的内容"从代码里拆出来。

到了项目中后期，内容数量上来以后，这种拆分会明显减轻程序和内容制作之间的耦合。

不过，配置驱动只解决了一部分问题。它让内容更容易扩展，但不会自动让项目更稳定。

### 配置越多，错误也越多

配置表本身也会出错，而且很多配置错误不会在编译阶段暴露。

代码写错了，通常会有编译错误或者明显的运行错误。配置填错了，项目可能照样能启动，甚至前几个流程都没问题，直到玩家进入某个关卡、释放某个技能、领取某个奖励时才暴露。

常见问题：怪物 Id 写重复了、技能引用了一个不存在的 Buff、掉落表里填了一个已经删除的道具、冷却时间填成了 0、预制体路径写错了、百分比字段有人填 30 有人填 0.3。

这些问题单独看都不复杂，但排查起来很消耗时间。

### 数据校验要尽量提前

配置错误发现得越晚，成本越高。

最差的情况，是玩家在线上遇到问题。稍好一点，是测试在某个流程里发现。更好的方式，是游戏启动时就发现。最好是在编辑器里、打包前、提交前就发现。

最基础的校验示例（怪物配置）：

```csharp
public static List<string> ValidateMonsters(List<MonsterConfig> configs)
{
    var errors = new List<string>();
    var ids = new HashSet<string>();

    foreach (var cfg in configs)
    {
        if (string.IsNullOrWhiteSpace(cfg.Id))
        {
            errors.Add("Monster Id is empty.");
            continue;
        }

        if (!ids.Add(cfg.Id))
            errors.Add($"Duplicate monster Id: {cfg.Id}");

        if (cfg.Hp <= 0)
            errors.Add($"Monster {cfg.Id} Hp must be greater than 0.");

        if (cfg.AttackInterval <= 0)
            errors.Add($"Monster {cfg.Id} AttackInterval must be greater than 0.");

        if (string.IsNullOrWhiteSpace(cfg.PrefabPath))
            errors.Add($"Monster {cfg.Id} PrefabPath is empty.");
    }

    return errors;
}
```

### 校验不只是检查有没有填

1. **字段合法性**：血量 > 0，冷却 > 0，价格非负，概率 ≤ 100%
2. **枚举合法性**：怪物类型只能是普通/精英/Boss，"Boss" 写成 "Bosss" 代码不一定报错
3. **引用合法性**：关卡引用的怪物 Id 必须存在，技能引用的 Buff Id 必须存在——表和表之间的引用关系一定要校验
4. **业务规则合法性**：新手关卡不能刷高压 Boss、测试道具不能出现在正式掉落中——这些规则可以随项目推进慢慢补

### 配置也需要边界感

不要为了灵活把特殊字段塞满配置表。如果一个技能表里有 IgnoreDefense、OnlyBossCanUse、CanTriggerExtraEffect、ExtraEffectType、ExtraEffectParam，填表的人很难判断哪些字段能组合。

简单内容直接配字段，复杂技能拆成"基础信息"+"效果列表"，复杂活动拆成"活动规则"+"奖励池"+"开放条件"。

### 错误提示要能定位问题

错误提示要具体：包含表名、字段名、配置 Id 和错误原因。如"MonsterConfig，Id=slime_elite，Hp 必须大于 0"。如果配置来自 Excel，最好还带上行号。

### 小项目最低限度校验

从最容易出问题的几张表开始：怪物表、技能表、道具表、掉落表、关卡表。

最低限度五件事：Id 是否为空、Id 是否重复、数值范围是否明显不合理、引用的配置是否存在、测试内容是否混进正式配置。

## 相关笔记
- game-内容架构设计工程（配置系统架构层面的最佳实践补充）
