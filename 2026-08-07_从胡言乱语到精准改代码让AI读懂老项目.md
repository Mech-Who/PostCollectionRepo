---
title: 从胡言乱语到精准改代码：我是如何让 AI 读懂老项目的
date: 2026-08-07
category: AI应用
depth: 深度
layer: layer2
tags: [AI上下文工程, AGENTS.md, 重构, 技术债务, 自动化测试, 视觉回归]
summary: 腾讯技术工程实战分享：在历史包袱沉重的老项目中，通过给 AI 搭建上下文工程（根目录 AGENTS.md 知识索引 + 就近落盘），让 AI 从"胡言乱语"变成能精准定位问题、主导重构方案的协作者。核心方法论五步：移除死代码、复杂架构做减法、定规范设边界、建自动化测试工程、债务治理常态化。作者金句："落地执行、沟通提效、记录沉淀都可以交给 AI，唯独思考不可以。"
source_url: https://mp.weixin.qq.com/s/acuC164Eh_5bfQJfS8hOXg
source: weixin
status: 📥已采集
sync_si: ✅已同步
---

> **摘要：** 腾讯开发者复盘小程序教育平台重构全过程：AI 在老项目里"胡言乱语"的根源是上下文不足，转折点在于开始搭建 AI 上下文工程。方案是根目录 `AGENTS.md` 只放知识索引（路径+1~2句摘要），细节就近落盘到对应模块。重构方法论五步：①移除不再起作用的死代码 ②复杂架构做减法（案例：下线 OT 协同改 HTTP）③定规范给项目设置约束边界 ④定标准建设自动化测试工程（单测/E2E/MR 流水线/视觉回归）⑤债务治理常态化。两次重构对比显示：第一次 AI 常判断不准需引导，第二次 AI 已能主导方案，验证了上下文沉淀的复利效应。

## 我的理解
> 由小林生成，供小涵审阅修改

1. **"AI 和开发的区别只在上下文"是本文的底层洞察**：开发能读懂老项目，是因为拥有业务历史背景、协作方式、文档、真实运行情况、未完成改造等上下文；AI 没有这些，所以胡言乱语。过去我们写文档、定规范、做平台化，都是为了降低对接成本——现在这套逻辑原封不动地迁移到"和 AI 协作"上。这与本库「harness-engineering」概念高度共振：AI 能力的上限由供给它的上下文决定。

2. **AGENTS.md 的"索引+就近落盘"分层是可直接复用的模式**：根目录只放索引（路径+1~2句摘要），不堆细节；对话中出现可复用规则/排障结论时，就近落盘到模块 AGENTS.md 并同步更新根索引。核心理念："同样的事情，应该只需要跟 AI 强调一遍即可。"——这是把团队知识建设从"没人愿意做"变成"AI 驱动的强制落盘"的巧妙设计。小涵的 tweet-knowledge-manager 体系其实也是这个思路：索引在 _index.md，细节在各文章。

3. **"重构的目的变了"是 AI 时代的新判断**：过去重构是为了治理架构问题，现在重构还多了一个目的——给 AI 添加足够的上下文信息，让 AI 能参与排障、功能迭代、架构优化，最终得到一个"AI 可维护的项目"。这意味着**重构从成本项变成了投资项**，重构的价值评估公式变了。

4. **死代码是 AI 最大的误导源，且"代码有引用≠真实运行"**：很多代码有真实引用，但某条链路在真实运行时永远不会走到（甚至判断条件写在环境变量里，开发自己都难辨认）。这解释了为什么"AI 读代码"会误判——这也意味着清理死代码对 AI 提效的杠杆作用比想象中大。

5. **测试工程从"成本中心"变成"AI 时代的必要基础设施"**：以前前端很少写大量测试，因为变化快、维护成本高；现在 AI 让用例开发成本大幅下降，维护成本只剩上下文和 token。"从去年每次发版必出核心链路问题，到近几个月发版基本很少用户反馈"——自动化测试成了 AI 大规模参与开发后的安全网。

6. **视觉回归的"双轨像素偏差"方案很精巧**：大截图用于检测大布局异常（像素偏差容忍高），组件级小用例用于精确问题（偏差严格）——解决了"一张大截图解决不了所有样式问题"的困境，也防止 AI 为了通过流水线而误改样式。

## 📌 关键要点
- **核心判断**：AI 在历史债务重的项目里作用有限的根源是**上下文不足**，不是模型能力不足
- **上下文工程四步**：
  1. 根目录 `AGENTS.md` 只放知识索引（路径 + 1~2句摘要），不堆细节
  2. 细节就近落盘到模块 AGENTS.md
  3. 对话中出现可复用规则/兼容性/排障结论 → 必须落盘
  4. 发现索引过期 → 主动修改
- **项目背景**：小程序教育平台（自由创作/课程学习/课程制作），前端复杂度 + WEB 外模块（编译、Node、AI Agent、付费、腾讯云/混元）
- **五步屎山清理法**：
  1. 移除不再起作用的代码（AI 辅助辨别无引用代码 + 结合真实运行情况）
  2. 做减法，复杂架构简单化（案例：OT 协同编辑 → 下线 websocket → 下线轮询，两次重构 AI 角色从辅助变主导）
  3. 定规范，设约束边界（统一组件 + AI 自查规范 + MR 规则检测）
  4. 定标准，自动化测试工程（单测/E2E + MR 流水线 + 视觉回归 + MR AI 评审）
  5. 债务治理常态化（把集中专项改造变成日常开发常规化改造）
- **架构金句**："架构设计之所以重要，不是因为它看起来高级，而是因为它能把复杂度压下来"
- **金句**："落地执行、沟通提效、记录沉淀都可以交给 AI，唯独思考不可以"

## 原文
（详见腾讯技术工程原文，核心内容已在摘要与关键要点中覆盖）

## 相关笔记
- [[harness-engineering]]
- [[ai-编程实践]]
- [[软件设计架构]]
- [[ai-知识基础设施协议化]]
