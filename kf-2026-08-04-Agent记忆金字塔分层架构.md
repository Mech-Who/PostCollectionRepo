---
title: Agent 记忆金字塔分层架构
date: 2026-08-04
category: AI技术
depth: 深度
layer: layer2
tags: [Agent, 记忆系统, 记忆金字塔, TencentDB, RAG, 语义召回, 冲突仲裁, 上下文工程]
source_article: 2026-08-04_腾讯Agent记忆系统TencentDB实测
---

# K-Fragment: Agent 记忆金字塔分层架构

## A. Applicable — 适用场景

- 构建 Agent 长期记忆系统（跨会话记忆、任务地图、多轮复杂任务上下文管理）
- 需要控制长链路 Agent 的 token 成本（全量塞上下文不可行时）
- 设计任何"知识分层"系统（原文 → 提炼 → 语义索引 → 图谱）时的架构参考
- 评估/选型托管记忆服务（TencentDB Agent Memory 等）时的判据清单

## B. Boundary — 边界与注意事项

- **语义召回默认关闭**：向量检索噪声大、召回片段上下文不全——先关键词后语义，语义作为按需增强而非默认路径
- **只写不删是负债**：必须有更新/遗忘机制（过期清理、相似合并），否则记忆库膨胀且互相矛盾
- **冲突必须仲裁**：新旧记忆矛盾时按"置信度 × 时间戳"裁决，高置信度新记忆胜出，低置信度降级/合并
- **分层是成本与保真的权衡**：层越高越精炼也越失真，L0 原始数据必须保留（可追溯/可重提炼）
- 记忆检索与 RAG 检索不同：RAG 检索文档片段，记忆检索要带"上下文完整性"（谁/何时/何情境）

## C. Core — 核心要点

1. **记忆金字塔 L0-L3**：L0 原始上下文（完整事件流，不加工不丢失）→ L1 记忆块（LLM 提炼的结构化片段）→ L2 语义记忆（向量索引层）→ L3 知识图谱（实体关系网络）
2. **四步闭环**：写入（事件进 L0）→ 提炼（LLM 压缩成 L1、升级 L2）→ 检索（关键词+语义混合）→ 更新（遗忘/合并/冲突仲裁）
3. **核心 ROI 不是"记得久"而是"花更少 token 记得久"**：实测任务地图场景 Token 2.21 亿 → 8500 万（-61.5%）
4. **与知识管线同构**：文章= L0、KF= L1/L2、概念连接= L3——分层记忆是数据系统的通用答案
5. **设计判据**：分层存储 + 按需召回（默认保守）+ 主动更新 + 冲突仲裁 = 生产级记忆系统

## D. Data — 关键数据与决策记录

```
记忆金字塔（每层职责与成本）:
L0 原始上下文 —— 全量事件流，成本最高，保真 100%
  ↓ LLM 提炼（异步、可调度）
L1 记忆块 —— 结构化摘要，成本中，保真 ~70%
  ↓ 向量化
L2 语义记忆 —— 向量索引，检索层，按需召回
  ↓ 实体抽取
L3 知识图谱 —— 实体关系，可查询网络

实测数据（任务地图场景）:
  Token 消耗: 2.21 亿 → 8500 万（降 61.5%）
  收益来源: 全量塞上下文 → 只塞相关记忆块

冲突仲裁规则:
  新记忆 vs 旧记忆矛盾
  → 置信度 × 时间戳 裁决
  → 高置信度新记忆胜出 / 低置信度降级合并
```
