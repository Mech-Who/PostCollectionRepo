---
title: SparkSubmit 源码阅读技巧——以类和方法作为切入点的实战方法论
date: 2026-06-27
category: 认知提升
depth: 深度
layer: layer2
tags: [源码阅读, SparkSubmit, 阅读技巧, 代码分析, 方法论, 涤生大数据, 主线阅读法, 成员变量优先]
summary: 涤生大数据以 SparkSubmit 源码为案例，系统性讲解阅读开源框架源码的两个核心切入点——类切入点（看 main 方法+成员变量区分实体类/工具类）和方法切入点（看局部变量+抓住主线忽略非核心过程），并实际演示 1600+ 行的 SparkSubmit 类如何缩减为仅需关注的几行关键代码。核心洞见：成员变量比方法更重要（方法是操作变量，变量是状态的核心）、遇到方法声明先跳过等到调用时再看、用对齐线快速定位代码块范围、500 行参数解析代码只需知道"它在解析参数"即可。
source_url: https://www.bilibili.com/video/BV1GCghzDEYY
source: bilibili
status: 📥已采集
sync_si: ❌未同步
---

> **摘要：** 这是一套可复用的源码阅读方法论。讲师以 SparkSubmit 的 1600+ 行源码为例，演示了如何系统性地筛选信息密度：先用"类切入点"判断是否有 main 方法（入口）和成员变量（区分实体类 vs 工具类），再用"方法切入点"定位关键方法并识别其局部变量和处理的成员变量，最后通过"抓住主线"策略忽略日志、异常处理、参数解析等非核心过程，将庞大代码缩减为仅需理解的几行。实际演示中，SparkSubmit 的核心流程从 1600 行缩减为 submit → doRunMain → runMain → app.start 一条直线。

## 我的理解
> 由小林生成，供小涵审阅修改

这是一个**极其务实**的源码阅读方法论。22 分钟的讲解，没有废话，全部是实操经验。核心价值不在 SparkSubmit 本身（大部分人不会去读 Spark 源码），而在于那套**可迁移到任何代码库**的阅读策略。

### 对你最相关的三个可迁移认知

**1. 信息密度筛选：三类东西可以直接忽略**
讲师反复强调一条原则——"遇到方法声明先跳过，等到调用时再研究"。这意味着：你不需要理解类里的每一个方法，只需要找到**实际被调用的那一行**。类似地，日志（log.xxx）、异常处理（try/catch）、参数解析（parseArgs）这三类在初次阅读时可以果断跳过。对于大型框架，这个筛选策略能把需要阅读的代码量缩减 90% 以上。

**2. 成员变量 > 方法——视角反转**
常规思维是"看方法做了什么"，讲师提出的视角是"方法只是操作员，成员变量才是状态的核心"。读类时优先看成员变量（它们决定这个类的状态和职责），方法只是对成员变量的增删改查。这个视角一换，读代码的层次就不同了——你不是在读流程，而是在读**状态机**。

**3. 对齐线 + 名字判断 + 逐层跳入 = 高效阅读三角**
三个实操技巧：(a) 用代码折叠/对齐线快速定位块的起止位置（讲师说"对齐线里的东西如果决定跳过，全部跳过，要果断"）；(b) 变量的命名决定要不要点进去看（如 `prepareSparkSubmitEnvironment` → 知道它是"准备环境"就行，不用管实现）；(c) 逐层跳入不贪多——每次只看当前层的核心一行代码，不要看到一个新类就点进去。

### 一句话概括

不是"我看了很多代码所以会读"，而是"我知道哪些代码**不需要看**"。这种"减法思维"对阅读任何大型代码库（Unity 引擎、游戏框架、Web 框架）都适用。

## 📌 关键要点

### 核心方法论：两个切入点

**以类为切入点（入口已知时）：**
1. 搜索类中是否有 `main` 方法 → 有则从 main 开始，它是主线程的入口
2. 检查类的成员变量 → 有成员变量是"实体类"（状态核心），无成员变量是"工具类"（可跳过，用到时再看）
3. 优先看成员变量（比方法更重要）→ 所有方法最终都是对成员变量的操作

**以方法为切入点（代码执行中跳入时）：**
1. 先看方法的局部变量 → 方法内部的操作围绕这些变量展开
2. 看方法对类成员变量的处理 → 这是方法的核心影响
3. 抓住主线，忽略不重要的过程：
   - ❌ 跳过日志（前期无意义，debug 时才需要）
   - ❌ 跳过异常处理（`catch.xxx` 无需看内部，知道它在"兜底"即可）
   - ❌ 跳过参数解析（知道"它在解析参数"就够了）
   - ❌ 跳过方法声明（等到实际调用时再研究）

### SparkSubmit 实战演示的关键路径

```
SparkSubmit.main
  → submit(appArgs)          // 核心就一行
    → doRunMain(...)         // 跳过解析参数、日志初始化
      → runMain(...)         // 反射加载主类
        → 找 childMainClass   // 500行解析→只找YarnClusterApplication
          → 反射 + 子类判断
            → app.start()     // yarn.Client.start()
```

讲师实际演示中，SparkSubmit 1600+ 行的代码，真正需要逐行理解的只有 4-5 个调用链。

### 核心原则

| 原则 | 说明 |
|------|------|
| **主动控制，不被代码带着走** | 不是你跟着代码跑，而是你决定什么时候进、什么时候跳过 |
| **名字即注释** | 变量名/方法名已经告诉你它做什么——`prepareXXX` 就是准备、`parseXXX` 就是解析，懂了就行 |
| **调用才研究** | 声明时跳过，被调用时再点进去——避免"无底洞"式跟代码 |
| **果断跳过，不犹豫** | 决定跳过的块就要全部跳过，不要"看一眼再看一眼" |
| **对齐线定位** | 用 IDE 的代码折叠/对齐线快速确定代码块的起止范围 |

### SparkSubmit 代码阅读的阶段性成果

- `SparkSubmit` 类 1600+ 行 → 实际阅读量约 50 行
- 核心方法 `submit()` → 只调用了 `doRunMain()`
- 500 行参数解析 → 只需知道它返回了 `childMainClass`（即 `YarnClusterApplication`）
- 最终跟踪到 `YarnClusterApplication.start()` → 这是一个新的入口

## 原文（Whisper 转录）

> 以下为 B 站 UP 主「涤生大数据」视频《【掌握】以SparkSubmit作为切入点带你学习源码分析技巧》的 Whisper medium 中文转录（含口语化表达及少量识别误差，如 "class" 被识别为 "类"、"main" 被识别为 "卖方法"、"YARN" 被识别为 "亚人" 等，已做标准化处理）。

### 阅读源码的两种切入点

我跟大家说阅读源码的一些技巧。你不要觉得听了这一次课就能知道所有东西，这个都是触类旁通的。只要你学了今天这些东西，以后你看开源的源码，特别是 Java 这种源码，大家心里就有数了。

**第一，以类作为切入点。**
切入点有两个。第一个，我先看这个类里面有没有 main 方法。如果有 main 方法，说明这个是主线程。那么主线程所在的进程就是主进程，这个线程就是程序最开始执行的地方。我们看代码一定要按照代码执行的顺序去看，所以如果有 main 方法，那就从 main 方法开始看。

第二个就是看这个类的成员变量。一个类里面无非有两种东西：一个是成员变量，一个是方法。方法的重要性没有成员变量高，因为所有方法的最终目的都是对成员变量进行调整、修正、增删改查。就像你有胳膊有腿，这是你的成员变量；你能拿东西能跑，这叫方法。方法依托于属性。所以有成员变量的类叫做实体类，没有成员变量的类叫做工具类（它只是包装了很多工具方法，比如做类型转换、进制转换）。工具类可以跳过不看，等到真正用到的时候再研究。

**第二，以方法作为切入点。**
拿到一个类后，里面代码可能几千行三万行，你不可能一行行去看。先确定一个方法，这个方法一定是业务支撑的。比如我想用 `flatMap`，我就去看 `flatMap` 里面做了什么。

进入方法后要看哪些东西？首先看方法里定义的局部变量，因为所有操作都是对这些局部变量进行增删改查。然后要忽略不重要的过程，比如日志——前期看没用，只有代码出问题的时候才有用。异常处理也直接跳过，比如 `catch` 里面东西不需要看，知道它是为了程序稳定性就行。

我们一定要抓住主线。Spark 有上百万行代码，一个团队几百上千人写的代码，你一个人去看肯定看不过来，而且你不知道人家当时怎么想的。所以一定要抓住主线。

### 两个切入点的使用场景

以类作为切入点，是我明确知道这个类是一个入口。比如我直接找到了 SparkSubmit 这个类，那就按类的流程来——先看有没有 main 方法，然后看成员变量。

大多数场景下其实是以方法作为切入点。比如执行代码时跳到一个源码里面，一般都是方法，那我就在这个方法里研究。

### 类切入点的实操：SparkSubmit

我看 SparkSubmit 这个类有 1600 多行，按流程来——第一件事，搜索有没有 main 方法。找到 main 方法后，发现它 `new SparkSubmit()` 然后调用 `submit()`。这就是最核心的代码。所有其他东西——日志初始化、参数解析——都可以跳过。

那 `submit` 里面干了什么？进去看，名字叫 `doRunMain`。名字已经告诉你了——"执行主方法"。那中间 500 行参数解析要不要看？不需要，你只需要知道它解析了参数，返回了四个值就行了。

真正核心的是最后一行——`app.start()`。`app` 就是 `YarnClusterApplication`。

### 抓住主线的关键

不要让自己跟着代码走，一定要自己主动控制阅读的层次。不要遇到一个东西就点进去看——你能点到无底洞。一定要果断，我说不看就不看，不要深陷其中。

遇到方法声明先跳过，等到调用的时候再研究。函数的定义不用在声明处看，调到方法被用到的时候才看。用对齐线快速定位——对齐线里的东西，如果你决定跳过，那全部跳过。

名字即注释：`prepareSparkSubmitEnvironment` 就是准备环境，`parseArguments` 就是解析参数。懂了它干什么就够了，不用管具体怎么实现。

### 总结

SparkSubmit 1600+ 行代码，真正要看的核心流程就一条线：
```
SparkSubmit.main → submit → doRunMain → runMain → app.start
```

中间的所有日志、异常处理、参数解析、方法声明，都可以在初读时跳过。

后续如何？`start` 开始后就是 `YarnClusterApplication` 的逻辑，会持续更新更多的源码分析。
